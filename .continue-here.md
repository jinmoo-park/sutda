# Continue Here — 2026-04-01

## 현재 위치
Phase 10 (UX) — UAT 2라운드 피드백 수정 완료, 빌드/브라우저 테스트 대기

## 이번 세션에서 완료한 작업 (미커밋)

다음 8개 파일 수정 완료 (아직 커밋 안 됨):

### 1. 밤일낮장 결과 표시 수정
- `packages/client/src/components/modals/DealerSelectModal.tsx`
  - 다른 플레이어가 선택한 카드도 face-up으로 표시 (기존: 내 카드만 공개)
  - 각 카드 아래 선택한 플레이어 닉네임 뱃지 표시
  - 선택된 카드는 HTML disabled 사용 안 함 (face-up 시각 유지)
- `packages/client/src/pages/RoomPage.tsx`
  - `DealerResultOverlay` 트리거 조건 수정: `dealer-select → mode-select`도 감지
  - (서버가 attend-school 자동 처리로 건너뛰는 문제 해결)

### 2. 데스크탑 레이아웃 개선
- `packages/client/src/pages/RoomPage.tsx`
  - 오른쪽 사이드 256px → 512px (2배)
  - 오른쪽 패널 `justify-end` 적용 (하단정렬)
  - 중앙 패널 `relative overflow-hidden` (배경 전체 채움 지원)

### 3. 베팅패널 1행 레이아웃
- `packages/client/src/components/layout/BettingPanel.tsx`
  - 칩 버튼: `grid-cols-2` → `grid-cols-4` (1행)
  - 액션 버튼 (체크/콜/레이즈/다이): `grid-cols-2` → `grid-cols-4` (1행)

### 4. 게임테이블 배경 전체 채움 + 오버레이
- `packages/client/src/components/layout/GameTable.tsx`
  - 데스크탑: `absolute inset-0`으로 중앙패널 전체 채움 (기존 560×560 고정 제거)
  - 상/하단 그라데이션 반투명 블랙 오버레이 추가
  - 모바일: `h-full`로 레터박스 빈 공간 제거

### 5. 카드 컨테이너 가변폭
- `packages/client/src/components/game/PlayerSeat.tsx`
  - Card 너비 `w-28` → `w-auto min-w-[7rem]`
  - 세장섯다 3번째 카드 렌더링 지원 (`[0, 1]` → `Array.from({ length: Math.max(cards.length, 2) })`)

### 6. MainPage 타이틀 이미지
- `packages/client/src/pages/MainPage.tsx`
  - `main_title_alt.jpg` 이미지 추가 (WaitingRoom과 동일)
  - 레이아웃 분리: 이미지 + `max-w-sm` 폼 컨테이너

### 7. 5인+ 플레이 버그 수정
- `packages/server/src/index.ts`
  - `join-room` 핸들러: 게임 진행 중 재접속/합류 시 `game-state`도 전송
  - (기존: room-state만 전송 → 클라이언트가 대기실에 묶히는 문제 해결)

## 다음에 해야 할 일

1. **브라우저 테스트** (최우선):
   - 서버/클라이언트 빌드 후 실제 브라우저에서 확인
   - `pnpm -w run dev` 또는 `pnpm --filter @sutda/server dev` + `pnpm --filter @sutda/client dev`

2. **검증 항목**:
   - [ ] 밤일낮장: 다른 사람 선택 카드 face-up 표시되는지
   - [ ] 밤일낮장 완료 시 DealerResultOverlay 3초 표시되는지
   - [ ] 데스크탑: 오른쪽 사이드 512px, 베팅패널 1행 레이아웃
   - [ ] 배경이미지 중앙패널 전체 채움 (레터박스 없음)
   - [ ] 오버레이로 담요 색 덜 쨍한지
   - [ ] 5명 접속 시 게임 정상 진행되는지

3. **미해결 이슈** (미확인/미수정):
   - 밤일낮장 마지막 선택 시 자연스러운 전환 (타이밍 검증 필요)

## 커밋 전 상태
모든 수정 파일이 unstaged 상태. 아직 커밋 안 됨.
커밋 준비되면: `git add packages/ && git commit -m "fix(10-ux): UAT 2라운드 피드백 반영 — 레이아웃/딜러결과/5인플레이 수정"`

## 재개 방법
```
/gsd:resume-work
```
