# Quick Task 260405-v63 요약

## 작업 내용

`packages/server/src/index.ts`의 `join-room` 핸들러 3개 경로에 `game-history` emit 추가

## 분석 결과

`gameHistories`는 이미 `Map<string, RoundHistoryEntry[]>` 구조로 roomId 키 관리 중이었음.
방장 승계/라운드 진행/방 삭제 시 이력 관리는 서버 측에서 정상 동작.
**문제:** 재접속/입장 시 클라이언트에 기존 이력을 전송하지 않아 이력이 사라져 보임.

## 수정 내용

| 경로 | 추가 내용 |
|------|-----------|
| 재접속 경로 (line ~355) | `socket.emit('game-history', { entries: gameHistory })` |
| 옵저버 입장 경로 (line ~422) | `socket.emit('game-history', { entries: gameHistoryObs })` |
| 대기실 입장 경로 (line ~445) | `socket.emit('game-history', { entries: gameHistoryWait })` |

## 커밋

- `fix(server): join-room 3개 경로에 game-history emit 추가 (재접속/옵저버/대기실)`
