---
phase: quick-260405-v63
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/index.ts
autonomous: true
must_haves:
  truths:
    - "재접속한 플레이어가 기존 게임 이력을 즉시 볼 수 있다"
    - "옵저버로 입장한 플레이어도 기존 게임 이력을 볼 수 있다"
    - "방장이 바뀌어도 이력이 유지된다"
    - "모든 플레이어가 퇴장하면 이력이 삭제된다"
  artifacts:
    - path: "packages/server/src/index.ts"
      provides: "game-history emit on join/reconnect"
  key_links:
    - from: "join-room handler (reconnect path)"
      to: "gameHistories Map"
      via: "socket.emit('game-history')"
    - from: "join-room handler (observer path)"
      to: "gameHistories Map"
      via: "socket.emit('game-history')"
    - from: "join-room handler (waiting room path)"
      to: "gameHistories Map"
      via: "socket.emit('game-history')"
---

<objective>
게임 이력(game history)을 room ID 기준으로 영속 유지하여, 재접속/옵저버 입장 시에도 기존 이력을 복원한다.

Purpose: 현재 gameHistories Map은 이미 roomId 키로 관리되어 방장 승계나 라운드 진행 시 이력이 유지된다. 그러나 재접속/신규 입장 시 game-history 이벤트를 보내지 않아 클라이언트에서 이력이 사라지는 버그가 있다.

Output: join-room 핸들러의 3개 경로(재접속, 옵저버 입장, 대기실 입장) 모두에서 기존 game-history를 전송하도록 수정
</objective>

<context>
@packages/server/src/index.ts

현재 구조:
- `gameHistories = new Map<string, RoundHistoryEntry[]>()` (line 95) — roomId 키, 이미 올바른 구조
- 이력 추가: `tryAdvanceNextRound` (line 221-227), `leave-room` (line 457-466)
- 이력 삭제: 방 빈 경우 (line 523) `gameHistories.delete(roomId)`
- 이력 broadcast: `io.to(roomId).emit('game-history', { entries })` — 전체 방에 emit

문제점 (3곳에서 game-history 미전송):
1. **재접속 경로** (line 337-375): `game-state`, `chat-history` emit하지만 `game-history` 누락
2. **옵저버 입장** (line 378-414): `game-state`, `chat-history` emit하지만 `game-history` 누락
3. **대기실 입장** (line 417-431): `chat-history` emit하지만 `game-history` 누락

패턴: chat-history 전송 코드 바로 뒤에 동일한 패턴으로 game-history 전송 추가
</context>

<tasks>

<task type="auto">
  <name>Task 1: join-room 핸들러 3개 경로에 game-history emit 추가</name>
  <files>packages/server/src/index.ts</files>
  <action>
join-room 핸들러 내 3개 입장 경로에서, chat-history 전송 코드 바로 뒤에 game-history 전송 코드를 추가한다.
패턴은 chat-history와 동일:

```typescript
const gameHistory = gameHistories.get(roomId);
if (gameHistory && gameHistory.length > 0) {
  socket.emit('game-history', { entries: gameHistory });
}
```

적용 위치 (3곳):

**1) 재접속 경로 (line ~350 부근, chat-history emit 이후)**
기존 `chat-history` 전송 후:
```typescript
// 게임 이력 전송 (재접속 시 기존 이력 복원)
const gameHistory = gameHistories.get(roomId);
if (gameHistory && gameHistory.length > 0) {
  socket.emit('game-history', { entries: gameHistory });
}
```

**2) 옵저버 입장 경로 (line ~410 부근, chat-history emit 이후)**
기존 `chat-history` 전송 후, `return;` 전에:
```typescript
// 게임 이력 전송 (옵저버에게도 이력 제공)
const gameHistoryObs = gameHistories.get(roomId);
if (gameHistoryObs && gameHistoryObs.length > 0) {
  socket.emit('game-history', { entries: gameHistoryObs });
}
```

**3) 대기실 입장 경로 (line ~430 부근, chat-history emit 이후)**
기존 `chat-history` 전송 후:
```typescript
// 게임 이력 전송 (대기실 재입장 시 이력 복원)
const gameHistoryWait = gameHistories.get(roomId);
if (gameHistoryWait && gameHistoryWait.length > 0) {
  socket.emit('game-history', { entries: gameHistoryWait });
}
```

주의: 변수명을 각 블록에서 다르게 하거나, 기존 chatHistory 변수와 충돌하지 않도록 한다. 스코프가 분리된 블록 내라면 같은 이름도 가능하지만, 안전하게 별도 이름 사용 권장.
  </action>
  <verify>
    <automated>cd D:/dev/sutda && grep -n "game-history" packages/server/src/index.ts | grep "socket.emit" | wc -l</automated>
    기대 결과: 3 (신규 추가 3곳). 기존 io.to().emit 2곳과 별도로, socket.emit 3곳이 추가되어야 한다.
  </verify>
  <done>
    - 재접속 플레이어가 join-room 시 기존 game-history를 수신한다
    - 옵저버 입장 시 기존 game-history를 수신한다
    - 대기실 입장 시 기존 game-history를 수신한다 (이전 게임 라운드 이력이 있는 경우)
    - 기존 broadcast (io.to) 로직은 변경 없음
    - 방 빈 경우 삭제 로직 (line 523) 변경 없음
  </done>
</task>

</tasks>

<verification>
1. `grep -n "game-history" packages/server/src/index.ts` 로 총 5곳 확인:
   - `io.to(roomId).emit('game-history', ...)` 2곳 (기존 broadcast)
   - `socket.emit('game-history', ...)` 3곳 (신규 join 시 복원)
2. 빌드 확인: `cd packages/server && npx tsc --noEmit`
3. 기존 테스트 통과 확인: `cd packages/server && npx vitest run`
</verification>

<success_criteria>
- join-room 핸들러의 3개 경로 모두에서 game-history를 전송한다
- 재접속/옵저버/대기실 입장 시 클라이언트 gameStore.roundHistory가 복원된다
- 기존 broadcast 및 삭제 로직에 영향 없음
- TypeScript 빌드 에러 없음
</success_criteria>

<output>
완료 후 `.planning/quick/260405-v63-room-id/260405-v63-SUMMARY.md` 작성
</output>
