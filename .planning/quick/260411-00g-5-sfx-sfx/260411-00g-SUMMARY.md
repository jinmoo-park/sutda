# Quick Task 260411-00g: 5가지 버그수정 — Summary

**완료일:** 2026-04-10
**커밋:** 3919d09, 5eb2f52, ef001b8

## 수정 내역

### Task 1: 비오리지날 모드 쇼다운 isRevealed 설정 (Bug 1 + Bug 3)
- `_resolveShowdownSejang()`: 승자 결정 후 `alivePlayers.forEach(p => { p.isRevealed = true })` 추가
- `_resolveShowdownHanjang()`: 동일 처리
- 효과: 세장섯다/한장공유 쇼다운 후 SFX(땡/한끗차이)가 승자/패자 모두에게 정상 재생

### Task 2: ResultScreen 칩 변동 표시 + 이력 chipDelta 정확성 (Bug 2 + Bug 5)
- `_generateRoundHistory()`: chipDelta를 `settleDelta - p.totalBet`으로 변경 (순수익 기준)
- `_generateRoundHistory()` 끝에 `(this.state as any).lastRoundHistory = this.lastRoundHistory` 추가
- ResultScreen: 서버 `lastRoundHistory.playerChipChanges` 우선 사용, fallback으로 기존 클라이언트 계산 유지
- 효과: 골라골라 체크-콜 후 +500/-500 정상 표시, 이력 기록도 순수익 반영

### Task 3: 오링 후 옵저버→플레이어 칩 할당 (Bug 4)
- `obs.chips = obs.observerChips ?? 100000` → `obs.chips = obs.observerChips || 100000`으로 변경
- 효과: observerChips가 0이거나 falsy일 때도 기본값 100000 적용, 0원 계승 방지

## 변경 파일
- `packages/server/src/game-engine.ts` — isRevealed 설정, chipDelta 수정, lastRoundHistory 저장
- `packages/client/src/components/layout/ResultScreen.tsx` — 서버 chipDelta 우선 사용
- `packages/server/src/index.ts` — 옵저버 칩 할당 안전성
