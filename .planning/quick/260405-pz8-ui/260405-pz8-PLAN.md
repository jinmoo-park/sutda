---
phase: quick-260405-pz8-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/pages/MainPage.tsx
  - packages/server/src/index.ts
autonomous: true
requirements: ["pz8-ui-rename"]
must_haves:
  truths:
    - "방 생성 입력 필드의 label과 placeholder가 '암구호'로 표시된다"
    - "잘못된 암구호 입력 시 서버 에러 메시지에 '암구호'가 표시된다"
    - "내부 변수명(password, hasPassword 등)은 변경되지 않는다"
  artifacts:
    - path: "packages/client/src/pages/MainPage.tsx"
      provides: "암구호 라벨/플레이스홀더"
    - path: "packages/server/src/index.ts"
      provides: "암구호 에러 메시지"
  key_links: []
---

<objective>
방 생성 UI의 "비밀번호" 텍스트를 "암구호"로 일괄 변경한다.

Purpose: 사용자 노출 텍스트 명칭 통일 (비밀번호 -> 암구호)
Output: 클라이언트 라벨/플레이스홀더 + 서버 에러메시지 텍스트 변경
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
변경 대상 파일과 위치:

**packages/client/src/pages/MainPage.tsx**
- L65: `<label>` 텍스트 "방 생성 비밀번호" -> "암구호"
- L69: `placeholder` 텍스트 "방 생성 비밀번호" -> "암구호"

**packages/server/src/index.ts**
- L116: INVALID_PASSWORD 에러 메시지 "방 생성 비밀번호가 올바르지 않습니다." -> "암구호가 올바르지 않습니다."

주의: `password`, `setPassword`, `room-password`, `INVALID_PASSWORD`, `ROOM_CREATE_PASSWORD` 등 내부 변수명/ID/환경변수명은 변경하지 않는다.
</context>

<tasks>

<task type="auto">
  <name>Task 1: 클라이언트 + 서버 UI 텍스트 일괄 변경</name>
  <files>packages/client/src/pages/MainPage.tsx, packages/server/src/index.ts</files>
  <action>
1. `packages/client/src/pages/MainPage.tsx`를 열고:
   - L65의 sr-only label 텍스트를 "방 생성 비밀번호" -> "암구호"로 변경
   - L69의 placeholder를 "방 생성 비밀번호" -> "암구호"로 변경
   - 변수명 password, setPassword, id="room-password", type="password"는 그대로 유지

2. `packages/server/src/index.ts`를 열고:
   - L116의 INVALID_PASSWORD 메시지를 "방 생성 비밀번호가 올바르지 않습니다." -> "암구호가 올바르지 않습니다."로 변경
   - 키명 INVALID_PASSWORD, 변수명 requiredPassword, password는 그대로 유지
  </action>
  <verify>
    <automated>cd D:/dev/sutda && grep -n "암구호" packages/client/src/pages/MainPage.tsx packages/server/src/index.ts | wc -l</automated>
    결과가 3이어야 한다 (클라이언트 2건 + 서버 1건). 또한 grep -n "비밀번호" 로 검색 시 해당 파일에서 UI 노출 텍스트 "비밀번호"가 없어야 한다.
  </verify>
  <done>
  - MainPage.tsx의 label과 placeholder가 "암구호"로 표시
  - 서버 에러 메시지가 "암구호가 올바르지 않습니다."로 표시
  - 내부 변수명/prop명은 모두 기존 그대로 유지
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "비밀번호" packages/client/src packages/server/src --include="*.ts" --include="*.tsx"` — 결과 없음
2. `grep -rn "암구호" packages/client/src packages/server/src --include="*.ts" --include="*.tsx"` — 3건 출력
3. `pnpm --filter client build` 빌드 성공
</verification>

<success_criteria>
- 사용자 노출 텍스트에서 "비밀번호" 문자열이 완전히 제거되고 "암구호"로 대체됨
- 내부 코드(변수명, prop명, 환경변수명)는 변경 없음
- 클라이언트 빌드 성공
</success_criteria>

<output>
완료 후 `.planning/quick/260405-pz8-ui/260405-pz8-SUMMARY.md` 작성
</output>
