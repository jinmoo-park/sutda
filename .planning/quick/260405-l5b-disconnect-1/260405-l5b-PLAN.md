---
phase: quick-260405-l5b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/game-engine.ts
  - packages/server/src/index.ts
autonomous: true
must_haves:
  truths:
    - "card-reveal phase에서 disconnect 발생 시 해당 플레이어 카드 자동 공개 처리되어 게임 진행"
    - "result phase에서 disconnect 발생 시 해당 플레이어 투표 불필요하게 되어 나머지 플레이어만으로 다음 판 진행 가능"
    - "disconnect 후 1분 경과 시 방에서 자동 퇴장 (기존 60초 타이머 유지)"
  artifacts:
    - path: "packages/server/src/game-engine.ts"
      provides: "card-reveal phase disconnect 자동 공개 로직"
    - path: "packages/server/src/index.ts"
      provides: "result phase disconnect 시 즉시 next-round 투표 통과 로직"
---

<objective>
결과 단계(card-reveal / result phase)에서 플레이어 disconnect 시 게임이 멈추는 버그 수정.

Purpose: 현재 card-reveal 또는 result phase에서 누군가 연결이 끊기면, 해당 플레이어의 카드 공개/투표를 영원히 기다려 게임이 멈춘다. disconnect된 플레이어를 자동 처리하여 나머지 플레이어가 즉시 게임을 계속할 수 있게 한다.

Output: 서버 game-engine.ts + index.ts 수정
</objective>

<context>
@.planning/STATE.md
@packages/server/src/game-engine.ts
@packages/server/src/index.ts
@packages/shared/src/types/game.ts

## 버그 근본 원인 분석

### 원인 1: card-reveal phase에서 disconnect 미처리
- `forceDisconnectedPlayerAction()` (game-engine.ts:1924)은 **베팅 phase만** 처리한다 (BETTING_PHASES 체크).
- card-reveal phase에서는 아무 것도 하지 않아, disconnect된 플레이어의 카드가 공개되지 않는다.
- `revealMyCard()` → `allFullyRevealed` 체크에서 disconnect 플레이어가 영원히 미공개로 남아 showdown 불가.

### 원인 2: result phase에서 next-round 투표 차단
- `next-round` 이벤트 핸들러 (index.ts:627): `nonAbsentCount = players.filter(p => !p.isAbsent && p.chips > 0).length`
- **isDisconnected 필터가 없어** disconnect된 플레이어도 투표 필요 인원에 포함됨.
- disconnect된 플레이어는 투표할 수 없어 영원히 전원 투표 조건 미달.

### 기존 인프라 (활용 가능)
- `isDisconnected` 필드: PlayerState에 이미 정의 (shared/types/game.ts:76)
- `forceDisconnectedPlayerAction()`: isDisconnected=true 마킹 + 베팅 차례면 auto-die
- 60초 타이머: index.ts:879에서 이미 구현 (방 퇴장 처리)
- `takeBreak()`: result phase에서 isAbsent=true 설정 (참고용)
</context>

<tasks>

<task type="auto">
  <name>Task 1: card-reveal phase disconnect 자동 카드 공개 처리</name>
  <files>packages/server/src/game-engine.ts</files>
  <action>
`forceDisconnectedPlayerAction()` 메서드를 수정하여 card-reveal phase도 처리한다.

현재 코드 (game-engine.ts:1924-1937):
```typescript
forceDisconnectedPlayerAction(playerId: string): void {
    const player = this.state.players.find(p => p.id === playerId);
    if (!player) return;
    player.isDisconnected = true;
    if (!player.isAlive) return;
    const currentPlayer = this.state.players.find(p => p.seatIndex === this.state.currentPlayerIndex);
    if (currentPlayer?.id === playerId && GameEngine.BETTING_PHASES.includes(this.state.phase)) {
      try { this.processBetAction(playerId, { type: 'die' }); } catch { /* no-op */ }
    }
}
```

수정 내용 - isDisconnected 마킹 후, 기존 베팅 분기 아래에 card-reveal 분기를 추가:

```typescript
// card-reveal phase: disconnect된 플레이어의 카드를 자동으로 전체 공개 처리
if (this.state.phase === 'card-reveal' && player.isAlive && !player.isRevealed) {
  // 모든 카드 인덱스를 revealedCardIndices에 추가
  const totalCards = (player as any).selectedCards?.length === 2
    ? 2
    : player.cards.filter(c => c != null).length;
  player.revealedCardIndices = Array.from({ length: totalCards }, (_, i) => i);
  player.isRevealed = true;

  // 모든 생존 플레이어가 공개 완료되었는지 확인 → showdown 트리거
  const alivePlayers = this.state.players.filter(p => p.isAlive);
  const allFullyRevealed = alivePlayers.every(p => p.isRevealed);
  if (allFullyRevealed) {
    const strategy = this.getModeStrategy();
    strategy.showdown(this, this.state);
  }
}
```

추가로, `muck-or-reveal` phase에서도 동일 패턴으로 disconnect 처리:
```typescript
// muck-or-reveal phase (전원 다이): disconnect된 플레이어는 자동 공개(reveal) 처리
if (this.state.phase === 'muck-or-reveal' && player.isAlive && !player.isRevealed) {
  player.isRevealed = true;
  // revealHand 로직과 동일 — 생존자 1명이면 즉시 정산, 전원 공개면 showdown
  const alivePlayers = this.state.players.filter(p => p.isAlive);
  if (alivePlayers.length === 1) {
    this.state.winnerId = alivePlayers[0].id;
    const chipsSnapshot = new Map(this.state.players.map(p => [p.id, p.chips]));
    this.settleChipsWithAllIn();
    this._settleTtaengValue();
    this._generateRoundHistory(chipsSnapshot);
    this.state.phase = 'result';
  } else {
    const allRevealed = alivePlayers.every(p => p.isRevealed);
    if (allRevealed) {
      this._resolveShowdown();
    }
  }
}
```
  </action>
  <verify>
    <automated>cd /d/dev/sutda && npx vitest run packages/server/src/game-engine.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
- card-reveal phase에서 disconnect 시 해당 플레이어 카드 자동 전체 공개
- 전원 공개 완료 시 자동 showdown 트리거
- muck-or-reveal phase에서도 동일하게 자동 공개
- 기존 테스트 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: result phase disconnect 시 즉시 투표 통과 + 자동 자리비움</name>
  <files>packages/server/src/index.ts</files>
  <action>
두 가지 수정이 필요하다.

### 수정 A: next-round 투표에서 disconnect 플레이어 제외

index.ts의 `next-round` 이벤트 핸들러 (line ~627):

현재:
```typescript
const nonAbsentCount = state.players.filter(p => !p.isAbsent && p.chips > 0).length;
```

수정:
```typescript
const nonAbsentCount = state.players.filter(p => !p.isAbsent && !p.isDisconnected && p.chips > 0).length;
```

이 한 줄 변경으로, disconnect된 플레이어는 투표 필요 인원에서 제외되어 나머지 플레이어만으로 다음 판 진행 가능.

### 수정 B: disconnect 이벤트에서 result phase도 즉시 처리

index.ts의 disconnect 핸들러 (line ~860 부근), `forceDisconnectedPlayerAction` 호출 후에 result phase 관련 로직 추가:

```typescript
// 즉시: 베팅 차례면 자동 다이 처리
const engine = gameEngines.get(roomId);
if (engine) {
  try { engine.forceDisconnectedPlayerAction(disconnectedPlayerId); } catch { /* no-op */ }

  // result phase: disconnect된 플레이어를 투표 불필요 처리 후,
  // 이미 투표한 인원이 충분하면 즉시 다음 판 진행
  const engineState = engine.getState();
  if (engineState.phase === 'result') {
    const votes = nextRoundVotes.get(roomId);
    if (votes) {
      const nonAbsentConnectedCount = engineState.players.filter(
        p => !p.isAbsent && !p.isDisconnected && p.chips > 0
      ).length;
      if (votes.size >= nonAbsentConnectedCount) {
        // 기존 next-round 전원 투표 완료 로직과 동일하게 진행
        // → 이 부분은 next-round 핸들러의 투표 완료 분기를 함수로 추출하여 호출하거나,
        //   단순히 next-round-votes 이벤트를 re-emit하여 기존 로직 재활용
        // 가장 안전한 방법: 투표 현황 브로드캐스트만 하고, 
        // 클라이언트가 자동 재투표하지 않아도 서버에서 자동 처리되게 함
        // → 실제로는 아래처럼 next-round 핸들러 내부 로직을 직접 트리거
      }
    }
  }

  // 즉시 게임 상태 broadcast (per-player emit)
  ...
```

**중요: 리팩터링 접근** - next-round 핸들러의 "전원 투표 완료 시 다음 판 시작" 로직(line 628-710)을 별도 함수 `tryAdvanceNextRound(roomId: string)` 로 추출한다. 그러면:

1. `next-round` 이벤트 핸들러: 투표 추가 후 `tryAdvanceNextRound(roomId)` 호출
2. `disconnect` 핸들러: result phase일 때 `tryAdvanceNextRound(roomId)` 호출
3. `take-break` 핸들러: 자리비움 후 `tryAdvanceNextRound(roomId)` 호출 (line 751 부근, 이미 유사 로직 있음)

`tryAdvanceNextRound` 함수 시그니처:
```typescript
function tryAdvanceNextRound(roomId: string): void {
  const engine = gameEngines.get(roomId);
  if (!engine) return;
  const state = engine.getState();
  if (state.phase !== 'result') return;

  const votes = nextRoundVotes.get(roomId);
  if (!votes) return;

  // disconnect + absent 제외한 투표 필요 인원
  const requiredCount = state.players.filter(
    p => !p.isAbsent && !p.isDisconnected && p.chips > 0
  ).length;

  if (votes.size < requiredCount) return;

  // === 기존 line 629-710의 전원 투표 완료 로직을 여기로 이동 ===
  nextRoundVotes.delete(roomId);
  schoolResponded.delete(roomId);
  // ... (칩 0 강퇴, observer 합류, engine.nextRound(), per-player emit 등)
}
```

### 수정 C: disconnect 핸들러 - card-reveal/result phase 게임 상태 즉시 브로드캐스트

disconnect 핸들러의 `forceDisconnectedPlayerAction` 호출 후 게임 상태 브로드캐스트 코드는 이미 존재한다 (line 865-870). card-reveal에서 자동 showdown이 트리거되면 phase가 result로 변경되므로, 기존 브로드캐스트 코드가 올바른 상태를 전달한다. 추가 수정 불필요.

단, result phase 진입 직후 `tryAdvanceNextRound`를 호출해야 하므로, 브로드캐스트 후에 호출:

```typescript
if (engine) {
  try { engine.forceDisconnectedPlayerAction(disconnectedPlayerId); } catch { /* no-op */ }
  // 즉시 게임 상태 broadcast
  (async () => {
    const sockets2 = await io.in(roomId).fetchSockets();
    for (const s of sockets2) {
      s.emit('game-state', engine.getStateFor(s.data.playerId) as GameState);
    }
    // result phase가 되었으면 (card-reveal → showdown → result 자동 전환 포함)
    // 투표 현황 재계산하여 즉시 다음 판 가능 여부 확인
    tryAdvanceNextRound(roomId);
  })();
}
```
  </action>
  <verify>
    <automated>cd /d/dev/sutda && npx vitest run packages/server/src/index.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
- result phase에서 disconnect 시 해당 플레이어가 투표 필요 인원에서 제외됨
- 이미 나머지 전원이 투표했다면 즉시 다음 판 진행
- card-reveal → showdown → result 자동 전환 후에도 투표 재계산 트리거
- disconnect 후 60초 타이머(기존)로 방 퇴장 처리 유지
- 기존 테스트 통과
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run packages/server/src/game-engine.test.ts` — 기존 엔진 테스트 통과
2. `npx vitest run packages/server/src/index.test.ts` — 기존 소켓 테스트 통과
3. 수동 검증: 2인 게임에서 card-reveal 단계 진입 후 한 명 탭 닫기 → 나머지 플레이어가 결과 화면 정상 진입 확인
4. 수동 검증: result 단계에서 한 명 탭 닫기 → 나머지 플레이어가 "다음 판" 버튼으로 즉시 진행 가능 확인
</verification>

<success_criteria>
- card-reveal phase에서 disconnect 시 게임이 멈추지 않고 자동으로 showdown 진행
- result phase에서 disconnect 시 나머지 플레이어만으로 다음 판 투표 가능
- 60초 후 disconnect된 플레이어 자동 방 퇴장 (기존 동작 유지)
- 기존 테스트 전체 통과
</success_criteria>

<output>
완료 후 `.planning/quick/260405-l5b-disconnect-1/260405-l5b-SUMMARY.md` 생성
</output>
