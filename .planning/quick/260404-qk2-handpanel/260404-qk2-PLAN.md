---
phase: quick-260404-qk2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/layout/HandPanel.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "HandPanel 손패 패널에 '나머지 카드를 탭하세요' 힌트 문구가 표시되지 않는다"
  artifacts:
    - path: "packages/client/src/components/layout/HandPanel.tsx"
      provides: "힌트 스팬 제거된 HandPanel"
  key_links: []
---

<objective>
HandPanel.tsx에서 "나머지 카드를 탭하세요" 힌트 스팬 제거

Purpose: 모바일에서 손패 패널 레이아웃을 깔끔하게 유지
Output: 힌트 문구가 제거된 HandPanel.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/client/src/components/layout/HandPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: HandPanel 힌트 스팬 제거</name>
  <files>packages/client/src/components/layout/HandPanel.tsx</files>
  <action>
HandPanel.tsx 206-208번 줄의 다음 JSX 블록을 삭제한다:

```tsx
{flippedIndices.size === 1 && cards.length >= 2 && (
  <span className="text-[10px] text-muted-foreground">나머지 카드를 탭하세요</span>
)}
```

이 블록은 Row 3 div 내부, handLabel Badge 바로 뒤에 위치한다.
삭제 후 주변 JSX 구조(Row 3 div, 족보 참고표 버튼, Badge)는 그대로 유지한다.

만약 flippedIndices가 이 블록 제거 후 더 이상 사용되지 않는 경우에도, flippedIndices는 다른 곳에서도 사용되므로 import/선언은 유지한다.
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && grep -n "나머지 카드를 탭하세요" packages/client/src/components/layout/HandPanel.tsx; echo "EXIT:$?"</automated>
  </verify>
  <done>grep 결과가 빈 문자열이고 EXIT:1 (매치 없음) — 힌트 문구가 완전히 제거됨</done>
</task>

</tasks>

<verification>
- HandPanel.tsx에서 "나머지 카드를 탭하세요" 문자열이 검색되지 않음
- TypeScript 빌드 에러 없음: `pnpm --filter client build` 성공
</verification>

<success_criteria>
- 힌트 스팬이 HandPanel에서 완전히 제거됨
- 클라이언트 빌드가 정상적으로 완료됨
</success_criteria>

<output>
완료 후 `.planning/quick/260404-qk2-handpanel/260404-qk2-SUMMARY.md` 생성
</output>
