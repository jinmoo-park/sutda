---
phase: quick
plan: 260404-i8w
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/game-engine.ts
  - packages/shared/src/types/game.ts
  - packages/server/src/index.ts
  - packages/client/src/components/layout/ResultScreen.tsx
  - packages/client/src/pages/RoomPage.tsx
autonomous: true
must_haves:
  truths:
    - "결과 페이지 진입 시 모든 카드가 뒷면으로 표시됨"
    - "플레이어가 자기 카드를 클릭하면 한장씩 앞면으로 공개됨"
    - "모든 플레이어의 모든 카드가 공개된 후에만 승패 결과가 노출됨"
    - "학교가기/학교대신가기 버튼은 승패 확정 후에만 표시됨"
    - "전원 다이 시 기존 showdown muck/reveal 플로우가 유지됨"
  artifacts:
    - path: "packages/shared/src/types/game.ts"
      provides: "card-reveal GamePhase 추가"
      contains: "card-reveal"
    - path: "packages/server/src/game-engine.ts"
      provides: "card-reveal phase 전환 + revealMyCard 메서드"
    - path: "packages/client/src/components/layout/ResultScreen.tsx"
      provides: "카드 뒤집기 UI + 승패 지연 표시"
  key_links:
    - from: "ResultScreen.tsx"
      to: "server index.ts reveal-my-card"
      via: "socket emit"
      pattern: "reveal-my-card"
    - from: "game-engine.ts card-reveal"
      to: "strategy.showdown()"
      via: "allRevealed 체크 후 자동 showdown"
---

<objective>
결과 공개 플로우를 수정하여, 베팅 종료 후 즉시 승패가 보이는 대신 모든 플레이어가 자기 카드를 한장씩 클릭해 뒤집는 긴장감 있는 공개 단계를 추가한다.

Purpose: 섯다의 핵심 재미인 "패 까기" 긴장감 구현
Output: card-reveal phase가 추가된 게임 플로우
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/shared/src/types/game.ts (GamePhase, PlayerState 타입)
@packages/server/src/game-engine.ts (FSM, _advanceBettingTurn, revealCard, showdown)
@packages/server/src/index.ts (소켓 핸들러: reveal-card, muck-hand)
@packages/client/src/components/layout/ResultScreen.tsx (결과 화면)
@packages/client/src/pages/RoomPage.tsx (showdown 분기, MuckChoiceModal)

<interfaces>
<!-- 현재 핵심 타입/메서드 -->

From packages/shared/src/types/game.ts:
```typescript
export type GamePhase =
  | 'waiting' | 'dealer-select' | 'attend-school' | 'mode-select'
  | 'shuffling' | 'cutting' | 'dealing' | 'betting'
  | 'betting-1' | 'betting-2' | 'card-select' | 'dealing-extra'
  | 'shared-card-select'
  | 'showdown' | 'result'
  | 'gusa-pending' | 'rematch-pending';

export interface PlayerState {
  isRevealed: boolean;    // showdown에서 패를 공개했는지
  cards: (Card | null)[];
  // ...
}
```

From packages/server/src/game-engine.ts:
```typescript
// 베팅 완료 시 자동 showdown (line 1353-1357):
this.state.players.filter(p => p.isAlive).forEach(p => { p.isRevealed = true; });
const strategy = this.getModeStrategy();
strategy.showdown(this, this.state);

// 생존자 1명일 때 showdown phase (line 1299-1304):
this.state.phase = 'showdown';  // 승자가 공개/숨기기 선택

// revealCard(playerId) — showdown phase 전용 (line 1414)
// muckHand(playerId) — showdown phase 전용 (line 1449)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 서버 — card-reveal phase 추가 및 개별 카드 공개 메서드 구현</name>
  <files>
    packages/shared/src/types/game.ts
    packages/server/src/game-engine.ts
    packages/server/src/index.ts
  </files>
  <action>
**1. GamePhase에 'card-reveal' 추가 (shared/src/types/game.ts)**

GamePhase union type에 `'card-reveal'`을 추가한다.

**2. PlayerState에 revealedCardIndices 추가 (shared/src/types/game.ts)**

PlayerState 인터페이스에 optional 필드 추가:
```typescript
revealedCardIndices?: number[];  // card-reveal phase에서 공개된 카드 인덱스 목록
```

**3. game-engine.ts — 베팅 완료 시 card-reveal phase로 전환**

`_advanceBettingTurn()` 에서 기존 자동 showdown 로직 (line ~1353-1357) 변경:

기존:
```typescript
this.state.players.filter(p => p.isAlive).forEach(p => { p.isRevealed = true; });
const strategy = this.getModeStrategy();
strategy.showdown(this, this.state);
```

변경:
```typescript
// 자동 showdown 대신 card-reveal phase로 전환 — 플레이어가 직접 카드 공개
this.state.players.filter(p => p.isAlive).forEach(p => {
  p.isRevealed = false;
  p.revealedCardIndices = [];
});
this.state.phase = 'card-reveal';
```

같은 로직이 `completeSejangCardSelect()` 등 세장섯다 card-select 완료 후에도 있을 수 있으므로, 모든 자동 showdown 진입점에서 동일하게 변경한다. 구체적으로:
- `_advanceBettingTurn()`의 betting/betting-2 완료 분기 (line ~1353)
- `completeSejangCardSelect()`에서 showdown 진입 분기 (있다면)

**주의:** 생존자 1명(전원 다이) 상황의 `showdown` phase 전환 (line ~1304)은 변경하지 않는다 — 기존 muck/reveal 선택 플로우 유지.

**4. game-engine.ts — revealMyCard 메서드 추가**

```typescript
/**
 * card-reveal phase에서 플레이어가 자신의 카드 한장을 공개
 * - 자기 카드만 공개 가능
 * - 모든 생존 플레이어의 모든 카드가 공개되면 자동으로 showdown 진행
 */
revealMyCard(playerId: string, cardIndex: number): void {
  if (this.state.phase !== 'card-reveal') {
    throw new Error('INVALID_PHASE');
  }

  const player = this.state.players.find(p => p.id === playerId);
  if (!player || !player.isAlive) {
    throw new Error('INVALID_ACTION');
  }

  // 유효한 카드 인덱스 확인
  const maxIndex = player.cards.length - 1;
  if (cardIndex < 0 || cardIndex > maxIndex || player.cards[cardIndex] == null) {
    throw new Error('INVALID_CARD_INDEX');
  }

  // 이미 공개된 카드인지 확인
  if (!player.revealedCardIndices) player.revealedCardIndices = [];
  if (player.revealedCardIndices.includes(cardIndex)) {
    return; // 중복 공개 무시 (에러 아님)
  }

  player.revealedCardIndices.push(cardIndex);

  // 해당 플레이어의 모든 카드가 공개되었는지 확인
  // 세장섯다: selectedCards 기준 2장, 그 외: cards 배열 길이
  const totalCards = player.selectedCards?.length === 2 ? 2 : player.cards.filter(c => c != null).length;
  if (player.revealedCardIndices.length >= totalCards) {
    player.isRevealed = true;
  }

  // 모든 생존 플레이어가 완전 공개되었는지 확인
  const alivePlayers = this.state.players.filter(p => p.isAlive);
  const allFullyRevealed = alivePlayers.every(p => p.isRevealed);

  if (allFullyRevealed) {
    // 기존 showdown 로직 실행 (Strategy 위임)
    const strategy = this.getModeStrategy();
    strategy.showdown(this, this.state);
  }
}
```

**5. game-engine.ts — getStateFor 업데이트**

`getStateFor()` 메서드에서 인디언 모드 `isResultPhase` 체크에 `'card-reveal'` 추가:
```typescript
const isResultPhase = phase === 'result' || phase === 'showdown' || phase === 'card-reveal';
```
card-reveal phase에서는 인디언 마스킹 없이 실제 카드 데이터를 보내야 하므로 (클라이언트가 revealedCardIndices로 표시 제어).

**6. game-engine.ts — nextRound/startRematch 등에서 revealedCardIndices 초기화**

`nextRound()`, `_startRematch()`, `_startGusaRematchImmediate()` 등 라운드 리셋 메서드에서:
```typescript
p.isRevealed = false;
p.revealedCardIndices = undefined;  // 추가
```

**7. index.ts — reveal-my-card 소켓 핸들러 추가**

기존 `reveal-card` 핸들러 근처에 추가:
```typescript
socket.on('reveal-my-card', ({ roomId, cardIndex }) => {
  handleGameAction(socket, roomId, () => {
    getEngine(roomId).revealMyCard(socket.data.playerId, cardIndex as number);
  });
});
```
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && npx turbo run build --filter=@sutda/shared --filter=@sutda/server 2>&1 | tail -5</automated>
  </verify>
  <done>
    - GamePhase에 'card-reveal' 추가됨
    - PlayerState에 revealedCardIndices 필드 추가됨
    - 베팅 완료 시 card-reveal phase로 전환 (기존 자동 showdown 대체)
    - revealMyCard() 메서드가 card-reveal phase에서 동작
    - 모든 카드 공개 시 자동으로 strategy.showdown() 호출
    - 전원 다이 showdown 플로우는 변경 없음
    - reveal-my-card 소켓 핸들러 등록됨
  </done>
</task>

<task type="auto">
  <name>Task 2: 클라이언트 — ResultScreen 카드 공개 인터랙션 및 승패 지연 표시</name>
  <files>
    packages/client/src/components/layout/ResultScreen.tsx
    packages/client/src/pages/RoomPage.tsx
  </files>
  <action>
**1. RoomPage.tsx — card-reveal phase 라우팅**

RoomPage에서 phase에 따른 화면 분기 로직에서 `'card-reveal'` phase도 ResultScreen을 렌더하도록 추가한다. 기존 `'result'` phase와 동일하게 ResultScreen 컴포넌트를 표시하되, gameState를 그대로 전달한다.

구체적으로 phase 분기 조건에서:
```typescript
// 기존: phase === 'result'
// 변경: phase === 'result' || phase === 'card-reveal'
```

**2. ResultScreen.tsx — card-reveal / result 이중 모드 구현**

ResultScreen이 `gameState.phase`를 직접 읽어 두 모드를 구분한다:

**card-reveal 모드 (phase === 'card-reveal'):**
- 승자 제목 (`{winnerNickname} 승리!`) 숨김 — 아직 승자 미결정
- 대신 "패를 공개하세요!" 같은 안내 메시지 표시
- 각 플레이어 카드 표시 시:
  - `player.revealedCardIndices`에 포함된 카드만 faceUp={true}로 표시
  - 나머지는 faceUp={false} (뒷면)
  - **내 카드 클릭 핸들러:** 내 플레이어의 아직 공개 안 된 카드를 클릭하면 `socket.emit('reveal-my-card', { roomId, cardIndex })` 전송
  - 다른 플레이어의 카드는 클릭 불가 (cursor-default)
  - 내 카드 중 미공개 카드에는 클릭 유도 시각 힌트 (예: `animate-pulse` 또는 `ring-2 ring-primary` border)
- 칩 변동 배지, 족보 이름 배지 — 숨김 (승패 미결정)
- 하단 버튼 영역 — 모두 숨김 (학교가기, 쉬기, 대신가기 등)

**result 모드 (phase === 'result'):**
- 기존 동작 그대로 유지
- 모든 카드 faceUp={true}
- 승자 제목, 칩 변동, 족보 배지, 하단 버튼 모두 표시

**카드 렌더링 로직 변경 (card-reveal 모드):**

기존 displayCards.map 부분을 수정:
```tsx
const isCardRevealPhase = gameState.phase === 'card-reveal';
const isMe = player.id === myPlayerId;
const revealedIndices = player.revealedCardIndices ?? [];

// card-reveal phase에서 카드 표시
{displayCards.map((card, idx) => {
  const isCardRevealed = isCardRevealPhase ? revealedIndices.includes(idx) : player.isRevealed;
  const canClick = isCardRevealPhase && isMe && !revealedIndices.includes(idx) && player.isAlive;

  return (
    <div
      key={idx}
      onClick={canClick ? () => socket?.emit('reveal-my-card', { roomId, cardIndex: idx }) : undefined}
      className={canClick ? 'cursor-pointer animate-pulse' : ''}
    >
      <HwatuCard
        card={isCardRevealed ? card! : undefined}
        faceUp={isCardRevealed}
        size={cardSize}
      />
    </div>
  );
})}
```

**다이한 플레이어 카드:** card-reveal phase에서도 기존과 동일하게 뒷면으로 표시 (isDied 분기 유지).

**족보 배지:** card-reveal phase에서는 플레이어의 모든 카드가 공개된 경우(`player.isRevealed === true`)에만 해당 플레이어의 족보를 표시한다.

**3. ResultScreen.tsx — roomId prop 활용**

소켓 emit에 roomId가 필요하므로 기존 props의 roomId를 활용한다 (이미 존재함).

**4. 세장섯다 호환:**

세장섯다의 경우 `player.selectedCards?.length === 2`이면 selectedCards를 displayCards로 사용하는 기존 로직 유지. revealedCardIndices는 displayCards 기준 인덱스(0, 1)로 동작한다.
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && npx turbo run build --filter=@sutda/client 2>&1 | tail -5</automated>
  </verify>
  <done>
    - card-reveal phase에서 ResultScreen이 렌더됨
    - 모든 카드가 초기에 뒷면으로 표시됨
    - 자기 카드 클릭 시 한장씩 공개됨 (reveal-my-card emit)
    - 미공개 내 카드에 시각적 클릭 유도 힌트 표시
    - 다른 플레이어 카드는 서버에서 revealedCardIndices 업데이트될 때 자동 반영
    - 승패 정보(제목, 칩변동, 족보)는 result phase에서만 표시
    - 하단 버튼(학교가기 등)은 result phase에서만 표시
    - 전원 다이 showdown의 MuckChoiceModal은 기존 그대로 동작
  </done>
</task>

</tasks>

<verification>
1. 2인 오리지날 모드: 베팅 완료 → card-reveal phase 진입 → 카드 뒷면 확인 → 각자 클릭으로 공개 → 모두 공개 시 승패 표시 + 버튼 노출
2. 한 플레이어만 카드 공개: 상대방이 아직 미공개면 승패 미노출 확인
3. 전원 다이: 기존 showdown phase → MuckChoiceModal 공개/숨기기 선택 동작 확인
4. 세장섯다: card-select 완료 후 card-reveal → 선택된 2장 기준 공개 동작 확인
5. 3인 이상: 모든 생존 플레이어가 모든 카드를 공개해야 승패 결정됨
</verification>

<success_criteria>
- 베팅 종료 후 card-reveal phase에서 모든 카드가 뒷면으로 시작
- 플레이어가 자기 카드를 클릭해 한장씩 공개 가능
- 모든 생존 플레이어의 모든 카드 공개 시 자동으로 승패 판정 + result phase 전환
- 학교가기/대신가기 버튼은 result phase에서만 노출
- 전원 다이 시 showdown muck/reveal 기존 플로우 유지
- 빌드 에러 없음
</success_criteria>

<output>
완료 후 `.planning/quick/260404-i8w-card-reveal-flow/260404-i8w-SUMMARY.md` 생성
</output>
