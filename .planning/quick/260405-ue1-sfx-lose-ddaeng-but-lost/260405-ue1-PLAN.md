---
phase: quick
plan: 260405-ue1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/layout/ResultScreen.tsx
autonomous: true
must_haves:
  truths:
    - "족보 한 단계 차이로 승패 결정 시 승자/패자 모두에게 lose-ddaeng-but-lost SFX 재생"
    - "기존 ttaeng 패배 SFX(lose-ddaeng-but-lost)와 중복 재생 안됨"
    - "기존 SFX 로직(win-ddaeng, lose-normal, lose-ddaeng-penalty 등) 정상 동작 유지"
  artifacts:
    - path: "packages/client/src/components/layout/ResultScreen.tsx"
      provides: "SCORE_RANK_ORDER 배열 + isOneRankApart 헬퍼 + 승자/패자 분기 한단계차이 체크"
      contains: "isOneRankApart"
  key_links:
    - from: "ResultScreen.tsx isOneRankApart"
      to: "evaluateHand().score"
      via: "SCORE_RANK_ORDER 배열 인덱스 비교"
      pattern: "isOneRankApart.*score"
---

<objective>
ResultScreen.tsx에서 족보 한 단계 차이로 승패가 결정될 때 lose-ddaeng-but-lost SFX가 재생되지 않는 버그 수정

## 근본 원인
커밋 `5eb3299`(260405-s6a 퀵태스크)에서 `SCORE_RANK_ORDER` + `isOneRankApart()` 로직을 복원했으나,
바로 다음 커밋 `032b0b0`(260405-sgp-01: player-disconnected/reconnected 이벤트 추가)에서
ResultScreen.tsx 전체를 덮어쓰면서 해당 로직이 **세 번째로 삭제됨**.

삭제 이력:
1. `a6bb668` UI 정규화에서 삭제
2. `5eb3299`에서 복원
3. `032b0b0`에서 다시 삭제 (현재 상태)

## 수정 방향
`isOneRankApart` 로직을 ResultScreen.tsx에 다시 복원한다.
이번에는 재삭제 방지를 위해 로직을 별도 유틸 함수로 분리하는 것도 고려하되,
ResultScreen.tsx 내 인라인이 가장 간결하므로 인라인으로 복원하고 주석으로 목적을 명확히 한다.

Output: 수정된 ResultScreen.tsx
</objective>

<context>
@packages/client/src/components/layout/ResultScreen.tsx
@.planning/quick/260405-s6a-lose-ddaeng-but-lost-sfx/260405-s6a-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ResultScreen.tsx에 isOneRankApart 로직 복원 (3차)</name>
  <files>packages/client/src/components/layout/ResultScreen.tsx</files>
  <action>
  커밋 `5eb3299`에서 추가된 코드와 동일한 로직을 현재 ResultScreen.tsx에 복원한다.
  `git diff 5eb3299..032b0b0 -- packages/client/src/components/layout/ResultScreen.tsx` 결과를 참고하여,
  삭제된 부분만 정확히 되살린다.

  **복원할 코드 3가지:**

  ### 1. 파일 상단 (import 아래, mdQuery 위)에 상수 + 헬퍼 추가:
  ```typescript
  /** 족보 score 순서 배열 -- 한 단계 차이 판별용 (삭제 금지: SFX lose-ddaeng-but-lost 트리거) */
  const SCORE_RANK_ORDER = [
    1300, 1200, 1100, // 광땡
    1010, 1009, 1008, 1007, 1006, 1005, 1004, 1003, 1002, 1001, // 땡
    60, 50, 40, 30, 20, 10, // 특수조합
    9, 8, 7, 6, 5, 4, 3, 2, 1, 0, // 끗
  ];

  function isOneRankApart(scoreA: number, scoreB: number): boolean {
    const idxA = SCORE_RANK_ORDER.indexOf(scoreA);
    const idxB = SCORE_RANK_ORDER.indexOf(scoreB);
    if (idxA === -1 || idxB === -1) return false;
    return Math.abs(idxA - idxB) === 1;
  }
  ```

  ### 2. SFX useEffect 내부, `stop('card-reveal');` 바로 뒤에 winnerCardsVisible/winnerHandCards 변수를 iAmWinner 분기 **상위**로 이동:
  현재 코드에서 `const winnerCardsVisible`과 `const winnerHandCards`는 패자 분기 내부(line ~154)에 있다.
  이 두 변수 선언을 `stop('card-reveal');` 바로 아래, `if (iAmWinner)` 바로 위로 이동한다:
  ```typescript
  stop('card-reveal');

  // 승자 카드 공개 여부 + 승자 핸드카드 (승자/패자 모두 사용)
  const winnerCardsVisible = gameState.mode === 'original' ? (winner?.isRevealed ?? false) : !!winner;
  const winnerHandCards = winner ? getHandCards(winner) : [];

  if (iAmWinner) {
  ```

  ### 3. 승자 분기 끝 (play('win-normal') 후, `} else {` 전)에 한 단계 차이 체크 추가:
  ```typescript
      // 한 단계 차이 체크 -- 승자에게도 lose-ddaeng-but-lost 추가 재생
      if (myHandCards.length >= 2) {
        try {
          const myResult = evaluateHand(myHandCards[0]!, myHandCards[1]!);
          const loserScores = gameState.players
            .filter(p => p.id !== myPlayerId && p.isAlive)
            .map(p => {
              const cards = getHandCards(p);
              if (cards.length < 2) return -1;
              try { return evaluateHand(cards[0]!, cards[1]!).score; } catch { return -1; }
            })
            .filter(s => s >= 0)
            .sort((a, b) => b - a);
          const topLoserScore = loserScores[0] ?? -1;
          if (topLoserScore >= 0 && isOneRankApart(myResult.score, topLoserScore)) {
            play('lose-ddaeng-but-lost');
          }
        } catch { /* 평가 실패 시 무시 */ }
      }
  ```

  ### 4. 패자 분기 끝 (win-ddaeng-loser 블록 이후, 닫는 `}` 전)에 한 단계 차이 체크 추가:
  ```typescript
      // 한 단계 차이 체크 -- 패자에게도 lose-ddaeng-but-lost 추가 재생
      if (winnerCardsVisible && myHandCards.length >= 2 && winnerHandCards.length >= 2) {
        try {
          const myResult = evaluateHand(myHandCards[0]!, myHandCards[1]!);
          const winnerResult = evaluateHand(winnerHandCards[0]!, winnerHandCards[1]!);
          if (isOneRankApart(winnerResult.score, myResult.score)) {
            // 이미 lose-ddaeng-but-lost가 재생된 경우(ttaeng 패배) 중복 방지
            if (!hasDdaengPenalty) {
              const isDdaeng = myResult.handType.includes('ttaeng');
              if (!isDdaeng) {
                play('lose-ddaeng-but-lost');
              }
            }
          }
        } catch { /* 평가 실패 시 무시 */ }
      }
  ```

  **주의:** 패자 분기에서 `winnerHandCards`를 참조하므로, 인라인 선언(`const winnerHandCards = getHandCards(winner)`)을 제거하고
  상위로 이동한 `winnerHandCards` 변수를 사용해야 한다. 현재 패자 분기 내부의 `const winnerCardsVisible`과
  `const winnerHandCards = getHandCards(winner)` 인라인 선언도 제거한다 (상위 변수로 대체).
  </action>
  <verify>
    <automated>cd D:/dev/sutda && npx tsc --noEmit --project packages/client/tsconfig.json 2>&1 | grep -i "ResultScreen" || echo "ResultScreen 관련 에러 없음"</automated>
  </verify>
  <done>
  - ResultScreen.tsx에 SCORE_RANK_ORDER + isOneRankApart 존재
  - 승자 분기: 패자 최고 score와 한 단계 차이 시 lose-ddaeng-but-lost 재생
  - 패자 분기: 승자 score와 한 단계 차이 + ttaeng 패배 아닐 때 lose-ddaeng-but-lost 재생
  - TypeScript 컴파일 에러 없음
  </done>
</task>

</tasks>

<verification>
- `grep -n "isOneRankApart" packages/client/src/components/layout/ResultScreen.tsx` 로 함수 존재 확인
- `grep -n "SCORE_RANK_ORDER" packages/client/src/components/layout/ResultScreen.tsx` 로 배열 존재 확인
- `grep -c "lose-ddaeng-but-lost" packages/client/src/components/layout/ResultScreen.tsx` 로 3회 이상 출현 확인 (기존 ttaeng 패배 1회 + 승자 분기 1회 + 패자 분기 1회)
- TypeScript 컴파일 성공
</verification>

<success_criteria>
- isOneRankApart 로직이 ResultScreen.tsx에 복원됨
- 족보 한 단계 차이 승패 시 승자/패자 모두에게 lose-ddaeng-but-lost SFX 트리거
- 기존 SFX 분기(win-ddaeng, lose-normal, lose-ddaeng-penalty, lose-ddaeng-but-lost(ttaeng)) 정상 동작
- 중복 재생 방지 로직 유지
</success_criteria>

<output>
완료 후 `.planning/quick/260405-ue1-sfx-lose-ddaeng-but-lost/260405-ue1-SUMMARY.md` 생성
</output>
