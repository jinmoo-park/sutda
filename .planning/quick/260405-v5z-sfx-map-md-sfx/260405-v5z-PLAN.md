---
phase: quick-260405-v5z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/sfx-map.md]
autonomous: true
requirements: [SFX-MAP-SYNC]
must_haves:
  truths:
    - "sfx-map.md의 모든 볼륨 값이 useSfxPlayer.ts SFX_MAP과 일치한다"
    - "lose-ddaeng-but-lost 트리거 조건이 isOneRankApart 포함 최신 로직을 반영한다"
    - "BGM 섹션이 추가되어 main_bgm, bgm_bigpot 정보를 포함한다"
    - "card-reveal SFX 정지 조건이 정확히 기술되어 있다"
  artifacts:
    - path: "docs/sfx-map.md"
      provides: "SFX/BGM 맵핑 최신 문서"
---

<objective>
docs/sfx-map.md를 현재 코드베이스(useSfxPlayer.ts, useBgmPlayer.ts, ResultScreen.tsx, RoomPage.tsx)와 완전히 일치하도록 최신화한다.

Purpose: SFX 설정 문서와 실제 코드 간 불일치를 해소하여 향후 유지보수/변경 시 정확한 참고 자료 제공
Output: 최신화된 docs/sfx-map.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/sfx-map.md
@packages/client/src/hooks/useSfxPlayer.ts
@packages/client/src/hooks/useBgmPlayer.ts
@packages/client/src/components/layout/ResultScreen.tsx (SFX 트리거 로직)
@packages/client/src/pages/RoomPage.tsx (card-reveal, deal, flip, giri SFX 트리거)
</context>

<tasks>

<task type="auto">
  <name>Task 1: sfx-map.md 코드 대비 불일치 수정 및 BGM 섹션 추가</name>
  <files>docs/sfx-map.md</files>
  <action>
아래 불일치 항목을 모두 수정한다. 반드시 현재 코드 파일을 읽어서 최종 확인 후 반영할 것.

## 볼륨 불일치 수정 (2건)
1. `win-normal` 볼륨: 0.6 → **0.2** (useSfxPlayer.ts line 16 확인)
2. `win-ddaeng-loser` 볼륨: 0.3 → **0.15** (useSfxPlayer.ts line 18 확인)

## 트리거 조건 수정 (2건)

3. `lose-ddaeng-but-lost` 트리거 조건 전면 재작성:
   현재 문서: "내가 패자이고 땡값은 없지만 내 패가 땡일 때"
   실제 코드(ResultScreen.tsx): 3가지 경우에 재생됨
   - (a) 승자: 한 단계 차이(isOneRankApart) — 승자의 score와 최상위 패자 score가 SCORE_RANK_ORDER에서 인접할 때 추가 재생
   - (b) 패자(기본): 땡인데 진 경우 (내 패가 ttaeng인데 패자)
   - (c) 패자(한 단계 차이): 승자 카드가 공개된 상태에서 승자-패자 score가 한 단계 차이이고, 땡값 납부도 아니고 내 패가 땡도 아닌 경우 추가 재생
   트리거 조건을 위 3가지 모두 기술한다.

4. `card-reveal` SFX 정지 조건 보완:
   현재 문서: "새 라운드 시작 phase 진입 시 정지"
   실제 코드: 두 곳에서 정지
   - RoomPage.tsx: `dealer-select`, `attend-school`, `mode-select`, `shuffling` phase 진입 시 stopSfx('card-reveal')
   - ResultScreen.tsx: result phase 진입 시 stop('card-reveal') (line 135)
   두 정지 지점을 모두 명시한다.

## BGM 섹션 신규 추가

5. "전체 SFX 목록" 테이블 아래에 `## BGM 목록` 섹션을 추가한다:

| BGM 키 | 파일명 | 볼륨 | 트리거 조건 | 트리거 위치 |
|--------|--------|------|------------|------------|
| main_bgm | main_bgm.mp3 | 0.1 | 방 입장 시 자동 재생 (loop) | useBgmPlayer.ts init() |
| bgm_bigpot | bgm_bigpot.mp3 | 0.25 | 팟 >= 20,000일 때 main_bgm 대신 전환 재생 (loop) | GameTable.tsx → setBigPot() / useBgmPlayer.ts |

6. "소스 파일 위치 요약" 테이블에 BGM 관련 항목 추가:
   - `packages/client/src/hooks/useBgmPlayer.ts` | BGM 재생/정지/뮤트/빅팟 전환 로직
   - `packages/client/src/components/layout/GameTable.tsx` | 빅팟 BGM 트리거 (pot >= 20000)

7. "원본 파일" 테이블에 BGM 파일 추가:
   - bgm_bigpot.mp3 → bgm_bigpot.mp3
   - main_bgm.mp3 → main_bgm.mp3

## 기타 확인 사항
- 나머지 SFX 항목(shuffle, giri, deal, flip, chip, bet-*, school-*)의 볼륨/트리거 조건이 코드와 일치하는지 확인. 불일치 발견 시 함께 수정.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const md=fs.readFileSync('docs/sfx-map.md','utf8'); const checks=['0.2','0.15','bgm_bigpot','main_bgm','isOneRankApart','setBigPot','20000','attend-school']; const missing=checks.filter(c=>!md.includes(c)); if(missing.length){console.error('MISSING:',missing);process.exit(1)}else{console.log('ALL CHECKS PASSED')}"</automated>
  </verify>
  <done>
    - win-normal 볼륨이 0.2로 표기됨
    - win-ddaeng-loser 볼륨이 0.15로 표기됨
    - lose-ddaeng-but-lost 트리거 조건에 isOneRankApart(한 단계 차이) 3가지 경우 모두 기술됨
    - card-reveal 정지 조건에 RoomPage(새 라운드 phase)와 ResultScreen(result phase) 두 지점 명시됨
    - BGM 목록 섹션이 추가되어 main_bgm(0.1), bgm_bigpot(0.25) 포함
    - 소스 파일 위치 요약에 useBgmPlayer.ts, GameTable.tsx 추가됨
    - 원본 파일 테이블에 BGM 파일 2건 추가됨
  </done>
</task>

</tasks>

<verification>
- docs/sfx-map.md의 모든 SFX 볼륨 값이 useSfxPlayer.ts SFX_MAP과 1:1 일치
- lose-ddaeng-but-lost 트리거 조건이 ResultScreen.tsx의 3가지 재생 경우를 모두 포함
- card-reveal 정지 조건이 2개 파일의 정지 로직을 모두 명시
- BGM 섹션이 존재하며 main_bgm, bgm_bigpot 정보 포함
</verification>

<success_criteria>
sfx-map.md가 현재 코드베이스의 SFX/BGM 설정과 완전히 일치하며, 자동 검증 스크립트가 통과한다.
</success_criteria>

<output>
완료 후 `.planning/quick/260405-v5z-sfx-map-md-sfx/260405-v5z-SUMMARY.md` 생성
</output>
