---
phase: quick
plan: 260405-s6a
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/layout/ResultScreen.tsx
autonomous: true
---

<objective>
인디언섯다/세장섯다에서 족보 한 단계 차이로 패배 시 lose-ddaeng-but-lost SFX가 재생되지 않는 버그 수정

배경: 260405-i6e 퀵태스크에서 `SCORE_RANK_ORDER` 배열 + `isOneRankApart()` 헬퍼를 추가하여 족보 한 단계 차이 패배 시 승자/패자 모두에게 `lose-ddaeng-but-lost` SFX를 재생하도록 구현했으나, 이후 `a6bb668` UI 정규화 커밋에서 해당 코드가 전부 삭제됨.

원인: `a6bb668 refactor(ui): ui-audit-260405` 커밋이 ResultScreen.tsx를 리팩토링하면서 i6e에서 추가한 `SCORE_RANK_ORDER`, `isOneRankApart()`, 승자/패자 분기의 한 단계 차이 체크 로직을 모두 제거함.

수정 방향: 삭제된 `isOneRankApart` 로직을 현재 코드에 복원. 현재 코드 구조(stop('card-reveal'), proxyBeneficiaryNicknames 등)는 유지하면서 한 단계 차이 체크만 재삽입.
</objective>

<context>
@packages/client/src/components/layout/ResultScreen.tsx
@.planning/quick/260405-i6e-lose-ddaeng-but-lost-sfx-2-bgm-bgm/260405-i6e-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ResultScreen.tsx에 isOneRankApart 로직 복원</name>
  <files>packages/client/src/components/layout/ResultScreen.tsx</files>
  <action>
1. 파일 상단(import 아래, mdQuery 위)에 다음 코드 복원:

```typescript
/** 족보 score 순서 배열 — 한 단계 차이 판별용 */
const SCORE_RANK_ORDER = [
  1300, 1200, 1100, // 광땡
  1010, 1009, 1008, 1007, 1006, 1005, 1004, 1003, 1002, 1001, // 땡
  60, 50, 40, 30, 20, 10, // 특수조합
  9, 8, 7, 6, 5, 4, 3, 2, 1, 0, // 끗
];

function isOneRankApart(scoreA: number, scoreB: number): boolean {
  const idxA = SCORE_RANK_ORDER.indexOf(scoreA);
  const idxB = SCORE_RANK_ORDER.indexOf(scoreB);
  if (idxA === -1 || idxB === -1) return false;
  return Math.abs(idxA - idxB) === 1;
}
```

2. result phase SFX useEffect 내부 (line ~98 `useEffect` 블록)에 한 단계 차이 로직 재삽입:

   **승자 분기** (iAmWinner === true 블록, 기존 win-ddaeng/win-normal 재생 후):
   - 패자(나를 제외한 생존 플레이어) 중 가장 높은 score를 찾음
   - `isOneRankApart(myResult.score, loserScore)` → true면 `play('lose-ddaeng-but-lost')` 추가 재생
   - winner를 찾기 위해 `getHandCards`와 `evaluateHand` 사용

   **패자 분기** (iAmWinner === false 블록, 기존 SFX 분기 실행 후):
   - 이미 `lose-ddaeng-but-lost`가 재생된 경우(isDdaeng 분기) 중복 재생 방지
   - 승자의 score와 내 score를 비교하여 `isOneRankApart` 체크
   - true이고 기존 SFX가 `lose-ddaeng-but-lost`가 아닌 경우에만 추가 `play('lose-ddaeng-but-lost')`

   구체적 코드 변경:
   - `stop('card-reveal')` 아래에 `winnerCardsVisible`과 `winnerHandCards` 변수 선언을 복원 (기존 코드에서 패자 분기 안에 있던 것을 상위로 이동):
     ```typescript
     const winnerCardsVisible = gameState.mode === 'original' ? (winner?.isRevealed ?? false) : !!winner;
     const winnerHandCards = winner ? getHandCards(winner) : [];
     ```
   - 주의: 기존 패자 분기 안의 `winnerCardsVisible`/`winnerHandCards` 선언은 상위로 이동시키므로 제거

   - iAmWinner 블록 끝에 추가:
     ```typescript
     // 한 단계 차이 체크 — 승자에게도 lose-ddaeng-but-lost 추가 재생
     if (myHandCards.length >= 2) {
       try {
         const myResult = evaluateHand(myHandCards[0]!, myHandCards[1]!);
         const loserScores = gameState.players
           .filter(p => p.id !== myPlayerId && p.isAlive)
           .map(p => {
             const cards = getHandCards(p);
             if (cards.length < 2) return -1;
             try { return evaluateHand(cards[0]!, cards[1]!).score; } catch { return -1; }
           })
           .filter(s => s >= 0)
           .sort((a, b) => b - a);
         const topLoserScore = loserScores[0] ?? -1;
         if (topLoserScore >= 0 && isOneRankApart(myResult.score, topLoserScore)) {
           play('lose-ddaeng-but-lost');
         }
       } catch { /* 평가 실패 시 무시 */ }
     }
     ```

   - 패자 분기에서 기존 SFX 재생 후, win-ddaeng-loser 블록 뒤에 추가:
     ```typescript
     // 한 단계 차이 체크 — 패자에게도 lose-ddaeng-but-lost 추가 재생
     if (winnerCardsVisible && myHandCards.length >= 2 && winnerHandCards.length >= 2) {
       try {
         const myResult = evaluateHand(myHandCards[0]!, myHandCards[1]!);
         const winnerResult = evaluateHand(winnerHandCards[0]!, winnerHandCards[1]!);
         if (isOneRankApart(winnerResult.score, myResult.score)) {
           // 이미 lose-ddaeng-but-lost가 재생된 경우(ttaeng 패배) 중복 방지
           if (!hasDdaengPenalty) {
             const isDdaeng = myResult.handType.includes('ttaeng');
             if (!isDdaeng) {
               play('lose-ddaeng-but-lost');
             }
           }
         }
       } catch { /* 평가 실패 시 무시 */ }
     }
     ```

3. 기존 패자 분기 내 `winnerCardsVisible`/`winnerHandCards` 로컬 변수 → 상위에서 선언한 것으로 대체 (중복 선언 제거)
  </action>
  <verify>
    <automated>cd D:/dev/sutda && npx tsc --noEmit --project packages/client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    - SCORE_RANK_ORDER 배열과 isOneRankApart 함수가 ResultScreen.tsx 상단에 복원됨
    - 승자 분기: 한 단계 차이 시 lose-ddaeng-but-lost 추가 재생
    - 패자 분기: 한 단계 차이 + 기존 SFX가 lose-ddaeng-but-lost가 아닌 경우에만 추가 재생
    - TypeScript 컴파일 에러 없음
    - 인디언섯다/세장섯다 모드에서도 result phase 진입 시 동일하게 동작 (getHandCards가 selectedCards/cards를 올바르게 처리)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 통과
- 인디언섯다/세장섯다에서 card-reveal 후 result 진입 시 SFX useEffect 정상 트리거 확인 (gameState.phase 의존성)
- 승자/패자 모두 한 단계 차이 시 lose-ddaeng-but-lost SFX 재생
- 오리지날 모드 기존 동작 유지 (ttaeng으로 패배 시 lose-ddaeng-but-lost 재생)
</verification>

<success_criteria>
- TypeScript 컴파일 성공
- isOneRankApart 로직이 모든 게임 모드(original, indian, three-card, shared-card, gollagolla)에서 result phase 진입 시 정상 동작
- 중복 SFX 재생 방지 (이미 lose-ddaeng-but-lost가 재생된 경우 추가 재생 안함)
</success_criteria>

<output>
완료 후 `.planning/quick/260405-s6a-lose-ddaeng-but-lost-sfx/260405-s6a-SUMMARY.md` 생성
</output>
