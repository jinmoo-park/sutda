---
phase: quick-260404-vl1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/layout/ResultScreen.tsx
autonomous: true
requirements: [SHARED-CARD-RESULT-DISPLAY]
must_haves:
  truths:
    - "한장공유 모드 결과화면에서 각 플레이어가 [본인 카드 1장 + 공유카드 1장] = 2장을 표시한다"
    - "2장 조합 기반 족보 뱃지가 올바르게 표시된다"
    - "card-reveal phase에서 공유카드(index 1)는 항상 앞면, 본인 카드(index 0)만 클릭 공개 가능하다"
  artifacts:
    - path: "packages/client/src/components/layout/ResultScreen.tsx"
      provides: "shared-card 모드 displayCards 2장 조합 + card-reveal 공유카드 자동 공개"
  key_links:
    - from: "ResultScreen.tsx displayCards"
      to: "gameState.sharedCard"
      via: "shared-card 모드 판별 후 [player.cards[0], sharedCard] 조합"
      pattern: "gameState\\.mode.*shared-card.*sharedCard"
---

<objective>
한장공유 섯다(shared-card 모드) 결과화면에서 각 플레이어의 패를 2장(본인 1장 + 공유카드 1장)으로 표시하고, 해당 2장 조합 족보 뱃지를 보여준다.

Purpose: 현재 shared-card 모드에서 결과화면이 1장만 표시하는 버그 수정
Output: ResultScreen.tsx 수정
</objective>

<execution_context>
@packages/client/src/components/layout/ResultScreen.tsx
</execution_context>

<context>
@packages/client/src/components/layout/ResultScreen.tsx

<interfaces>
<!-- SFX 쪽에 이미 shared-card 모드 처리 패턴 존재 (line 103-108) -->
```typescript
// 기존 패턴 (SFX 영역, line 103-108):
const getHandCards = (player: typeof me) => {
  if (player.selectedCards && player.selectedCards.length >= 2) return player.selectedCards;
  if (gameState.mode === 'shared-card' && gameState.sharedCard && player.cards[0]) {
    return [player.cards[0], gameState.sharedCard];
  }
  return player.cards?.filter((c): c is Card => c != null) ?? [];
};
```

```typescript
// GameState 관련 타입 (이미 import 됨):
interface GameState {
  mode?: string;
  sharedCard?: Card;
  phase: string;
  // ... etc
}

interface PlayerState {
  cards: (Card | null)[];
  selectedCards?: Card[];
  revealedCardIndices?: number[];
  isRevealed?: boolean;
  // ... etc
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: shared-card 모드 displayCards 2장 조합 + card-reveal 공유카드 자동 공개</name>
  <files>packages/client/src/components/layout/ResultScreen.tsx</files>
  <action>
ResultScreen.tsx의 `allPlayers.map()` 루프 내부 (현재 line 217-219) `displayCards` 로직을 수정한다.

**1. displayCards 로직 수정 (line 217-219):**

현재:
```typescript
const displayCards = player.selectedCards?.length === 2
  ? player.selectedCards
  : player.cards;
```

수정:
```typescript
const isSharedCardMode = gameState.mode === 'shared-card' && gameState.sharedCard != null;
const displayCards = player.selectedCards?.length === 2
  ? player.selectedCards
  : isSharedCardMode && player.cards[0]
    ? [player.cards[0], gameState.sharedCard!]
    : player.cards;
```

이렇게 하면 세장섯다(selectedCards 있음)는 기존대로, shared-card 모드는 [본인카드, 공유카드] 2장, 나머지 모드는 기존 player.cards 그대로 사용.

**2. card-reveal phase 공유카드 자동 공개 (line 261-277):**

card-reveal phase 렌더링에서 shared-card 모드일 때 공유카드(index 1)는 항상 앞면으로 표시하고 클릭 불가로 처리한다.

현재의 `isCardRevealed` / `canClick` 로직을 수정:
```typescript
// card-reveal phase에서 shared-card 모드의 공유카드는 항상 공개
const isSharedCardAtIndex = isSharedCardMode && idx === 1;
const isCardRevealed = isSharedCardAtIndex || revealedIndices.includes(idx);
const canClick = isMe && !isCardRevealed && player.isAlive;
```

`isSharedCardMode` 변수는 이미 위에서 선언했으므로 그대로 사용.

**3. reveal-my-card emit 시 cardIndex 보정 불필요:**
서버 측에서 shared-card 모드의 카드 인덱스는 0만 유효하므로, 공유카드(idx=1)는 canClick=false로 클릭 자체가 차단됨. 추가 보정 불필요.

**주의사항:**
- `isSharedCardMode` 변수를 `allPlayers.map()` 바깥이 아닌 루프 안에서 선언해도 되지만, gameState 레벨 판별이므로 루프 밖(line 180 부근, `allPlayers` 선언 근처)에 한 번만 선언하는 것이 효율적
- Card 타입은 이미 import되어 있음 (line 3)
- handLabel 계산은 displayCards 기반이므로 자동으로 2장 조합 족보가 계산됨 (변경 불필요)
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && npx tsc --noEmit -p packages/client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
- shared-card 모드 결과화면에서 각 플레이어가 2장(본인 + 공유카드) 표시
- 2장 조합 족보 뱃지 정상 표시
- card-reveal phase에서 공유카드는 항상 앞면, 본인 카드만 클릭 공개
- 타입 체크 통과
  </done>
</task>

</tasks>

<verification>
1. TypeScript 컴파일 에러 없음
2. 수동 테스트: 한장공유 모드로 게임 진행 -> 결과화면에서 2장 표시 + 족보 뱃지 확인
3. card-reveal phase에서 공유카드 앞면 고정 + 본인 카드만 클릭 공개 확인
</verification>

<success_criteria>
- shared-card 모드 결과화면에서 모든 플레이어가 2장 카드 표시
- 족보 뱃지가 2장 조합 기준으로 정확히 계산/표시
- card-reveal phase에서 공유카드 항상 앞면, 본인 카드만 인터랙티브
- 기존 오리지날/세장섯다/인디언/후회 모드 동작에 영향 없음
</success_criteria>

<output>
완료 후 `.planning/quick/260404-vl1-2/260404-vl1-SUMMARY.md` 작성
</output>
