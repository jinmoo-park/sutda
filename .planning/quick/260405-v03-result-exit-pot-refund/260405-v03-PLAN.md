---
phase: quick-260405-v03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/index.ts
autonomous: true
must_haves:
  truths:
    - "결과화면에서 leave-room 시 engine chips가 room player에 동기화됨"
    - "2인 게임에서 1명 퇴장 시 남은 플레이어가 정산된 chips로 대기실 전환됨"
    - "퇴장 전 마지막 판 이력(lastRoundHistory)이 gameHistories에 기록되고 emit됨"
  artifacts:
    - path: "packages/server/src/index.ts"
      provides: "leave-room 핸들러 게임 중 퇴장 처리 강화"
  key_links:
    - from: "leave-room handler"
      to: "gameEngines, roomManager"
      via: "engine chips -> room chips 동기화 + 대기실 전환"
---

<objective>
결과화면(result phase)에서 플레이어가 leave-room으로 나갈 때, engine chips 동기화 누락 및 이력 미기록 버그를 수정한다.

Purpose: 현재 leave-room 핸들러는 게임 상태(engine)를 전혀 참조하지 않아, (1) 정산된 chips가 room에 반영되지 않고, (2) 2인 게임에서 1명 퇴장 시 대기실 전환 + engine 삭제가 안 되며, (3) 마지막 판 이력이 기록/emit되지 않는다.
Output: 수정된 leave-room 핸들러
</objective>

<context>
@packages/server/src/index.ts
@packages/server/src/game-engine.ts
@packages/server/src/room-manager.ts
</context>

<root_cause>
## 버그 원인 분석

### 코드 경로
`leave-room` 핸들러 (`index.ts:451-466`)는 `roomManager.leaveRoom()`만 호출하고 끝남.
게임 중 처리 로직이 전혀 없음 — engine chips 동기화, engine에서 플레이어 제거, 이력 기록, 대기실 전환 모두 누락.

### 대조: disconnect 핸들러 (`index.ts:909-923`)
disconnect 타이머 만료 시에는 다음을 수행:
1. `eng.forcePlayerLeave(playerInRoom.id)` — engine에서 제거
2. `roomManager.leaveRoom()` — room에서 제거
3. 남은 인원 < 2 체크 → engine chips를 room에 동기화 → `gamePhase='waiting'` → engine 삭제

### 문제 3가지
1. **Chips 미동기화:** Result phase에서 pot은 이미 승자에게 정산됨(settleChips). 하지만 engine.players.chips → room.players.chips 복사가 leave-room에서는 안 됨. 대기실 복귀 시 게임 시작 전 chips로 표시됨.
2. **대기실 전환 누락:** 2인 게임에서 1명 leave → 남은 1명이 playing 상태로 갇힘. disconnect 핸들러에만 있는 전환 로직이 leave-room에는 없음.
3. **이력 미기록:** `lastRoundHistory`는 showdown에서 생성되지만, `gameHistories`에 push + `game-history` emit은 `tryAdvanceNextRound` (line 221-227)에서만 수행. leave-room 경로에서는 이 코드를 안 탐.
</root_cause>

<tasks>

<task type="auto">
  <name>Task 1: leave-room 핸들러에 게임 중 퇴장 처리 로직 추가</name>
  <files>packages/server/src/index.ts</files>
  <action>
`leave-room` 핸들러 (`socket.on('leave-room', ...)` — 현재 line 451 부근)를 수정한다.

기존 코드:
```typescript
socket.on('leave-room', ({ roomId }) => {
  const result = roomManager.leaveRoom(roomId, socket.id);
  if (result) {
    socket.leave(roomId);
    io.to(roomId).emit('player-left', { ... });
    // 방이 비었으면 채팅 이력 정리
  }
});
```

수정 후: `roomManager.leaveRoom` 호출 전에 게임 엔진 처리를 추가한다.

```typescript
socket.on('leave-room', ({ roomId }) => {
  const room = roomManager.getRoom(roomId);
  const engine = gameEngines.get(roomId);

  // --- 게임 중 퇴장 처리 (playing 상태일 때만) ---
  if (room && room.gamePhase === 'playing' && engine) {
    // 1) 이력 기록: lastRoundHistory가 있고 아직 gameHistories에 push 안 된 경우 기록
    const lastHistory = (engine as any).lastRoundHistory;
    if (lastHistory) {
      if (!gameHistories.has(roomId)) gameHistories.set(roomId, []);
      const histories = gameHistories.get(roomId)!;
      // 중복 방지: 같은 roundNumber 이력이 이미 있으면 skip
      if (!histories.some(h => h.roundNumber === lastHistory.roundNumber)) {
        histories.push(lastHistory);
        io.to(roomId).emit('game-history', { entries: histories });
      }
    }

    // 2) engine에서 플레이어 제거
    try { engine.forcePlayerLeave(socket.id); } catch { /* no-op */ }

    // 3) engine chips → room chips 동기화 (leave 전에 수행)
    const enginePs = engine.getState().players;
    for (const rp of room.players) {
      const ep = enginePs.find(p => p.id === rp.id);
      if (ep) rp.chips = ep.chips;
    }
  }

  // --- 기존 leaveRoom 호출 ---
  const result = roomManager.leaveRoom(roomId, socket.id);
  if (result) {
    socket.leave(roomId);
    io.to(roomId).emit('player-left', {
      playerId: result.removedPlayerId,
      newHostId: result.newHostId,
      nickname: socket.data.nickname,
    });

    // 게임 중이었다면: 남은 인원 < 2 → 대기실 전환
    if (engine && room && room.gamePhase === 'playing') {
      const remainingRoom = roomManager.getRoom(roomId);
      if (remainingRoom) {
        const activePlayers = remainingRoom.players.filter(p => !p.isObserver);
        if (activePlayers.length < 2) {
          // engine chips 동기화 (위에서 이미 했지만, leaveRoom 이후 남은 플레이어 재확인)
          const enginePs2 = engine.getState().players;
          for (const rp of remainingRoom.players) {
            const ep = enginePs2.find(p => p.id === rp.id);
            if (ep) rp.chips = ep.chips;
          }
          remainingRoom.gamePhase = 'waiting';
          gameEngines.delete(roomId);
          io.to(roomId).emit('room-state', remainingRoom);
        } else {
          // 3인 이상에서 1명 나간 경우: engine players 동기화 + room-state broadcast
          const engineState = engine.getState();
          (engineState as any).players = engineState.players.filter(
            p => activePlayers.some(rp => rp.id === p.id)
          );
          (engineState as any).players.forEach((p: any, i: number) => { p.seatIndex = i; });
          io.to(roomId).emit('room-state', remainingRoom);

          // result phase에서 나갔으면 투표 재계산
          if (engineState.phase === 'result') {
            (async () => { await tryAdvanceNextRound(roomId); })();
          }
        }
      }
    }

    // 방이 비었으면 채팅 이력 정리
    const roomAfter = roomManager.getRoom(roomId);
    if (!roomAfter || roomAfter.players.length === 0) {
      chatHistories.delete(roomId);
      gameEngines.delete(roomId);
      gameHistories.delete(roomId);
    }
  }
});
```

핵심 변경 사항:
- Engine의 `forcePlayerLeave`로 engine에서 플레이어 제거
- Engine chips → room chips 동기화 (정산 결과 반영)
- 마지막 판 `lastRoundHistory`를 `gameHistories`에 기록 + emit
- 2인→1인 시 `gamePhase='waiting'` + engine 삭제 (disconnect 핸들러 line 909-923 패턴 복제)
- 3인 이상에서 1명 나간 경우 engine players 동기화 + result phase 투표 재계산
- 방 빈 경우 gameHistories도 정리
  </action>
  <verify>
    <automated>cd D:/dev/sutda && npx tsc --noEmit -p packages/server/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
- leave-room 핸들러가 게임 중 퇴장 시 engine chips를 room에 동기화
- 2인 게임에서 1명 leave-room 시 남은 플레이어가 정산된 chips로 대기실 전환됨
- lastRoundHistory가 gameHistories에 기록되고 game-history 이벤트 emit됨
- TypeScript 컴파일 에러 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: 기존 테스트 통과 확인 및 동작 검증</name>
  <files>packages/server/src/index.ts</files>
  <action>
1. 기존 서버 테스트 실행하여 회귀 없음을 확인한다.
2. `pnpm --filter server build` 로 빌드 성공을 확인한다.
3. `pnpm --filter shared build` 도 선행 확인 (의존성).
  </action>
  <verify>
    <automated>cd D:/dev/sutda && pnpm --filter shared build && pnpm --filter server build 2>&1 | tail -5</automated>
  </verify>
  <done>
- shared + server 빌드 성공
- 기존 테스트 통과 (회귀 없음)
  </done>
</task>

</tasks>

<verification>
1. TypeScript 컴파일 에러 없음
2. 서버 빌드 성공
3. leave-room 핸들러에서 engine 존재 시 chips 동기화 + 이력 기록 + 대기실 전환 로직 존재
</verification>

<success_criteria>
- 결과화면에서 leave-room 시 engine의 정산된 chips가 room player에 반영됨
- 2인 게임에서 1명 퇴장 시 남은 플레이어가 올바른 chips로 대기실 복귀
- 마지막 판 이력이 gameHistories에 기록되고 전체 플레이어에게 emit됨
- 기존 테스트 및 빌드 통과
</success_criteria>

<output>
완료 후 `.planning/quick/260405-v03-result-exit-pot-refund/260405-v03-SUMMARY.md` 생성
</output>
