---
phase: quick-260405-l55
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/game-engine.ts
  - packages/client/src/components/layout/ResultScreen.tsx
  - packages/client/src/store/gameStore.ts
  - packages/shared/src/types/protocol.ts
  - packages/server/src/index.ts
autonomous: true
must_haves:
  truths:
    - "학교대신가주기 수혜자의 결과화면에서 칩 변동(chipDelta)이 정확히 표시된다"
    - "학교대신가주기 수혜자의 결과화면에서 카드가 앞면으로 표시된다"
    - "학교대신가주기 후원자(sponsor)의 결과화면에서도 대납 비용이 반영된 칩 변동이 표시된다"
    - "대리출석 배지가 수혜자 닉네임 옆에 표시된다"
  artifacts:
    - path: "packages/server/src/game-engine.ts"
      provides: "attendSchoolProxy totalBet 수정"
    - path: "packages/client/src/components/layout/ResultScreen.tsx"
      provides: "대리출석 배지 + SFX 수혜자 한정"
  key_links:
    - from: "game-engine.ts attendSchoolProxy"
      to: "ResultScreen chipDelta 계산"
      via: "PlayerState.totalBet"
---

<objective>
학교대신가주기(proxy-ante) 수혜자의 결과화면 버그 수정

**문제 분석:**
1. `attendSchoolProxy()`에서 수혜자의 `totalBet`을 증가시키지 않음 -- ResultScreen의 chipDelta 계산이 잘못됨 (앤티 500원이 누락)
2. 후원자(sponsor)의 `totalBet`도 갱신되지 않아 후원자의 chipDelta도 부정확
3. commit 72ae604 (kf2 레이아웃 수정)에서 commit 12f742f (ihi 대리출석 배지+SFX)의 변경사항이 전부 revert됨 -- 대리출석 배지, SFX 수혜자 한정, 통합 토스트 기능 소실

**수정 범위:**
- Task 1: game-engine.ts `attendSchoolProxy` 칩/베팅 추적 수정
- Task 2: ihi 변경사항 재적용 (대리출석 배지, SFX 수혜자 한정, 통합 토스트)

Purpose: 학교대신가주기 기능의 결과화면 정확성 + UX 복원
Output: 수정된 서버 로직 + 클라이언트 표시
</objective>

<execution_context>
@.planning/STATE.md
</execution_context>

<context>
@packages/server/src/game-engine.ts (attendSchoolProxy: line 1980-2011)
@packages/server/src/game-engine.ts (attendSchool: line 475-500)
@packages/client/src/components/layout/ResultScreen.tsx
@packages/client/src/store/gameStore.ts
@packages/shared/src/types/protocol.ts
@packages/server/src/index.ts (proxy-ante handler: line 820-842)

<interfaces>
From packages/shared/src/types/game.ts:
```typescript
interface PlayerState {
  totalBet: number;       // 이번 판 전체 누적 베팅액 (표시용)
  totalCommitted?: number; // 이번 판 전체 기여 총액 (앤티 포함, 올인 정산용)
  chips: number;
  // ...
}
```

From packages/shared/src/types/protocol.ts:
```typescript
// 현재 (singular):
'proxy-ante-applied': (data: { sponsorNickname: string; beneficiaryNickname: string }) => void;
// 수정 후 (plural array):
'proxy-ante-applied': (data: { sponsorNickname: string; beneficiaryNicknames: string[] }) => void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: attendSchoolProxy totalBet 수정</name>
  <files>packages/server/src/game-engine.ts</files>
  <action>
`attendSchoolProxy()` 메서드 (약 line 1980-2011)에서 수혜자의 `totalBet` 누적 누락 수정.

**현재 코드 (문제):**
```typescript
// 후원자 칩 차감
sponsor.chips -= 500;
sponsor.totalCommitted = (sponsor.totalCommitted ?? 0) + 500;

// 수혜자는 무료 참여 (chips 차감 없음)
this.state.pot += 500;
this.state.attendedPlayerIds.push(beneficiaryId);
```

**수정 후:**
```typescript
// 후원자 칩 차감 + totalBet 추적
sponsor.chips -= 500;
sponsor.totalBet += 500;  // 앤티 대납은 후원자의 totalBet에 반영 (chipDelta 정확성)
sponsor.totalCommitted = (sponsor.totalCommitted ?? 0) + 500;

// 수혜자는 무료 참여 (chips 차감 없음, totalBet도 0 유지)
// totalBet을 증가시키지 않는 이유: 수혜자는 실제로 돈을 내지 않았으므로 chipDelta에서 빼면 안 됨
this.state.pot += 500;
this.state.attendedPlayerIds.push(beneficiaryId);
```

핵심: `sponsor.totalBet += 500` 추가. 후원자가 실제로 칩을 지불했으므로 totalBet에 반영해야 ResultScreen의 chipDelta(`-player.totalBet`)가 정확함. 수혜자의 totalBet은 0 유지가 올바름 (돈을 내지 않았으므로).
  </action>
  <verify>
    <automated>cd /d/dev/sutda && npx vitest run packages/server/src/game-engine.test.ts -t "attendSchoolProxy" --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
- attendSchoolProxy에서 sponsor.totalBet += 500 추가
- 기존 테스트 통과 (수혜자 chips 미차감, 후원자 chips -= 500, attendedPlayerIds 포함)
- 후원자의 totalBet이 500 증가함을 확인
  </done>
</task>

<task type="auto">
  <name>Task 2: 대리출석 배지 + SFX + 통합 토스트 재적용 (ihi revert 복구)</name>
  <files>
    packages/shared/src/types/protocol.ts
    packages/server/src/index.ts
    packages/client/src/store/gameStore.ts
    packages/client/src/components/layout/ResultScreen.tsx
  </files>
  <action>
commit 72ae604 (kf2)에서 실수로 revert된 commit 30a08ea + 12f742f (ihi) 변경사항을 재적용한다. 단, 현재 ResultScreen의 최신 코드 베이스 위에 적용해야 함 (kf2 이후 추가된 stop('card-reveal') 등은 유지).

**1) protocol.ts** (line ~89):
```typescript
// 변경:
'proxy-ante-applied': (data: { sponsorNickname: string; beneficiaryNicknames: string[] }) => void;
```

**2) server/index.ts** proxy-ante 핸들러 (line ~831-841):
수혜자별 반복 emit 제거, 단일 emit으로 변경:
```typescript
const sponsorNickname = socket.data.nickname;
const beneficiaryNicknames = beneficiaryIds
  .map(bid => room.players.find(p => p.id === bid)?.nickname)
  .filter((n): n is string => !!n);
if (beneficiaryNicknames.length > 0) {
  io.to(roomId).emit('proxy-ante-applied', {
    sponsorNickname,
    beneficiaryNicknames,
  });
}
```

**3) gameStore.ts**:
- `GameStore` 인터페이스에 `proxyBeneficiaryNicknames: string[]` 추가
- 초기값 `[]`
- `game-state` 핸들러: result phase 탈출 시 `proxyBeneficiaryNicknames: []` 초기화
- `proxy-ante-applied` 핸들러:
  ```typescript
  socket.on('proxy-ante-applied', ({ sponsorNickname, beneficiaryNicknames }) => {
    const names = beneficiaryNicknames.join(', ');
    toast(`${sponsorNickname}님이 ${names}의 학교를 대신 가줬습니다`);
    set(state => ({
      proxyBeneficiaryNicknames: [...new Set([...state.proxyBeneficiaryNicknames, ...beneficiaryNicknames])]
    }));
  });
  ```
- `disconnect` 액션에 `proxyBeneficiaryNicknames: []` 추가

**4) ResultScreen.tsx**:
- `useGameStore()`에서 `proxyBeneficiaryNicknames` 추출
- SFX 핸들러 수정 (수혜자 본인에게만 school-proxy SFX 재생):
  ```tsx
  useEffect(() => {
    if (!socket) return;
    const myNickname = myPlayer?.nickname;
    const handler = ({ beneficiaryNicknames }: { beneficiaryNicknames: string[] }) => {
      if (myNickname && beneficiaryNicknames.includes(myNickname)) {
        play('school-proxy');
      }
    };
    socket.on('proxy-ante-applied', handler);
    return () => { socket.off('proxy-ante-applied', handler); };
  }, [socket, myPlayer?.nickname]);
  ```
- 플레이어 닉네임 옆 대리출석 배지 추가 (닉네임 div 내부, "학교" 배지 앞):
  ```tsx
  {proxyBeneficiaryNicknames.includes(player.nickname) && (
    <span className="text-[9px] px-1 py-0.5 rounded bg-blue-600/80 text-white font-medium">대리출석</span>
  )}
  ```

주의: 현재 ResultScreen에 있는 `stop('card-reveal')` (line 113), `getHandCards` winner 로직 (line 147-157) 등 kf2 이후 추가된 변경사항은 반드시 유지. ihi 변경사항만 선택적으로 재적용.
  </action>
  <verify>
    <automated>cd /d/dev/sutda && npx tsc --noEmit -p packages/shared/tsconfig.json 2>&1 | tail -10 && npx tsc --noEmit -p packages/server/tsconfig.json 2>&1 | tail -10 && npx tsc --noEmit -p packages/client/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <done>
- protocol.ts: beneficiaryNicknames (복수 배열) 타입으로 변경됨
- server index.ts: 단일 emit 패턴 적용
- gameStore.ts: proxyBeneficiaryNicknames 상태 추가, 통합 토스트, 초기화 로직
- ResultScreen.tsx: 대리출석 배지 표시, SFX 수혜자 한정
- TypeScript 컴파일 에러 없음
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run packages/server/src/game-engine.test.ts -t "attendSchoolProxy"` -- 기존 테스트 통과
2. TypeScript 전체 컴파일 에러 없음
3. proxy-ante-applied 이벤트 페이로드가 서버/클라이언트 일치 (beneficiaryNicknames: string[])
</verification>

<success_criteria>
- attendSchoolProxy에서 후원자의 totalBet이 정확히 반영됨
- ResultScreen에서 대리출석 배지가 수혜자 닉네임 옆에 표시됨
- school-proxy SFX가 수혜자에게만 재생됨
- 통합 토스트가 수혜자 목록을 한 줄로 표시함
</success_criteria>

<output>
완료 후 `.planning/quick/260405-l55-hakgyo-result-card/260405-l55-SUMMARY.md` 생성
</output>
