---
phase: quick-260405-l55
plan: 01
subsystem: server, client, shared
tags: [bugfix, proxy-ante, result-screen, sfx, badge]
dependency_graph:
  requires: []
  provides: [attendSchoolProxy totalBet 정확성, 대리출석 배지, SFX 수혜자 한정, 통합 토스트]
  affects: [game-engine.ts, protocol.ts, index.ts, gameStore.ts, ResultScreen.tsx]
tech_stack:
  added: []
  patterns: [단일 emit 통합 패턴]
key_files:
  created: []
  modified:
    - packages/server/src/game-engine.ts
    - packages/shared/src/types/protocol.ts
    - packages/server/src/index.ts
    - packages/client/src/store/gameStore.ts
    - packages/client/src/components/layout/ResultScreen.tsx
decisions:
  - "proxy-ante-applied 이벤트를 단일 emit으로 통합 — 수혜자별 루프 emit 제거"
  - "수혜자 totalBet은 0 유지 (실제 지불 없음), 후원자 totalBet += 500"
metrics:
  duration: ~12min
  completed: "2026-04-05"
  tasks: 2
  files: 5
---

# Quick Task 260405-l55: 학교대신가주기 결과화면 버그 수정 요약

한 줄 요약: attendSchoolProxy 후원자 totalBet 누락 수정 + ihi commit revert로 소실된 대리출석 배지·SFX·통합 토스트 재적용.

## 완료된 태스크

| Task | 이름 | 커밋 | 수정 파일 |
|------|------|------|-----------|
| 1 | attendSchoolProxy totalBet 수정 | 5b0e640 | game-engine.ts |
| 2 | 대리출석 배지+SFX+통합토스트 재적용 | 6e0ee5d | protocol.ts, index.ts, gameStore.ts, ResultScreen.tsx |

## 변경 내용

### Task 1: game-engine.ts attendSchoolProxy 수정

`attendSchoolProxy()`에서 후원자(sponsor)의 `totalBet += 500`이 누락되어 결과화면의 chipDelta 계산이 부정확했다. `attendSchool()`은 `totalBet += 500`을 정상 추가하지만 proxy 경로는 해당 라인이 없었다.

- `sponsor.totalBet += 500` 추가
- 수혜자 totalBet은 0 유지 (실제 지불 없음 — chipDelta에서 차감 불필요)
- 기존 attendSchoolProxy 테스트 4개 전부 통과 확인

### Task 2: ihi revert 복구

commit 72ae604 (kf2 레이아웃 수정)에서 실수로 commit 30a08ea + 12f742f (ihi) 변경사항이 revert된 것을 재적용.

**protocol.ts:**
- `beneficiaryNickname: string` → `beneficiaryNicknames: string[]` (복수 배열)

**server/index.ts:**
- 수혜자 수만큼 반복 emit하던 패턴 → 단일 통합 emit으로 변경

**gameStore.ts:**
- `proxyBeneficiaryNicknames: string[]` 상태 추가 (초기값 `[]`)
- `proxy-ante-applied` 핸들러: 통합 토스트 + Set으로 중복 제거하며 누적
- `game-state` 핸들러: result phase 탈출 시 `proxyBeneficiaryNicknames: []` 초기화
- `disconnect` 액션: `proxyBeneficiaryNicknames: []` 포함

**ResultScreen.tsx:**
- `proxyBeneficiaryNicknames` 추출
- school-proxy SFX: 전체 재생 → 수혜자 닉네임 포함 시에만 재생
- 플레이어 닉네임 옆 파란색 "대리출석" 배지 추가

## 검증

- attendSchoolProxy 테스트 4개 통과
- TypeScript 컴파일: server 오류 없음, client 2개 에러는 기존 pre-existing 오류 (ResultScreen.tsx:154 `winner undefined`, CutModal.tsx:145 `GiriPhase`) — 본 태스크 변경사항과 무관함을 stash 테스트로 확인

## 편차 사항

없음 — 계획 그대로 실행.

## Known Stubs

없음.

## Self-Check: PASSED

- `packages/server/src/game-engine.ts` — 수정됨 (5b0e640)
- `packages/shared/src/types/protocol.ts` — 수정됨 (6e0ee5d)
- `packages/server/src/index.ts` — 수정됨 (6e0ee5d)
- `packages/client/src/store/gameStore.ts` — 수정됨 (6e0ee5d)
- `packages/client/src/components/layout/ResultScreen.tsx` — 수정됨 (6e0ee5d)
