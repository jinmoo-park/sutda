# Quick Plan 260404-t1o: SFX 버그 수정 5종

**Date:** 2026-04-04  
**Mode:** quick

## 버그 원인 분석

### Bug 1: school-proxy 수혜자 미재생
`SchoolProxySection`은 `gameState.winnerId !== myPlayerId`일 때 `return null` → 수혜자는 렌더링 안 됨 → useEffect 미실행.  
**Fix:** `ResultScreen` 메인 컴포넌트에서 `proxy-ante-applied` 소켓 이벤트를 리슨 → 전체 재생.  
`handleProxyConfirm`의 `play('school-proxy')` 제거 (중복 방지).

### Bug 2 & 3: win-ddaeng / lose-ddaeng-but-lost 미재생
세장섯다에서 `me.cards[0]`+`me.cards[1]`이 선택한 2장이 아닐 수 있음.  
**Fix:** `me.selectedCards`가 있으면 우선 사용. winner 평가도 동일.

### Bug 4: win-normal 볼륨 0.2
단순 수치 변경.

### Bug 5: 베팅 SFX 전체 브로드캐스트
`lastBetAction`은 이미 서버가 모든 클라이언트에 broadcast. 감지 useEffect가 없었을 뿐.  
**Fix:** `RoomPage`에서 `gameState.players`의 `lastBetAction` 변화를 감지하여 상대 액션 재생.  
`roundNumber`를 키에 포함해 라운드 간 중복 무시 방지.

---

## Tasks

### Task 1: useSfxPlayer.ts — win-normal 볼륨 수정
- **File:** `packages/client/src/hooks/useSfxPlayer.ts`
- **Action:** `win-normal` volume 0.6 → 0.2

### Task 2: ResultScreen.tsx — school-proxy + win-ddaeng + lose-ddaeng-but-lost
- **File:** `packages/client/src/components/layout/ResultScreen.tsx`
- **Changes:**
  - `proxy-ante-applied` 소켓 리스너 추가 (메인 ResultScreen useEffect)
  - `handleProxyConfirm`에서 `play('school-proxy')` 제거
  - `SchoolProxySection`의 `schoolProxyBeneficiaryIds` useEffect 제거
  - SFX useEffect에서 `selectedCards` 우선 사용 헬퍼 적용 (winner/loser 공통)

### Task 3: RoomPage.tsx — 상대 베팅 SFX
- **File:** `packages/client/src/pages/RoomPage.tsx`
- **Action:** `prevBetActionsRef` ref + `gameState.players` useEffect 추가
  - 상대 플레이어의 `lastBetAction` 변화 감지 → 해당 sfx 재생
  - key = `${roundNumber}-${JSON.stringify(lastBetAction)}` 로 라운드 간 재생 보장
