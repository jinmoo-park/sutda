---
phase: 08-huhwi-indian-modes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/game.ts
  - packages/server/src/game-engine.ts
autonomous: true
requirements:
  - MODE-HR-01
  - MODE-HR-02
  - MODE-HR-03
  - MODE-HR-04
  - MODE-IN-01
  - MODE-IN-02
  - MODE-IN-03
  - MODE-IN-04
  - MODE-IN-05

must_haves:
  truths:
    - "GameMode 타입에 'gollagolla'와 'indian'이 포함되어 있다"
    - "GamePhase 타입에 'gollagolla-select'가 포함되어 있다"
    - "GameEngine이 selectGollaCards(playerId, cardIndices) 메서드를 가진다"
    - "GameEngine이 getStateFor(playerId) 메서드를 가지며 인디언 모드에서 카드를 마스킹한다"
    - "GollagollaModeStrategy.deal()이 20장 전부를 deck에서 꺼내 공개 배열로 설정하고 phase를 'gollagolla-select'로 전환한다"
    - "IndianModeStrategy.deal()이 각 플레이어에게 1장 배분 후 phase를 'betting-1'로 전환한다"
  artifacts:
    - path: "packages/shared/src/types/game.ts"
      provides: "GameMode, GamePhase 타입 확장"
      contains: "'gollagolla' | 'indian'"
    - path: "packages/server/src/game-engine.ts"
      provides: "두 신규 Strategy + selectGollaCards + getStateFor"
      exports: ["GameEngine"]
  key_links:
    - from: "GollagollaModeStrategy"
      to: "GameEngine.state.phase"
      via: "deal() → phase='gollagolla-select' 전환"
      pattern: "gollagolla-select"
    - from: "IndianModeStrategy"
      to: "GameEngine.state.phase"
      via: "deal() → phase='betting-1' 전환"
      pattern: "betting-1"
    - from: "getStateFor"
      to: "PlayerState.cards"
      via: "인디언 모드 마스킹: cards[0] = null (dealing/betting-1), cards[1] = null (dealing-extra/betting-2에서 타인)"
      pattern: "getStateFor"
---

<objective>
공유 타입을 확장하고 GameEngine에 두 신규 모드 Strategy와 핵심 메서드를 추가한다.

Purpose: Phase 8 서버 로직의 토대 — 소켓 핸들러(08-02)와 UI(08-03)가 이 플랜에서 정의한 메서드를 호출한다.
Output: 타입 확장된 game.ts + GollagollaModeStrategy/IndianModeStrategy/selectGollaCards/getStateFor가 추가된 game-engine.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-huhwi-indian-modes/08-CONTEXT.md
@.planning/phases/08-huhwi-indian-modes/08-RESEARCH.md

# 핵심 참조 파일
@packages/shared/src/types/game.ts
@packages/server/src/game-engine.ts
</context>

<interfaces>
<!-- 실행자가 사용해야 하는 기존 타입/인터페이스 계약 -->

현재 GameMode (game.ts line 31-37):
```typescript
export type GameMode =
  | 'original'
  | 'three-card'
  | 'shared-card'
  | 'regret'      // 미사용이지만 존재
  | 'indian';
```

현재 GamePhase (game.ts line 12-29):
```typescript
// 'betting-1' | 'dealing-extra' | 'betting-2' 이미 존재 (Phase 7에서 추가됨)
// 'gollagolla-select' 추가 필요
```

GameModeStrategy 인터페이스 (game-engine.ts line 9-12):
```typescript
interface GameModeStrategy {
  deal(engine: GameEngine, state: GameState): void;
  showdown(engine: GameEngine, state: GameState): void;
}
```

getModeStrategy() (game-engine.ts line 56-62):
```typescript
private getModeStrategy(): GameModeStrategy {
  switch (this.state.mode) {
    case 'three-card': return new SejangModeStrategy();
    case 'shared-card': return new HanjangModeStrategy();
    default: return new OriginalModeStrategy();
  }
}
```

_dealCardsOriginal() 패턴 (참고용):
```typescript
private _dealCardsOriginal(): void {
  const alivePlayers = this.state.players.filter(p => p.isAlive);
  const dealerSeatIndex = this.getDealerSeatIndex();
  const orderedByDealerCounterClockwise = this._getAlivePlayersInCounterClockwiseOrder(
    dealerSeatIndex, alivePlayers
  );
  // 1장씩 2라운드 배분 후 phase = 'betting'
}
```

BETTING_PHASES 상수:
```typescript
private static readonly BETTING_PHASES: GameState['phase'][] = ['betting', 'betting-1', 'betting-2'];
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 공유 타입 확장 — GameMode/GamePhase에 골라골라/인디언 추가</name>
  <read_first>
    - packages/shared/src/types/game.ts (수정 대상 — 현재 상태 확인 필수)
  </read_first>
  <files>packages/shared/src/types/game.ts</files>
  <action>
packages/shared/src/types/game.ts를 다음과 같이 수정한다.

**1. GamePhase 타입에 'gollagolla-select' 추가** (per D-11):
`'card-select'` 항목 다음 줄에 추가:
```typescript
  | 'gollagolla-select' // 골라골라: 20장 오픈 그리드, 동시 선착순 선택
```

**2. GameMode 타입에 'gollagolla' 추가** (per D-01, D-11):
현재 `'regret'` 항목을 `'gollagolla'`로 교체(또는 기존 indian 앞에 추가):
```typescript
export type GameMode =
  | 'original'
  | 'three-card'
  | 'shared-card'
  | 'gollagolla'   // 골라골라: 20장 오픈 동시 선착순 선택
  | 'indian';      // 인디언섯다: 첫 카드 가시성 반전
```
주의: 기존 'regret' 타입이 있으면 'gollagolla'로 rename. 서버/클라이언트 코드에서 'regret'을 참조하는 곳이 없으므로 안전하게 교체 가능.

**3. GameState 인터페이스에 gollaOpenDeck 필드 추가** — 골라골라 선택 phase에서 공개된 20장을 저장:
```typescript
  gollaOpenDeck?: Card[];  // 골라골라: 공개된 20장 덱 (gollagolla-select phase에서 설정)
```
`sharedCard?: Card;` 다음 줄에 추가.
  </action>
  <verify>
    <automated>cd /c/Users/'Jinmoo Park'/Desktop/sutda && pnpm --filter @sutda/shared build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/types/game.ts에 `'gollagolla-select'` 문자열이 포함됨
    - packages/shared/src/types/game.ts에 `'gollagolla'` 문자열이 포함됨
    - packages/shared/src/types/game.ts에 `gollaOpenDeck?: Card[]` 가 포함됨
    - `pnpm --filter @sutda/shared build`가 에러 없이 성공
  </acceptance_criteria>
  <done>shared 패키지 빌드 성공. GameMode/GamePhase/GameState 타입 확장 완료.</done>
</task>

<task type="auto">
  <name>Task 2: game-engine.ts — GollagollaModeStrategy + IndianModeStrategy + selectGollaCards + getStateFor 구현</name>
  <read_first>
    - packages/server/src/game-engine.ts (수정 대상 — 전체 구조 파악 필수)
    - packages/shared/src/types/game.ts (Task 1에서 수정한 타입 확인)
  </read_first>
  <files>packages/server/src/game-engine.ts</files>
  <action>
game-engine.ts에 4가지를 추가한다. 기존 Strategy 패턴(D-10)을 그대로 따른다.

---

**A. GollagollaModeStrategy 클래스 추가** — HanjangModeStrategy 다음에 추가:

```typescript
class GollagollaModeStrategy implements GameModeStrategy {
  deal(engine: GameEngine, state: GameState): void {
    engine['_dealCardsGollagolla']();
  }
  showdown(engine: GameEngine, _state: GameState): void {
    engine['_resolveShowdownOriginal']();  // 족보 비교는 오리지날과 동일
  }
}
```

**B. IndianModeStrategy 클래스 추가**:

```typescript
class IndianModeStrategy implements GameModeStrategy {
  deal(engine: GameEngine, _state: GameState): void {
    engine['_dealCardsIndian']();
  }
  showdown(engine: GameEngine, _state: GameState): void {
    engine['_resolveShowdownOriginal']();  // 족보 비교는 오리지날과 동일 (D-09)
  }
}
```

**C. getModeStrategy() 수정** — 두 신규 모드 case 추가:

```typescript
private getModeStrategy(): GameModeStrategy {
  switch (this.state.mode) {
    case 'three-card': return new SejangModeStrategy();
    case 'shared-card': return new HanjangModeStrategy();
    case 'gollagolla': return new GollagollaModeStrategy();
    case 'indian': return new IndianModeStrategy();
    default: return new OriginalModeStrategy();
  }
}
```

---

**D. _dealCardsGollagolla() private 메서드 추가** — 기존 _dealCardsOriginal() 다음에 추가:

```typescript
/**
 * 골라골라 모드 패 배분 (GollagollaModeStrategy에서 호출)
 * - 덱 20장 전체를 공개 배열로 state.gollaOpenDeck에 설정
 * - 모든 플레이어 cards는 빈 배열 유지 (선택 완료 시 채워짐)
 * - phase = 'gollagolla-select' 전환
 * - 참고: cut() 호출 이후이므로 deck은 이미 셔플+기리 완료 상태
 */
private _dealCardsGollagolla(): void {
  // 셔플된 덱 20장 전체를 오픈 배열로 복사
  this.state.gollaOpenDeck = [...this.state.deck];
  // 선택 전까지 플레이어 cards는 비움
  this.state.players.forEach(p => { p.cards = []; });
  this.state.phase = 'gollagolla-select';
}
```

**E. _dealCardsIndian() private 메서드 추가**:

```typescript
/**
 * 인디언섯다 모드 패 배분 (IndianModeStrategy에서 호출)
 * - 각 플레이어에게 1장씩 배분 (반시계 방향)
 * - phase = 'betting-1' 전환 (BETTING_PHASES에 포함됨)
 * - getStateFor()에서 dealing/betting-1 phase에 cards[0]를 마스킹하여 per-player emit
 */
private _dealCardsIndian(): void {
  const alivePlayers = this.state.players.filter(p => p.isAlive);
  const dealerSeatIndex = this.getDealerSeatIndex();
  const ordered = this._getAlivePlayersInCounterClockwiseOrder(dealerSeatIndex, alivePlayers);

  // 1장씩 배분
  for (const player of ordered) {
    const card = this.state.deck.shift();
    if (card) player.cards.push(card);
  }

  // 베팅 1차 시작
  this.state.phase = 'betting-1';
  this.state.currentPlayerIndex = dealerSeatIndex;
  this.state.openingBettorSeatIndex = dealerSeatIndex;
  this.state.currentBetAmount = 0;
  this._bettingActed = new Set();
  alivePlayers.forEach(p => { p.currentBet = 0; });
  this._updateEffectiveMaxBet();
}
```

**F. _dealExtraCardIndian() private 메서드 추가** — betting-1 → dealing-extra → betting-2 전환:

```typescript
/**
 * 인디언섯다: 2번째 카드 배분 (dealing-extra phase)
 * - 각 플레이어에게 1장씩 추가 배분 (반시계 방향)
 * - 본인에게만 공개 (getStateFor에서 타인에게는 cards[1]을 마스킹)
 * - phase = 'betting-2' 전환
 */
private _dealExtraCardIndian(): void {
  const alivePlayers = this.state.players.filter(p => p.isAlive);
  const dealerSeatIndex = this.getDealerSeatIndex();
  const ordered = this._getAlivePlayersInCounterClockwiseOrder(dealerSeatIndex, alivePlayers);

  for (const player of ordered) {
    const card = this.state.deck.shift();
    if (card) player.cards.push(card);
  }

  // 2차 베팅 시작
  this.state.phase = 'betting-2';
  this.state.currentPlayerIndex = dealerSeatIndex;
  this.state.openingBettorSeatIndex = dealerSeatIndex;
  this.state.currentBetAmount = 0;
  this._bettingActed = new Set();
  alivePlayers.forEach(p => { p.currentBet = 0; });
  this._updateEffectiveMaxBet();
}
```

**G. selectGollaCards() public 메서드 추가** — 골라골라 선착순 카드 선택:

```typescript
/**
 * 골라골라 카드 선택 (per D-02, D-03)
 * - phase가 'gollagolla-select'인지 검증
 * - cardIndices: [number, number] — gollaOpenDeck에서의 인덱스 2개
 * - 선착순: 이미 다른 플레이어가 선택한 카드이면 'CARD_ALREADY_TAKEN' 에러
 * - 2장 선택 완료 → player.cards에 추가
 * - 모든 생존자 2장 선택 완료 시 → phase='betting' 전환
 */
selectGollaCards(playerId: string, cardIndices: [number, number]): void {
  this.assertPhase('gollagolla-select');
  if (cardIndices[0] === cardIndices[1]) throw new Error('INVALID_ACTION');

  const player = this.state.players.find(p => p.id === playerId);
  if (!player || !player.isAlive) throw new Error('INVALID_ACTION');
  if (player.cards.length >= 2) throw new Error('ALREADY_ATTENDED');

  const openDeck = this.state.gollaOpenDeck;
  if (!openDeck) throw new Error('INVALID_ACTION');

  // 이미 선택된 카드 인덱스 수집 (모든 플레이어의 cards를 gollaOpenDeck 기준 인덱스로 역추적)
  const takenIndices = new Set<number>();
  for (const p of this.state.players) {
    for (const card of p.cards) {
      const idx = openDeck.findIndex(c => c.rank === card.rank && c.type === card.type && !takenIndices.has(openDeck.indexOf(c)));
      // 덱 내 동일 카드(같은 rank+type)가 2장 존재하므로 정확한 인덱스 추적 필요
      // 더 안전한 방법: gollaOpenDeck의 고유 인덱스를 player.gollaSelectedIndices에 저장
      if (idx >= 0) takenIndices.add(idx);
    }
  }

  // 충돌 검사 (위 로직이 복잡하므로 심플한 방식으로 재구현)
  // player마다 이미 선택한 openDeck 인덱스를 별도로 추적해야 함
  // → _gollaSelectedIndices: Map<playerId, [number, number]> private 필드 활용
  // 이 필드가 없으면 아래에서 초기화

  if (!this._gollaSelectedIndices) {
    this._gollaSelectedIndices = new Map();
  }

  // 타인이 이미 선택한 인덱스 수집
  const takenByOthers = new Set<number>();
  for (const [pid, indices] of this._gollaSelectedIndices.entries()) {
    if (pid !== playerId) {
      indices.forEach(i => takenByOthers.add(i));
    }
  }

  if (takenByOthers.has(cardIndices[0]) || takenByOthers.has(cardIndices[1])) {
    throw new Error('CARD_ALREADY_TAKEN');
  }
  if (cardIndices[0] < 0 || cardIndices[0] >= openDeck.length ||
      cardIndices[1] < 0 || cardIndices[1] >= openDeck.length) {
    throw new Error('INVALID_ACTION');
  }

  // 선택 확정
  this._gollaSelectedIndices.set(playerId, cardIndices);
  player.cards = [openDeck[cardIndices[0]], openDeck[cardIndices[1]]];

  // 모든 생존자 선택 완료 확인
  const alivePlayers = this.state.players.filter(p => p.isAlive);
  const allDone = alivePlayers.every(p => p.cards.length >= 2);
  if (allDone) {
    const dealerSeatIndex = this.getDealerSeatIndex();
    this.state.phase = 'betting';
    this.state.currentPlayerIndex = dealerSeatIndex;
    this.state.openingBettorSeatIndex = dealerSeatIndex;
    this.state.currentBetAmount = 0;
    this._bettingActed = new Set();
    alivePlayers.forEach(p => { p.currentBet = 0; });
    this._updateEffectiveMaxBet();
  }
}
```

GameEngine 클래스에 private 필드로 추가:
```typescript
private _gollaSelectedIndices: Map<string, [number, number]> | null = null;
```

**H. dealExtraCardIndian() public 메서드 추가** — 소켓 핸들러에서 호출 가능하도록:

```typescript
/**
 * 인디언섯다 2번째 카드 배분 — 서버 자동 처리 (betting-1 종료 시)
 * betting-1 phase의 showdown/진행 후 자동 호출됨
 */
dealExtraCardIndian(): void {
  if (this.state.mode !== 'indian') throw new Error('INVALID_ACTION');
  if (this.state.phase !== 'dealing-extra') throw new Error('INVALID_PHASE');
  this._dealExtraCardIndian();
}
```

주의: betting-1 완료 시 자동으로 'dealing-extra' phase로 전환하는 로직은 기존 _advanceBetting() 또는 showdown 흐름에서 처리해야 한다. 기존 _resolveShowdown 또는 betting 완료 코드를 확인하여 인디언 모드에서 betting-1 종료 후 phase='dealing-extra'로 전환되도록 수정한다.

**I. getStateFor(playerId) public 메서드 추가** (per D-07, D-08):

```typescript
/**
 * 플레이어별 필터링된 게임 상태 반환 (인디언 모드 카드 마스킹)
 * - 인디언 모드 + dealing/betting-1 phase:
 *   각 플레이어의 cards[0]를 요청자 본인에게는 null(as any)로 마스킹
 *   타인 cards[0]는 정상 공개
 * - 인디언 모드 + dealing-extra/betting-2 phase:
 *   각 플레이어의 cards[1]를 본인에게만 공개, 타인에게는 null로 마스킹
 * - result phase: 모든 카드 공개
 * - 다른 모드: 기존 getState()와 동일 (마스킹 없음)
 */
getStateFor(playerId: string): GameState {
  const state = this.state;

  if (state.mode !== 'indian') {
    return state as GameState;
  }

  const phase = state.phase;
  const isResultPhase = phase === 'result' || phase === 'showdown';

  if (isResultPhase) {
    return state as GameState;
  }

  // 인디언 모드 마스킹 적용
  const maskedPlayers = state.players.map(p => {
    const cards = [...p.cards];

    if (phase === 'dealing' || phase === 'betting-1') {
      // 1차: cards[0]가 있는 경우 — 본인에게는 숨김, 타인에게는 공개
      if (p.id === playerId && cards.length > 0) {
        cards[0] = null as any;  // 클라이언트는 null을 CardBack으로 렌더링
      }
    } else if (phase === 'dealing-extra' || phase === 'betting-2') {
      // 2차: cards[0] = 반전 (타인 공개, 본인 숨김) 유지
      // cards[1] = 본인에게만 공개, 타인에게는 숨김
      if (p.id === playerId && cards.length > 0) {
        cards[0] = null as any;  // 여전히 본인에게 숨김
      }
      if (p.id !== playerId && cards.length > 1) {
        cards[1] = null as any;  // 타인의 2번째 카드는 숨김
      }
    }

    return { ...p, cards };
  });

  return { ...state, players: maskedPlayers } as GameState;
}
```

---

**기존 betting 완료 로직 수정** — 인디언 모드에서 betting-1 완료 후 'dealing-extra'로 전환:

기존 코드에서 betting 완료 시 showdown으로 전환하는 부분(아마 `_checkBettingComplete()` 또는 `placeBet()` 내부)을 찾아서 인디언 모드 분기를 추가:

```typescript
// betting-1 완료 시 (인디언 모드)
if (this.state.mode === 'indian' && this.state.phase === 'betting-1') {
  this.state.phase = 'dealing-extra';
  // dealing-extra는 서버가 자동으로 즉시 처리 (handleGameAction에서)
  return;
}
```

이 분기는 betting 완료 후 showdown으로 가기 직전에 삽입한다. 기존 코드를 읽어 정확한 위치를 파악하고 삽입한다.
  </action>
  <verify>
    <automated>cd /c/Users/'Jinmoo Park'/Desktop/sutda && pnpm --filter @sutda/server build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - packages/server/src/game-engine.ts에 `class GollagollaModeStrategy` 가 포함됨
    - packages/server/src/game-engine.ts에 `class IndianModeStrategy` 가 포함됨
    - packages/server/src/game-engine.ts에 `selectGollaCards(` 가 포함됨
    - packages/server/src/game-engine.ts에 `getStateFor(` 가 포함됨
    - packages/server/src/game-engine.ts에 `_dealCardsGollagolla(` 가 포함됨
    - packages/server/src/game-engine.ts에 `_dealCardsIndian(` 가 포함됨
    - packages/server/src/game-engine.ts에 `_dealExtraCardIndian(` 가 포함됨
    - packages/server/src/game-engine.ts에 `case 'gollagolla':` 가 포함됨
    - packages/server/src/game-engine.ts에 `case 'indian':` 가 포함됨
    - `pnpm --filter @sutda/server build`가 TypeScript 에러 없이 성공
  </acceptance_criteria>
  <done>서버 빌드 성공. 두 신규 Strategy, selectGollaCards, getStateFor 구현 완료.</done>
</task>

</tasks>

<verification>
```bash
# 전체 빌드 확인
cd /c/Users/'Jinmoo Park'/Desktop/sutda && pnpm build 2>&1 | tail -15

# 타입 확인
grep -n "gollagolla\|indian\|gollagolla-select\|gollaOpenDeck" packages/shared/src/types/game.ts

# 메서드 확인
grep -n "selectGollaCards\|getStateFor\|_dealCardsGollagolla\|_dealCardsIndian\|_dealExtraCardIndian\|GollagollaModeStrategy\|IndianModeStrategy" packages/server/src/game-engine.ts
```
</verification>

<success_criteria>
- GameMode 타입에 'gollagolla'와 'indian' 포함
- GamePhase 타입에 'gollagolla-select' 포함
- GameState에 gollaOpenDeck?: Card[] 필드 존재
- game-engine.ts에 GollagollaModeStrategy, IndianModeStrategy 클래스 존재
- game-engine.ts에 selectGollaCards(), getStateFor(), dealExtraCardIndian() 공개 메서드 존재
- pnpm build (전체 모노레포)가 타입 에러 없이 성공
</success_criteria>

<output>
완료 후 `.planning/phases/08-huhwi-indian-modes/08-01-SUMMARY.md`를 생성하세요.
</output>
