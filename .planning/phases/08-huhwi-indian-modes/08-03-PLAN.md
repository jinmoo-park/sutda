---
phase: 08-huhwi-indian-modes
plan: "03"
type: execute
wave: 3
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - packages/client/src/components/modals/ModeSelectModal.tsx
  - packages/client/src/components/modals/GollaSelectModal.tsx
  - packages/client/src/components/layout/HandPanel.tsx
  - packages/client/src/pages/RoomPage.tsx
autonomous: true
requirements:
  - MODE-HR-01
  - MODE-HR-02
  - MODE-HR-03
  - MODE-HR-04
  - MODE-IN-01
  - MODE-IN-02
  - MODE-IN-03
  - MODE-IN-04
  - MODE-IN-05

must_haves:
  truths:
    - "ModeSelectModal에 '골라골라'와 '인디언섯다' 버튼이 표시된다"
    - "gollagolla-select phase에서 GollaSelectModal이 열리고 20장 카드 그리드가 표시된다"
    - "이미 타인이 선택한 카드는 opacity-40 + cursor-not-allowed로 dim 처리된다"
    - "내가 선택한 카드(1장)에는 ring-2 ring-primary 하이라이트가 적용된다"
    - "2장 선택 완료 시 자동으로 select-gollagolla-cards emit이 발생한다"
    - "HandPanel에서 cards[0]가 null이면 CardBack으로 렌더링된다"
    - "인디언 모드 betting-1 phase에서 내 첫 카드가 CardBack으로 표시된다"
    - "game-error 수신 시 sonner toast로 에러 메시지가 표시된다"
  artifacts:
    - path: "packages/client/src/components/modals/GollaSelectModal.tsx"
      provides: "골라골라 카드 선택 모달 컴포넌트"
      contains: "GollaSelectModal"
    - path: "packages/client/src/components/modals/ModeSelectModal.tsx"
      provides: "골라골라/인디언섯다 버튼 추가"
      contains: "'골라골라'"
    - path: "packages/client/src/components/layout/HandPanel.tsx"
      provides: "인디언 모드 null 카드 CardBack 렌더링"
      contains: "CardBack"
    - path: "packages/client/src/pages/RoomPage.tsx"
      provides: "GollaSelectModal 마운트 + game-error 소켓 핸들러"
      contains: "GollaSelectModal"
  key_links:
    - from: "GollaSelectModal"
      to: "socket.emit('select-gollagolla-cards')"
      via: "2장 선택 완료 시 자동 emit"
      pattern: "select-gollagolla-cards"
    - from: "RoomPage"
      to: "GollaSelectModal"
      via: "gameState.phase === 'gollagolla-select' 조건"
      pattern: "gollagolla-select"
    - from: "HandPanel"
      to: "CardBack"
      via: "card === null 조건 분기"
      pattern: "card === null"
---

<objective>
골라골라 선택 UI(GollaSelectModal)를 신규 생성하고, ModeSelectModal/HandPanel/RoomPage를 수정하여 두 신규 모드의 클라이언트 경험을 완성한다.

Purpose: Phase 8의 가시적 완성 — 플레이어가 실제로 골라골라와 인디언섯다 모드를 플레이할 수 있게 된다.
Output: GollaSelectModal.tsx (신규), 수정된 ModeSelectModal/HandPanel/RoomPage
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-huhwi-indian-modes/08-CONTEXT.md
@.planning/phases/08-huhwi-indian-modes/08-UI-SPEC.md
@packages/client/src/components/modals/DealerSelectModal.tsx
@packages/client/src/components/modals/ModeSelectModal.tsx
@packages/client/src/components/layout/HandPanel.tsx
@packages/client/src/pages/RoomPage.tsx
</context>

<interfaces>
<!-- 실행자가 사용해야 하는 기존 인터페이스/패턴 -->

DealerSelectModal 그리드 패턴 (재활용 기준):
```typescript
// grid grid-cols-5 gap-2 — 20장 5열 4행 그리드
// 선택 불가: 'cursor-not-allowed opacity-40'
// hover 가능: 'cursor-pointer hover:ring-2 hover:ring-primary'
// 내가 선택: 'opacity-100 ring-2 ring-primary'
// 카드: CardFace / CardBack 컴포넌트
```

GameState (08-01에서 확장됨):
```typescript
gameState.phase: GamePhase  // 'gollagolla-select' 포함
gameState.mode: GameMode    // 'gollagolla' | 'indian' 포함
gameState.gollaOpenDeck?: Card[]  // 골라골라 공개 덱 20장
gameState.players: PlayerState[]  // players[i].cards: (Card | null)[]
```

useGameStore:
```typescript
const { socket, gameState, myPlayerId } = useGameStore();
```

sonner toast:
```typescript
import { toast } from 'sonner';
toast.error('메시지');  // game-error 수신 시
```

RoomPage 기존 모달 마운트 패턴:
```tsx
<DealerSelectModal open={gameState?.phase === 'dealer-select'} roomId={roomId!} />
<ModeSelectModal open={gameState?.phase === 'mode-select'} roomId={roomId!} isDealer={myPlayer?.isDealer ?? false} />
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: GollaSelectModal.tsx 신규 생성 + ModeSelectModal 버튼 추가</name>
  <read_first>
    - packages/client/src/components/modals/DealerSelectModal.tsx (그리드 패턴 재활용 기준)
    - packages/client/src/components/modals/ModeSelectModal.tsx (수정 대상)
    - packages/client/src/components/game/CardFace.tsx (카드 컴포넌트 import 경로 확인)
    - packages/client/src/components/game/CardBack.tsx (카드 컴포넌트 import 경로 확인)
  </read_first>
  <files>
    packages/client/src/components/modals/GollaSelectModal.tsx,
    packages/client/src/components/modals/ModeSelectModal.tsx
  </files>
  <action>
**A. GollaSelectModal.tsx 신규 생성**

DealerSelectModal의 그리드 패턴을 재활용하되, 골라골라 전용 로직으로 교체한다.

```typescript
import { useState } from 'react';
import { useGameStore } from '@/store/gameStore';
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
} from '@/components/ui/dialog';
import { CardFace } from '@/components/game/CardFace';
import { cn } from '@/lib/utils';

interface GollaSelectModalProps {
  open: boolean;
  roomId: string;
}

export function GollaSelectModal({ open, roomId }: GollaSelectModalProps) {
  const { socket, gameState, myPlayerId } = useGameStore();
  const [pendingIndices, setPendingIndices] = useState<number[]>([]);
  const [submitted, setSubmitted] = useState(false);

  const openDeck = gameState?.gollaOpenDeck ?? [];

  // 타인이 이미 선택한 카드 인덱스 — 서버가 game-state를 갱신하면 players.cards로 파악 가능하지만
  // 골라골라 선택 중에는 서버가 선택 완료된 플레이어 cards를 채워 줌.
  // 단순화: 모든 플레이어 cards의 카드를 openDeck 인덱스로 역추적하여 takenIndices 구성.
  const takenByOthers = new Set<number>();
  if (gameState) {
    for (const p of gameState.players) {
      if (p.id === myPlayerId) continue;
      for (const card of p.cards) {
        const idx = openDeck.findIndex(
          (c, i) => c.rank === card.rank && c.type === card.type && !takenByOthers.has(i)
        );
        if (idx >= 0) takenByOthers.add(idx);
      }
    }
  }

  // 내가 이미 서버에 확정 제출했는지 (myPlayer.cards.length >= 2)
  const myPlayer = gameState?.players.find(p => p.id === myPlayerId);
  const alreadyDone = (myPlayer?.cards.length ?? 0) >= 2;

  const handleCardClick = (idx: number) => {
    if (alreadyDone || submitted) return;
    if (takenByOthers.has(idx)) return;

    setPendingIndices(prev => {
      if (prev.includes(idx)) {
        // 토글 해제
        return prev.filter(i => i !== idx);
      }
      if (prev.length >= 2) return prev;  // 이미 2장 선택 중이면 무시
      const next = [...prev, idx];
      if (next.length === 2) {
        // 2장 완료 → 자동 확정 emit
        setSubmitted(true);
        socket?.emit('select-gollagolla-cards', {
          roomId,
          cardIndices: [next[0], next[1]] as [number, number],
        });
      }
      return next;
    });
  };

  // phase가 gollagolla-select를 벗어나면(betting으로 전환) 상태 리셋
  // (open prop이 false가 되므로 별도 리셋 불필요)

  const selectedCount = alreadyDone ? 2 : pendingIndices.length;

  return (
    <Dialog open={open}>
      <DialogContent
        onInteractOutside={(e) => e.preventDefault()}
        onEscapeKeyDown={(e) => e.preventDefault()}
      >
        <DialogHeader>
          <DialogTitle>골라골라 — 카드를 2장 선택하세요</DialogTitle>
        </DialogHeader>
        <p className="text-sm text-muted-foreground text-center">
          {alreadyDone
            ? '선택 완료! 다른 플레이어를 기다리는 중…'
            : `2장을 선택하세요 (${selectedCount}/2)`}
        </p>
        <div className="grid grid-cols-5 gap-2 mt-2">
          {openDeck.map((card, idx) => {
            const isTakenByOther = takenByOthers.has(idx);
            const isMyPick = pendingIndices.includes(idx) || (alreadyDone && myPlayer?.cards.some(
              c => c.rank === card.rank && c.type === card.type
            ));
            const disabled = isTakenByOther || alreadyDone || submitted;

            return (
              <button
                key={idx}
                onClick={() => handleCardClick(idx)}
                disabled={disabled}
                className={cn(
                  'rounded-md transition-opacity',
                  isTakenByOther
                    ? 'opacity-40 cursor-not-allowed'
                    : disabled
                    ? 'cursor-not-allowed opacity-40'
                    : 'cursor-pointer hover:ring-2 hover:ring-primary',
                  isMyPick && !isTakenByOther && 'ring-2 ring-primary opacity-100'
                )}
              >
                <CardFace card={card} />
              </button>
            );
          })}
        </div>
      </DialogContent>
    </Dialog>
  );
}
```

---

**B. ModeSelectModal.tsx 수정** — 골라골라/인디언섯다 버튼 2개 추가 (per D-01, D-06):

기존 `한장공유` 버튼 다음에 추가:
```tsx
<Button
  className="w-full"
  variant="outline"
  onClick={() => socket?.emit('select-mode', { roomId, mode: 'gollagolla' })}
  disabled={!isDealer}
>
  골라골라
</Button>
<Button
  className="w-full"
  variant="outline"
  onClick={() => socket?.emit('select-mode', { roomId, mode: 'indian' })}
  disabled={!isDealer}
>
  인디언섯다
</Button>
```

카피라이팅 계약 (UI-SPEC): 버튼 텍스트는 정확히 `"골라골라"`, `"인디언섯다"` (공백 없음).
  </action>
  <verify>
    <automated>cd /c/Users/'Jinmoo Park'/Desktop/sutda && pnpm --filter @sutda/client build 2>&1 | grep -E "error|Error|ERROR" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/client/src/components/modals/GollaSelectModal.tsx 파일이 존재함
    - GollaSelectModal.tsx에 `'select-gollagolla-cards'` emit 코드가 포함됨
    - GollaSelectModal.tsx에 `grid grid-cols-5 gap-2` 클래스가 포함됨
    - GollaSelectModal.tsx에 `opacity-40 cursor-not-allowed` 클래스가 포함됨
    - GollaSelectModal.tsx에 `ring-2 ring-primary` 클래스가 포함됨
    - ModeSelectModal.tsx에 `골라골라` 텍스트가 포함됨
    - ModeSelectModal.tsx에 `인디언섯다` 텍스트가 포함됨
    - ModeSelectModal.tsx에 `mode: 'gollagolla'` 가 포함됨
    - ModeSelectModal.tsx에 `mode: 'indian'` 가 포함됨
    - 클라이언트 빌드에 TypeScript 에러 없음
  </acceptance_criteria>
  <done>GollaSelectModal 신규 생성 완료. ModeSelectModal에 두 버튼 추가 완료. 클라이언트 빌드 성공.</done>
</task>

<task type="auto">
  <name>Task 2: HandPanel 인디언 null 카드 처리 + RoomPage GollaSelectModal 마운트 + game-error 토스트</name>
  <read_first>
    - packages/client/src/components/layout/HandPanel.tsx (수정 대상 — 현재 카드 렌더링 로직 확인 필수)
    - packages/client/src/pages/RoomPage.tsx (수정 대상 — 모달 마운트 패턴과 소켓 이벤트 핸들러 확인 필수)
    - packages/client/src/components/game/CardBack.tsx (import 경로 확인)
  </read_first>
  <files>
    packages/client/src/components/layout/HandPanel.tsx,
    packages/client/src/pages/RoomPage.tsx
  </files>
  <action>
**A. HandPanel.tsx 수정** — 인디언 모드에서 cards[0]가 null이면 CardBack 렌더링 (per D-08):

1. import에 CardBack 추가:
```typescript
import { CardBack } from '@/components/game/CardBack';
```

2. 카드 렌더링 부분(`cards.map((card, idx) => ...)`)을 수정:

현재 코드는 `<CardFace card={card} />`를 항상 렌더링한다. 이를 null 체크 분기로 교체:

```tsx
{cards.map((card, idx) => (
  <button
    key={idx}
    onClick={() => toggleCard(idx)}
    disabled={!isCardSelectMode}
    className={cn(
      'rounded-md transition-all',
      isCardSelectMode
        ? selectedIndices.includes(idx)
          ? 'ring-2 ring-primary scale-105 opacity-100 cursor-pointer'
          : 'opacity-70 hover:opacity-100 hover:ring-1 hover:ring-primary/50 cursor-pointer'
        : 'cursor-default'
    )}
  >
    {card === null ? (
      <CardBack />   // 인디언 모드에서 마스킹된 카드 (서버가 null로 전송)
    ) : (
      <CardFace card={card} />
    )}
  </button>
))}
```

3. 족보 계산 부분에서 null 카드가 있으면 evaluateHand를 건너뛰도록 수정:

기존 `else if (cards.length >= 2 && phase !== 'card-select')` 조건에 null 체크 추가:
```typescript
} else if (cards.length >= 2 && cards[0] !== null && cards[1] !== null && phase !== 'card-select') {
  const result = evaluateHand(cards[0], cards[1]);
  ...
}
```

이미 `try { ... } catch {}` 블록 안에 있으므로 null 참조 시 evaluateHand 내부에서 에러가 발생해도 catch로 처리되지만, 명시적 null 가드가 더 안전하다.

---

**B. RoomPage.tsx 수정**:

**B-1. GollaSelectModal import 추가**:
```typescript
import { GollaSelectModal } from '@/components/modals/GollaSelectModal';
```
기존 모달 import 목록에 추가.

**B-2. GollaSelectModal 마운트** — 기존 DealerSelectModal 다음에 추가:
```tsx
<GollaSelectModal
  open={gameState?.phase === 'gollagolla-select'}
  roomId={roomId!}
/>
```

**B-3. game-error 소켓 이벤트 핸들러에 골라골라 에러 토스트 처리 추가**:

RoomPage에서 socket 이벤트를 구독하는 useEffect를 찾아 game-error 핸들러를 확인한다. 기존에 game-error 핸들러가 있으면 골라골라 에러 코드 분기를 추가하고, 없으면 새로 추가한다:

```typescript
socket?.on('game-error', ({ code, message }) => {
  if (code === 'CARD_ALREADY_TAKEN') {
    toast.error('이미 선택된 카드입니다. 다른 카드를 선택하세요.');
  } else {
    toast.error(message || '오류가 발생했습니다. 다시 시도해 주세요.');
  }
});
```

주의: RoomPage에 기존 game-error 핸들러가 있으면 위 코드로 교체하거나 병합. 없으면 socket이 존재하는 useEffect 내에 추가. toast는 이미 `import { toast } from 'sonner'`로 import되어 있는지 확인.

**B-4. gollagolla 모드에서 cutting → betting 딜링 애니메이션 건너뛰기**:

기존 `cutting → betting` 감지 useEffect를 찾아 gollagolla 모드에서는 딜링 애니메이션(visibleCardCounts 설정)을 건너뛰도록 수정:

```typescript
// cutting → betting 전환 감지 — 골라골라 모드에서는 해당 없음 (직접 카드 선택)
if (prevPhaseRef.current === 'cutting' && (gameState?.phase === 'betting' || gameState?.phase === 'betting-1')) {
  if (gameState?.mode === 'gollagolla') {
    // 골라골라: 딜링 애니메이션 없음, visibleCardCounts를 즉시 최종 값으로 설정
    const counts: Record<string, number> = {};
    gameState.players.forEach(p => { counts[p.id] = 2; });
    setVisibleCardCounts(counts);
    // showCardConfirm 표시 없음 (직접 선택이므로 불필요)
    prevPhaseRef.current = gameState.phase;
    return;
  }
  // ... 기존 딜링 애니메이션 코드 유지
}
```

주의: 정확한 기존 코드 위치를 읽어 확인 후 수정. 기존 흐름을 깨지 않도록 주의.

주의: 인디언 모드에서도 cutting → betting-1 전환 감지가 작동해야 함. 기존 코드가 `betting | betting-1` 모두 처리하면 그대로 두되, 인디언 모드는 기존 딜링 애니메이션 로직을 그대로 활용 (1장만 배분).
  </action>
  <verify>
    <automated>cd /c/Users/'Jinmoo Park'/Desktop/sutda && pnpm --filter @sutda/client build 2>&1 | grep -E "error|Error|ERROR" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/client/src/components/layout/HandPanel.tsx에 `import { CardBack }` 가 포함됨
    - packages/client/src/components/layout/HandPanel.tsx에 `card === null` 조건 분기가 포함됨
    - packages/client/src/components/layout/HandPanel.tsx에 `<CardBack />` 렌더링 코드가 포함됨
    - packages/client/src/pages/RoomPage.tsx에 `GollaSelectModal` import가 포함됨
    - packages/client/src/pages/RoomPage.tsx에 `phase === 'gollagolla-select'` 조건이 포함됨
    - packages/client/src/pages/RoomPage.tsx에 `CARD_ALREADY_TAKEN` 처리 코드가 포함됨
    - 클라이언트 빌드에 TypeScript 에러 없음
  </acceptance_criteria>
  <done>HandPanel null 카드 처리 완료. RoomPage GollaSelectModal 마운트 및 game-error 토스트 처리 완료. 전체 클라이언트 빌드 성공.</done>
</task>

</tasks>

<verification>
```bash
# 전체 모노레포 빌드
cd /c/Users/'Jinmoo Park'/Desktop/sutda && pnpm build 2>&1 | tail -20

# 컴포넌트 확인
ls packages/client/src/components/modals/
grep -n "골라골라\|인디언섯다" packages/client/src/components/modals/ModeSelectModal.tsx
grep -n "CARD_ALREADY_TAKEN\|gollagolla-select\|GollaSelectModal" packages/client/src/pages/RoomPage.tsx
grep -n "card === null\|CardBack" packages/client/src/components/layout/HandPanel.tsx
```
</verification>

<success_criteria>
- GollaSelectModal.tsx 파일이 존재하며 20장 그리드 + 선착순 선택 로직 구현
- ModeSelectModal에 '골라골라', '인디언섯다' 버튼 존재
- HandPanel이 null 카드를 CardBack으로 렌더링
- RoomPage가 gollagolla-select phase에서 GollaSelectModal을 열고, game-error에서 토스트 표시
- 전체 모노레포 pnpm build가 타입 에러 없이 성공
- Phase 8 목표: 5가지 모드 전체(오리지날/세장/한장공유/골라골라/인디언섯다)가 선택 가능하고, 골라골라/인디언 모드의 고유 플로우가 작동
</success_criteria>

<output>
완료 후 `.planning/phases/08-huhwi-indian-modes/08-03-SUMMARY.md`를 생성하세요.
</output>
