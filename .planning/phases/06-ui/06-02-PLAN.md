---
phase: 06-ui
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - packages/client/src/pages/MainPage.tsx
  - packages/client/src/pages/RoomPage.tsx
  - packages/client/src/components/game/CardFace.tsx
  - packages/client/src/components/game/CardBack.tsx
  - packages/client/src/components/game/PlayerSeat.tsx
  - packages/client/src/components/game/ChipDisplay.tsx
  - packages/client/src/components/layout/GameTable.tsx
  - packages/client/src/components/layout/HandPanel.tsx
  - packages/client/src/components/layout/BettingPanel.tsx
  - packages/client/src/components/layout/InfoPanel.tsx
  - packages/client/src/components/layout/ChatPanel.tsx
  - packages/client/src/components/layout/WaitingRoom.tsx
  - packages/client/src/components/layout/HandReferenceDialog.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-07
  - UI-08

must_haves:
  truths:
    - "메인 화면에서 방 생성 버튼과 링크 참여 버튼이 표시된다"
    - "대기실에서 참가자 목록과 URL 복사 버튼이 표시된다"
    - "게임 테이블에 원형으로 플레이어 자리가 배치된다"
    - "자신의 카드는 앞면(숫자+속성 텍스트), 타인의 카드는 뒷면으로 표시된다"
    - "베팅 버튼(콜/레이즈/다이/체크)과 칩 단위 입력이 표시된다"
    - "현재 판돈(팟) 금액이 테이블 중앙에 표시된다"
    - "족보 참고표를 버튼 하나로 열어볼 수 있다"
  artifacts:
    - path: "packages/client/src/components/game/CardFace.tsx"
      provides: "앞면 카드 컴포넌트 (rank + attribute 한국어 텍스트)"
      exports: ["CardFace"]
    - path: "packages/client/src/components/game/CardBack.tsx"
      provides: "뒷면 카드 컴포넌트"
      exports: ["CardBack"]
    - path: "packages/client/src/components/game/PlayerSeat.tsx"
      provides: "원형 배치 플레이어 자리"
      exports: ["PlayerSeat"]
    - path: "packages/client/src/components/layout/GameTable.tsx"
      provides: "원형 플레이어 배치 컨테이너 + 팟 중앙"
      exports: ["GameTable"]
    - path: "packages/client/src/components/layout/BettingPanel.tsx"
      provides: "콜/레이즈/다이/체크 버튼 + 칩 단위 입력"
      exports: ["BettingPanel"]
    - path: "packages/client/src/components/layout/WaitingRoom.tsx"
      provides: "대기실 (참가자 목록 + URL 복사 + 게임 시작)"
      exports: ["WaitingRoom"]
    - path: "packages/client/src/components/layout/HandReferenceDialog.tsx"
      provides: "족보 참고표 Dialog (버튼 하나로 열람)"
      exports: ["HandReferenceDialog"]
  key_links:
    - from: "packages/client/src/pages/RoomPage.tsx"
      to: "GameTable, WaitingRoom, BettingPanel, HandPanel, InfoPanel"
      via: "gameState.phase 기반 조건부 렌더"
      pattern: "phase ===|gameState\\.phase"
    - from: "packages/client/src/components/game/PlayerSeat.tsx"
      to: "CardFace, CardBack"
      via: "isMe 조건으로 앞면/뒷면 분기"
      pattern: "isMe.*CardFace|CardBack"
    - from: "packages/client/src/components/layout/BettingPanel.tsx"
      to: "socket.emit('bet-action')"
      via: "Zustand 스토어 socket으로 이벤트 전송"
      pattern: "emit.*bet-action"
    - from: "packages/client/src/components/layout/HandPanel.tsx"
      to: "HandReferenceDialog"
      via: "족보 참고표 버튼 클릭으로 Dialog 오픈"
      pattern: "HandReferenceDialog|setShowReference"
---

<objective>
게임의 핵심 UI 컴포넌트를 구현한다. 메인 화면(방 생성/참여), 대기실, 게임 테이블(원형 플레이어 배치), 카드(앞면/뒷면), 칩 표시, 베팅 패널, 정보 패널, 족보 참고표 Dialog를 모두 구축한다.

Purpose: 플레이어가 로비 입장부터 게임 테이블에서 카드를 보고 베팅하는 전체 경험을 할 수 있도록 한다. 족보 참고표를 버튼 하나로 열람할 수 있어 UI-07을 충족한다.
Output: 13개 React 컴포넌트 (game 4개 + layout 7개 + pages 2개 업데이트)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ui/06-CONTEXT.md
@.planning/phases/06-ui/06-RESEARCH.md
@.planning/phases/06-ui/06-UI-SPEC.md
@.planning/phases/06-ui/06-01-SUMMARY.md

@packages/shared/src/types/game.ts
@packages/shared/src/types/protocol.ts
@packages/shared/src/types/room.ts
@packages/shared/src/types/card.ts

<interfaces>
<!-- Plan 01에서 생성된 모듈. 이 인터페이스들을 사용해야 한다. -->

From packages/client/src/store/gameStore.ts:
```typescript
interface GameStore {
  socket: AppSocket | null;
  gameState: GameState | null;
  roomState: RoomState | null;
  myPlayerId: string | null;
  error: string | null;
  rechargeRequest: RechargeRequest | null;
  connect: (serverUrl: string) => void;
  disconnect: () => void;
  clearError: () => void;
}
export const useGameStore = create<GameStore>(...);
```

From packages/client/src/lib/cardUtils.ts:
```typescript
export const RANK_KOREAN: Record<number, string>;
export const ATTRIBUTE_LABELS: Record<CardAttribute, string>;
export function rankToKorean(rank: CardRank | number): string;
export function cardToText(card: Card): string;
```

From packages/shared/src/types/game.ts:
```typescript
export interface PlayerState { id: string; nickname: string; chips: number; cards: Card[]; isAlive: boolean; isRevealed: boolean; currentBet: number; isDealer: boolean; seatIndex: number; chipBreakdown: ChipBreakdown; }
export interface GameState { roomId: string; phase: GamePhase; mode: GameMode; players: PlayerState[]; pot: number; currentPlayerIndex: number; currentBetAmount: number; roundNumber: number; winnerId?: string; effectiveMaxBet?: number; /* ... */ }
export interface ChipBreakdown { ten_thousand: number; five_thousand: number; one_thousand: number; five_hundred: number; }
export type BetAction = { type: 'call' } | { type: 'raise'; amount: number } | { type: 'die' } | { type: 'check' };
```

From packages/shared/src/types/room.ts:
```typescript
export interface RoomPlayer { id: string; nickname: string; chips: number; seatIndex: number; isConnected: boolean; }
export interface RoomState { roomId: string; hostId: string; players: RoomPlayer[]; maxPlayers: number; gamePhase: 'waiting' | 'playing'; createdAt: number; }
```

From packages/shared/src/types/protocol.ts:
```typescript
// 클라이언트 -> 서버
'create-room': (data: { nickname: string; initialChips: number }) => void;
'join-room': (data: { roomId: string; nickname: string; initialChips: number }) => void;
'start-game': (data: { roomId: string }) => void;
'bet-action': (data: { roomId: string; action: BetAction }) => void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 게임 도메인 컴포넌트 (CardFace, CardBack, PlayerSeat, ChipDisplay)</name>
  <files>
    packages/client/src/components/game/CardFace.tsx,
    packages/client/src/components/game/CardBack.tsx,
    packages/client/src/components/game/PlayerSeat.tsx,
    packages/client/src/components/game/ChipDisplay.tsx
  </files>
  <read_first>
    packages/shared/src/types/card.ts,
    packages/shared/src/types/game.ts,
    packages/client/src/lib/cardUtils.ts,
    packages/client/src/lib/utils.ts,
    packages/client/src/components/ui/badge.tsx,
    packages/client/src/components/ui/card.tsx,
    .planning/phases/06-ui/06-UI-SPEC.md
  </read_first>
  <action>
    **1. src/components/game/CardFace.tsx** (per UI-SPEC Card Display Contract, UI-08):

    Props: `{ card: Card; className?: string }`

    앞면 카드 -- 숫자(rank) + 속성(attribute) 한국어 텍스트 표시.
    - 컨테이너: `bg-white text-gray-900 rounded-md border border-gray-200 w-16 h-24 flex flex-col items-center justify-center gap-1 p-2`
    - 상단: `rankToKorean(card.rank)` 텍스트 (`text-lg font-semibold`)
    - 하단 (attribute !== 'normal'일 때만):
      - gwang: Badge `className="bg-yellow-400 text-yellow-900"` 내용 "광"
      - yeolkkeut: Badge `className="bg-blue-400 text-blue-900"` 내용 "열끗"
    - `cardToText`를 import하여 사용 가능하지만, 시각적으로 rank와 attribute를 분리 표시하므로 `rankToKorean`과 `ATTRIBUTE_LABELS`를 개별 사용.

    **2. src/components/game/CardBack.tsx** (per UI-SPEC):

    Props: `{ className?: string }`

    뒷면 카드 -- muted 단색 블록.
    - 컨테이너: `bg-muted rounded-md border border-border w-16 h-24 flex items-center justify-center`
    - 내부: `text-muted-foreground text-lg` "?" 텍스트

    **3. src/components/game/PlayerSeat.tsx** (per D-08, UI-02, UI-03):

    Props: `{ seatIndex: number; totalPlayers: number; player: PlayerState; isMe: boolean; isCurrentTurn: boolean; }`

    CSS custom properties 원형 배치:
    ```tsx
    const style = {
      '--angle': `calc(360deg / ${totalPlayers} * ${seatIndex})`,
    } as React.CSSProperties;

    // 데스크톱에서만 원형 배치, 모바일은 별도
    <div
      className="absolute top-1/2 left-1/2 -mt-14 -ml-14 hidden md:block transition-transform duration-500"
      style={{
        ...style,
        transform: `rotate(var(--angle)) translateY(-200px) rotate(calc(var(--angle) * -1))`,
      }}
    >
    ```

    구조:
    - shadcn Card 래퍼: `className={cn("w-28 p-2 space-y-1", isCurrentTurn && "ring-2 ring-primary")}` (현재 턴 하이라이트 per UI-SPEC Accent 사용처 3번)
    - 닉네임: `<p className="text-xs font-semibold truncate">{player.nickname}</p>`
    - isDealer 표시: Badge `variant="outline"` "선" (dealer일 때만)
    - 카드 영역 (2장 나란히):
      - isMe이면: `<CardFace card={card} />` (앞면)
      - isMe가 아니면: `<CardBack />` (뒷면)
      - cards가 비어있으면 표시 안 함
    - isAlive === false이면: 전체에 `opacity-50` 적용 + Badge "다이" (destructive variant)
    - 칩 잔액: `<p className="text-xs tabular-nums">{player.chips.toLocaleString()}원</p>`
    - 현재 베팅 금액 (currentBet > 0일 때): Badge `variant="secondary"` `{player.currentBet.toLocaleString()}원`

    모바일 레이아웃 (md 미만): 원형 배치 대신 일반 flex 아이템으로 렌더. `className="md:hidden"` 별도 div으로 같은 내용을 수평 카드 형태로 표시.

    **4. src/components/game/ChipDisplay.tsx** (per UI-SPEC Chip Display Contract, CHIP-04):

    Props: `{ breakdown: ChipBreakdown; className?: string }`

    칩 단위별 색상 배지 표시:
    - `ten_thousand > 0`이면: Badge `className="bg-red-500 text-white"` `만 x${breakdown.ten_thousand}`
    - `five_thousand > 0`이면: Badge `className="bg-green-500 text-white"` `5천 x${breakdown.five_thousand}`
    - `one_thousand > 0`이면: Badge `className="bg-blue-500 text-white"` `천 x${breakdown.one_thousand}`
    - `five_hundred > 0`이면: Badge `className="bg-gray-400 text-gray-900"` `5백 x${breakdown.five_hundred}`
    - 각 Badge 사이 `gap-1`
  </action>
  <verify>
    <automated>cd packages/client && pnpm build 2>&1 | tail -5 && pnpm test -- CardFace 2>&1 | tail -10</automated>
  </verify>
  <done>CardFace(숫자+속성 텍스트), CardBack(muted "?"), PlayerSeat(CSS 원형 배치 + 턴 하이라이트 + 앞면/뒷면 분기), ChipDisplay(4색 칩 배지) 4개 게임 도메인 컴포넌트 완성, CardFace 테스트 스텁 통과</done>
</task>

<task type="auto">
  <name>Task 2: 레이아웃 패널 5개 + 족보 참고표 Dialog (HandPanel, BettingPanel, InfoPanel, ChatPanel, HandReferenceDialog)</name>
  <files>
    packages/client/src/components/layout/HandPanel.tsx,
    packages/client/src/components/layout/BettingPanel.tsx,
    packages/client/src/components/layout/InfoPanel.tsx,
    packages/client/src/components/layout/ChatPanel.tsx,
    packages/client/src/components/layout/HandReferenceDialog.tsx
  </files>
  <read_first>
    packages/client/src/components/game/PlayerSeat.tsx,
    packages/client/src/components/game/CardFace.tsx,
    packages/client/src/components/game/ChipDisplay.tsx,
    packages/client/src/store/gameStore.ts,
    packages/client/src/lib/cardUtils.ts,
    packages/shared/src/types/game.ts,
    packages/shared/src/types/protocol.ts,
    .planning/phases/06-ui/06-UI-SPEC.md,
    .planning/phases/06-ui/06-CONTEXT.md
  </read_first>
  <action>
    **1. src/components/layout/HandPanel.tsx** (per D-10, D-11, UI-03, UI-07):

    Props: `{ myPlayer: PlayerState | null; }`

    내 손패 + 족보 텍스트 자동 표시 (per D-11: 족보명 자동 인라인 표시) + 족보 참고표 버튼 (per UI-07).
    - 카드 2장 나란히: `CardFace` 컴포넌트 사용
    - cards가 비어있으면 "카드가 아직 없어요" muted 텍스트
    - `@sutda/shared`에서 `evaluateHand` 사용 가능하면 import하여 족보명 Badge 표시. 없으면 족보 표시 생략 (서버에서 result phase에 포함).
    - 족보명은 `HandType` 문자열을 한국어로 매핑하는 간단한 Record 사용:
      ```typescript
      const HAND_TYPE_KOREAN: Record<string, string> = {
        'sam-pal-gwang-ttaeng': '삼팔광땡',
        'il-pal-gwang-ttaeng': '일팔광땡',
        'il-sam-gwang-ttaeng': '일삼광땡',
        'jang-ttaeng': '장땡', 'gu-ttaeng': '구땡', 'pal-ttaeng': '팔땡',
        'chil-ttaeng': '칠땡', 'yuk-ttaeng': '육땡', 'o-ttaeng': '오땡',
        'sa-ttaeng': '사땡', 'sam-ttaeng': '삼땡', 'i-ttaeng': '이땡', 'il-ttaeng': '일땡',
        'ali': '알리', 'dok-sa': '독사', 'gu-bbing': '구삥',
        'jang-bbing': '장삥', 'jang-sa': '장사', 'sae-ryuk': '새륙',
        'kkut': '끗',
      };
      ```
    - evaluateHand를 import하여 `const result = evaluateHand(myPlayer.cards)` 호출 후 `HAND_TYPE_KOREAN[result.handType]` + (kkut이면 `${result.score}끗`) Badge로 표시.
    - **족보 참고표 버튼** (per UI-07: "족보 참고표를 버튼 하나로 볼 수 있다"):
      ```tsx
      const [showReference, setShowReference] = useState(false);
      // 카드 영역 아래에:
      <Button variant="ghost" size="sm" onClick={() => setShowReference(true)}>
        족보 참고표
      </Button>
      <HandReferenceDialog open={showReference} onOpenChange={setShowReference} />
      ```

    **2. src/components/layout/HandReferenceDialog.tsx** (per UI-07: "족보 참고표를 버튼 하나로 볼 수 있다"):

    Props: `{ open: boolean; onOpenChange: (open: boolean) => void; }`

    족보 전체 순위 참고표를 Dialog로 표시. D-11에 따라 족보명 텍스트는 인라인 자동 표시하되, UI-07 요구사항("족보 참고표를 버튼 하나로 볼 수 있다")을 충족하기 위해 전체 순위표를 별도 Dialog로 제공.
    - shadcn Dialog 사용
    - DialogTitle: "족보 순위표"
    - 내용: 전체 족보를 순위 순으로 나열:
      ```tsx
      const HAND_RANKINGS = [
        { rank: 1, name: '삼팔광땡', desc: '3광 + 8광', tier: '광땡' },
        { rank: 2, name: '일팔광땡', desc: '1광 + 8광', tier: '광땡' },
        { rank: 3, name: '일삼광땡', desc: '1광 + 3광', tier: '광땡' },
        { rank: 4, name: '장땡', desc: '10 + 10', tier: '땡' },
        { rank: 5, name: '구땡', desc: '9 + 9', tier: '땡' },
        // ... 일땡까지
        { rank: 14, name: '알리', desc: '1 + 2', tier: '특수' },
        { rank: 15, name: '독사', desc: '1 + 4', tier: '특수' },
        { rank: 16, name: '구삥', desc: '1 + 9', tier: '특수' },
        { rank: 17, name: '장삥', desc: '1 + 10', tier: '특수' },
        { rank: 18, name: '장사', desc: '4 + 10', tier: '특수' },
        { rank: 19, name: '새륙', desc: '4 + 6', tier: '특수' },
        { rank: 20, name: '끗', desc: '합의 일의 자리 (9끗~0끗)', tier: '끗' },
      ];
      ```
    - tier별 Separator로 구분
    - 특수 카드 설명 추가: "땡잡이 (3+7): 장땡 미만의 땡을 이김", "암행어사 (열끗4+열끗7): 일팔광땡, 일삼광땡을 이김"
    - 스크롤 가능: `<DialogContent className="max-h-[80vh] overflow-y-auto">`

    **3. src/components/layout/BettingPanel.tsx** (per D-10, UI-04, CHIP-05):

    Props: `{ isMyTurn: boolean; currentBetAmount: number; myChips: number; roomId: string; effectiveMaxBet?: number; }`

    콜/레이즈/다이/체크 버튼 + 칩 단위 입력 (per Phase 5 D-09):
    - `const [raiseAmount, setRaiseAmount] = useState(0);` 내부 상태
    - 칩 단위 버튼 4개 (per UI-SPEC Chip Display Contract):
      - `<Button variant="secondary" onClick={() => setRaiseAmount(prev => prev + 500)}>` 회색 배지 + "+500"
      - `<Button variant="secondary" onClick={() => setRaiseAmount(prev => prev + 1000)}>` 파란 배지 + "+1,000"
      - `<Button variant="secondary" onClick={() => setRaiseAmount(prev => prev + 5000)}>` 초록 배지 + "+5,000"
      - `<Button variant="secondary" onClick={() => setRaiseAmount(prev => prev + 10000)}>` 빨간 배지 + "+10,000"
    - 현재 입력 금액: `<p className="text-[28px] font-semibold tabular-nums">{raiseAmount.toLocaleString()}원</p>`
    - "초기화" ghost 버튼: `onClick={() => setRaiseAmount(0)}`
    - 액션 버튼:
      - "콜" `<Button onClick={() => emitAction({ type: 'call' })}>콜</Button>` -- primary variant (per UI-SPEC Accent 사용처 1번)
      - "레이즈" `<Button variant="secondary" onClick={() => emitAction({ type: 'raise', amount: raiseAmount })}>레이즈</Button>` -- secondary variant (per UI-SPEC muted 규칙)
      - "다이" `<Button variant="destructive" onClick={() => emitAction({ type: 'die' })}>다이</Button>` -- destructive variant (per UI-SPEC Destructive)
      - "체크" `<Button variant="ghost" onClick={() => emitAction({ type: 'check' })}>체크</Button>` -- ghost variant (per UI-SPEC muted 규칙)
    - 체크 버튼은 `currentBetAmount === 0`일 때만 표시 (아직 아무도 베팅 안 했을 때)
    - 모든 버튼 `min-h-11` (44px 터치 타깃 per UI-SPEC Accessibility)
    - `disabled={!isMyTurn}` -- 내 턴이 아닐 때 전체 비활성
    - 내 턴이 아닐 때 상태 텍스트: `<p className="text-sm text-muted-foreground">{currentPlayerNickname}의 차례예요</p>` (per UI-SPEC Empty State)

    emitAction 함수:
    ```typescript
    const emitAction = (action: BetAction) => {
      const socket = useGameStore.getState().socket;
      if (!socket || !isMyTurn) return;
      socket.emit('bet-action', { roomId, action });
    };
    ```

    **4. src/components/layout/InfoPanel.tsx** (per D-10):

    Props: `{ myChips: number; pot: number; players: PlayerState[]; }`

    내 잔액 + 상대 잔액 요약 + 팟 금액:
    - "내 잔액" `<p>{myChips.toLocaleString()}원</p>` (font-semibold tabular-nums)
    - "팟" `<p>{pot.toLocaleString()}원</p>`
    - 상대 잔액 목록: 각 플레이어 닉네임 + 칩 잔액 리스트
    - Separator로 구분

    **5. src/components/layout/ChatPanel.tsx** (per D-10, Deferred: 레이아웃만 예약):

    `<div className="h-16 border-t border-border flex items-center justify-center text-muted-foreground text-sm">채팅 (준비 중)</div>`
  </action>
  <verify>
    <automated>cd packages/client && pnpm build 2>&1 | tail -5 && pnpm test -- "BettingPanel|HandPanel" 2>&1 | tail -10</automated>
  </verify>
  <done>HandPanel(손패+족보 자동표시+족보 참고표 버튼), HandReferenceDialog(전체 족보 순위표 Dialog), BettingPanel(콜/레이즈/다이/체크+칩 입력), InfoPanel(잔액+팟), ChatPanel(placeholder) 5개 레이아웃 컴포넌트 + 족보 참고표 Dialog 완성</done>
</task>

<task type="auto">
  <name>Task 3: WaitingRoom + GameTable + RoomPage 상태 머신 조립 + MainPage 완성</name>
  <files>
    packages/client/src/components/layout/GameTable.tsx,
    packages/client/src/components/layout/WaitingRoom.tsx,
    packages/client/src/pages/RoomPage.tsx,
    packages/client/src/pages/MainPage.tsx
  </files>
  <read_first>
    packages/client/src/components/game/PlayerSeat.tsx,
    packages/client/src/components/game/CardFace.tsx,
    packages/client/src/components/game/ChipDisplay.tsx,
    packages/client/src/components/layout/HandPanel.tsx,
    packages/client/src/components/layout/BettingPanel.tsx,
    packages/client/src/components/layout/InfoPanel.tsx,
    packages/client/src/components/layout/ChatPanel.tsx,
    packages/client/src/store/gameStore.ts,
    packages/client/src/pages/RoomPage.tsx,
    packages/client/src/pages/MainPage.tsx,
    packages/shared/src/types/game.ts,
    packages/shared/src/types/protocol.ts,
    packages/shared/src/types/room.ts,
    .planning/phases/06-ui/06-UI-SPEC.md,
    .planning/phases/06-ui/06-CONTEXT.md
  </read_first>
  <action>
    **1. src/components/layout/GameTable.tsx** (per D-08, D-09, UI-02, UI-05):

    Props: `{ players: PlayerState[]; myPlayerId: string | null; currentPlayerIndex: number; pot: number; }`

    데스크톱 (md 이상): 원형 컨테이너
    ```tsx
    <div className="relative mx-auto hidden md:block" style={{ width: '480px', height: '480px' }}>
      {/* 중앙 팟 표시 */}
      <div className="absolute inset-0 flex items-center justify-center">
        <div className="text-center">
          <p className="text-sm text-muted-foreground">판돈</p>
          <p className="text-[28px] font-semibold tabular-nums">{pot.toLocaleString()}원</p>
        </div>
      </div>
      {/* 플레이어 원형 배치 */}
      {players.map((p, i) => (
        <PlayerSeat
          key={p.id}
          seatIndex={i}
          totalPlayers={players.length}
          player={p}
          isMe={p.id === myPlayerId}
          isCurrentTurn={i === currentPlayerIndex}
        />
      ))}
    </div>
    ```

    모바일 (md 미만): 세로 스택
    ```tsx
    <div className="md:hidden space-y-2 p-4">
      {/* 팟 한줄 요약 */}
      <div className="text-center py-2">
        <span className="text-sm text-muted-foreground">판돈 </span>
        <span className="font-semibold tabular-nums">{pot.toLocaleString()}원</span>
      </div>
      {/* 그리드 배치 */}
      <div className="grid grid-cols-2 gap-2">
        {players.map((p, i) => (
          <PlayerSeat key={p.id} seatIndex={i} totalPlayers={players.length} player={p} isMe={p.id === myPlayerId} isCurrentTurn={i === currentPlayerIndex} />
        ))}
      </div>
    </div>
    ```

    **2. src/components/layout/WaitingRoom.tsx** (per D-07, UI-01):

    Props: `{ roomState: RoomState; myPlayerId: string | null; roomId: string; }`

    대기실:
    - 방 URL 표시: `window.location.origin + '/room/' + roomId`
    - "링크 복사" 버튼: `navigator.clipboard.writeText(url)` + 1.5초간 "복사됨!" 텍스트 전환 (per UI-SPEC Copywriting)
    - 참가자 목록: `roomState.players.map(p => <div>{p.nickname} -- {p.chips.toLocaleString()}원</div>)`
    - `roomState.players.length === 0`이면: "아직 아무도 없어요" heading + "친구에게 링크를 공유해 보세요." body (per UI-SPEC Empty State)
    - 방장인 경우 (myPlayerId === roomState.hostId): "게임 시작" Button (primary, per UI-SPEC Accent 사용처 4번)
      - `onClick`: `socket.emit('start-game', { roomId })`
      - 참가자 2명 미만이면 disabled
    - 방장이 아닌 경우: "방장이 게임을 시작할 때까지 기다려 주세요" muted 텍스트

    **3. src/pages/RoomPage.tsx** 업데이트 (per D-06, D-07, RESEARCH Pattern 4):

    Plan 01의 스켈레톤을 확장하여 상태 머신 기반 화면 전환 구현.
    **중요:** 이 태스크에서는 join-room emit을 구현하지 않는다 (06-03 Task 2에서 닉네임 입력 + join-room 최종 통합).
    현재는 socket 연결만 하고, gameState.phase에 따라 화면을 전환하는 기본 구조를 구축한다.

    - `gameState.phase`에 따라:
      - `'waiting'` || gameState 없음: `<WaitingRoom />`
      - `'dealer-select'`, `'attend-school'`, `'shuffling'`, `'cutting'`, `'dealing'`, `'betting'`, `'showdown'`: GameTable + HandPanel + BettingPanel + InfoPanel 표시
      - `'result'`: placeholder (Plan 03에서 ResultScreen으로 교체)
    - Sonner `<Toaster />` 마운트 + error state 변경 시 toast 호출 (per UI-SPEC Error State)
    - 게임 진행 중 레이아웃:
      ```tsx
      <div className="flex min-h-screen flex-col bg-background text-foreground">
        <div className="flex-1 flex items-center justify-center">
          <GameTable players={gameState.players} myPlayerId={myPlayerId} currentPlayerIndex={gameState.currentPlayerIndex} pot={gameState.pot} />
        </div>
        <div className="border-t border-border">
          <div className="flex flex-col md:flex-row gap-4 p-4">
            <HandPanel myPlayer={myPlayer ?? null} />
            <InfoPanel myChips={myPlayer?.chips ?? 0} pot={gameState.pot} players={gameState.players} />
          </div>
          {phase === 'betting' && (
            <BettingPanel isMyTurn={isMyTurn} currentBetAmount={gameState.currentBetAmount} myChips={myPlayer?.chips ?? 0} roomId={roomId!} effectiveMaxBet={gameState.effectiveMaxBet} />
          )}
          <ChatPanel />
        </div>
        <Toaster />
      </div>
      ```

    **4. src/pages/MainPage.tsx** 완성 (per D-07, UI-01):

    Plan 01의 스켈레톤을 UI-SPEC Copywriting Contract에 맞게 보완:
    - "방 만들기" primary 버튼, "링크로 참여" secondary 버튼
    - 칩 금액 입력 추가: 만원 단위 Input (default 100000) (per INFRA-06)
    - 에러 토스트 표시 (`<Toaster />`)
    - Sonner import: `import { Toaster, toast } from 'sonner'`
  </action>
  <verify>
    <automated>cd packages/client && pnpm build 2>&1 | tail -5 && pnpm test -- "MainPage|GameTable" 2>&1 | tail -10</automated>
  </verify>
  <done>GameTable(원형 배치+팟 중앙), WaitingRoom(참가자+URL복사+게임시작), RoomPage(phase 기반 화면 전환 기본 구조), MainPage(칩 금액 입력+에러 토스트) 완성. join-room emit은 06-03에서 최종 통합.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @sutda/client build` 성공
- `pnpm --filter @sutda/client test` 모든 테스트 통과
- GameTable에서 원형 배치 CSS (`translateY(-200px)`) 적용됨
- BettingPanel에서 4개 액션 버튼(콜/레이즈/다이/체크) 구현됨
- WaitingRoom에서 URL 복사 기능 구현됨
- RoomPage에서 phase 기반 조건부 렌더링 작동
- HandPanel에서 족보 참고표 버튼이 HandReferenceDialog를 트리거
</verification>

<success_criteria>
1. 13개 React 컴포넌트 모두 생성되어 빌드 성공
2. PlayerSeat에서 CSS custom properties로 2~6인 원형 배치 작동
3. CardFace에 숫자+속성 한국어 텍스트 표시 (UI-08)
4. 자신의 카드 앞면, 타인의 카드 뒷면 분기 (UI-03)
5. 콜/레이즈/다이/체크 4개 베팅 버튼 + 칩 단위 입력 구현 (UI-04)
6. 판돈(팟) 금액 중앙 표시 (UI-05)
7. 대기실에서 URL 복사 및 게임 시작 버튼 작동 (UI-01)
8. 족보 참고표 버튼 클릭 시 전체 순위표 Dialog 표시 (UI-07)
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui/06-02-SUMMARY.md`
</output>
