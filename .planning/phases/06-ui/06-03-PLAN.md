---
phase: 06-ui
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - packages/client/src/components/modals/DealerSelectModal.tsx
  - packages/client/src/components/modals/AttendSchoolModal.tsx
  - packages/client/src/components/modals/ShuffleModal.tsx
  - packages/client/src/components/modals/CutModal.tsx
  - packages/client/src/components/modals/RechargeVoteModal.tsx
  - packages/client/src/components/modals/LeaveRoomDialog.tsx
  - packages/client/src/components/layout/ResultScreen.tsx
  - packages/client/src/pages/RoomPage.tsx
autonomous: false
requirements:
  - UI-06
  - UI-07

must_haves:
  truths:
    - "판 결과 화면에서 모든 생존 플레이어 카드가 공개되고 승자와 칩 변동이 표시된다"
    - "족보명이 각 플레이어 옆에 Badge로 표시된다"
    - "밤일낮장 모달에서 20장 카드 보드 중 1장 선택이 가능하다"
    - "등교 모달에서 학교간다/잠시쉬기 선택이 가능하다"
    - "기리 모달에서 더미 분할 또는 퉁 선언이 가능하다"
    - "재충전 투표 모달에서 동의/거부가 가능하다"
    - "방 나가기 확인 다이얼로그가 표시된다"
  artifacts:
    - path: "packages/client/src/components/layout/ResultScreen.tsx"
      provides: "판 결과 화면 (카드 공개, 승자, 칩 변동)"
      exports: ["ResultScreen"]
    - path: "packages/client/src/components/modals/DealerSelectModal.tsx"
      provides: "밤일낮장 20장 카드 선택 모달"
      exports: ["DealerSelectModal"]
    - path: "packages/client/src/components/modals/AttendSchoolModal.tsx"
      provides: "등교 모달"
      exports: ["AttendSchoolModal"]
    - path: "packages/client/src/components/modals/CutModal.tsx"
      provides: "기리 모달 (더미 분할 + 퉁 선언)"
      exports: ["CutModal"]
    - path: "packages/client/src/components/modals/RechargeVoteModal.tsx"
      provides: "재충전 투표 모달"
      exports: ["RechargeVoteModal"]
    - path: "packages/client/src/components/modals/LeaveRoomDialog.tsx"
      provides: "방 나가기 확인 다이얼로그"
      exports: ["LeaveRoomDialog"]
  key_links:
    - from: "packages/client/src/pages/RoomPage.tsx"
      to: "DealerSelectModal, AttendSchoolModal, ShuffleModal, CutModal, RechargeVoteModal, ResultScreen"
      via: "gameState.phase 기반 조건부 렌더 + rechargeRequest 상태"
      pattern: "phase === .*(dealer-select|attend-school|shuffling|cutting|result)"
    - from: "packages/client/src/components/modals/DealerSelectModal.tsx"
      to: "socket.emit('select-dealer-card')"
      via: "카드 클릭 핸들러"
      pattern: "emit.*select-dealer-card"
    - from: "packages/client/src/components/modals/CutModal.tsx"
      to: "socket.emit('cut') / socket.emit('declare-ttong')"
      via: "더미 확인 또는 퉁 버튼"
      pattern: "emit.*(cut|declare-ttong)"
    - from: "packages/client/src/pages/RoomPage.tsx"
      to: "socket.emit('join-room')"
      via: "닉네임 입력 후 입장 버튼 클릭"
      pattern: "emit.*join-room"
---

<objective>
5개 특수 액션 모달, 결과 화면, 방 나가기 확인 다이얼로그를 구현하고 RoomPage에 최종 통합한다. Phase 6의 마무리 단계로, 모든 UI 요소를 연결하여 전체 게임 플로우가 UI에서 작동하도록 한다.

Purpose: 게임의 특수 액션(밤일낮장, 등교, 셔플, 기리, 재충전 투표)과 판 결과 표시를 완성하여 전체 게임 UX 루프를 닫는다.
Output: 7개 모달/화면 컴포넌트 + RoomPage 최종 통합 (닉네임 입력 + join-room emit 포함)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ui/06-CONTEXT.md
@.planning/phases/06-ui/06-UI-SPEC.md
@.planning/phases/06-ui/06-01-SUMMARY.md
@.planning/phases/06-ui/06-02-SUMMARY.md

@packages/shared/src/types/game.ts
@packages/shared/src/types/protocol.ts

<interfaces>
<!-- Plan 01-02에서 생성된 모듈 -->

From packages/client/src/store/gameStore.ts:
```typescript
interface GameStore {
  socket: AppSocket | null;
  gameState: GameState | null;
  roomState: RoomState | null;
  myPlayerId: string | null;
  error: string | null;
  rechargeRequest: { requesterId: string; requesterNickname: string; amount: number } | null;
  connect: (serverUrl: string) => void;
  disconnect: () => void;
  clearError: () => void;
}
export const useGameStore = create<GameStore>(...);
```

From packages/client/src/components/game/CardFace.tsx:
```typescript
export function CardFace({ card, className }: { card: Card; className?: string }): JSX.Element;
```

From packages/client/src/components/game/CardBack.tsx:
```typescript
export function CardBack({ className }: { className?: string }): JSX.Element;
```

From packages/shared/src/types/protocol.ts:
```typescript
// 클라이언트 -> 서버 이벤트 (모달 관련)
'select-dealer-card': (data: { roomId: string; cardIndex: number }) => void;
'attend-school': (data: { roomId: string }) => void;
'shuffle': (data: { roomId: string }) => void;
'cut': (data: { roomId: string; cutPoints: number[]; order: number[] }) => void;
'declare-ttong': (data: { roomId: string }) => void;
'next-round': (data: { roomId: string }) => void;
'leave-room': (data: { roomId: string }) => void;
'recharge-vote': (data: { roomId: string; approved: boolean }) => void;
'join-room': (data: { roomId: string; nickname: string; initialChips: number }) => void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5개 특수 모달 + 방 나가기 다이얼로그 + 결과 화면</name>
  <files>
    packages/client/src/components/modals/DealerSelectModal.tsx,
    packages/client/src/components/modals/AttendSchoolModal.tsx,
    packages/client/src/components/modals/ShuffleModal.tsx,
    packages/client/src/components/modals/CutModal.tsx,
    packages/client/src/components/modals/RechargeVoteModal.tsx,
    packages/client/src/components/modals/LeaveRoomDialog.tsx,
    packages/client/src/components/layout/ResultScreen.tsx
  </files>
  <read_first>
    packages/client/src/store/gameStore.ts,
    packages/client/src/components/game/CardFace.tsx,
    packages/client/src/components/game/CardBack.tsx,
    packages/client/src/components/ui/dialog.tsx,
    packages/client/src/components/ui/button.tsx,
    packages/client/src/components/ui/badge.tsx,
    packages/shared/src/types/game.ts,
    packages/shared/src/types/protocol.ts,
    .planning/phases/06-ui/06-UI-SPEC.md,
    .planning/phases/06-ui/06-CONTEXT.md
  </read_first>
  <action>
    **1. src/components/modals/DealerSelectModal.tsx** (per UI-SPEC Modal 1, D-12):

    Props: `{ open: boolean; roomId: string; }`

    밤일낮장: 20장 뒤집힌 카드 그리드에서 1장 선택.
    - shadcn Dialog: `<Dialog open={open}>` -- `onOpenChange` 없음 (닫기 방지)
    - `<DialogContent onInteractOutside={(e) => e.preventDefault()} onEscapeKeyDown={(e) => e.preventDefault()}>` -- 외부 클릭/ESC 방지
    - DialogTitle: "선 결정 -- 카드를 선택하세요"
    - 4x5 그리드: `<div className="grid grid-cols-5 gap-2">`
    - 20개 CardBack 컴포넌트, 각각 클릭 가능:
      ```tsx
      {Array.from({ length: 20 }, (_, i) => (
        <button key={i} onClick={() => handleSelect(i)} className="cursor-pointer hover:ring-2 hover:ring-primary rounded-md">
          <CardBack />
        </button>
      ))}
      ```
    - 선택 시: `socket.emit('select-dealer-card', { roomId, cardIndex: i })` -- 모달은 서버에서 phase 변경 시 자동 닫힘 (open prop이 false로 바뀜)

    **2. src/components/modals/AttendSchoolModal.tsx** (per UI-SPEC Modal 2, D-12):

    Props: `{ open: boolean; roomId: string; }`

    등교: "학교 간다" / "잠시 쉬기" 두 버튼.
    - Dialog: 외부 클릭/ESC 방지 (DealerSelectModal과 동일 패턴)
    - DialogTitle: "이번 판에 참여하시겠어요?"
    - DialogDescription: "학교에 가면 앤티 500원을 납부합니다."
    - "학교 간다" Button (primary, per UI-SPEC Accent 사용처 5번): `socket.emit('attend-school', { roomId })`
    - "잠시 쉬기" Button (secondary):
      - **프로토콜 확인 결과:** 서버 프로토콜에 별도 `skip-school` 이벤트가 없다. `attend-school` 이벤트만 존재하며, 서버의 `completeAttendSchool()`이 타임아웃으로 미응답 플레이어를 skip 처리한다.
      - **구현 방식:** "잠시 쉬기" 클릭 시 아무 이벤트도 emit하지 않고 모달만 닫는다 (모달의 open prop은 phase가 변경되면 자동으로 false가 됨). 서버가 해당 플레이어의 응답 타임아웃으로 skip 처리한다.
      - **향후 개선:** 프로토콜에 `skip-school` 이벤트를 추가하면 명시적 skip이 가능해진다. 현재는 UX적으로 "잠시 쉬기" 클릭 후 모달이 닫히면서 대기 상태가 되는 것으로 충분하다.
      - 구현: `onClick={() => { /* 의도적으로 이벤트 미전송 -- 서버 타임아웃이 skip 처리 */ }}` + 모달 UI에서 "잠시 쉬기를 선택하면 이번 판을 건너뜁니다" 안내 텍스트 추가

    **3. src/components/modals/ShuffleModal.tsx** (per UI-SPEC Modal 3, D-12):

    Props: `{ open: boolean; roomId: string; }`

    셔플 확인: 버튼 1개.
    - Dialog: 외부 클릭/ESC 방지
    - DialogTitle: "패를 섞을까요?"
    - "섞기" Button (primary): `socket.emit('shuffle', { roomId })`

    **4. src/components/modals/CutModal.tsx** (per UI-SPEC Modal 4, D-12, Phase 4 D-26):

    Props: `{ open: boolean; roomId: string; }`

    기리: 1~5개 더미 분할 + "퉁 선언".
    - Dialog: 외부 클릭/ESC 방지
    - DialogTitle: "기리 -- 카드 더미를 나누세요"
    - 상단 우측: "퉁 선언" Button (ghost, per UI-SPEC Copywriting): `socket.emit('declare-ttong', { roomId })`
    - 더미 수 선택: `const [numCuts, setNumCuts] = useState(1)` -- 1~5 선택 버튼 그룹
    - 더미 시각화: 각 더미를 CardBack 스택으로 표시. 20장을 `numCuts`개 더미로 균등 분할 표시.
    - cutPoints 계산: 예를 들어 2더미이면 cutPoints=[10], 3더미이면 cutPoints=[7, 14] 등. 순서(order)는 1-indexed 더미 번호 배열.
    - "확인" Button (primary): `socket.emit('cut', { roomId, cutPoints, order })` -- cutPoints와 order를 numCuts에서 계산.
      ```typescript
      const handleConfirm = () => {
        const deckSize = 20;
        const pileSize = Math.floor(deckSize / numCuts);
        const cutPoints: number[] = [];
        for (let i = 1; i < numCuts; i++) {
          cutPoints.push(pileSize * i);
        }
        // order는 간단히 역순 (마지막 더미부터)
        const order = Array.from({ length: numCuts }, (_, i) => numCuts - i);
        socket.emit('cut', { roomId, cutPoints, order });
      };
      ```

    **5. src/components/modals/RechargeVoteModal.tsx** (per UI-SPEC Modal 5, D-12):

    Props: `{ roomId: string; }` -- open 상태는 Zustand `rechargeRequest`로 관리

    재충전 투표: 동의/거부.
    - `const { rechargeRequest } = useGameStore();`
    - `<Dialog open={!!rechargeRequest}>` -- rechargeRequest가 있을 때 자동 오픈
    - DialogTitle: `{rechargeRequest.requesterNickname}님이 칩 재충전을 요청했어요.`
    - DialogDescription: `{rechargeRequest.amount.toLocaleString()}원`
    - "동의" Button (primary): `socket.emit('recharge-vote', { roomId, approved: true })`
    - "거부" Button (destructive, per UI-SPEC Destructive Actions): `socket.emit('recharge-vote', { roomId, approved: false })`
    - 자동 닫힘: `recharge-result` 이벤트 수신 시 스토어가 rechargeRequest를 null로 설정 -> Dialog 자동 닫힘
    - 결과 Toast: "재충전이 승인되었어요!" 또는 "재충전이 거부되었어요."

    **6. src/components/modals/LeaveRoomDialog.tsx** (per UI-SPEC Destructive Actions):

    Props: `{ open: boolean; onOpenChange: (open: boolean) => void; roomId: string; }`

    방 나가기 확인:
    - DialogTitle: "방에서 나가시겠어요?"
    - DialogDescription: "게임 중이라면 다이 처리됩니다."
    - "나가기" Button (destructive): `socket.emit('leave-room', { roomId })` + `navigate('/')`
    - "취소" Button (ghost)

    **7. src/components/layout/ResultScreen.tsx** (per UI-06, UI-07):

    Props: `{ gameState: GameState; myPlayerId: string | null; roomId: string; }`

    판 결과 화면:
    - 승자 표시: `<h2 className="text-xl font-semibold">{winnerNickname} 승리!</h2>` (per UI-SPEC Copywriting, Heading 20px semibold)
    - 모든 생존 플레이어 카드 공개: 각 플레이어의 cards를 `CardFace`로 표시
    - 족보명 Badge: `evaluateHand(player.cards)` -> HAND_TYPE_KOREAN 매핑 (HandPanel과 동일 패턴)
    - 칩 변동: 각 플레이어에 `<Badge>{변동 > 0 ? '+' : ''}{변동.toLocaleString()}원</Badge>` (per UI-SPEC)
      - 변동 계산: currentBet 기반 또는 서버에서 result phase에 제공하는 값 사용. 간단히 `pot / survivorCount`를 승자에 더하고, 나머지는 `-currentBet` 표시.
    - "다음 판" Button (secondary, per UI-SPEC Copywriting): `socket.emit('next-round', { roomId })`
    - "방 나가기" Button (ghost, per UI-SPEC Copywriting): LeaveRoomDialog 트리거
  </action>
  <verify>
    <automated>cd packages/client && pnpm build 2>&1 | tail -5 && pnpm test -- ResultScreen 2>&1 | tail -10</automated>
  </verify>
  <done>5개 특수 액션 모달(밤일낮장/등교/셔플/기리/재충전투표), 방 나가기 확인 다이얼로그, 결과 화면이 모두 구현되고 빌드 성공</done>
</task>

<task type="auto">
  <name>Task 2: RoomPage 최종 통합 -- 모달 연결 + 닉네임 입력 + join-room emit</name>
  <files>
    packages/client/src/pages/RoomPage.tsx
  </files>
  <read_first>
    packages/client/src/pages/RoomPage.tsx,
    packages/client/src/components/modals/DealerSelectModal.tsx,
    packages/client/src/components/modals/AttendSchoolModal.tsx,
    packages/client/src/components/modals/ShuffleModal.tsx,
    packages/client/src/components/modals/CutModal.tsx,
    packages/client/src/components/modals/RechargeVoteModal.tsx,
    packages/client/src/components/modals/LeaveRoomDialog.tsx,
    packages/client/src/components/layout/ResultScreen.tsx,
    packages/client/src/components/layout/GameTable.tsx,
    packages/client/src/components/layout/HandPanel.tsx,
    packages/client/src/components/layout/BettingPanel.tsx,
    packages/client/src/components/layout/InfoPanel.tsx,
    packages/client/src/components/layout/ChatPanel.tsx,
    packages/client/src/components/layout/WaitingRoom.tsx,
    packages/client/src/store/gameStore.ts,
    packages/shared/src/types/game.ts
  </read_first>
  <action>
    RoomPage.tsx를 최종 업데이트하여 모든 컴포넌트를 통합. **이 태스크가 join-room emit의 유일한 책임자이다** (06-02에서는 join-room을 구현하지 않음).

    **Import 추가:**
    ```typescript
    import { DealerSelectModal } from '@/components/modals/DealerSelectModal';
    import { AttendSchoolModal } from '@/components/modals/AttendSchoolModal';
    import { ShuffleModal } from '@/components/modals/ShuffleModal';
    import { CutModal } from '@/components/modals/CutModal';
    import { RechargeVoteModal } from '@/components/modals/RechargeVoteModal';
    import { LeaveRoomDialog } from '@/components/modals/LeaveRoomDialog';
    import { ResultScreen } from '@/components/layout/ResultScreen';
    // GameTable, HandPanel, BettingPanel, InfoPanel, ChatPanel, WaitingRoom은 06-02에서 이미 import됨
    import { Toaster, toast } from 'sonner';
    ```

    **핵심 로직:**

    1. 소켓 연결: useEffect로 mount 시 connect, unmount 시 disconnect

    2. **닉네임 입력 + join-room emit (이 태스크의 핵심 책임):**
       - `const [nickname, setNickname] = useState('')`
       - `const [hasJoined, setHasJoined] = useState(false)`
       - `const [initialChips, setInitialChips] = useState(100000)`
       - hasJoined가 false이면 닉네임+칩 금액 입력 폼 표시
       - 입장 버튼 클릭: `socket.emit('join-room', { roomId, nickname, initialChips })` -> `setHasJoined(true)`

    3. error 상태 변경 -> `toast.error(error)` 호출 (per UI-SPEC Error State)

    4. phase 기반 조건부 렌더:

    ```tsx
    const phase = gameState?.phase;
    const myPlayer = gameState?.players.find(p => p.id === myPlayerId);
    const isMyTurn = gameState ? gameState.players[gameState.currentPlayerIndex]?.id === myPlayerId : false;
    const isDealer = myPlayer?.isDealer ?? false;

    // 닉네임 미입력 상태
    if (!hasJoined) {
      return (/* 닉네임 + 칩 입력 폼 */);
    }

    // 대기실 (게임 시작 전)
    if (!gameState || phase === 'waiting') {
      return <WaitingRoom ... />;
    }

    // 결과 화면
    if (phase === 'result') {
      return <ResultScreen gameState={gameState} myPlayerId={myPlayerId} roomId={roomId!} />;
    }

    // 게임 진행 중
    return (
      <div className="flex min-h-screen flex-col bg-background text-foreground">
        {/* 상단 헤더 -- 방 나가기 버튼 */}
        <div className="flex items-center justify-between p-2 border-b border-border">
          <span className="text-sm text-muted-foreground">방 {roomId}</span>
          <Button variant="ghost" size="sm" onClick={() => setShowLeaveDialog(true)}>나가기</Button>
        </div>

        {/* 게임테이블패널 -- 상단 영역 */}
        <div className="flex-1 flex items-center justify-center">
          <GameTable
            players={gameState.players}
            myPlayerId={myPlayerId}
            currentPlayerIndex={gameState.currentPlayerIndex}
            pot={gameState.pot}
          />
        </div>

        {/* 하단 패널들 */}
        <div className="border-t border-border">
          <div className="flex flex-col md:flex-row gap-4 p-4">
            <HandPanel myPlayer={myPlayer ?? null} />
            <InfoPanel myChips={myPlayer?.chips ?? 0} pot={gameState.pot} players={gameState.players} />
          </div>

          {phase === 'betting' && (
            <BettingPanel
              isMyTurn={isMyTurn}
              currentBetAmount={gameState.currentBetAmount}
              myChips={myPlayer?.chips ?? 0}
              roomId={roomId!}
              effectiveMaxBet={gameState.effectiveMaxBet}
            />
          )}

          <ChatPanel />
        </div>

        {/* 특수 액션 모달 -- phase에 따라 조건부 표시 */}
        <DealerSelectModal open={phase === 'dealer-select'} roomId={roomId!} />
        <AttendSchoolModal open={phase === 'attend-school' && isMyTurn} roomId={roomId!} />
        <ShuffleModal open={phase === 'shuffling' && isDealer} roomId={roomId!} />
        <CutModal open={phase === 'cutting' && isMyTurn} roomId={roomId!} />

        {/* 재충전 모달 -- phase 무관, rechargeRequest 있을 때 */}
        <RechargeVoteModal roomId={roomId!} />

        {/* 방 나가기 다이얼로그 */}
        <LeaveRoomDialog open={showLeaveDialog} onOpenChange={setShowLeaveDialog} roomId={roomId!} />

        {/* Toast 컨테이너 */}
        <Toaster />
      </div>
    );
    ```

    5. attend-school phase에서 `isMyTurn` 판별:
       - attend-school은 순차적으로 각 플레이어에게 돌아감
       - `gameState.currentPlayerIndex`로 현재 차례 플레이어 확인
  </action>
  <verify>
    <automated>cd packages/client && pnpm build 2>&1 | tail -5 && pnpm test 2>&1 | tail -15</automated>
  </verify>
  <done>RoomPage가 모든 컴포넌트(GameTable, HandPanel, BettingPanel, InfoPanel, ChatPanel, WaitingRoom, ResultScreen, 5개 모달, LeaveRoomDialog)를 gameState.phase 기반으로 조건부 렌더링하며, 닉네임 입력 + join-room emit이 이 태스크에서 유일하게 구현되어 전체 게임 플로우 UI가 연결된다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UI 시각 검증 (human-verify)</name>
  <what-built>Phase 6 전체 클라이언트 UI -- 메인 화면, 대기실, 게임 테이블(원형 배치), 카드/칩 표시, 베팅 패널, 족보 참고표 Dialog, 5개 특수 모달, 결과 화면</what-built>
  <how-to-verify>
    1. `cd packages/client && pnpm dev` 실행
    2. 브라우저에서 `http://localhost:5173` 접속
    3. 메인 화면: "방 만들기"와 "링크로 참여" 버튼 2개 확인
    4. 다크 테마(어두운 배경) 확인
    5. "방 만들기" 클릭 시 (서버 미연결 상태에서) 에러 토스트 표시 확인
    6. Chrome DevTools에서 320px, 768px, 1440px 뷰포트로 반응형 확인
    7. 별도 터미널에서 서버 시작(`cd packages/server && pnpm dev`)한 뒤:
       a. 방 생성 -> 대기실 이동 -> URL 복사 버튼 확인
       b. 다른 탭에서 URL 접속 -> 닉네임 입력 -> 대기실 합류
       c. 게임 시작 -> 게임 테이블 원형 배치 확인
       d. 카드가 숫자+속성 텍스트로 표시되는지 확인
       e. 베팅 버튼(콜/레이즈/다이/체크) 표시 확인
       f. HandPanel의 "족보 참고표" 버튼 클릭 -> 전체 족보 순위표 Dialog 확인
  </how-to-verify>
  <resume-signal>"approved" 또는 수정 사항 설명</resume-signal>
  <verify>
    <automated>cd packages/client && pnpm build 2>&1 | tail -3</automated>
  </verify>
  <done>사용자가 UI를 시각적으로 검증하고 "approved" 또는 수정 사항을 전달</done>
</task>

</tasks>

<verification>
- `pnpm --filter @sutda/client build` 성공
- `pnpm --filter @sutda/client test` 모든 테스트 통과
- 5개 모달 컴포넌트 파일 모두 존재
- ResultScreen 컴포넌트에서 카드 공개 + 승자 + 칩 변동 표시
- RoomPage에서 모든 phase에 대한 조건부 렌더링 완성
- join-room emit이 06-03 Task 2에만 존재 (06-02에는 없음)
</verification>

<success_criteria>
1. 5개 특수 모달 (밤일낮장/등교/셔플/기리/재충전투표) 모두 구현
2. 결과 화면에서 카드 공개, 승자, 칩 변동 표시 (UI-06)
3. 족보명 Badge 표시 (UI-07 -- HandPanel 인라인 + HandReferenceDialog)
4. 방 나가기 확인 다이얼로그 작동
5. RoomPage가 gameState.phase로 전체 화면 전환 관리
6. 닉네임 입력 + join-room emit이 06-03 Task 2에서 유일하게 구현됨
7. 빌드 성공 + 시각 검증 통과
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui/06-03-SUMMARY.md`
</output>
