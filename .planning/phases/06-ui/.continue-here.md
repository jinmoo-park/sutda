---
phase: 06-ui
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-30T03:00:00.000Z
---

<current_state>
Phase 6 UI — 06-03 버그 수정 단계. ngrok 실제 테스트에서 발견된 버그들을 수정 완료.
베팅까지 동작 확인됨. ResultScreen(result phase) 실제 테스트 필요.

**최근 커밋:** 2883174 (fix(06-ui): betting UX + auto-showdown + player-id fix + cut modal redesign)
</current_state>

<completed_work>

- Wave 1~3 executor 완료 ✓
- 버그 수정 1차: 포트 3001, 소켓 연결 타이밍, join-room 브로드캐스트 ✓
- ngrok 테스트 환경 구성 (vite proxy) ✓
- **이번 세션 수정 완료:**
  - [BUG-1] 닉네임 이중 입력: MainPage → RoomPage navigate state 전달 + 자동 join ✓
  - [BUG-4,5] 등교 모달: 전체 플레이어 표시 + submitted 상태 처리 ✓
  - [BUG-6] 카드/베팅 패널 미표시: phase 값 불일치 수정 ✓
  - [BUG-2,3] 밤일낮장 선택 결과 표시 ✓
  - 올리브 다크 테마 CSS 변수 적용 ✓
  - **자동 쇼다운**: 베팅 완료 → 서버가 족보비교+정산 자동처리 (showdown phase 건너뜀)
  - **myPlayerId 버그 근본 수정**: `set-player-id` 이벤트 추가 — 서버가 명시적으로 player ID 전달
  - **ModeSelectModal**: 전체 표시 후 서버 검증 패턴으로 변경 (isDealer 클라이언트 조건 제거)
  - **BettingPanel**: 선 최초 체크/레이즈만 활성화, 콜/레이즈 납부금액 명확 표시
  - **CutModal**: 2/3/4/5더미+퉁 5가지 선택지 (1더미 제거, 퉁 별도 버튼 제거)
  - **카드 확인 오버레이**: cutting→betting 전환 시 내 패 확인 UI (기리/퉁 구분)

</completed_work>

<remaining_work>

1. **ResultScreen 실제 테스트** — 자동 쇼다운 후 result phase UI 확인
   - 카드 공개 표시 확인
   - 승자 + 칩 변동 표시 확인
   - "다음 판" 버튼 → 2판 이후 흐름 (attend-school) 확인

2. **2판 이후 전체 흐름 테스트**
   - next-round → attend-school → mode-select → shuffling → cutting → betting → result 반복

3. **06-03-SUMMARY.md 작성** — Phase 6 완료 처리

4. **(선택) UI 소소한 개선**
   - 현재 베팅 턴 플레이어 강조 표시
   - 판돈(pot) 표시 위치 개선

</remaining_work>

<decisions_made>

- 자동 쇼다운 채택: 베팅 완료 시 서버에서 revealCard 없이 _resolveShowdown() 자동 호출
- set-player-id 이벤트: create-room/join-room 성공 시 서버가 해당 소켓에만 emit → myPlayerId 신뢰성 확보
- 모달 표시 패턴: 클라이언트 isDealer 조건 제거 → 전체 표시 + 서버 권위 검증
- 기리 모달: 1더미 = 퉁과 동일하므로 제거, 퉁을 같은 레벨 선택지로 통합
</decisions_made>

<blockers>

없음 (기술적 블로커 없음)
</blockers>

<context>
서버 실행: packages/server (port 3001, tsx watch)
클라이언트 실행: packages/client (port 5174 — 5173 이미 사용 중)
ngrok: https://sunniest-fancily-jerry.ngrok-free.dev (5174 터널)

핵심 아키텍처 결정:
- GameEngine phase 순서: dealer-select → attend-school → mode-select → shuffling → cutting → (dealing 자동) → betting → result
- 2판부터: attend-school → mode-select → shuffling → cutting → betting → result
- 베팅 종료 조건: _isBettingComplete() → 자동 resolveShowdown → settleChips → phase='result'
- myPlayerId: socket.on('set-player-id') 이벤트로 수신 (socket.id 불일치 방지)
</context>

<next_action>
1. pnpm dev (server + client) 실행
2. 2인 테스트: 방 생성 → 입장 → 게임 시작 → 밤일낮장 → 등교 → 모드선택 → 셔플 → 기리 → 패확인 → 베팅 → result 화면 확인
3. "다음 판" 클릭 → 2판 흐름 확인
4. 이상 없으면 06-03-SUMMARY.md 작성 + Phase 6 완료 처리
</next_action>
