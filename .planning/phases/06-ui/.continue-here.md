---
phase: 06-ui
task: 3
total_tasks: 3
status: in_progress
last_updated: "2026-03-30T09:33:27.689Z"
---

<current_state>
Phase 6 UI — 06-03 버그 수정 세션 2차. 다이한 플레이어 결과화면 표시, 게임 룰 수정(94재경기/밤일낮장 동률/모달 제거) 완료. 첫 판 밤일낮장 → 모드선택 미표시 버그 수정까지 완료. 실제 테스트 필요 상태.
</current_state>

<completed_work>

**이전 세션까지 완료 (Wave 1~3 + 버그 수정 1~3차):**
- Wave 1~3 executor 완료 ✓
- BUG-1~6 수정, 자동 쇼다운, set-player-id, 모달 개선 ✓
- 올리브 다크 테마 ✓
- 턴 강조, Pot 표시, 딜링 애니메이션 ✓
- MuckChoiceModal ✓
- isAbsent 기반 잠시쉬기/다이 구분 ✓
- ReturnFromBreakModal, 복귀 배너 ✓
- 선 플레이어 잠시쉬기 금지 ✓
- 결과화면 자동 넘어가기, 다음판 쉬기 버튼 ✓

**이번 세션 완료:**
- [FIX] ResultScreen: 다이한 플레이어도 카드 뒷면 + "다이" 배지 + 잃은 금액 표시
  → survivingPlayers → allPlayers(비-absent 전체)로 변경
  → isDied = !player.isAlive로 구분
  → 다이 플레이어: 카드 뒷면, 빨간 "다이" 배지, opacity-70

- [FEAT] 밤일낮장 동률자 재추첨
  → shared/types/game.ts: dealerSelectEligibleIds?: string[] 추가
  → game-engine.ts: _resolveDealer()에서 동률 감지 시 dealerSelectCards 초기화 + eligibleIds 설정
  → game-engine.ts: selectDealerCard()에서 eligibleIds 기반 자격 검증 + targetCount 계산
  → DealerSelectModal.tsx: 동률 메시지 표시, 비적격 플레이어 카드 비활성화

- [FEAT] 94재경기 → 구사 보유자가 선
  → shared/types/game.ts: rematchDealerId?: string 추가
  → game-engine.ts: _resolveShowdown()에 checkGusaTrigger 통합
  → game-engine.ts: startRematch()에서 rematchDealerId 우선 사용

- [FEAT] AttendSchoolModal 제거 + 자동 앤티
  → server/index.ts: next-round 투표 완료 후 자동 attendSchool 처리
  → server/index.ts: take-break 후 투표 완료 시도 자동 앤티
  → server/index.ts: select-dealer-card 완료 후 attend-school이면 자동 앤티 (첫 판 대응)
  → RoomPage.tsx: AttendSchoolModal, ReturnFromBreakModal 렌더링 제거
  → RoomPage.tsx: 잠시쉬기 배너 모든 phase에서 표시 (attend-school 제한 제거)

- [FIX] ResultScreen: "다음 판" → "학교 가기"로 버튼명 변경

- [BUG-FIX] 첫 판 밤일낮장 후 모드선택창 미표시
  → select-dealer-card 핸들러에 자동 앤티 로직 추가

- [FIX] evaluateHand 호출 인자 오류 수정
  → HandPanel.tsx, ResultScreen.tsx: evaluateHand(cards) → evaluateHand(cards[0], cards[1])

</completed_work>

<remaining_work>

1. **실제 플레이테스트** — 전체 흐름 확인
   - 첫 판: 밤일낮장 → 동률 재추첨 → 모드 선택 → 셔플 → 기리 → 베팅 → 결과
   - 결과화면: 다이한 플레이어 카드 뒷면 + 마이너스 표시 확인
   - 결과화면: "학교 가기" → 모드 선택으로 바로 이동 확인 (모달 없이)
   - 94 재경기: 구사 보유자가 선이 되는지 확인

2. **06-03-SUMMARY.md 작성** — Phase 6 완료 처리

</remaining_work>

<decisions_made>

- AttendSchoolModal 제거: 결과화면 "학교 가기"가 곧 등교 의사 표현이므로, 별도 모달 불필요. 서버에서 자동 앤티 처리.
- 첫 판 attend-school 자동 앤티: select-dealer-card 핸들러에서 phase === 'attend-school' 감지 후 즉시 처리
- 다이한 플레이어 결과화면 표시: survivingPlayers → allPlayers(비-absent), isDied 조건으로 카드 뒷면 표시
- 밤일낮장 재추첨: dealerSelectCards 초기화 + dealerSelectEligibleIds 설정으로 phase 유지하며 재추첨
- 94재경기: rematchDealerId 필드로 구사 보유자를 별도 추적하여 startRematch에서 우선 적용

</decisions_made>

<blockers>

없음 — 테스트만 남음

</blockers>

<context>
서버 실행: packages/server (port 3001, tsx watch)
클라이언트 실행: packages/client (port 5174 또는 5173)

핵심 변경사항:
- AttendSchoolModal 완전 제거 (클라이언트에서 렌더링 안 함)
- 자동 앤티: next-round 투표 완료 시 + dealer-select 완료 시 서버에서 자동 처리
- 밤일낮장 동률: dealerSelectEligibleIds로 재추첨 대상 추적
- 구사 재경기: rematchDealerId로 선 결정
- 결과화면: 다이+생존 모두 표시

import.meta.env TypeScript 오류는 기존 Vite 설정 이슈 (내 변경과 무관, 런타임에는 정상 동작)
</context>

<next_action>
1. pnpm dev (server + client) 실행
2. 2인 테스트: 밤일낮장 → 모드 선택 → 전체 플로우
3. 결과화면: 다이한 플레이어 마이너스 표시 확인
4. 이상 없으면 06-03-SUMMARY.md 작성 + Phase 6 완료
</next_action>
