---
phase: 06-ui
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-30T01:20:53.927Z
---

<current_state>
Phase 6 UI — 06-03 Task 3 (human-verify 체크포인트). ngrok으로 실제 테스트 완료.
핵심 플로우는 동작하지만 버그 6종 발견. 수정 후 재승인 필요.

**미커밋 파일:**
- `packages/client/src/pages/MainPage.tsx` — serverUrl 빈 문자열
- `packages/client/src/pages/RoomPage.tsx` — serverUrl 빈 문자열
- `packages/client/vite.config.ts` — allowedHosts:true + socket.io proxy 추가
</current_state>

<completed_work>

- Wave 1~3 executor 모두 완료 ✓
- 버그 수정 1차: 포트 3001, 소켓 연결 타이밍, join-room 브로드캐스트 ✓ (커밋: 68b9f6f)
- ngrok 테스트 환경 구성 (vite proxy) ✓ [미커밋]
- 실제 테스트로 동작 확인된 것:
  - 방 만들기 / 링크로 참여 버튼 ✓
  - 다크 테마 ✓
  - 방 생성 → 대기실 이동 ✓
  - 링크 복사 ✓
  - 참가자 합류 → 방장 화면 갱신 ✓
  - 게임 시작 버튼 활성화 ✓
  - 원형 플레이어 배치 ✓
  - 족보 참고표 Dialog ✓
</completed_work>

<remaining_work>

**버그 수정 필요 (우선순위 순):**

1. **[BUG-1] 닉네임 이중 입력** (UX)
   - 방장: MainPage 닉네임 입력 → 방 만들기 → RoomPage에서 또 닉네임 입력
   - 수정: MainPage navigate 시 `state: { nickname, initialChips }` 전달
     → RoomPage에서 location.state 있으면 자동 join-room emit + hasJoined=true

2. **[BUG-4,5] 등교 모달** (기능)
   - 방장만 모달 표시 (다른 플레이어 미표시)
   - 학교간다 클릭 후 모달 미닫힘 + "이미 등교" 토스트 반복
   - 수정 전: 서버 attend-school phase 동작 확인 필요
     → `packages/server/src/game-engine.ts`에서 attendSchool() 로직 확인
     → 동시결정이면 `open={phase === 'attend-school'}` (isMyTurn 제거)
     → submitted 상태로 버튼 즉시 비활성화

3. **[BUG-6] 카드 미표시 + 베팅 패널 미표시** (기능)
   - 게임 진행 중 HandPanel 카드 미표시
   - BettingPanel 미표시
   - 수정 전: 서버 실제 phase 값 확인
     → `game-engine.ts`에서 phase enum 값 목록 확인
     → BettingPanel 조건 `phase === 'betting'` 실제 값과 대조

4. **[BUG-2,3] 밤일낮장 모달** (기능)
   - 선택 결과 미표시
   - 이미 선택된 카드 비활성화 안 됨
   - 수정: gameState에서 선택된 카드 인덱스 필드 확인 후 UI 반영

5. **[디자인] 다크 테마 컬러** — 진한 올리브 컬러를 메인으로
   - `packages/client/src/index.css`의 CSS 변수 수정
</remaining_work>

<decisions_made>

- vite proxy로 단일 ngrok 터널에서 클라이언트+서버 동시 접근 해결
- serverUrl 빈 문자열('')로 socket.io 상대 경로 연결
- 등교 모달 조건 isMyTurn은 검토 필요 — 서버 로직 확인 후 결정
</decisions_made>

<blockers>

- 없음 (기술적 블로커 없음 — 버그 수정 작업만 남음)
</blockers>

<context>
실제 플레이 테스트에서 게임의 핵심 흐름은 동작함. 게임 진행 단계(attend-school, dealer-select, betting)
의 UI 연결 버그가 남아있음. 서버 game-engine.ts의 phase 값과 클라이언트 조건문이 불일치할 가능성 높음.
수정 시작 전 game-engine.ts 전체 phase enum 확인이 필수.

테마: 사용자가 "진한 올리브 컬러를 메인으로" 요청 — CSS 변수 수정으로 해결 가능.
</context>

<next_action>
1. 미커밋 파일 커밋 (vite.config.ts, MainPage.tsx, RoomPage.tsx)
2. `packages/server/src/game-engine.ts` 읽어서 phase enum 값 전체 목록 파악
3. BUG-1 수정: MainPage → RoomPage navigate state 전달 + RoomPage 자동 join 처리
4. BUG-4,5 수정: 서버 attendSchool() 동작 파악 후 AttendSchoolModal 조건 수정
5. BUG-6 수정: 실제 phase 값 기반으로 BettingPanel + HandPanel 조건 수정
6. BUG-2,3 수정: DealerSelectModal 선택 결과 표시 + 비활성화
7. 디자인: 올리브 테마 CSS 변수 적용
8. ngrok 재테스트 → approved
</next_action>
