---
phase: 14-room-password
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/protocol.ts
  - packages/server/src/index.ts
  - packages/client/src/pages/MainPage.tsx
autonomous: true
requirements: [ROOM-CREATE-PASSWORD-SERVER, ROOM-CREATE-PASSWORD-CLIENT]

must_haves:
  truths:
    - "ROOM_CREATE_PASSWORD 환경변수가 설정된 상태에서, 비밀번호 없이 방 생성 시 에러가 반환된다"
    - "올바른 비밀번호를 입력하면 정상적으로 방이 생성된다"
    - "ROOM_CREATE_PASSWORD 환경변수가 미설정이면 비밀번호 없이도 방 생성이 가능하다 (하위 호환)"
    - "클라이언트에 비밀번호 입력 필드가 표시되고, 에러 시 toast 메시지가 표시된다"
  artifacts:
    - path: "packages/shared/src/types/protocol.ts"
      provides: "INVALID_PASSWORD 에러 코드 + create-room password 필드"
      contains: "INVALID_PASSWORD"
    - path: "packages/server/src/index.ts"
      provides: "ROOM_CREATE_PASSWORD 환경변수 비교 로직"
      contains: "ROOM_CREATE_PASSWORD"
    - path: "packages/client/src/pages/MainPage.tsx"
      provides: "비밀번호 입력 필드 + 에러 처리"
      contains: "password"
  key_links:
    - from: "packages/client/src/pages/MainPage.tsx"
      to: "packages/server/src/index.ts"
      via: "create-room 소켓 이벤트에 password 필드 포함"
      pattern: "emit.*create-room.*password"
    - from: "packages/server/src/index.ts"
      to: "process.env.ROOM_CREATE_PASSWORD"
      via: "환경변수 비교"
      pattern: "process\\.env\\.ROOM_CREATE_PASSWORD"
---

<objective>
방 생성 시 서버 관리자 비밀번호를 요구하는 기능을 추가한다.

Purpose: 낯선 사람이 무단으로 방을 생성하는 것을 방지한다. 비밀번호는 서버 환경변수(.env)로 관리하며 검증은 서버 권위로 동작한다.
Output: 프로토콜 타입 확장 + 서버 검증 로직 + 클라이언트 UI
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 기존 프로토콜 타입: executor가 수정해야 할 계약 -->

From packages/shared/src/types/protocol.ts:
```typescript
export interface ErrorPayload {
  code:
    | 'ROOM_NOT_FOUND'
    | 'ROOM_FULL'
    | 'NICKNAME_TAKEN'
    // ... 기존 코드들
    | 'CHAT_TOO_FAST';
  message: string;
}

export interface ClientToServerEvents {
  'create-room': (data: { nickname: string; initialChips: number }) => void;
  // ...
}
```

From packages/server/src/index.ts (create-room 핸들러, line 143-155):
```typescript
socket.on('create-room', ({ nickname, initialChips }) => {
  if (!RoomManager.validateChips(initialChips)) {
    return emitError(socket, 'INVALID_CHIPS');
  }
  const room = roomManager.createRoom(socket.id, nickname, initialChips);
  socket.data.playerId = socket.id;
  socket.data.nickname = nickname;
  socket.data.roomId = room.roomId;
  socket.join(room.roomId);
  socket.emit('set-player-id', { playerId: socket.id });
  socket.emit('room-created', { roomId: room.roomId, roomState: room });
});
```

From packages/server/src/index.ts (ERROR_MESSAGES, line 81-98):
```typescript
const ERROR_MESSAGES: Record<string, string> = {
  ROOM_NOT_FOUND: '존재하지 않는 방입니다. 링크를 다시 확인해주세요.',
  ROOM_FULL: '방이 가득 찼습니다. (최대 6명)',
  // ...
};
```

From packages/server/src/index.ts (환경변수 사용 패턴):
```typescript
const PORT = Number(process.env.PORT) || 3001;
// process.env.CLIENT_ORIGIN || true
// process.env.NODE_ENV !== 'test'
```

From packages/client/src/pages/MainPage.tsx (전체 파일 — 75줄):
- handleCreateRoom(): emit('create-room', { nickname, initialChips })
- s.once('room-created', ...) 로 성공 처리
- gameStore의 error 상태로 에러 자동 처리 (gameStore.ts line 79: socket.on('error'))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 프로토콜 타입 확장 + 서버 비밀번호 검증</name>
  <files>packages/shared/src/types/protocol.ts, packages/server/src/index.ts</files>
  <read_first>
    - packages/shared/src/types/protocol.ts (ErrorPayload code union + ClientToServerEvents 수정 대상)
    - packages/server/src/index.ts (create-room 핸들러 line 143-155, ERROR_MESSAGES line 81-98)
  </read_first>
  <action>
1. packages/shared/src/types/protocol.ts 수정:
   - ErrorPayload.code union에 `| 'INVALID_PASSWORD'` 추가 (CHAT_TOO_FAST 다음에)
   - ClientToServerEvents의 'create-room' 시그니처를 `(data: { nickname: string; initialChips: number; password?: string }) => void;` 로 변경 (password를 optional로 — 하위 호환)

2. packages/server/src/index.ts 수정:
   - ERROR_MESSAGES 객체에 추가: `INVALID_PASSWORD: '방 생성 비밀번호가 올바르지 않습니다.',`
   - create-room 핸들러의 destructuring을 `({ nickname, initialChips, password })` 로 변경
   - validateChips 검증 직후, createRoom 호출 직전에 비밀번호 검증 로직 추가:
     ```typescript
     const requiredPassword = process.env.ROOM_CREATE_PASSWORD;
     if (requiredPassword && password !== requiredPassword) {
       return emitError(socket, 'INVALID_PASSWORD');
     }
     ```
   - 이 로직은 ROOM_CREATE_PASSWORD 환경변수가 설정된 경우에만 검증하고, 미설정(undefined/빈문자열)이면 통과시킨다 (하위 호환)
  </action>
  <verify>
    <automated>cd packages/shared && npx tsc --noEmit && cd ../server && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "INVALID_PASSWORD" packages/shared/src/types/protocol.ts
    - grep -q "password\?" packages/shared/src/types/protocol.ts (create-room 시그니처에 optional password)
    - grep -q "ROOM_CREATE_PASSWORD" packages/server/src/index.ts
    - grep -q "INVALID_PASSWORD" packages/server/src/index.ts (ERROR_MESSAGES에 등록)
    - grep -q "비밀번호가 올바르지" packages/server/src/index.ts (한국어 에러 메시지)
  </acceptance_criteria>
  <done>
    - ROOM_CREATE_PASSWORD 환경변수 설정 시 잘못된 비밀번호로 create-room 요청하면 INVALID_PASSWORD 에러 반환
    - ROOM_CREATE_PASSWORD 미설정 시 비밀번호 없이도 방 생성 가능
    - TypeScript 컴파일 에러 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: 클라이언트 비밀번호 입력 UI</name>
  <files>packages/client/src/pages/MainPage.tsx</files>
  <read_first>
    - packages/client/src/pages/MainPage.tsx (전체 — 75줄, handleCreateRoom 함수 + JSX 구조)
    - packages/client/src/store/gameStore.ts (line 75-81: error 이벤트 리스너로 toast 자동 처리 확인)
  </read_first>
  <action>
1. packages/client/src/pages/MainPage.tsx 수정:
   - password 상태 추가: `const [password, setPassword] = useState('');`
   - handleCreateRoom에서 emit 데이터에 password 추가:
     `s.emit('create-room', { nickname: nickname.trim(), initialChips, password: password.trim() || undefined });`
     (빈 문자열이면 undefined로 전송하여 하위 호환 유지)
   - handleCreateRoom에서 서버 에러 처리 추가 — s.once('error') 리스너 등록:
     ```typescript
     s.once('error', ({ message }: { message: string }) => {
       toast.error(message);
     });
     ```
     (room-created를 받으면 error 리스너 해제 필요 — room-created once 콜백 안에서 `s.off('error', errorHandler)` 호출)
   - JSX: 닉네임 Input 다음, 초기 칩 금액 div 이전에 비밀번호 입력 필드 추가:
     ```tsx
     <Input
       type="password"
       placeholder="방 생성 비밀번호"
       value={password}
       onChange={(e) => setPassword(e.target.value)}
     />
     ```
  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "password" packages/client/src/pages/MainPage.tsx (password 상태 존재)
    - grep -q 'type="password"' packages/client/src/pages/MainPage.tsx (password input 필드)
    - grep -q "방 생성 비밀번호" packages/client/src/pages/MainPage.tsx (placeholder 텍스트)
    - grep -q "password.*trim" packages/client/src/pages/MainPage.tsx (emit 시 password 포함)
  </acceptance_criteria>
  <done>
    - MainPage에 비밀번호 입력 필드(type="password")가 표시됨
    - create-room emit 시 password 필드가 함께 전송됨
    - 서버에서 INVALID_PASSWORD 에러 반환 시 toast로 에러 메시지 표시
    - TypeScript 컴파일 에러 없음
  </done>
</task>

</tasks>

<verification>
1. TypeScript 컴파일 검증: `cd packages/shared && npx tsc --noEmit && cd ../server && npx tsc --noEmit && cd ../client && npx tsc --noEmit`
2. 기존 테스트 통과: `npx turbo test` (기존 create-room 테스트는 password 미전송 — ROOM_CREATE_PASSWORD 미설정이므로 하위 호환으로 통과)
3. grep 검증:
   - `grep "INVALID_PASSWORD" packages/shared/src/types/protocol.ts` — 에러 코드 존재
   - `grep "ROOM_CREATE_PASSWORD" packages/server/src/index.ts` — 환경변수 참조 존재
   - `grep 'type="password"' packages/client/src/pages/MainPage.tsx` — 비밀번호 입력 필드 존재
</verification>

<success_criteria>
- ROOM_CREATE_PASSWORD 환경변수 설정 시: 올바른 비밀번호 입력 -> 방 생성 성공, 틀린 비밀번호 -> 에러 toast
- ROOM_CREATE_PASSWORD 미설정 시: 비밀번호 없이 방 생성 가능 (하위 호환)
- 기존 테스트 전부 통과
- 3개 패키지 모두 TypeScript 컴파일 성공
</success_criteria>

<output>
완료 후 `.planning/phases/14-room-password/14-01-SUMMARY.md` 생성
</output>
