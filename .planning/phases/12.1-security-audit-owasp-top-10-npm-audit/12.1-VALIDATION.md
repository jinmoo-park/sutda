---
phase: 12.1
slug: security-audit-owasp-top-10-npm-audit
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-02
---

# Phase 12.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 3.x |
| **Config file** | `vitest.config.ts` (모노레포 루트) |
| **Quick run command** | `pnpm --filter @sutda/server test` |
| **Full suite command** | `pnpm test` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `pnpm --filter @sutda/server test`
- **After every plan wave:** Run `pnpm test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | 항목 | Test Type | Automated Command | File Exists | Status |
|---------|------|------|------|-----------|-------------------|-------------|--------|
| 12.1-01-01 | 01 | 1 | D-03 (.env gitignore) | 수동 | `git status .env` | N/A | ⬜ pending |
| 12.1-01-02 | 01 | 1 | D-04 (CORS fallback) | 단위 | `pnpm --filter @sutda/server test` | ✅ 기존 | ⬜ pending |
| 12.1-01-03 | 01 | 2 | D-07 (Rate Limiting) | 통합 | `pnpm --filter @sutda/server test` | ❌ W0 신규 | ⬜ pending |
| 12.1-02-01 | 02 | 2 | D-05 (보안 헤더) | 수동 | `curl -I https://sutda.duckdns.org` | N/A | ⬜ pending |
| 12.1-02-02 | 02 | 2 | A01 (접근 제어) | 통합 | `pnpm --filter @sutda/server test` | ❌ W0 신규 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `packages/server/src/index.test.ts` — rate limiting 통합 테스트 추가 (D-07: 소켓당 초당 20개 이벤트 제한 검증)
- [ ] `packages/server/src/index.test.ts` — A01 접근 제어 테스트 추가 (send-chat roomId 검증)

*기존 인프라(vitest, 서버 테스트 파일)는 이미 설치됨 — 테스트 케이스만 추가 필요.*

---

## Manual-Only Verifications

| 동작 | 항목 | 수동 이유 | 테스트 방법 |
|------|------|-----------|------------|
| `.env` git untracked 상태 유지 | D-03 | git 동작 — vitest로 검증 불가 | `git status .env` — `??` 상태여야 함 |
| VM `CLIENT_ORIGIN` 환경변수 설정 | D-04 (VM 확인) | Oracle VM SSH 접속 필요 | SSH 접속 후 `echo $CLIENT_ORIGIN` 또는 `pm2 env 0` |
| nginx 보안 헤더 적용 확인 | D-05 | nginx 서버에 직접 접속 필요 | `curl -I https://sutda.duckdns.org` 응답에 `X-Content-Type-Options`, `X-Frame-Options`, `Content-Security-Policy` 존재 확인 |
| WebSocket 연결 limit_conn 동작 | D-07 (nginx) | 실제 VM nginx 설정 변경 필요 | nginx 설정 적용 후 `curl -v https://sutda.duckdns.org` + nginx 로그 확인 |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
