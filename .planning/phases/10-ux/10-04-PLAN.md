---
phase: 10-ux
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - packages/client/src/components/modals/ShuffleModal.tsx
  - packages/client/src/components/modals/CutModal.tsx
  - packages/client/src/store/shuffleStore.ts
  - packages/client/src/store/giriStore.ts
  - packages/client/src/components/game/CardFace.tsx
  - packages/client/src/components/game/CardBack.tsx
autonomous: true
requirements: [UX-07, UX-08]

must_haves:
  truths:
    - "셔플 버튼을 누르는 동안 rAF 기반 셔플 애니메이션이 루프하고, 떼면 즉시 종료된다"
    - "셔플 애니메이션은 peek→hold→rise→drop→rest 5단계 사이클(약 820ms)로 동작한다"
    - "기리 화면에서 드래그(threshold 8px)로 더미를 분리할 수 있다"
    - "기리 화면에서 탭으로 합치기 순서를 지정하고 순서 번호 배지가 표시된다"
    - "합치기 완료 버튼을 누르면 합치기 애니메이션(easeInOut 380ms) 후 서버로 emit된다"
  artifacts:
    - path: "packages/client/src/store/shuffleStore.ts"
      provides: "ShuffleState Zustand 스토어"
      exports: ["useShuffleStore"]
    - path: "packages/client/src/store/giriStore.ts"
      provides: "GiriState Zustand 스토어"
      exports: ["useGiriStore"]
    - path: "packages/client/src/components/modals/ShuffleModal.tsx"
      provides: "rAF 기반 셔플 애니메이션 UI"
      contains: "requestAnimationFrame"
    - path: "packages/client/src/components/modals/CutModal.tsx"
      provides: "드래그 분리 + 탭 순서 기리 UI"
      contains: "pointerdown"
  key_links:
    - from: "ShuffleModal.tsx"
      to: "shuffleStore.ts"
      via: "useShuffleStore import"
      pattern: "import.*useShuffleStore"
    - from: "CutModal.tsx"
      to: "giriStore.ts"
      via: "useGiriStore import"
      pattern: "import.*useGiriStore"
    - from: "ShuffleModal.tsx"
      to: "requestAnimationFrame"
      via: "rAF 루프"
      pattern: "requestAnimationFrame"
---

<objective>
셔플 모달을 rAF 기반 애니메이션으로 고도화하고, 기리 모달을 드래그 분리 + 탭 순서 지정 UX로 재설계한다. Zustand ShuffleState/GiriState 스토어를 신규 생성한다.

Purpose: sutda-shuffle-giri-ux.md 스펙 전체 구현으로 실제 섯다 셔플/기리 경험 제공
Output: ShuffleModal rAF 애니메이션, CutModal 드래그/탭 UX, Zustand 스토어 2개
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-ux/10-CONTEXT.md
@.planning/phases/10-ux/10-UI-SPEC.md
@sutda-shuffle-giri-ux.md

@packages/client/src/components/modals/ShuffleModal.tsx
@packages/client/src/components/modals/CutModal.tsx
@packages/client/src/store/gameStore.ts
@packages/client/src/components/game/HwatuCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zustand 스토어 생성 + ShuffleModal rAF 애니메이션 구현</name>
  <files>
    packages/client/src/store/shuffleStore.ts,
    packages/client/src/components/modals/ShuffleModal.tsx
  </files>
  <read_first>
    packages/client/src/components/modals/ShuffleModal.tsx,
    packages/client/src/store/gameStore.ts,
    packages/client/src/components/game/HwatuCard.tsx,
    sutda-shuffle-giri-ux.md,
    .planning/phases/10-ux/10-UI-SPEC.md
  </read_first>
  <action>
    **shuffleStore.ts 신규 생성 (per D-12):**
    ```typescript
    import { create } from 'zustand';

    export type ShufflePhase = 'idle' | 'peek' | 'hold' | 'rise' | 'drop' | 'rest';

    interface ShuffleState {
      isShuffling: boolean;
      phase: ShufflePhase;
      pickedIdx: number;  // 0 또는 1 — 두 카드 중 어느 쪽이 올라가는지
      startShuffle: () => void;
      stopShuffle: () => void;
      setPhase: (phase: ShufflePhase) => void;
      setPickedIdx: (idx: number) => void;
    }

    export const useShuffleStore = create<ShuffleState>((set) => ({
      isShuffling: false,
      phase: 'idle',
      pickedIdx: 0,
      startShuffle: () => set({ isShuffling: true, phase: 'peek' }),
      stopShuffle: () => set({ isShuffling: false, phase: 'idle', pickedIdx: 0 }),
      setPhase: (phase) => set({ phase }),
      setPickedIdx: (idx) => set({ pickedIdx: idx }),
    }));
    ```

    **ShuffleModal.tsx 전면 재설계 (per D-10, D-11 — UX-07):**

    기존: Dialog 모달 + "섞기" 버튼 한 개.
    새로운: 전체화면 오버레이 + 카드 더미 시각화 + 셔플 버튼 (pointerdown으로 셔플).

    구조:
    ```jsx
    <Dialog open={open}>
      <DialogContent className="max-w-sm" onInteractOutside={e => e.preventDefault()} onEscapeKeyDown={e => e.preventDefault()}>
        <DialogHeader>
          <DialogTitle>패를 섞으세요</DialogTitle>
        </DialogHeader>

        {/* 카드 더미 시각화 영역 */}
        <div className="relative h-40 flex items-center justify-center">
          {/* 두 장의 카드 (대표) — 셔플 애니메이션 대상 */}
          <div className="relative" style={{ width: 68, height: 110 }}>
            {[0, 1].map(idx => (
              <div
                key={idx}
                className="absolute inset-0 transition-none"
                style={{
                  transform: getCardTransform(idx, phase, pickedIdx),
                  zIndex: idx === pickedIdx ? 2 : 1,
                }}
              >
                <HwatuCard faceUp={false} size="md" />
              </div>
            ))}
          </div>
        </div>

        {/* 셔플 버튼 — pointerdown으로 시작, pointerup/leave로 종료 */}
        <div className="flex justify-center">
          <Button
            className="min-h-12 px-8"
            onPointerDown={handlePointerDown}
            onPointerUp={handlePointerUp}
            onPointerLeave={handlePointerUp}
          >
            {isShuffling ? '셔플 중...' : '셔플'}
          </Button>
        </div>

        {/* 셔플 후 확인 버튼 */}
        <DialogFooter>
          <Button
            onClick={handleConfirmShuffle}
            disabled={!hasShuffled}
          >
            확인
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
    ```

    **rAF 애니메이션 루프 (핵심 로직):**
    ```typescript
    const rafRef = useRef<number>(0);
    const startTimeRef = useRef<number>(0);
    const CYCLE_MS = 820;

    // 5 페이즈 타이밍 (per D-11):
    // peek:  0~120ms  — translateY(-8px)
    // hold:  120~300ms — 정지
    // rise:  300~480ms — translateY(-20px)
    // drop:  480~700ms — 빠르게 내리기 + 위치 교환 (easeIn)
    // rest:  700~820ms — 제자리 안정화

    function getPhaseFromElapsed(elapsed: number): ShufflePhase {
      const t = elapsed % CYCLE_MS;
      if (t < 120) return 'peek';
      if (t < 300) return 'hold';
      if (t < 480) return 'rise';
      if (t < 700) return 'drop';
      return 'rest';
    }

    function getCardTransform(cardIdx: number, phase: ShufflePhase, pickedIdx: number): string {
      const isPicked = cardIdx === pickedIdx;
      if (!isPicked) return 'translateY(0)';

      switch (phase) {
        case 'peek': return 'translateY(-8px)';
        case 'hold': return 'translateY(-8px)';
        case 'rise': return 'translateY(-20px)';
        case 'drop': return 'translateY(0)';
        case 'rest': return 'translateY(0)';
        default: return 'translateY(0)';
      }
    }

    function animate(timestamp: number) {
      if (!startTimeRef.current) startTimeRef.current = timestamp;
      const elapsed = timestamp - startTimeRef.current;

      const currentPhase = getPhaseFromElapsed(elapsed);
      setPhase(currentPhase);

      // rest 끝에서 pickedIdx 토글 (다음 사이클은 다른 카드)
      const t = elapsed % CYCLE_MS;
      if (currentPhase === 'rest' && t > 810) {
        setPickedIdx(prev => prev === 0 ? 1 : 0);
      }

      rafRef.current = requestAnimationFrame(animate);
    }

    const handlePointerDown = () => {
      startShuffle();
      setHasShuffled(true);
      startTimeRef.current = 0;
      rafRef.current = requestAnimationFrame(animate);
    };

    const handlePointerUp = () => {
      cancelAnimationFrame(rafRef.current);
      stopShuffle();
    };

    // cleanup
    useEffect(() => () => cancelAnimationFrame(rafRef.current), []);
    ```

    **handleConfirmShuffle:** `socket?.emit('shuffle', { roomId });` — 기존과 동일하지만, 셔플 애니메이션을 한 번 이상 수행해야(hasShuffled) 활성화.
  </action>
  <verify>
    <automated>cd packages/client && npx vitest run --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/client/src/store/shuffleStore.ts에 `export const useShuffleStore` 존재
    - shuffleStore.ts에 `isShuffling` 필드 존재
    - shuffleStore.ts에 `phase: ShufflePhase` 타입 존재
    - ShuffleModal.tsx에 `requestAnimationFrame` 존재
    - ShuffleModal.tsx에 `onPointerDown` 존재
    - ShuffleModal.tsx에 `onPointerUp` 존재
    - ShuffleModal.tsx에 `onPointerLeave` 존재
    - ShuffleModal.tsx에 `cancelAnimationFrame` 존재
    - ShuffleModal.tsx에 `셔플 중...` 문자열 존재
    - ShuffleModal.tsx에 `import.*useShuffleStore` 존재
    - ShuffleModal.tsx에 `import.*HwatuCard` 존재
    - 전체 클라이언트 테스트가 exit 0
  </acceptance_criteria>
  <done>셔플 버튼을 누르는 동안 rAF 기반 5단계 애니메이션이 루프하고, 떼면 즉시 종료된다. 한 번 이상 셔플 후 확인 버튼으로 서버에 emit한다.</done>
</task>

<task type="auto">
  <name>Task 2: GiriStore + CutModal 드래그 분리/탭 순서 UX 재설계 + CardFace/CardBack 삭제</name>
  <files>
    packages/client/src/store/giriStore.ts,
    packages/client/src/components/modals/CutModal.tsx,
    packages/client/src/components/game/CardFace.tsx,
    packages/client/src/components/game/CardBack.tsx
  </files>
  <read_first>
    packages/client/src/components/modals/CutModal.tsx,
    packages/client/src/store/gameStore.ts,
    packages/client/src/components/game/HwatuCard.tsx,
    sutda-shuffle-giri-ux.md,
    .planning/phases/10-ux/10-UI-SPEC.md
  </read_first>
  <action>
    **giriStore.ts 신규 생성 (per D-14):**
    ```typescript
    import { create } from 'zustand';

    export type GiriPhase = 'split' | 'tap' | 'merging' | 'done';

    interface Pile {
      id: number;       // 더미 식별자
      cardCount: number; // 해당 더미의 카드 수
      offsetX: number;  // 드래그 결과 x 위치 오프셋
    }

    interface GiriState {
      phase: GiriPhase;
      piles: Pile[];
      tapOrder: number[];  // 더미 id 순서 (1번 탭 = 맨 아래)
      isTtong: boolean;

      // Actions
      initSplit: () => void;        // 초기 상태 (20장 1더미)
      splitPile: (pileId: number, splitPoint: number) => void;  // 더미 분리
      tapPile: (pileId: number) => void;     // 순서 지정
      untapPile: (pileId: number) => void;   // 순서 해제
      setMerging: () => void;
      setDone: () => void;
      setTtong: () => void;
      reset: () => void;
    }

    export const useGiriStore = create<GiriState>((set) => ({
      phase: 'split',
      piles: [{ id: 0, cardCount: 20, offsetX: 0 }],
      tapOrder: [],
      isTtong: false,

      initSplit: () => set({
        phase: 'split',
        piles: [{ id: 0, cardCount: 20, offsetX: 0 }],
        tapOrder: [],
        isTtong: false,
      }),

      splitPile: (pileId, splitPoint) => set((state) => {
        const idx = state.piles.findIndex(p => p.id === pileId);
        if (idx === -1) return state;
        const pile = state.piles[idx];
        if (splitPoint <= 0 || splitPoint >= pile.cardCount) return state;
        const newPiles = [...state.piles];
        const newId = Math.max(...state.piles.map(p => p.id)) + 1;
        newPiles.splice(idx, 1,
          { id: pile.id, cardCount: splitPoint, offsetX: pile.offsetX - 30 },
          { id: newId, cardCount: pile.cardCount - splitPoint, offsetX: pile.offsetX + 30 },
        );
        return { piles: newPiles };
      }),

      tapPile: (pileId) => set((state) => {
        if (state.tapOrder.includes(pileId)) return state;
        const newOrder = [...state.tapOrder, pileId];
        // 모든 더미가 선택되면 자동으로 tap phase 유지 (합치기 버튼 활성화)
        return { tapOrder: newOrder };
      }),

      untapPile: (pileId) => set((state) => ({
        tapOrder: state.tapOrder.filter(id => id !== pileId),
      })),

      setMerging: () => set({ phase: 'merging' }),
      setDone: () => set({ phase: 'done' }),
      setTtong: () => set({ isTtong: true, phase: 'done' }),
      reset: () => set({
        phase: 'split',
        piles: [{ id: 0, cardCount: 20, offsetX: 0 }],
        tapOrder: [],
        isTtong: false,
      }),
    }));
    ```

    **CutModal.tsx 전면 재설계 (per D-13, D-14, D-15 — UX-08):**

    기존: 5개 선택 버튼(2더미/3더미/4더미/5더미/퉁) + 클릭으로 순서 지정.
    새로운: 드래그로 더미 분리 + 탭으로 순서 지정 + 합치기 애니메이션.

    **Phase: split**
    - 20장 더미가 카드 스택으로 시각화 (HwatuCard size="sm" 뒷면 겹침)
    - 더미를 드래그하면 분리됨 (drag threshold 8px, per D-13)
    - 안내 텍스트: "드래그해서 더미를 나누세요"
    - 분리 완료 후 "순서 정하기" 버튼으로 tap phase 전환
    - 퉁 버튼도 하단에 유지 (기존 UX 겸용)

    **드래그 로직:**
    ```typescript
    const DRAG_THRESHOLD = 8;
    const pointerStartRef = useRef<{ x: number; y: number; pileId: number } | null>(null);

    function handlePilePointerDown(e: React.PointerEvent, pileId: number) {
      pointerStartRef.current = { x: e.clientX, y: e.clientY, pileId };
      (e.target as HTMLElement).setPointerCapture(e.pointerId);
    }

    function handlePilePointerMove(e: React.PointerEvent) {
      if (!pointerStartRef.current) return;
      const dx = e.clientX - pointerStartRef.current.x;
      if (Math.abs(dx) > DRAG_THRESHOLD) {
        // 더미 분리: 절반 지점에서 나누기
        const pile = piles.find(p => p.id === pointerStartRef.current!.pileId);
        if (pile && pile.cardCount > 1) {
          const splitPoint = Math.floor(pile.cardCount / 2);
          splitPile(pointerStartRef.current.pileId, splitPoint);
        }
        pointerStartRef.current = null;
      }
    }

    function handlePilePointerUp() {
      pointerStartRef.current = null;
    }
    ```

    **Phase: tap**
    - 분리된 더미들이 나란히 표시
    - 각 더미 탭 시 순서 번호 배지(원형, 1부터) 표시 (per D-15: 1 = 맨 아래)
    - 이미 탭한 더미 재탭 시 순서 해제
    - 모든 더미에 순서 지정 완료 시 "합치기" 버튼 활성화

    **Phase: merging**
    - 합치기 애니메이션: 더미들이 tapOrder 순서대로 중앙으로 이동 (easeInOut 380ms, per D-15)
    - CSS transition: `transition: transform 380ms cubic-bezier(0.42, 0, 0.58, 1)`
    - 애니메이션 완료 후 서버로 emit

    **Phase: done**
    - 서버 emit 후 모달 닫힘 (기존 로직)

    **서버 emit 변환:**
    tapOrder(더미 id 배열)를 기존 서버 API(`cut` 이벤트의 `cutPoints` + `order`)로 변환:
    ```typescript
    function emitCutResult() {
      if (isTtong) {
        socket?.emit('declare-ttong', { roomId });
        return;
      }

      const deckSize = 20;
      // cutPoints: 각 더미 경계의 누적 카드 수
      const cutPoints: number[] = [];
      let acc = 0;
      for (let i = 0; i < piles.length - 1; i++) {
        acc += piles[i].cardCount;
        cutPoints.push(acc);
      }

      // order: tapOrder를 piles 배열 인덱스로 매핑
      const order = tapOrder.map(pileId => piles.findIndex(p => p.id === pileId));

      socket?.emit('cut', { roomId, cutPoints, order });
    }
    ```

    **CardFace.tsx, CardBack.tsx 삭제:**
    Plan 03에서 모든 사용처가 HwatuCard로 교체되었고, 이 Task에서 CutModal도 HwatuCard로 전환되었으므로, CardFace.tsx와 CardBack.tsx를 삭제한다.

    삭제 전 확인: `grep -r "CardFace\|CardBack" packages/client/src/ --include="*.tsx" --include="*.ts"` 에서 import가 없는지 검증.
  </action>
  <verify>
    <automated>cd packages/client && npx vitest run --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/client/src/store/giriStore.ts에 `export const useGiriStore` 존재
    - giriStore.ts에 `phase: GiriPhase` 타입 존재
    - giriStore.ts에 `piles` 필드 존재
    - giriStore.ts에 `tapOrder` 필드 존재
    - CutModal.tsx에 `onPointerDown` 또는 `handlePilePointerDown` 존재
    - CutModal.tsx에 `DRAG_THRESHOLD` 또는 threshold 8 관련 상수 존재
    - CutModal.tsx에 `import.*useGiriStore` 존재
    - CutModal.tsx에 `import.*HwatuCard` 존재
    - CutModal.tsx에 `합치기` 문자열 존재
    - CutModal.tsx에 `380ms` 또는 `380` 타이밍 존재
    - ShuffleModal.tsx에 `CardBack` import 없음
    - CutModal.tsx에 `CardBack` import 없음
    - packages/client/src/components/game/CardFace.tsx 파일이 존재하지 않음
    - packages/client/src/components/game/CardBack.tsx 파일이 존재하지 않음
    - 전체 클라이언트 테스트가 exit 0
  </acceptance_criteria>
  <done>기리 모달이 드래그 분리 + 탭 순서 UX로 재설계되고, 합치기 애니메이션(380ms)이 작동하며, CardFace/CardBack 파일이 삭제되었다</done>
</task>

</tasks>

<verification>
- `cd packages/client && npx vitest run --run` 전체 통과
- `ls packages/client/src/components/game/CardFace.tsx 2>/dev/null` 파일 없음
- `ls packages/client/src/components/game/CardBack.tsx 2>/dev/null` 파일 없음
- ShuffleModal에서 pointerdown 셔플 인터랙션 작동 (수동 확인)
- CutModal에서 드래그 분리 + 탭 순서 작동 (수동 확인)
</verification>

<success_criteria>
- 셔플: pointerdown 누르는 동안 rAF 5단계 사이클 애니메이션 루프, 떼면 즉시 종료
- 기리: 드래그로 더미 분리, 탭으로 순서 지정, 합치기 애니메이션 380ms
- Zustand 스토어 2개(shuffleStore, giriStore) 생성
- CardFace.tsx, CardBack.tsx 파일 완전 삭제 (0 참조)
</success_criteria>

<output>
완료 후 `.planning/phases/10-ux/10-04-SUMMARY.md` 생성
</output>
