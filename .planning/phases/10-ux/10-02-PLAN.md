---
phase: 10-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/pages/RoomPage.tsx
  - packages/client/src/components/layout/GameTable.tsx
  - packages/client/src/components/layout/BettingPanel.tsx
  - packages/client/src/components/layout/WaitingRoom.tsx
  - packages/client/src/components/layout/ResultScreen.tsx
  - packages/client/src/components/layout/__tests__/GameLayout.test.tsx
  - packages/client/src/components/layout/__tests__/BettingPanel.test.tsx
autonomous: true
requirements: [UX-09, IMG-03, BET-HIGHLIGHT]

must_haves:
  truths:
    - "데스크탑(768px+)에서 3열 레이아웃(좌사이드|중앙|우사이드)이 스크롤 없이 표시된다"
    - "모바일(<768px)에서 수직 레이아웃(테이블|베팅+손패|채팅ph)이 스크롤 없이 표시된다"
    - "게임 테이블에 배경 이미지(background.jpg)가 적용된다"
    - "대기실에 메인 타이틀 이미지(main_tilte.jpg)가 표시된다"
    - "재경기 시 ResultScreen 위에 regame.png 오버레이가 표시된다"
    - "내 베팅 차례일 때 BettingPanel에 ring+glow 강조가 표시된다"
  artifacts:
    - path: "packages/client/src/pages/RoomPage.tsx"
      provides: "3열/수직 반응형 레이아웃"
      contains: "grid-cols-[256px_1fr_256px]"
    - path: "packages/client/src/components/layout/GameTable.tsx"
      provides: "배경 이미지 적용"
      contains: "background.jpg"
    - path: "packages/client/src/components/layout/BettingPanel.tsx"
      provides: "BET-HIGHLIGHT ring+glow"
      contains: "ring-primary"
    - path: "packages/client/src/components/layout/WaitingRoom.tsx"
      provides: "메인 타이틀 이미지"
      contains: "main_tilte.jpg"
    - path: "packages/client/src/components/layout/ResultScreen.tsx"
      provides: "재경기 오버레이"
      contains: "regame.png"
  key_links:
    - from: "RoomPage.tsx"
      to: "GameTable/BettingPanel/HandPanel"
      via: "3열 그리드 배치"
      pattern: "grid-cols"
    - from: "BettingPanel.tsx"
      to: "isMyTurn prop"
      via: "조건부 ring 클래스"
      pattern: "isMyTurn.*ring"
---

<objective>
RoomPage의 게임 진행 화면을 데스크탑 3열/모바일 수직 반응형 레이아웃으로 재설계하고, 배경/타이틀/재경기 이미지를 적용하며, 베팅 패널 강조 표시를 추가한다.

Purpose: 스크롤 없는 단일 화면 게임 경험 제공 + 시각적 완성도 향상
Output: RoomPage 레이아웃 재설계, 이미지 적용, BET-HIGHLIGHT
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ux/10-CONTEXT.md
@.planning/phases/10-ux/10-UI-SPEC.md

@packages/client/src/pages/RoomPage.tsx
@packages/client/src/components/layout/GameTable.tsx
@packages/client/src/components/layout/BettingPanel.tsx
@packages/client/src/components/layout/WaitingRoom.tsx
@packages/client/src/components/layout/ResultScreen.tsx
@packages/client/src/components/layout/InfoPanel.tsx
@packages/client/src/components/layout/ChatPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: RoomPage 3열/수직 반응형 레이아웃 재설계 + GameTable 배경 이미지</name>
  <files>
    packages/client/src/pages/RoomPage.tsx,
    packages/client/src/components/layout/GameTable.tsx,
    packages/client/src/components/layout/__tests__/GameLayout.test.tsx
  </files>
  <read_first>
    packages/client/src/pages/RoomPage.tsx,
    packages/client/src/components/layout/GameTable.tsx,
    packages/client/src/components/layout/InfoPanel.tsx,
    packages/client/src/components/layout/ChatPanel.tsx,
    packages/client/src/components/layout/HandPanel.tsx,
    packages/client/src/components/layout/BettingPanel.tsx,
    .planning/phases/10-ux/10-UI-SPEC.md
  </read_first>
  <action>
    **RoomPage.tsx 게임 진행 중 렌더링 부분을 완전 재설계 (per D-16, D-17, D-18).**

    현재 구조: `flex flex-col` → `상단헤더 | flex-1 GameTable | 하단패널(HandPanel+InfoPanel+BettingPanel+ChatPanel)`

    새 구조:

    **데스크탑 (md 이상, 768px+) — 3열 그리드:**
    ```jsx
    <div className="hidden md:grid grid-cols-[256px_1fr_256px] h-dvh overflow-hidden">
      {/* 좌사이드 */}
      <div className="flex flex-col border-r border-border overflow-y-auto">
        <BettingPanel ... />
        <HandPanel ... />
      </div>

      {/* 중앙 */}
      <div className="flex items-center justify-center overflow-hidden">
        <GameTable ... />
      </div>

      {/* 우사이드 */}
      <div className="flex flex-col border-l border-border overflow-y-auto">
        <InfoPanel ... />
        <ChatPanel />
      </div>
    </div>
    ```

    **모바일 (<768px) — 수직 flex:**
    ```jsx
    <div className="md:hidden flex flex-col h-dvh overflow-hidden">
      {/* 상단: 게임 테이블 + 정보 레이어 */}
      <div className="relative flex-1 min-h-0 overflow-hidden">
        <GameTable ... />
        {/* InfoPanel을 absolute로 오버레이 (테이블 위 코너) */}
        <div className="absolute top-2 right-2 z-10">
          <InfoPanel compact ... />
        </div>
      </div>

      {/* 중단: 베팅 + 손패 */}
      <div className="shrink-0 border-t border-border">
        <HandPanel ... />
        <BettingPanel ... />
      </div>

      {/* 하단: 채팅 placeholder */}
      <div className="shrink-0 h-12 border-t border-border flex items-center justify-center">
        <ChatPanel />
      </div>
    </div>
    ```

    **기존 상단 헤더 (`방 {roomId}` 표시) 제거** — 3열 레이아웃에서는 공간 낭비. 방 ID는 InfoPanel에 이미 표시 가능하거나 생략.

    **모달들은 기존 그대로 유지** — fixed z-50 이므로 레이아웃 변경에 영향 없음.

    **잠시쉬기 배너, showCardConfirm 오버레이, 세장 3번째 카드 오버레이도 기존 그대로 유지** (fixed position).

    **GameTable.tsx 수정:**
    데스크탑 원형 배치 컨테이너에 배경 이미지 추가 (per D-02):
    ```jsx
    <div
      className="relative mx-auto hidden md:block rounded-2xl overflow-hidden"
      style={{
        width: '480px',
        height: '480px',
        backgroundImage: "url('/img/background.jpg')",
        backgroundSize: 'cover',
        backgroundPosition: 'center',
      }}
    >
    ```

    모바일 GameTable에도 동일 배경 적용:
    ```jsx
    <div
      className="md:hidden p-4"
      style={{
        backgroundImage: "url('/img/background.jpg')",
        backgroundSize: 'cover',
        backgroundPosition: 'center',
      }}
    >
    ```

    **GameLayout.test.tsx 신규 생성 (Wave 0):**
    - RoomPage 내 3열 그리드 클래스 `grid-cols-[256px_1fr_256px]` 존재 확인
    - `h-dvh` 클래스 존재 확인
    - `overflow-hidden` 클래스 존재 확인

    **BettingPanel이 보이는 조건 변경:** 현재 RoomPage에서 betting phase + cardConfirmed + isAlive 조건으로 BettingPanel 렌더링. 새 레이아웃에서는 좌사이드에 항상 BettingPanel 영역을 배치하되, 베팅 불가 상태에서는 "대기 중" 메시지 표시. 기존 조건부 렌더링을 BettingPanel 내부로 이동하거나, 래퍼 div에서 처리.

    **구현 방식:** BettingPanel을 항상 렌더링하되, betting phase가 아닐 때는 내부에서 빈 상태/대기 메시지 표시. 이를 위해 BettingPanel에 `active` prop 추가하거나, 기존 `isMyTurn`=false + 현재 phase를 전달.

    대안 (더 단순): 좌사이드에서 베팅 가능 상태일 때만 BettingPanel 렌더, 불가능할 때는 빈 div. 이 방식으로 진행 — 기존 조건부 렌더링 로직을 좌사이드 div 내부에서 유지.
  </action>
  <verify>
    <automated>cd packages/client && npx vitest run src/components/layout/__tests__/GameLayout.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RoomPage.tsx에 `grid-cols-[256px_1fr_256px]` 문자열 존재
    - RoomPage.tsx에 `h-dvh` 클래스 존재
    - RoomPage.tsx에 `overflow-hidden` 클래스 존재
    - RoomPage.tsx에 `md:hidden flex flex-col` 모바일 레이아웃 존재
    - GameTable.tsx에 `background.jpg` 문자열 존재
    - GameTable.tsx에 `backgroundSize` 또는 `bg-cover` 존재
    - GameLayout.test.tsx가 exit 0
  </acceptance_criteria>
  <done>데스크탑에서 3열(256px|auto|256px) 그리드, 모바일에서 수직 flex 레이아웃이 스크롤 없이(h-dvh overflow-hidden) 렌더링되고, GameTable에 배경 이미지가 적용된다</done>
</task>

<task type="auto">
  <name>Task 2: WaitingRoom 타이틀 이미지 + ResultScreen 재경기 오버레이 + BettingPanel 강조</name>
  <files>
    packages/client/src/components/layout/WaitingRoom.tsx,
    packages/client/src/components/layout/ResultScreen.tsx,
    packages/client/src/components/layout/BettingPanel.tsx,
    packages/client/src/components/layout/__tests__/BettingPanel.test.tsx
  </files>
  <read_first>
    packages/client/src/components/layout/WaitingRoom.tsx,
    packages/client/src/components/layout/ResultScreen.tsx,
    packages/client/src/components/layout/BettingPanel.tsx,
    packages/client/src/components/layout/__tests__/BettingPanel.test.tsx,
    .planning/phases/10-ux/10-UI-SPEC.md
  </read_first>
  <action>
    **WaitingRoom.tsx 수정 (per D-02 — IMG-03):**
    현재 `<h2 className="text-xl font-semibold">대기실</h2>` 를 메인 타이틀 이미지로 교체:
    ```jsx
    <img
      src="/img/main_tilte.jpg"
      alt="섯다"
      style={{ width: 'auto', height: '80px', objectFit: 'contain' }}
    />
    ```

    **ResultScreen.tsx 수정 (per D-03 — IMG-03):**
    ResultScreen의 최상위 래퍼에 `relative` 추가 (이미 있다면 유지).
    gameState에서 재경기 여부를 판단: `gameState.isRematch === true` 또는 `gameState.gusaTriggered === true` 일 때 regame.png 오버레이 표시.

    재경기 판단 조건: ResultScreen에 전달되는 gameState 에서 `tiedPlayerIds` 배열 길이 > 0 이거나, `gusaTriggered === true` 이거나, phase 전환 이력으로 판단. **가장 단순한 방법:** ResultScreen에 `isRematch?: boolean` prop 추가하고, RoomPage에서 이전 phase가 `rematch-pending` 또는 `gusa-pending` 이었는지로 판단하여 전달.

    대안 (prop 추가 없이): gameState에 `round > 1` 이면 재경기로 간주. `gameState.round`가 존재하는지 확인 필요.

    **실제로 gameState.round 확인이 필요하다. round 정보가 없다면 다른 방법을 사용.**

    구현: ResultScreen에서 조건부로 오버레이 렌더링. 조건은 RoomPage에서 `isRematch` prop으로 전달. RoomPage에서는 이전 phase 기록(`prevPhaseRef`)을 활용: 이전 phase가 `gusa-pending`, `gusa-announce`, `rematch-pending` 중 하나였으면 `isRematch=true`.

    ```jsx
    {/* ResultScreen 내부, 최상위 wrapper 안 맨 위 */}
    {isRematch && (
      <div
        className="absolute inset-0 z-10 pointer-events-none flex items-center justify-center"
        style={{ animation: 'fadeIn 0.4s ease-in' }}
      >
        <img
          src="/img/regame.png"
          alt="재경기"
          className="object-contain opacity-85"
          style={{ maxWidth: '80%', maxHeight: '80%' }}
        />
      </div>
    )}
    ```

    CSS fadeIn 키프레임은 인라인 style 또는 index.css에 추가:
    ```css
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
    ```
    Tailwind v4에서는 `animate-in fade-in` 유틸리티를 사용할 수 있으나 설치 여부 확인 필요. 없으면 style 태그나 index.css에 직접 정의.

    **BettingPanel.tsx 수정 (per D-21 — BET-HIGHLIGHT):**
    최상위 `<div className="p-4 space-y-3">` 에 isMyTurn 조건부 클래스 추가:
    ```jsx
    <div className={cn(
      "p-4 space-y-3 rounded-lg transition-shadow",
      isMyTurn && "ring-2 ring-primary ring-offset-2 ring-offset-background shadow-[0_0_12px_hsl(75_55%_42%/0.35)]"
    )}>
    ```

    `cn` 유틸리티 import 추가: `import { cn } from '@/lib/utils';`

    **BettingPanel.test.tsx 수정:**
    기존 테스트에 BET-HIGHLIGHT 테스트 케이스 추가:
    - isMyTurn=true일 때: wrapper에 `ring-primary` 클래스 존재
    - isMyTurn=false일 때: wrapper에 `ring-primary` 클래스 미존재
  </action>
  <verify>
    <automated>cd packages/client && npx vitest run src/components/layout/__tests__/BettingPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - WaitingRoom.tsx에 `main_tilte.jpg` 문자열 존재
    - WaitingRoom.tsx에 `<img` 태그 존재 (타이틀 이미지)
    - ResultScreen.tsx에 `regame.png` 문자열 존재
    - ResultScreen.tsx에 `pointer-events-none` 클래스 존재 (오버레이)
    - BettingPanel.tsx에 `ring-primary` 문자열 존재
    - BettingPanel.tsx에 `ring-offset-background` 문자열 존재
    - BettingPanel.tsx에 `shadow-[0_0_12px` 문자열 존재
    - BettingPanel.tsx에 `cn` import 존재
    - BettingPanel.test.tsx가 exit 0
  </acceptance_criteria>
  <done>WaitingRoom에 타이틀 이미지, ResultScreen에 재경기 오버레이, BettingPanel에 베팅 차례 ring+glow 강조가 적용된다</done>
</task>

</tasks>

<verification>
- `cd packages/client && npx vitest run src/components/layout/__tests__/` 전체 통과
- RoomPage.tsx에서 `grid-cols-[256px_1fr_256px]` grep 확인
- BettingPanel.tsx에서 `ring-primary` grep 확인
- GameTable.tsx에서 `background.jpg` grep 확인
</verification>

<success_criteria>
- 데스크탑 3열 / 모바일 수직 레이아웃이 h-dvh overflow-hidden으로 스크롤 없이 렌더링
- GameTable에 배경 이미지, WaitingRoom에 타이틀 이미지, ResultScreen에 재경기 오버레이
- 베팅 차례에 BettingPanel ring+glow 강조
</success_criteria>

<output>
완료 후 `.planning/phases/10-ux/10-02-SUMMARY.md` 생성
</output>
