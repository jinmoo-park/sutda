---
phase: 01-ai-watchdog-mvp
plan: 02
title: "로컬 pull-debug.sh + Claude Code 세션 훅"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pull-debug.sh
  - .claude/hooks/UserPromptSubmit.js
  - CLAUDE.md
autonomous: true
requirements: ["WD-04", "WD-05"]
must_haves:
  truths:
    - "pull-debug.sh 실행 시 VM .debug/ 폴더가 로컬로 동기화됨"
    - "Claude Code 세션 시작 시 pull-debug.sh가 자동 실행됨"
    - "VM 접속 불가 시에도 pull-debug.sh가 비정상 종료되지 않음"
  artifacts:
    - path: "scripts/pull-debug.sh"
      provides: "VM .debug/ -> 로컬 .debug/ 동기화"
    - path: "CLAUDE.md"
      provides: "pull-debug.sh 자동 실행 지시 (Claude Code에게)"
  key_links:
    - from: "scripts/pull-debug.sh"
      to: "VM /opt/ai-watchdog/.debug/"
      via: "scp/rsync over SSH"
      pattern: "rsync.*sutda\\.duckdns\\.org.*\\.debug"
    - from: "CLAUDE.md"
      to: "scripts/pull-debug.sh"
      via: "세션 시작 시 실행 지시"
      pattern: "pull-debug"
---

<objective>
로컬 머신에서 VM의 watchdog 분석 결과를 동기화하는 pull 스크립트와 Claude Code 자동 실행 훅 구성

Purpose: Claude Code 디버그 세션 시작 시 항상 최신 VM 로그 분석 결과(.debug/ MD 파일)가 로컬에 있도록 자동화. (per D-04, D-05)
Output: scripts/pull-debug.sh + CLAUDE.md 업데이트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ai-watchdog-mvp/01-CONTEXT.md
@CLAUDE.md
</context>

<interfaces>
<!-- 기존 CLAUDE.md의 SSH 접속 정보 -->
SSH 키: ~/.ssh/ssh-key-2026-04-02.key
VM 서버: ubuntu@sutda.duckdns.org
VM watchdog 경로: /opt/ai-watchdog/.debug/
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: pull-debug.sh 로컬 동기화 스크립트 작성</name>
  <files>scripts/pull-debug.sh</files>
  <read_first>
    - CLAUDE.md (기존 SSH 접속 정보, 배포 절차 참고)
    - .planning/phases/01-ai-watchdog-mvp/01-CONTEXT.md (D-04 결정: scp/rsync)
  </read_first>
  <action>
프로젝트 루트에 `scripts/` 디렉토리 생성 후 `pull-debug.sh` 작성.

**scripts/pull-debug.sh** 내용:
```bash
#!/usr/bin/env bash
# VM watchdog .debug/ 폴더를 로컬로 동기화
# Usage: bash scripts/pull-debug.sh

set -euo pipefail

SSH_KEY="$HOME/.ssh/ssh-key-2026-04-02.key"
REMOTE_USER="ubuntu"
REMOTE_HOST="sutda.duckdns.org"
REMOTE_DIR="/opt/ai-watchdog/.debug/"
LOCAL_DIR=".debug/"

# 로컬 .debug/ 디렉토리 생성
mkdir -p "$LOCAL_DIR"

echo "[pull-debug] Syncing VM .debug/ -> local .debug/ ..."

# rsync로 동기화 (SSH 타임아웃 10초, 실패 시 에러 메시지만 출력)
if rsync -avz --timeout=10 \
  -e "ssh -i $SSH_KEY -o ConnectTimeout=10 -o StrictHostKeyChecking=no" \
  "${REMOTE_USER}@${REMOTE_HOST}:${REMOTE_DIR}" \
  "$LOCAL_DIR" 2>/dev/null; then
  FILE_COUNT=$(find "$LOCAL_DIR" -name "*.md" -type f 2>/dev/null | wc -l)
  echo "[pull-debug] Done. ${FILE_COUNT} report(s) in .debug/"
else
  echo "[pull-debug] WARNING: VM sync failed (network issue?). Continuing with existing local .debug/"
fi
```

- chmod +x scripts/pull-debug.sh
- .gitignore에 `.debug/` 추가 (분석 결과 파일은 커밋 대상 아님)
  </action>
  <verify>
    <automated>test -f scripts/pull-debug.sh && test -x scripts/pull-debug.sh && head -1 scripts/pull-debug.sh | grep -q "bash" && grep -q "rsync" scripts/pull-debug.sh && grep -q "sutda.duckdns.org" scripts/pull-debug.sh && grep -q ".debug" scripts/pull-debug.sh && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/pull-debug.sh 파일 존재하고 실행 권한(+x) 있음
    - 첫 줄이 `#!/usr/bin/env bash`
    - `rsync` 명령 포함
    - `sutda.duckdns.org` 호스트 참조 포함
    - `ssh-key-2026-04-02.key` SSH 키 참조 포함
    - `/opt/ai-watchdog/.debug/` 원격 경로 포함
    - `.debug/` 로컬 경로 포함
    - ConnectTimeout 설정 포함 (네트워크 실패 시 무한 대기 방지)
    - .gitignore에 `.debug/` 항목 포함
  </acceptance_criteria>
  <done>pull-debug.sh가 실행 가능하고, rsync로 VM .debug/ -> 로컬 .debug/ 동기화하며, 네트워크 실패 시 안전하게 경고 출력 후 계속 진행</done>
</task>

<task type="auto">
  <name>Task 2: Claude Code 세션 시작 시 자동 실행 설정</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (기존 내용 보존하며 watchdog 섹션 추가)
    - scripts/pull-debug.sh (Task 1에서 생성됨 - 경로 확인)
    - .planning/phases/01-ai-watchdog-mvp/01-CONTEXT.md (D-05 결정)
  </read_first>
  <action>
CLAUDE.md에 다음 섹션을 **기존 내용 아래에** 추가한다 (per D-05).

기존 `## 배포 절차` 섹션 아래에 새 섹션 추가:

```markdown
## AI Watchdog 디버그 컨텍스트

### 세션 시작 시 자동 실행

새 대화 세션을 시작할 때, 첫 번째 응답 전에 다음 명령을 실행한다:

```bash
bash scripts/pull-debug.sh
```

이 스크립트는 VM의 AI watchdog 분석 결과(.debug/*.md)를 로컬로 동기화한다.
VM 접속 실패 시 경고만 출력하고 계속 진행하므로 세션 시작을 차단하지 않는다.

### .debug/ 파일 활용

`.debug/` 디렉토리에 최신 MD 파일이 있으면, 디버깅 작업 시 참고 컨텍스트로 활용한다:
- 파일명 형식: `YYYY-MM-DD-HH.md`
- 내용: VM 로그 분석 결과 (severity, 원인 추정, 조치 제안)
- 크리티컬 이슈가 있으면 사용자에게 알린다
```

**Hook 방식 대안 검토:**
- Claude Code의 `UserPromptSubmit` hook이 현재 환경에서 지원되는지 확인
- 지원되면 `.claude/hooks/UserPromptSubmit.js`로 구현:
  ```javascript
  // .claude/hooks/UserPromptSubmit.js
  const { execSync } = require('child_process');
  try {
    execSync('bash scripts/pull-debug.sh', { timeout: 15000, stdio: 'inherit' });
  } catch (e) {
    console.log('[pull-debug] Sync skipped:', e.message);
  }
  ```
- Hook이 미지원이면 CLAUDE.md 지시만으로 충분 (Claude Code가 세션 시작 시 CLAUDE.md를 읽고 지시를 따름)
  </action>
  <verify>
    <automated>grep -q "pull-debug" CLAUDE.md && grep -q ".debug/" CLAUDE.md && grep -q "watchdog" CLAUDE.md && echo "CLAUDE.MD UPDATED"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md에 `pull-debug.sh` 참조 포함
    - CLAUDE.md에 `## AI Watchdog` 섹션 헤더 포함
    - CLAUDE.md에 `bash scripts/pull-debug.sh` 실행 명령 포함
    - CLAUDE.md에 `.debug/` 디렉토리 참조 포함
    - CLAUDE.md의 기존 `## 배포 절차` 섹션이 그대로 보존됨
    - 기존 `### 주의사항` 섹션이 그대로 보존됨
  </acceptance_criteria>
  <done>CLAUDE.md에 watchdog 자동 실행 지시가 추가되어, Claude Code 세션 시작 시 pull-debug.sh를 실행하도록 설정됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 -> VM SSH | pull-debug.sh가 SSH로 VM에 접속하여 파일 동기화 |
| .debug/ 파일 | VM에서 동기화된 분석 결과 로컬 저장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I (Information Disclosure) | .debug/ MD 파일 | mitigate | .gitignore에 .debug/ 추가하여 실수로 커밋되지 않도록 방지 |
| T-02-02 | D (Denial of Service) | SSH timeout | mitigate | ConnectTimeout=10, rsync --timeout=10으로 네트워크 장애 시 빠른 실패 |
| T-02-03 | T (Tampering) | rsync 동기화 | accept | 읽기 전용 동기화, VM에서 로컬로만 복사 |
</threat_model>

<verification>
1. `bash scripts/pull-debug.sh` 실행 시 VM .debug/ 동기화 확인 (또는 네트워크 없을 때 경고 메시지)
2. `grep "pull-debug" CLAUDE.md` 로 CLAUDE.md 업데이트 확인
3. `.gitignore`에 `.debug/` 포함 확인
</verification>

<success_criteria>
- pull-debug.sh 수동 실행 시 VM .debug/ 파일이 로컬 .debug/로 동기화됨
- VM 접속 불가 시 경고 메시지 출력 후 정상 종료 (exit 0)
- CLAUDE.md에 세션 시작 시 자동 실행 지시가 명시됨
- .debug/ 디렉토리가 .gitignore에 포함됨
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-watchdog-mvp/01-02-SUMMARY.md`
</output>
