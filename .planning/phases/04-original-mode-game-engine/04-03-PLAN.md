---
phase: 04-original-mode-game-engine
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - packages/server/src/index.ts
  - packages/server/src/index.test.ts
autonomous: true
requirements: [SEAT-01, MODE-OG-02]

must_haves:
  truths:
    - "start-game 이벤트 후 GameEngine이 생성되어 게임 플로우가 시작된다"
    - "클라이언트의 게임 이벤트(attend-school, shuffle, cut, bet-action, reveal-card 등)가 GameEngine 메서드로 전달된다"
    - "GameEngine 상태 변경 후 game-state 이벤트로 방 전체에 브로드캐스트된다"
    - "잘못된 액션 시 game-error 이벤트가 해당 소켓에만 전송된다"
    - "select-mode 이벤트를 통해 선 플레이어가 모드를 선택할 수 있다"
    - "next-round 이벤트를 통해 result 이후 다음 판을 시작할 수 있다"
  artifacts:
    - path: "packages/server/src/index.ts"
      provides: "GameEngine Map + Socket.IO 게임 이벤트 핸들러"
      contains: "GameEngine"
    - path: "packages/server/src/index.test.ts"
      provides: "Socket.IO 게임 이벤트 통합 테스트"
      contains: "game-state"
  key_links:
    - from: "packages/server/src/index.ts"
      to: "packages/server/src/game-engine.ts"
      via: "import { GameEngine } from './game-engine.js'"
      pattern: "import.*GameEngine.*game-engine"
    - from: "packages/server/src/index.ts"
      to: "io.to(roomId).emit('game-state')"
      via: "GameEngine 상태 변경 후 브로드캐스트"
      pattern: "emit.*game-state"
---

<objective>
GameEngine을 Socket.IO 서버에 연결하여 클라이언트의 게임 이벤트를 처리하고 상태를 브로드캐스트한다.

Purpose: Phase 4의 마지막 조각 -- 독립된 GameEngine을 실제 네트워크 계층과 연결. Phase 6 UI가 이 이벤트에 바인딩됨.
Output: Socket.IO 게임 이벤트 핸들러 + 통합 테스트
</objective>

<execution_context>
@.planning/phases/04-original-mode-game-engine/04-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@packages/server/src/index.ts
@packages/shared/src/types/protocol.ts

<interfaces>
<!-- Plan 01-02에서 생성된 GameEngine 전체 계약 -->

From packages/server/src/game-engine.ts (Plan 01+02 산출물):
```typescript
export class GameEngine {
  constructor(roomId: string, players: RoomPlayer[], mode: GameMode, roundNumber?: number);
  getState(): Readonly<GameState>;
  selectDealerCard(playerId: string, cardIndex: number): void;
  setDealerFromPreviousWinner(winnerId: string): void;
  attendSchool(playerId: string): void;
  selectMode(playerId: string, mode: GameMode): void;
  shuffle(playerId: string): void;
  cut(playerId: string, cutPoints: number[], order: number[]): void;
  declareTtong(playerId: string): void;
  processBetAction(playerId: string, action: BetAction): void;
  revealCard(playerId: string): void;
  startRematch(): void;
  nextRound(): void;
}
```

From packages/shared/src/types/protocol.ts (Plan 01 확장):
```typescript
export interface ClientToServerEvents {
  // 기존 방 관리 이벤트...
  'attend-school': (data: { roomId: string }) => void;
  'select-dealer-card': (data: { roomId: string; cardIndex: number }) => void;
  'select-mode': (data: { roomId: string; mode: GameMode }) => void;
  'shuffle': (data: { roomId: string }) => void;
  'cut': (data: { roomId: string; cutPoints: number[]; order: number[] }) => void;
  'declare-ttong': (data: { roomId: string }) => void;
  'bet-action': (data: { roomId: string; action: BetAction }) => void;
  'reveal-card': (data: { roomId: string }) => void;
  'next-round': (data: { roomId: string }) => void;
}
export interface ServerToClientEvents {
  // 기존 방 관리 이벤트...
  'game-state': (data: GameState) => void;
  'game-error': (data: { code: string; message: string }) => void;
}
```

From packages/server/src/index.ts (현재 상태):
```typescript
// io, roomManager, httpServer가 export됨
// io.on('connection', (socket) => { ... }) 내에 방 관리 핸들러 존재
// start-game 핸들러에서 roomManager.startGame() 호출 후 room-state 브로드캐스트
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Socket.IO 게임 이벤트 핸들러 연결 + 통합 테스트</name>
  <files>
    packages/server/src/index.ts,
    packages/server/src/index.test.ts
  </files>
  <read_first>
    packages/server/src/index.ts,
    packages/server/src/game-engine.ts,
    packages/shared/src/types/protocol.ts,
    packages/shared/src/types/game.ts,
    packages/server/src/room-manager.ts
  </read_first>
  <action>
    **Step 1: GameEngine Map 추가 (index.ts)**

    파일 상단에:
    ```typescript
    import { GameEngine } from './game-engine.js';
    ```

    roomManager 선언 아래에:
    ```typescript
    const gameEngines: Map<string, GameEngine> = new Map();
    ```

    export에 추가:
    ```typescript
    export { io, roomManager, httpServer, gameEngines };
    ```

    **Step 2: start-game 핸들러 수정 -- 모드 하드코딩 금지 (per D-01, MODE-OG-01)**

    기존 start-game 핸들러 내에서 `roomManager.startGame(roomId)` 성공 후:
    ```typescript
    socket.on('start-game', ({ roomId }) => {
      const check = roomManager.canStartGame(roomId, socket.id);
      if (!check.ok) {
        return emitError(socket, check.code as ErrorCode);
      }
      const room = roomManager.startGame(roomId);
      if (room) {
        // GameEngine 생성 -- mode를 하드코딩하지 않는다.
        // constructor의 mode 파라미터는 초기값으로만 사용되며,
        // 실제 모드 선택은 select-mode 이벤트에서 selectMode() 메서드를 통해 이루어진다.
        // 첫 판(roundNumber=1)이면 dealer-select phase로 시작하고,
        // 모드 선택은 attend-school 이후 mode-select phase에서 처리된다.
        const engine = new GameEngine(roomId, room.players, 'original', 1);
        gameEngines.set(roomId, engine);
        io.to(roomId).emit('room-state', room);
        io.to(roomId).emit('game-state', engine.getState() as GameState);
      }
    });
    ```

    **중요**: constructor에 전달하는 `'original'`은 GameState.mode의 초기값일 뿐이다.
    GameEngine의 FSM에서 mode-select phase에 도달하면 선 플레이어가 `selectMode(playerId, mode)` 호출로 실제 모드를 선택한다. 이는 D-01과 MODE-OG-01 결정에 부합한다.
    만약 GameEngine constructor가 mode 없이도 동작하도록 설계되었다면 (예: mode가 optional이면),
    `new GameEngine(roomId, room.players)` 형태로 호출해도 무방하다.
    핵심: start-game 핸들러에서 mode를 최종 결정하지 않으며, select-mode 이벤트 플로우가 반드시 동작해야 한다.

    **Step 3: 게임 이벤트 핸들러 추가 (io.on('connection') 내부)**

    헬퍼 함수:
    ```typescript
    function getEngine(roomId: string): GameEngine {
      const engine = gameEngines.get(roomId);
      if (!engine) throw new Error('GAME_NOT_FOUND');
      return engine;
    }

    function handleGameAction(
      socket: TypedSocket,
      roomId: string,
      action: () => void
    ): void {
      try {
        action();
        const engine = getEngine(roomId);
        io.to(roomId).emit('game-state', engine.getState() as GameState);
      } catch (err: any) {
        socket.emit('game-error', {
          code: err.message || 'UNKNOWN_ERROR',
          message: ERROR_MESSAGES[err.message] || err.message || '알 수 없는 오류',
        });
      }
    }
    ```

    각 이벤트 핸들러:
    ```typescript
    socket.on('select-dealer-card', ({ roomId, cardIndex }) => {
      handleGameAction(socket, roomId, () => {
        getEngine(roomId).selectDealerCard(socket.data.playerId, cardIndex);
      });
    });

    socket.on('attend-school', ({ roomId }) => {
      handleGameAction(socket, roomId, () => {
        getEngine(roomId).attendSchool(socket.data.playerId);
      });
    });

    socket.on('select-mode', ({ roomId, mode }) => {
      handleGameAction(socket, roomId, () => {
        getEngine(roomId).selectMode(socket.data.playerId, mode);
      });
    });

    socket.on('shuffle', ({ roomId }) => {
      handleGameAction(socket, roomId, () => {
        getEngine(roomId).shuffle(socket.data.playerId);
      });
    });

    socket.on('cut', ({ roomId, cutPoints, order }) => {
      handleGameAction(socket, roomId, () => {
        getEngine(roomId).cut(socket.data.playerId, cutPoints, order);
      });
    });

    socket.on('declare-ttong', ({ roomId }) => {
      handleGameAction(socket, roomId, () => {
        getEngine(roomId).declareTtong(socket.data.playerId);
      });
    });

    socket.on('bet-action', ({ roomId, action }) => {
      handleGameAction(socket, roomId, () => {
        getEngine(roomId).processBetAction(socket.data.playerId, action);
      });
    });

    socket.on('reveal-card', ({ roomId }) => {
      handleGameAction(socket, roomId, () => {
        getEngine(roomId).revealCard(socket.data.playerId);
      });
    });

    socket.on('next-round', ({ roomId }) => {
      handleGameAction(socket, roomId, () => {
        getEngine(roomId).nextRound();
      });
    });
    ```

    **Step 4: protocol.ts에 'next-round' 이벤트 추가**

    Plan 01에서 protocol.ts를 확장할 때 `'next-round'` 이벤트가 빠져 있을 수 있다.
    `packages/shared/src/types/protocol.ts`의 ClientToServerEvents에 다음이 없으면 추가:
    ```typescript
    'next-round': (data: { roomId: string }) => void;
    ```

    **Step 5: ERROR_MESSAGES 확장**

    기존 ERROR_MESSAGES에 게임 에러 메시지 추가:
    ```typescript
    INVALID_PHASE: '현재 단계에서 수행할 수 없는 액션입니다.',
    NOT_YOUR_TURN: '아직 당신의 차례가 아닙니다.',
    INVALID_CUT: '잘못된 기리입니다. 카드를 다시 확인해주세요.',
    ALREADY_ATTENDED: '이미 등교했습니다.',
    INVALID_ACTION: '수행할 수 없는 액션입니다.',
    GAME_NOT_FOUND: '게임을 찾을 수 없습니다.',
    ```

    ErrorPayload의 code에 새 에러 코드 추가는 Plan 01에서 protocol.ts에 이미 처리됨.

    **Step 6: index.test.ts 통합 테스트**

    `packages/server/src/index.test.ts` 파일에 게임 이벤트 테스트 추가 (기존 테스트 파일이 있으면 추가, 없으면 생성):

    기존 Phase 03 테스트 패턴을 따름 (Socket.IO Client + 서버 연결):
    ```typescript
    import { io as ioc } from 'socket.io-client';
    import { describe, it, expect, beforeAll, afterAll } from 'vitest';
    ```

    테스트:
    1. 'start-game 후 game-state 이벤트 수신 확인'
       - 2명 방 생성 + 참여, start-game 이벤트 emit
       - game-state 이벤트 수신, phase가 'dealer-select'인지 확인

    2. 'attend-school 이벤트로 등교 처리'
       - game을 attend-school phase로 진행
       - attend-school emit 후 game-state 수신, pot 증가 확인

    3. 'select-mode 이벤트로 모드 선택 처리'
       - mode-select phase에서 select-mode emit
       - game-state 수신, mode가 선택한 값으로 변경되고 phase가 'shuffling'으로 전환 확인

    4. 'bet-action 이벤트로 베팅 처리'
       - game을 betting phase로 진행
       - bet-action emit 후 game-state 수신

    5. 'next-round 이벤트로 다음 판 시작'
       - game을 result phase로 진행
       - next-round emit 후 game-state 수신, roundNumber 증가 확인

    6. '잘못된 phase에서 액션 시 game-error 수신'
       - dealer-select phase에서 bet-action emit
       - game-error 이벤트 수신, code='INVALID_PHASE'

    **참고**: Phase 03-02 테스트(packages/server/src/index.test.ts)의 기존 패턴(Promise 기반 beforeAll/afterAll, NODE_ENV=test 조건부 listen)을 그대로 따른다.
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/server test</automated>
  </verify>
  <acceptance_criteria>
    - packages/server/src/index.ts에 `import { GameEngine }` 존재
    - packages/server/src/index.ts에 `gameEngines` Map 선언 존재
    - packages/server/src/index.ts에 `socket.on('attend-school'` 핸들러 존재
    - packages/server/src/index.ts에 `socket.on('select-mode'` 핸들러 존재
    - packages/server/src/index.ts에 `socket.on('shuffle'` 핸들러 존재
    - packages/server/src/index.ts에 `socket.on('bet-action'` 핸들러 존재
    - packages/server/src/index.ts에 `socket.on('reveal-card'` 핸들러 존재
    - packages/server/src/index.ts에 `socket.on('next-round'` 핸들러 존재
    - packages/server/src/index.ts에 `io.to(roomId).emit('game-state'` 패턴 존재
    - packages/server/src/index.ts에 `emit('game-error'` 패턴 존재
    - GameEngine 생성 시 mode가 start-game에서 최종 결정되지 않고 select-mode 플로우를 통해 결정됨
    - packages/server/src/index.test.ts에 'game-state' 관련 테스트 존재
    - packages/server/src/index.test.ts에 'select-mode' 테스트 존재
    - packages/server/src/index.test.ts에 'next-round' 테스트 존재
    - `pnpm --filter @sutda/server test` 명령이 exit code 0
    - `pnpm test` 명령이 exit code 0
  </acceptance_criteria>
  <done>
    Socket.IO 서버가 GameEngine의 모든 게임 이벤트(select-mode, next-round 포함)를 처리하고, 상태 변경을 game-state로 브로드캐스트하며, 에러를 game-error로 개별 소켓에 전송한다. 모드 선택이 start-game 핸들러에서 하드코딩되지 않고 select-mode 이벤트 플로우를 통해 동작한다. 통합 테스트가 통과한다.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @sutda/server test` -- 게임 이벤트 핸들러 + 통합 테스트 통과
- `pnpm test` -- 전체 모노레포 테스트 통과 (Phase 1~4 전체)
</verification>

<success_criteria>
- start-game 후 GameEngine이 생성되어 game-state가 브로드캐스트됨
- 모든 게임 이벤트(attend-school, select-mode, shuffle, cut, bet-action, reveal-card, next-round 등)가 GameEngine 메서드로 전달됨
- select-mode 이벤트를 통해 선 플레이어가 모드를 선택할 수 있음 (하드코딩 아님)
- next-round 이벤트를 통해 result 이후 다음 판을 시작할 수 있음
- 에러 발생 시 game-error 이벤트가 해당 소켓에만 전송됨
- 통합 테스트 통과
</success_criteria>

<output>
완료 후 `.planning/phases/04-original-mode-game-engine/04-03-SUMMARY.md` 생성
</output>
