---
phase: 09-94
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - packages/server/src/index.ts
  - packages/client/src/components/modals/GusaRejoinModal.tsx
  - packages/client/src/components/layout/ResultScreen.tsx
  - packages/client/src/pages/RoomPage.tsx
autonomous: false
requirements: [RULE-03, MODE-OG-03]

must_haves:
  truths:
    - "gusa-rejoin 소켓 이벤트가 서버에서 처리되어 per-player game-state가 브로드캐스트된다"
    - "다이한 플레이어에게 재참여 모달이 15초 카운트다운으로 표시된다"
    - "result 화면에 땡값 납부 내역이 별도 섹션으로 표시된다"
    - "RoomPage에서 gusa-pending phase가 정상 렌더링된다"
  artifacts:
    - path: "packages/server/src/index.ts"
      provides: "gusa-rejoin 소켓 핸들러"
      contains: "gusa-rejoin"
    - path: "packages/client/src/components/modals/GusaRejoinModal.tsx"
      provides: "구사 재참여 결정 모달 (15초 타임아웃)"
      min_lines: 30
    - path: "packages/client/src/components/layout/ResultScreen.tsx"
      provides: "땡값 납부 내역 UI 섹션"
      contains: "ttaengPayments"
    - path: "packages/client/src/pages/RoomPage.tsx"
      provides: "gusa-pending phase 렌더링 분기"
      contains: "gusa-pending"
  key_links:
    - from: "packages/client/src/components/modals/GusaRejoinModal.tsx"
      to: "packages/server/src/index.ts"
      via: "socket.emit('gusa-rejoin')"
      pattern: "gusa-rejoin"
    - from: "packages/client/src/pages/RoomPage.tsx"
      to: "packages/client/src/components/modals/GusaRejoinModal.tsx"
      via: "phase === 'gusa-pending' 조건 렌더링"
      pattern: "gusa-pending"
    - from: "packages/client/src/components/layout/ResultScreen.tsx"
      to: "packages/shared/src/types/game.ts"
      via: "gameState.ttaengPayments 읽기"
      pattern: "ttaengPayments"
---

<objective>
소켓 핸들러 + 클라이언트 UI를 구현하여 땡값 표시와 구사 재경기 모달을 완성한다.

Purpose: Plan 01의 서버 엔진 로직을 소켓 이벤트로 연결하고, 사용자가 재경기 결정 및 땡값 결과를 확인할 수 있는 UI 제공
Output: gusa-rejoin 핸들러, GusaRejoinModal, ResultScreen 땡값 섹션, RoomPage gusa-pending 분기
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-94/09-CONTEXT.md
@.planning/phases/09-94/09-RESEARCH.md
@.planning/phases/09-94/09-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 추가된 타입 (packages/shared/src/types/game.ts) -->
```typescript
export type GamePhase = ... | 'gusa-pending' | ...;

export interface GameState {
  ...
  gusaPendingDecisions?: Record<string, boolean | null>;
  ttaengPayments?: { playerId: string; amount: number }[];
}
```

<!-- Plan 01에서 추가된 프로토콜 (packages/shared/src/types/protocol.ts) -->
```typescript
export interface ClientToServerEvents {
  ...
  'gusa-rejoin': (data: { roomId: string; join: boolean }) => void;
}
```

<!-- Plan 01에서 추가된 메서드 (packages/server/src/game-engine.ts) -->
```typescript
class GameEngine {
  recordGusaRejoinDecision(playerId: string, join: boolean): void;
  // gusa-pending phase에서만 호출 가능
  // join=true: pot 절반 차감, isAlive 복원, pot 증가
  // join=false: 불참 처리
  // 모든 결정 수집 완료 시 자동으로 startGusaRematch() 호출 -> phase='shuffling'
}
```

<!-- 기존 소켓 핸들러 패턴 (packages/server/src/index.ts) -->
```typescript
// handleGameAction 헬퍼 패턴: 액션 실행 -> per-player game-state 브로드캐스트
function handleGameAction(socket, roomId, fn) { ... }

socket.on('start-rematch', ({ roomId }) => {
  handleGameAction(socket, roomId, () => {
    const engine = gameEngines.get(roomId);
    if (!engine) throw new Error('ROOM_NOT_FOUND');
    engine.startRematch();
  });
});
```

<!-- 기존 모달 패턴 (RechargeVoteModal.tsx) — 카운트다운 + emit -->
```tsx
const [countdown, setCountdown] = useState(15);
useEffect(() => {
  if (countdown <= 0) { socket?.emit(...); return; }
  const timer = setTimeout(() => setCountdown(c => c - 1), 1000);
  return () => clearTimeout(timer);
}, [countdown]);
```

<!-- 기존 RoomPage rematch-pending 분기 (line 362-378) -->
```tsx
if (phase === 'rematch-pending') {
  return (
    <div>
      <h2>동점!</h2>
      <Button onClick={() => socket?.emit('start-rematch', { roomId: roomId! })}>
        재경기 시작
      </Button>
    </div>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 소켓 핸들러 + GusaRejoinModal + ResultScreen 땡값 + RoomPage 통합</name>
  <files>
    packages/server/src/index.ts,
    packages/client/src/components/modals/GusaRejoinModal.tsx,
    packages/client/src/components/layout/ResultScreen.tsx,
    packages/client/src/pages/RoomPage.tsx
  </files>
  <read_first>
    packages/server/src/index.ts,
    packages/client/src/components/modals/GusaRejoinModal.tsx,
    packages/client/src/components/layout/ResultScreen.tsx,
    packages/client/src/pages/RoomPage.tsx,
    packages/client/src/components/modals/RechargeVoteModal.tsx,
    packages/client/src/store/gameStore.ts,
    packages/shared/src/types/game.ts,
    packages/shared/src/types/protocol.ts
  </read_first>
  <action>
    **A. 서버 소켓 핸들러 (`packages/server/src/index.ts`)**

    기존 `start-rematch` 핸들러 근처에 `gusa-rejoin` 핸들러 추가:
    ```typescript
    socket.on('gusa-rejoin', ({ roomId, join }) => {
      handleGameAction(socket, roomId, () => {
        const engine = gameEngines.get(roomId);
        if (!engine) throw new Error('ROOM_NOT_FOUND');
        engine.recordGusaRejoinDecision(socket.data.playerId, join);
      });
    });
    ```

    **B. GusaRejoinModal (`packages/client/src/components/modals/GusaRejoinModal.tsx`)**

    새 파일 생성. RechargeVoteModal 패턴 참고.

    Props:
    ```typescript
    interface GusaRejoinModalProps {
      roomId: string;
      potAmount: number;        // gameState.pot
      myChips: number;          // myPlayer.chips
    }
    ```

    컴포넌트 내부:
    - `const [countdown, setCountdown] = useState(15);`
    - `const [decided, setDecided] = useState(false);`
    - `const { socket } = useGameStore();`
    - 재참여 비용: `const rejoinCost = Math.floor(potAmount / 2);`
    - 잔액 부족 여부: `const canAfford = myChips >= rejoinCost;`
    - useEffect 카운트다운 (15초): countdown <= 0이면 자동 거절 emit
    - 승낙 버튼: `socket?.emit('gusa-rejoin', { roomId, join: true })`, setDecided(true)
    - 거절 버튼: `socket?.emit('gusa-rejoin', { roomId, join: false })`, setDecided(true)
    - canAfford=false이면 승낙 버튼 disabled + "잔액 부족" 표시

    UI 레이아웃 (Tailwind + shadcn Button):
    ```tsx
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50">
      <div className="bg-card rounded-lg p-6 flex flex-col items-center gap-4 max-w-sm mx-4">
        <h3 className="text-lg font-bold">구사 재경기</h3>
        <p className="text-sm text-center text-muted-foreground">
          판돈 {potAmount.toLocaleString()}원의 절반 = <span className="font-bold text-foreground">{rejoinCost.toLocaleString()}원</span>을 내고 재참여하시겠습니까?
        </p>
        {!canAfford && (
          <p className="text-sm text-red-500">잔액 부족으로 참여할 수 없습니다</p>
        )}
        <p className="text-xs text-muted-foreground">{countdown}초 후 자동 거절</p>
        {!decided ? (
          <div className="flex gap-3">
            <Button onClick={handleJoin} disabled={!canAfford}>참여</Button>
            <Button variant="ghost" onClick={handleDecline}>거절</Button>
          </div>
        ) : (
          <p className="text-sm text-muted-foreground">결정 완료</p>
        )}
      </div>
    </div>
    ```

    **C. ResultScreen 땡값 표시 (`packages/client/src/components/layout/ResultScreen.tsx`)**

    기존 칩 델타 표시 아래에 땡값 섹션 추가. `gameState.ttaengPayments` 존재 시에만 렌더:

    기존 `</div>` (allPlayers.map 닫는 div) 아래, 버튼 영역 위에:
    ```tsx
    {gameState.ttaengPayments && gameState.ttaengPayments.length > 0 && (
      <div className="flex flex-col items-center gap-1 mt-2">
        <p className="text-sm font-semibold">땡값</p>
        {gameState.ttaengPayments.map((payment) => {
          const payer = gameState.players.find(p => p.id === payment.playerId);
          return (
            <p key={payment.playerId} className="text-sm text-red-500">
              {payer?.nickname ?? '?'}: -{payment.amount.toLocaleString()}원
            </p>
          );
        })}
        {(() => {
          const totalTtaeng = gameState.ttaengPayments!.reduce((sum, p) => sum + p.amount, 0);
          const winnerName = gameState.players.find(p => p.id === gameState.winnerId)?.nickname ?? '?';
          return (
            <p className="text-sm text-green-500">
              {winnerName}: +{totalTtaeng.toLocaleString()}원 (땡값)
            </p>
          );
        })()}
      </div>
    )}
    ```

    **D. RoomPage gusa-pending 분기 (`packages/client/src/pages/RoomPage.tsx`)**

    기존 `rematch-pending` 분기 (line 362 근처) 위에 `gusa-pending` 분기 추가:

    ```tsx
    // 구사 재경기 대기
    if (phase === 'gusa-pending') {
      const myDecision = gameState?.gusaPendingDecisions?.[myPlayerId ?? ''];
      const amDied = myPlayer && !myPlayer.isAlive;
      const needsDecision = amDied && myDecision === null;

      return (
        <div className="flex min-h-screen flex-col items-center justify-center bg-background text-foreground gap-6 p-6">
          <h2 className="text-xl font-semibold">구사 재경기!</h2>
          <p className="text-muted-foreground">구사 패가 감지되어 재경기를 진행합니다</p>
          {needsDecision ? (
            <GusaRejoinModal
              roomId={roomId!}
              potAmount={gameState!.pot}
              myChips={myPlayer!.chips}
            />
          ) : amDied ? (
            <p className="text-sm text-muted-foreground">
              {myDecision === true ? '재참여 결정 완료 -- 대기 중...' : '불참 -- 대기 중...'}
            </p>
          ) : (
            <p className="text-sm text-muted-foreground">다이한 플레이어의 결정을 기다리는 중...</p>
          )}
        </div>
      );
    }
    ```

    GusaRejoinModal import 추가:
    ```typescript
    import { GusaRejoinModal } from '@/components/modals/GusaRejoinModal';
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/server test --run && pnpm --filter @sutda/client build</automated>
  </verify>
  <acceptance_criteria>
    - packages/server/src/index.ts에 `'gusa-rejoin'` 소켓 핸들러 존재
    - packages/server/src/index.ts에 `recordGusaRejoinDecision` 호출 존재
    - packages/client/src/components/modals/GusaRejoinModal.tsx 파일 존재 (30줄 이상)
    - GusaRejoinModal.tsx에 `useState(15)` (카운트다운) 존재
    - GusaRejoinModal.tsx에 `gusa-rejoin` emit 존재
    - packages/client/src/components/layout/ResultScreen.tsx에 `ttaengPayments` 문자열 존재
    - ResultScreen.tsx에 `땡값` 텍스트 존재
    - packages/client/src/pages/RoomPage.tsx에 `gusa-pending` 문자열 존재
    - RoomPage.tsx에 `GusaRejoinModal` import 존재
    - `pnpm --filter @sutda/server test --run` 종료 코드 0
    - `pnpm --filter @sutda/client build` 종료 코드 0
  </acceptance_criteria>
  <done>gusa-rejoin 소켓 핸들러가 동작하고, 다이한 플레이어에게 15초 재참여 모달이 표시되며, result 화면에 땡값 납부 내역이 표시됨</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 땡값 + 구사 재경기 플로우 수동 검증</name>
  <files>N/A</files>
  <action>
    수동 검증 checkpoint. Phase 9 전체 기능(땡값 자동 정산, 구사/멍텅구리구사 재경기 FSM, 재참여 모달, result 화면 땡값 표시)을 아래 절차대로 확인한다.

    1. `pnpm dev`로 서버+클라이언트 실행
    2. 브라우저 2~3개 탭에서 방 생성 + 참여
    3. 땡값 테스트 (오리지날 모드): 오리지날 모드 선택 후 게임 진행, 땡 보유 플레이어 다이 후 result 화면에서 "땡값" 섹션 표시 확인, 승자에게 +500원/+1000원 합산 확인
    4. 구사 재경기 테스트: 4+9 조합 + 알리 이하 최고패 상황에서 "구사 재경기!" 화면 표시 확인, 15초 카운트다운 모달 확인, 재참여/거절 후 재경기 정상 진행 확인
    5. 비오리지날 모드에서 땡값 미적용 확인
  </action>
  <verify>"approved" 또는 발견된 문제 설명을 사용자가 입력</verify>
  <done>사용자가 "approved"로 승인하면 Phase 9 완료</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @sutda/server test --run` -- 서버 테스트 통과
2. `pnpm --filter @sutda/client build` -- 클라이언트 빌드 성공
3. gusa-rejoin 소켓 이벤트가 per-player emit으로 브로드캐스트됨
4. GusaRejoinModal에서 15초 타임아웃 후 자동 거절 emit됨
5. ResultScreen에서 ttaengPayments 존재 시 별도 섹션 렌더링
</verification>

<success_criteria>
- gusa-rejoin 소켓 핸들러가 handleGameAction 패턴으로 동작
- 다이한 플레이어에게 15초 카운트다운 재참여 모달 표시
- result 화면에 "땡값" 섹션이 정상 표시 (오리지날 모드 + 승자가 땡으로 승리한 경우)
- gusa-pending phase에서 RoomPage가 올바른 UI 렌더링
- 클라이언트 빌드 + 서버 테스트 모두 통과
</success_criteria>

<output>
완료 후 `.planning/phases/09-94/09-02-SUMMARY.md` 생성
</output>
