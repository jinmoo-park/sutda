---
phase: 09-94
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/game.ts
  - packages/shared/src/types/protocol.ts
  - packages/server/src/game-engine.ts
  - packages/server/src/game-engine.test.ts
autonomous: true
requirements: [MODE-OG-03, RULE-01, RULE-02, RULE-03, RULE-04]

must_haves:
  truths:
    - "오리지날 모드에서 승자가 땡으로 이겼을 때 다이한 모든 플레이어가 승자에게 땡값(일땡~구땡 500원, 장땡/광땡 1000원)을 자동 납부한다"
    - "땡잡이/암행어사 승리 시 땡값이 면제된다"
    - "구사 보유자 생존 + 알리 이하 최고패 → gusa-pending phase로 전환된다"
    - "멍텅구리구사 보유자 생존 + 팔땡 이하 → gusa-pending phase로 전환된다"
    - "gusa-pending에서 다이 플레이어가 재참여 결정을 하면 pot 증가 + isAlive 복원된다"
    - "모든 결정 수집 완료 시 shuffling phase로 전환되며 기존 모드가 유지된다"
  artifacts:
    - path: "packages/shared/src/types/game.ts"
      provides: "GamePhase에 gusa-pending, GameState에 gusaPendingDecisions/ttaengPayments"
      contains: "gusa-pending"
    - path: "packages/shared/src/types/protocol.ts"
      provides: "gusa-rejoin 이벤트 타입"
      contains: "gusa-rejoin"
    - path: "packages/server/src/game-engine.ts"
      provides: "_settleTtaengValue, recordGusaRejoinDecision, startGusaRematch 메서드"
      exports: ["recordGusaRejoinDecision", "startGusaRematch"]
    - path: "packages/server/src/game-engine.test.ts"
      provides: "땡값 + 구사 재경기 단위 테스트"
      contains: "describe.*ttaeng.*value|describe.*gusa.*pending"
  key_links:
    - from: "packages/server/src/game-engine.ts"
      to: "packages/shared/src/types/game.ts"
      via: "GamePhase/GameState 타입 import"
      pattern: "gusa-pending"
    - from: "packages/server/src/game-engine.ts (_resolveShowdownOriginal)"
      to: "packages/server/src/game-engine.ts (_settleTtaengValue)"
      via: "settleChips() 직후 호출"
      pattern: "_settleTtaengValue"
---

<objective>
땡값 자동 정산 + 구사/멍텅구리구사 재경기 FSM을 GameEngine에 구현한다.

Purpose: Phase 9의 핵심 비즈니스 로직 — 땡값 계산/정산과 gusa-pending 상태 머신을 TDD로 구현
Output: 타입 확장 + GameEngine 메서드 + 단위 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-94/09-CONTEXT.md
@.planning/phases/09-94/09-RESEARCH.md

<interfaces>
<!-- packages/shared/src/types/hand.ts -->
```typescript
interface HandResult {
  handType: string;
  score: number;
  isSpecialBeater: boolean;  // 땡잡이(score=0) 또는 암행어사(score=1)
  isGusa: boolean;
  isMeongtteongguriGusa: boolean;
}
```

<!-- packages/shared/src/hand/gusa.ts -->
```typescript
export function checkGusaTrigger(
  gusaHand: HandResult,
  allSurvivingHands: HandResult[]
): { shouldRedeal: boolean }
// 일반 구사: maxScore <= 60 (알리)
// 멍텅구리구사: maxScore <= 1008 (팔땡)
```

<!-- packages/server/src/game-engine.ts (현재 구사 분기, line 1242-1252) -->
```typescript
for (const { player, hand } of hands) {
  if (hand.isGusa || hand.isMeongtteongguriGusa) {
    const { shouldRedeal } = checkGusaTrigger(hand, allHands);
    if (shouldRedeal) {
      this.state.phase = 'rematch-pending';  // → 'gusa-pending'으로 변경
      this.state.tiedPlayerIds = alivePlayers.map(p => p.id);
      this.state.rematchDealerId = player.id;
      return;
    }
  }
}
```

<!-- packages/server/src/game-engine.ts settleChips() line 193 -->
```typescript
private settleChips(): void {
  if (!this.state.winnerId) return;
  const winner = this.state.players.find(p => p.id === this.state.winnerId);
  if (winner) { winner.chips += this.state.pot; }
  this._updateChipBreakdowns();
}
```

<!-- packages/server/src/game-engine.ts startRematch() line 1395 -->
```typescript
startRematch(): void {
  // 동점 재경기 전용 — mode = 'original'로 강제
  this.state.mode = 'original';
  this.state.phase = 'shuffling';
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 공유 타입 확장 + 땡값 정산 로직 TDD</name>
  <files>
    packages/shared/src/types/game.ts,
    packages/shared/src/types/protocol.ts,
    packages/server/src/game-engine.ts,
    packages/server/src/game-engine.test.ts
  </files>
  <read_first>
    packages/shared/src/types/game.ts,
    packages/shared/src/types/protocol.ts,
    packages/shared/src/types/hand.ts,
    packages/server/src/game-engine.ts,
    packages/server/src/game-engine.test.ts
  </read_first>
  <behavior>
    - Test: 오리지날 모드, 승자가 일땡~구땡(score 1001~1009)으로 승리 + 다이한 플레이어 1명 → 다이 플레이어 -500, 승자 +500 (D-02)
    - Test: 오리지날 모드, 승자가 장땡(score 1010)으로 승리 + 다이한 플레이어 1명 → 다이 플레이어 -1000, 승자 +1000
    - Test: 오리지날 모드, 승자가 광땡(삼팔/일팔/일삼, score >=1011)으로 승리 + 다이한 플레이어 1명 → 다이 플레이어 -1000, 승자 +1000
    - Test: 오리지날 모드, 승자가 일땡으로 승리 + 다이한 플레이어 2명 → 각자 -500, 승자 +1000 합산 (다이 플레이어 패 무관)
    - Test: 승자가 이패(땡 미만, score < 1001) → 땡값 없음 (트리거 없음)
    - Test: 승자가 isSpecialBeater=true(땡잡이, score=0) → 땡값 없음 (RULE-04, D-03)
    - Test: 승자가 isSpecialBeater=true(암행어사, score=1) → 땡값 없음 (RULE-04, D-03)
    - Test: 다이하지 않은 생존 패배자는 땡값 대상 아님 (D-04)
    - Test: 세장섯다/한장공유/골라골라/인디언 모드 → 땡값 없음 (D-01)
    - Test: state.ttaengPayments에 다이한 플레이어 playerId + 납부금액 정확히 기록됨
    - Test: nextRound() 호출 시 ttaengPayments가 undefined로 초기화됨
  </behavior>
  <action>
    **Step 0: 공유 타입 확장**

    `packages/shared/src/types/game.ts`에서:
    1. GamePhase 타입에 `| 'gusa-pending'` 추가 ('rematch-pending' 다음에 배치)
    2. GameState 인터페이스에 다음 필드 추가:
       ```typescript
       gusaPendingDecisions?: Record<string, boolean | null>;  // playerId -> 재참여 결정 (null=미결정)
       ttaengPayments?: { playerId: string; amount: number }[];  // 땡값 납부 내역
       ```

    `packages/shared/src/types/protocol.ts`에서:
    1. ClientToServerEvents에 추가:
       ```typescript
       'gusa-rejoin': (data: { roomId: string; join: boolean }) => void;
       ```

    **Step 1: RED — 땡값 테스트 작성**

    `packages/server/src/game-engine.test.ts`에 `describe('땡값 정산 (_settleTtaengValue)')` 블록 추가.

    테스트 헬퍼: GameEngine을 생성하고 state를 직접 조작하여 result 직전 상태(winnerId 설정됨, 다이한 플레이어들의 cards/isAlive 설정)를 만든 뒤 `engine['_settleTtaengValue']()` 호출.

    **Step 2: GREEN — 땡값 로직 구현**

    `packages/server/src/game-engine.ts`에:

    1. 모듈 레벨 헬퍼 함수 `getTtaengValueAmount(hand: HandResult): number` 추가:
       - `hand.score >= 1010` → 1000 (장땡/광땡)
       - `hand.score >= 1001` → 500 (일땡~구땡)
       - 그 외 → 0

    2. private 메서드 `_settleTtaengValue(): void` 추가:
       - `this.state.mode !== 'original'` → return (D-01)
       - `!this.state.winnerId` → return
       - 승자 찾기, 승자의 evaluateHand 결과 계산
       - 승자의 `isSpecialBeater === true` (땡잡이/암행어사) → return (D-03, RULE-04)
       - `getTtaengValueAmount(winnerHand) === 0` (승자가 땡 미만으로 승리) → return (트리거 없음)
       - `const perPlayerAmount = getTtaengValueAmount(winnerHand)` — **승자의 땡 등급으로 금액 결정**
       - `this.state.players` 순회: `!p.isAlive` (다이한 플레이어)만 대상 (D-04)
       - 다이한 플레이어 각각: `player.chips -= perPlayerAmount`, ttaengPayments 배열에 push `{ playerId: p.id, amount: perPlayerAmount }`
       - `winner.chips += perPlayerAmount * diedPlayers.length`, `this.state.ttaengPayments = ttaengPayments`, `this._updateChipBreakdowns()` 호출

    3. `_resolveShowdownOriginal()` 수정: `this.settleChips()` 호출 직후 `this._settleTtaengValue()` 호출 추가 (line 1278 근처)

    4. `nextRound()` 수정: 리셋 블록에 `this.state.ttaengPayments = undefined;` 추가 (line 1464 근처, `this.state.winnerId = undefined;` 다음)

    **Step 3: REFACTOR** — 필요시 정리

    테스트 실행: `pnpm --filter @sutda/server test --run`
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/server test --run</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/types/game.ts에 `| 'gusa-pending'` 문자열이 존재
    - packages/shared/src/types/game.ts에 `gusaPendingDecisions` 문자열이 존재
    - packages/shared/src/types/game.ts에 `ttaengPayments` 문자열이 존재
    - packages/shared/src/types/protocol.ts에 `'gusa-rejoin'` 문자열이 존재
    - packages/server/src/game-engine.ts에 `_settleTtaengValue` 메서드 존재
    - packages/server/src/game-engine.ts에 `getTtaengValueAmount` 함수 존재
    - packages/server/src/game-engine.test.ts에 `땡값 정산` 문자열이 존재
    - `pnpm --filter @sutda/server test --run` 종료 코드 0
    - `pnpm --filter @sutda/shared test --run` 종료 코드 0
  </acceptance_criteria>
  <done>오리지날 모드 땡값 정산이 TDD로 구현되고, 공유 타입에 gusa-pending/ttaengPayments가 추가됨</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 구사 재경기 FSM (gusa-pending → startGusaRematch) TDD</name>
  <files>
    packages/server/src/game-engine.ts,
    packages/server/src/game-engine.test.ts
  </files>
  <read_first>
    packages/server/src/game-engine.ts,
    packages/server/src/game-engine.test.ts,
    packages/shared/src/types/game.ts,
    packages/shared/src/hand/gusa.ts,
    packages/shared/src/hand/gusa.test.ts
  </read_first>
  <behavior>
    - Test: 일반 구사 + 생존자 최고패 알리 이하 → phase='gusa-pending' (RULE-01)
    - Test: 멍텅구리구사 + 생존자 최고패 팔땡 이하 → phase='gusa-pending' (RULE-02)
    - Test: 구사 트리거 시 gusaPendingDecisions에 다이한 플레이어만 포함 (null=미결정)
    - Test: 다이 플레이어 0명 시 → 즉시 shuffling (gusa-pending 거치지 않고)
    - Test: recordGusaRejoinDecision(playerId, true) → gusaPendingDecisions[playerId] = true
    - Test: recordGusaRejoinDecision(playerId, true) → player.chips -= pot/2, pot += pot/2, player.isAlive = true
    - Test: recordGusaRejoinDecision(playerId, false) → gusaPendingDecisions[playerId] = false, isAlive 변경 없음
    - Test: 잔액 부족 플레이어가 join=true → 자동 거절 처리 (gusaPendingDecisions[id] = false)
    - Test: 모든 결정 수집 완료 → startGusaRematch() 자동 호출 → phase='shuffling'
    - Test: startGusaRematch()에서 기존 모드(mode) 유지됨 (original이 아닐 수 있음)
    - Test: startGusaRematch()에서 rematchDealerId(구사 보유자)가 dealer로 설정됨
    - Test: startGusaRematch()에서 재참여한 플레이어 isAlive=true, 거절 플레이어 isAlive=false
    - Test: gusaPendingDecisions가 startGusaRematch() 후 undefined로 초기화됨
    - Test: 땡잡이 승리 + 일반 구사 보유자 → gusa-pending 없음 (D-07: 땡잡이가 구땡 이하를 이기므로 조건 불충족)
    - Test: 땡잡이 승리 + 멍텅구리구사 → gusa-pending 트리거됨 (D-07)
    - Test: 암행어사 승리 + 구사/멍텅구리구사 → gusa-pending 없음 (D-07)
    - Test: _resolveShowdownSejang()에서도 gusa-pending으로 전환됨
    - Test: _resolveShowdownHanjang()에서도 gusa-pending으로 전환됨
  </behavior>
  <action>
    **Step 1: RED — 구사 재경기 테스트 작성**

    `packages/server/src/game-engine.test.ts`에 `describe('구사 재경기 (gusa-pending)')` 블록 추가.

    테스트 시나리오:
    - 3인 게임, player A가 구사(4+9 일반), player B 구삥(1+9), player C 다이. 생존자 최고패=구삥(score=40, 알리(60) 이하) → phase='gusa-pending', gusaPendingDecisions = { C: null }
    - 멍텅구리구사 시나리오: player A 멍텅구리구사(열끗4+열끗9), player B 오땡(score=1005, 팔땡(1008) 이하) → gusa-pending
    - recordGusaRejoinDecision 테스트: engine 상태를 gusa-pending으로 설정 후 호출

    `describe('D-07 우선순위')` 블록 추가: 땡잡이/암행어사 엣지케이스

    **Step 2: GREEN — 구사 재경기 로직 구현**

    `packages/server/src/game-engine.ts`에:

    1. `_resolveShowdownOriginal()` 수정 (line 1247):
       `this.state.phase = 'rematch-pending'` → `this.state.phase = 'gusa-pending'`

       그리고 gusaPendingDecisions 초기화 추가:
       ```typescript
       const diedPlayers = this.state.players.filter(p => !p.isAlive);
       if (diedPlayers.length === 0) {
         // 다이 플레이어 없으면 즉시 재경기
         this._startGusaRematchImmediate();
         return;
       }
       this.state.gusaPendingDecisions = Object.fromEntries(
         diedPlayers.map(p => [p.id, null])
       );
       ```

    2. 동일한 변경을 `_resolveShowdownSejang()` (line 1306)과 `_resolveShowdownHanjang()` (line 1357)에도 적용

    3. D-07 엣지케이스 처리: 구사 체크 루프 전에 승자를 먼저 결정하거나, 구사 체크 내부에서 `isSpecialBeater` 조건 분기 추가.
       구체적으로: 구사 shouldRedeal이 true여도, 최강 패가 isSpecialBeater인 경우:
       - 암행어사 → 재경기 없음 (모든 구사 무시)
       - 땡잡이 + isMeongtteongguriGusa → 재경기 트리거됨
       - 땡잡이 + isGusa(일반) → 재경기 없음

       **구현 방식:** 구사 체크를 승자 결정 후로 이동하거나, 구사 체크 시 최강패를 먼저 계산하여 isSpecialBeater 판별.

       **권장 구현:** 3개 showdown 메서드 모두에서:
       ```typescript
       // 먼저 최강 패 찾기 (기존 코드)
       let best = hands[0]; ...
       // 승자 결정 후 구사 체크
       const winnerHand = best.hand;
       for (const { player, hand } of hands) {
         if (hand.isGusa || hand.isMeongtteongguriGusa) {
           const { shouldRedeal } = checkGusaTrigger(hand, allHands);
           if (shouldRedeal) {
             // D-07: 특수패 승리 시 면제 체크
             if (winnerHand.isSpecialBeater) {
               if (winnerHand.score === 1) continue;  // 암행어사 → 모든 구사 무시
               if (winnerHand.score === 0 && !hand.isMeongtteongguriGusa) continue;  // 땡잡이 + 일반구사 → 무시
               // 땡잡이 + 멍텅구리구사 → 아래로 진행 (트리거)
             }
             this.state.phase = 'gusa-pending';
             ...
           }
         }
       }
       ```

       **중요:** 구사 체크를 승자 결정 코드 뒤로 이동시키되, 동점 재경기 분기보다는 앞에 두어야 한다. 하지만 D-07에 의하면 구사 재경기가 동점 재경기보다 우선이므로, 구사 체크 → 동점 체크 → 승자 확정 순서를 유지하되 최강패 계산만 먼저 수행.

       **실제 코드 흐름 수정 (3개 메서드 모두):**
       ```
       1. hands 생성 (기존)
       2. 최강패 + 동점 계산 (기존 코드 이동)
       3. 구사 체크 (D-07 조건 포함): shouldRedeal → gusa-pending
       4. 동점 체크: tiedPlayers.length > 1 → rematch-pending
       5. 승자 확정 → settleChips → _settleTtaengValue(original만) → result
       ```

    4. `recordGusaRejoinDecision(playerId: string, join: boolean): void` public 메서드 추가:
       - phase가 'gusa-pending'이 아니면 throw Error('INVALID_PHASE')
       - gusaPendingDecisions에 playerId가 없으면 throw Error('INVALID_ACTION')
       - join=true이고 player.chips < this.state.pot / 2 → 자동 거절 (join=false로 변경)
       - join=true: `player.chips -= Math.floor(this.state.pot / 2)`, `this.state.pot += Math.floor(this.state.pot / 2)`, `player.isAlive = true`
       - gusaPendingDecisions[playerId] = join
       - 모든 값이 null이 아니면 → `this._startGusaRematch()` 호출

    5. `private _startGusaRematch(): void` 메서드 추가:
       - gusaPendingDecisions 초기화: `this.state.gusaPendingDecisions = undefined`
       - 참여자(기존 생존자 + 재참여 승낙자)의 isAlive=true, 나머지 isAlive=false
       - 카드/베팅 상태 초기화 (startRematch 패턴 참고하되 mode는 유지)
       - 새 덱 생성: `this.state.deck = createDeck()`
       - 베팅 상태: `this.state.currentBetAmount = 0`, `this._bettingActed = new Set()`
       - dealer: `this.state.rematchDealerId`(구사 보유자)를 dealer로 설정
       - `this.state.rematchDealerId = undefined`
       - sharedCard, selectedCards 초기화
       - **mode 유지 (변경하지 않음)** — startRematch()와의 핵심 차이
       - `this.state.phase = 'shuffling'`

    6. `private _startGusaRematchImmediate(): void` — 다이 플레이어 0명 시 즉시 재경기:
       - gusaPendingDecisions 설정 없이 바로 `_startGusaRematch()` 호출

    **Step 3: REFACTOR** — 3개 showdown 메서드의 구사 분기 중복 코드를 private 헬퍼로 추출 가능

    테스트 실행: `pnpm --filter @sutda/server test --run`
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/server test --run && pnpm --filter @sutda/shared test --run</automated>
  </verify>
  <acceptance_criteria>
    - packages/server/src/game-engine.ts에 `recordGusaRejoinDecision` 메서드 존재
    - packages/server/src/game-engine.ts에 `_startGusaRematch` 메서드 존재
    - packages/server/src/game-engine.ts에 `_settleTtaengValue` 호출이 `_resolveShowdownOriginal` 내부에 존재
    - packages/server/src/game-engine.ts에서 `'rematch-pending'`이 구사 분기에 사용되지 않음 (구사 분기는 'gusa-pending'만 사용)
    - packages/server/src/game-engine.ts의 _resolveShowdownSejang에 `gusa-pending` 문자열 존재
    - packages/server/src/game-engine.ts의 _resolveShowdownHanjang에 `gusa-pending` 문자열 존재
    - packages/server/src/game-engine.test.ts에 `구사 재경기` 문자열 존재
    - packages/server/src/game-engine.test.ts에 `D-07` 문자열 존재
    - `pnpm --filter @sutda/server test --run` 종료 코드 0
    - `pnpm --filter @sutda/shared test --run` 종료 코드 0
  </acceptance_criteria>
  <done>구사 재경기 FSM이 gusa-pending phase로 구현되고, D-07 엣지케이스(땡잡이/암행어사)가 처리됨. 3개 showdown 메서드 모두 적용됨.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @sutda/server test --run` — 전체 서버 테스트 통과
2. `pnpm --filter @sutda/shared test --run` — 공유 패키지 테스트 통과
3. game-engine.ts에서 구사 분기가 'gusa-pending'만 사용하고 'rematch-pending'은 동점 전용으로 유지
4. ttaengPayments가 result phase에서 설정되고 nextRound()에서 초기화됨
</verification>

<success_criteria>
- 땡값 정산: 오리지날 모드에서 승자가 땡으로 이겼을 때 → 다이한 모든 플레이어가 승자에게 500/1000원(승자 땡 등급 기준) 자동 납부
- 땡값 면제: 땡잡이/암행어사 승리 시 면제 (RULE-04)
- 구사 재경기: gusa-pending phase에서 다이 플레이어 재참여 결정 수집 후 shuffling 전환
- D-07 우선순위: 땡잡이+일반구사=면제, 땡잡이+멍텅구리구사=트리거, 암행어사=면제
- 모든 기존 테스트 통과 (회귀 없음)
</success_criteria>

<output>
완료 후 `.planning/phases/09-94/09-01-SUMMARY.md` 생성
</output>
