---
phase: 09-94
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-03-31T13:00:00.000Z
---

<current_state>
Phase 09 Plan 02 Task 2 — 6차 UAT 수정 완료 (미커밋).
버그 수정 2건:
1. ResultScreen 세장섯다 족보/카드 표시: selectedCards 기준으로 변경 (기존: cards[0]/cards[1])
2. 골라골라 베팅패널 안나옴: gollagolla-select→betting 전환 시 cardConfirmed=true 설정 추가
빌드 성공. UAT 재검증 필요.
</current_state>

<completed_work>

### 이전 세션들 (2026-03-30~31)
- Plan 01 Task 1: 공유 타입 + 땡값 정산 TDD (commit 27ab857)
- Plan 01 Task 2: 구사 재경기 FSM TDD (commit 555bc87)
- Plan 02 Task 1: 소켓+GusaRejoinModal+ResultScreen+RoomPage (commit e7e2028)
- Hotfix: revealCard 단일 생존자 _settleTtaengValue 누락 (commit a4715c4)
- UAT 1차 버그 A~G (commit 97ff996): 세장섯다 FSM 순서/특수패 이름/재경기 규칙/gusa-announce
- UAT 1차 버그 H (commit 46863cf): totalBet 필드 도입 (세장섯다 잃은돈 합계 오류)
- 2차 UAT 버그 10건 (commit 9a1f228): evaluator 규칙/세장섯다 모달/동점재경기/비참여자/쉬기/UI

### 이번 세션 (2026-03-31) — 3차 UAT 버그 수정 (commit d926b94)
1. **B-1/B-2 (3번째 카드 표시/모달 미작동)**
   - RoomPage: prevPhaseRef 기반 betting-2→card-select 감지 제거
   - phase === 'card-select' 직접 조건으로 "3번째 카드!" 오버레이 표시
   - sejangThirdCardDismissed state 도입: 확인 후 SejangCardSelectModal 열림
   - HandPanel visibleCardCount: card-select phase에서 myPlayer.cards.length 직접 전달

2. **F-1 (다음판 쉬기 버튼 — 직전 판 선에 적용되던 버그)**
   - result phase에서 isDealer는 직전 판 딜러(현재 판 선)를 가리킴
   - 서버 takeBreak(): player.isDealer → player.id === this.state.winnerId
   - 클라이언트 ResultScreen: !myPlayer?.isDealer → myPlayerId !== gameState.winnerId

3. **신규: 세장섯다 오픈 카드 기능**
   - shared/types/game.ts: sejang-open GamePhase 추가, PlayerState.openedCardIndex?: 0|1 추가
   - shared/types/protocol.ts: open-sejang-card 소켓 이벤트 추가
   - game-engine.ts: _dealCardsSejang → sejang-open phase로 전환
   - game-engine.ts: openSejangCard() 메서드 추가 (전원 선택 시 betting-1 전환)
   - game-engine.ts: nextRound/TieRematch/GusaRematch에서 openedCardIndex 초기화
   - index.ts: open-sejang-card 소켓 핸들러 추가
   - SejangOpenCardModal.tsx 신규: 2장 중 1장 클릭 선택 → emit
   - PlayerSeat.tsx: openedCardIndex 있으면 해당 카드 face-up (전원 공개)
   - RoomPage.tsx: sejang-open 딜링 애니메이션 트리거, showCardConfirm 스킵,
     sejang-open→betting-1 시 cardConfirmed 자동 설정

</completed_work>

<remaining_work>

6차 UAT 검증:

**세장섯다 카드 선택 반영 검증**
- 세장섯다 card-select에서 3장 중 2장 선택
- 결과화면에서 선택한 2장이 표시되는지 확인 (기존: 항상 앞 두장 표시)
- 결과화면 족보 라벨이 선택한 카드 기준인지 확인

**골라골라 베팅패널 검증**
- 골라골라 카드 선택 완료 후 betting phase 진입 시 베팅패널이 나오는지 확인
- 베팅 정상 진행 → 결과화면 확인

**세장섯다 전체 흐름 확인 (이전 검증 유지)**
- B-1~B-4 전체 흐름: sejang-open → betting-1 → 3번째 카드 → betting-2 → card-select → showdown
- 결과화면: 선택한 2장 카드 및 족보 표시

</remaining_work>

<decisions_made>

### 이번 세션 결정
- B-1/B-2: prevPhaseRef 기반 betting-2→card-select 전환 감지 방식 폐기
  → phase === 'card-select' 직접 조건으로 단순화 (React ref 타이밍 의존성 제거)
- B-1/B-2 재수정 (5차): 3번째 카드 배분을 betting-2 완료 후 → betting-1 완료 후로 이동
  → betting-2 진입 시점에 "3번째 카드!" 오버레이 표시, 손패 3장 표시
  → card-select에서는 SejangCardSelectModal 바로 표시 (별도 확인 불필요)
- F-1: result phase의 isDealer는 current round dealer (직전 판 승자)임
  → winnerId(현재 판 승자 = 다음 선 예정자)로 비교해야 올바름
- 세장섯다 오픈 카드: sejang-open 신규 phase 도입
  cutting/shuffling → sejang-open → (전원 선택 완료) → betting-1
- openedCardIndex?: 0|1 PlayerState에 추가 (Card 객체가 아닌 인덱스로 저장)
- SejangOpenCardModal: alreadyOpened 시 "다른 플레이어를 기다리는 중..." 대기 화면 표시

</decisions_made>

<blockers>
없음
</blockers>

<context>
Phase 09 핵심 로직 완성 + 3차 UAT 버그 수정 + 세장섯다 오픈 카드 신규 기능 구현 완료.
빌드 성공 (shared/server/client 모두 통과).
4차 UAT에서 B-1/B-2/F-1 재검증 + 신규 오픈 카드 기능 N-1~N-5 검증 필요.
통과 시 09-02-SUMMARY.md 업데이트 + Phase 09 완료 처리.
</context>

<next_action>
pnpm dev → 5차 UAT (B-1/B-2 타이밍 재검증 + 전체 흐름 확인) → 통과 시 커밋 + Phase 09 완료.
</next_action>
