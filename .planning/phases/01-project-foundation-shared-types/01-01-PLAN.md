---
phase: 01-project-foundation-shared-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.base.json
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/server/package.json
  - packages/server/tsconfig.json
  - packages/server/src/index.ts
  - packages/client/package.json
  - packages/client/tsconfig.json
  - packages/client/src/main.tsx
  - packages/client/index.html
  - packages/client/vite.config.ts
  - .gitignore
autonomous: true
requirements: []
must_haves:
  truths:
    - "루트에서 pnpm install이 성공하고 모든 워크스페이스가 인식된다"
    - "pnpm run build가 shared -> server, shared -> client 순서로 빌드된다"
    - "server와 client에서 @sutda/shared를 import할 수 있다"
  artifacts:
    - path: "package.json"
      provides: "루트 패키지 정의"
      contains: "\"name\": \"sutda\""
    - path: "pnpm-workspace.yaml"
      provides: "워크스페이스 설정"
      contains: "packages/*"
    - path: "turbo.json"
      provides: "빌드 파이프라인"
      contains: "\"build\""
    - path: "tsconfig.base.json"
      provides: "공유 TypeScript 설정"
      contains: "\"strict\": true"
    - path: "packages/shared/package.json"
      provides: "shared 패키지 정의"
      contains: "\"name\": \"@sutda/shared\""
    - path: "packages/server/package.json"
      provides: "server 패키지 정의"
      contains: "\"@sutda/shared\": \"workspace:*\""
    - path: "packages/client/package.json"
      provides: "client 패키지 정의"
      contains: "\"@sutda/shared\": \"workspace:*\""
  key_links:
    - from: "packages/server/package.json"
      to: "packages/shared"
      via: "workspace: 프로토콜 의존성"
      pattern: "@sutda/shared.*workspace"
    - from: "packages/client/package.json"
      to: "packages/shared"
      via: "workspace: 프로토콜 의존성"
      pattern: "@sutda/shared.*workspace"
    - from: "turbo.json"
      to: "packages/shared/package.json"
      via: "빌드 파이프라인 의존성"
      pattern: "dependsOn.*\\^build"
---

<objective>
pnpm workspaces + turborepo 기반 모노레포 초기 구조를 세팅한다. shared/server/client 세 패키지가 올바르게 연결되어 빌드되고, server와 client에서 @sutda/shared를 import할 수 있는 상태를 만든다.

Purpose: 이후 모든 Phase의 코드가 이 모노레포 구조 위에서 작성된다. 빌드 인프라가 없으면 아무것도 시작할 수 없다.
Output: 빌드 가능한 모노레포 스캐폴드 (packages/shared, packages/server, packages/client)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation-shared-types/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 모노레포 루트 설정 (pnpm + turborepo + tsconfig)</name>
  <files>
    package.json,
    pnpm-workspace.yaml,
    turbo.json,
    tsconfig.base.json,
    .gitignore
  </files>
  <read_first>
    .planning/phases/01-project-foundation-shared-types/01-CONTEXT.md
  </read_first>
  <action>
    프로젝트 루트에 모노레포 인프라 파일들을 생성한다. (per D-01, D-02, D-03)

    1. **package.json** (루트):
    ```json
    {
      "name": "sutda",
      "private": true,
      "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "clean": "turbo run clean",
        "typecheck": "turbo run typecheck"
      },
      "devDependencies": {
        "turbo": "^2",
        "typescript": "^5.7"
      },
      "packageManager": "pnpm@9.15.4"
    }
    ```

    2. **pnpm-workspace.yaml**:
    ```yaml
    packages:
      - "packages/*"
    ```

    3. **turbo.json** (per D-03: shared -> server, shared -> client 의존성 파이프라인):
    ```json
    {
      "$schema": "https://turbo.build/schema.json",
      "tasks": {
        "build": {
          "dependsOn": ["^build"],
          "outputs": ["dist/**"]
        },
        "dev": {
          "cache": false,
          "persistent": true
        },
        "typecheck": {
          "dependsOn": ["^build"]
        },
        "clean": {
          "cache": false
        }
      }
    }
    ```

    4. **tsconfig.base.json** (TypeScript strict mode per Claude's Discretion):
    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "module": "ESNext",
        "moduleResolution": "bundler",
        "strict": true,
        "esModuleInterop": true,
        "skipLibCheck": true,
        "forceConsistentCasingInFileNames": true,
        "resolveJsonModule": true,
        "isolatedModules": true,
        "declaration": true,
        "declarationMap": true,
        "sourceMap": true
      }
    }
    ```

    5. **.gitignore** 에 다음을 추가:
    ```
    node_modules/
    dist/
    .turbo/
    *.tsbuildinfo
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && cat package.json | grep -q '"turbo"' && cat pnpm-workspace.yaml | grep -q 'packages/*' && cat turbo.json | grep -q 'dependsOn' && cat tsconfig.base.json | grep -q '"strict": true' && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - package.json에 "name": "sutda"와 "turbo": "^2" devDependency가 존재
    - pnpm-workspace.yaml에 "packages/*"가 존재
    - turbo.json에 "dependsOn": ["^build"]가 존재
    - tsconfig.base.json에 "strict": true가 존재
    - .gitignore에 node_modules/, dist/, .turbo/가 포함
  </acceptance_criteria>
  <done>모노레포 루트 인프라 파일 5개가 생성되고 올바른 설정값을 포함한다</done>
</task>

<task type="auto">
  <name>Task 2: shared/server/client 패키지 스캐폴드 생성 및 빌드 검증</name>
  <files>
    packages/shared/package.json,
    packages/shared/tsconfig.json,
    packages/shared/src/index.ts,
    packages/server/package.json,
    packages/server/tsconfig.json,
    packages/server/src/index.ts,
    packages/client/package.json,
    packages/client/tsconfig.json,
    packages/client/src/main.tsx,
    packages/client/src/App.tsx,
    packages/client/index.html,
    packages/client/vite.config.ts
  </files>
  <read_first>
    package.json,
    pnpm-workspace.yaml,
    turbo.json,
    tsconfig.base.json
  </read_first>
  <action>
    세 워크스페이스 패키지를 생성한다. (per D-04: Node.js server, per D-05: React + Vite client)

    1. **packages/shared/package.json**:
    ```json
    {
      "name": "@sutda/shared",
      "version": "0.0.0",
      "private": true,
      "type": "module",
      "main": "./dist/index.js",
      "types": "./dist/index.d.ts",
      "exports": {
        ".": {
          "import": "./dist/index.js",
          "types": "./dist/index.d.ts"
        }
      },
      "scripts": {
        "build": "tsc",
        "typecheck": "tsc --noEmit",
        "clean": "rm -rf dist"
      },
      "devDependencies": {
        "typescript": "^5.7"
      }
    }
    ```

    2. **packages/shared/tsconfig.json** (extends base):
    ```json
    {
      "extends": "../../tsconfig.base.json",
      "compilerOptions": {
        "outDir": "./dist",
        "rootDir": "./src"
      },
      "include": ["src"]
    }
    ```

    3. **packages/shared/src/index.ts**: 빈 export 파일
    ```typescript
    // @sutda/shared - 공유 타입 및 유틸리티
    // Phase 1 Plan 02에서 카드 타입과 덱 생성 함수를 추가
    export {};
    ```

    4. **packages/server/package.json**:
    ```json
    {
      "name": "@sutda/server",
      "version": "0.0.0",
      "private": true,
      "type": "module",
      "scripts": {
        "build": "tsc",
        "dev": "tsx watch src/index.ts",
        "typecheck": "tsc --noEmit",
        "clean": "rm -rf dist"
      },
      "dependencies": {
        "@sutda/shared": "workspace:*"
      },
      "devDependencies": {
        "typescript": "^5.7",
        "tsx": "^4"
      }
    }
    ```

    5. **packages/server/tsconfig.json**:
    ```json
    {
      "extends": "../../tsconfig.base.json",
      "compilerOptions": {
        "outDir": "./dist",
        "rootDir": "./src"
      },
      "include": ["src"]
    }
    ```

    6. **packages/server/src/index.ts**: @sutda/shared import 검증용 placeholder
    ```typescript
    // @sutda/server
    // shared 패키지 import 검증
    import {} from '@sutda/shared';
    console.log('Server ready');
    ```

    7. **packages/client/package.json** (per D-05: React + Vite, per D-06: SSR 불필요):
    ```json
    {
      "name": "@sutda/client",
      "version": "0.0.0",
      "private": true,
      "type": "module",
      "scripts": {
        "build": "vite build",
        "dev": "vite",
        "typecheck": "tsc --noEmit",
        "clean": "rm -rf dist"
      },
      "dependencies": {
        "@sutda/shared": "workspace:*",
        "react": "^19",
        "react-dom": "^19"
      },
      "devDependencies": {
        "@types/react": "^19",
        "@types/react-dom": "^19",
        "@vitejs/plugin-react": "^4",
        "typescript": "^5.7",
        "vite": "^6"
      }
    }
    ```

    8. **packages/client/tsconfig.json**:
    ```json
    {
      "extends": "../../tsconfig.base.json",
      "compilerOptions": {
        "jsx": "react-jsx",
        "outDir": "./dist",
        "rootDir": "./src",
        "noEmit": true
      },
      "include": ["src"]
    }
    ```

    9. **packages/client/vite.config.ts**:
    ```typescript
    import { defineConfig } from 'vite';
    import react from '@vitejs/plugin-react';

    export default defineConfig({
      plugins: [react()],
    });
    ```

    10. **packages/client/index.html**:
    ```html
    <!DOCTYPE html>
    <html lang="ko">
      <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>섯다</title>
      </head>
      <body>
        <div id="root"></div>
        <script type="module" src="/src/main.tsx"></script>
      </body>
    </html>
    ```

    11. **packages/client/src/main.tsx**:
    ```tsx
    import React from 'react';
    import ReactDOM from 'react-dom/client';
    import App from './App';

    ReactDOM.createRoot(document.getElementById('root')!).render(
      <React.StrictMode>
        <App />
      </React.StrictMode>
    );
    ```

    12. **packages/client/src/App.tsx**: @sutda/shared import 검증용 placeholder
    ```tsx
    import {} from '@sutda/shared';

    export default function App() {
      return <div>섯다</div>;
    }
    ```

    파일 생성 후 다음을 실행:
    ```bash
    cd {project_root}
    pnpm install
    pnpm run build
    ```

    빌드가 성공하면 server와 client 모두에서 @sutda/shared를 import 가능한 상태가 된다.
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm install && pnpm run build && echo "BUILD PASS"</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/package.json에 "name": "@sutda/shared"가 존재
    - packages/server/package.json에 "@sutda/shared": "workspace:*"가 존재
    - packages/client/package.json에 "@sutda/shared": "workspace:*"가 존재
    - packages/client/package.json에 "react": "^19"가 존재
    - packages/client/vite.config.ts에 "plugin-react"가 존재
    - pnpm install 이 에러 없이 완료
    - pnpm run build 가 exit code 0으로 완료
    - packages/shared/dist/index.js 파일이 생성됨
  </acceptance_criteria>
  <done>세 패키지가 모두 빌드 성공하고, server/client에서 @sutda/shared import가 타입 체크를 통과한다</done>
</task>

</tasks>

<verification>
- `pnpm install` 성공 (exit code 0)
- `pnpm run build` 성공 (exit code 0, shared -> server/client 순서)
- `packages/shared/dist/index.js` 존재
- server/client의 tsconfig에서 @sutda/shared import가 에러 없음
</verification>

<success_criteria>
1. 루트에서 `pnpm install && pnpm run build`가 에러 없이 완료된다
2. packages/shared, packages/server, packages/client 세 워크스페이스가 pnpm에 인식된다
3. server와 client에서 `import {} from '@sutda/shared'`가 타입 에러 없이 동작한다
</success_criteria>

<output>
완료 후 `.planning/phases/01-project-foundation-shared-types/01-01-SUMMARY.md` 생성
</output>
