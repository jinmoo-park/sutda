---
phase: 11-social-features
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - packages/server/src/game-engine.ts
  - packages/server/src/game-engine.test.ts
autonomous: true
requirements: [ALLIN-POT, SCHOOL-PROXY, HIST-01, HIST-02]

must_haves:
  truths:
    - "올인 플레이어가 승리하면 자신의 totalCommitted만큼만 각 플레이어에게서 수령하고 초과분은 2등에게 반환된다"
    - "올인 플레이어는 베팅 순환에서 자동 스킵된다"
    - "학교 대신 가주기 후 수혜자의 앤티 비용이 후원자 칩에서 차감된다"
    - "각 판 showdown 완료 시 RoundHistoryEntry가 생성된다"
    - "nextRound()에서 isAllIn/totalCommitted/schoolProxyBeneficiaryIds가 리셋된다"
  artifacts:
    - path: "packages/server/src/game-engine.ts"
      provides: "올인 정산, 학교 대납, 이력 생성 로직"
      contains: "settleChipsWithAllIn"
    - path: "packages/server/src/game-engine.test.ts"
      provides: "올인 정산/학교 대납/이력 생성 테스트"
      contains: "allIn"
  key_links:
    - from: "game-engine.ts settleChipsWithAllIn()"
      to: "game-engine.ts settleChips()"
      via: "올인 여부에 따라 분기"
      pattern: "settleChipsWithAllIn|settleChips"
    - from: "game-engine.ts attendSchoolProxy()"
      to: "game-engine.ts attendSchool()"
      via: "후원자 칩 차감 + 수혜자 무료 참여"
      pattern: "attendSchoolProxy"
---

<objective>
GameEngine 내부 로직을 확장하여 올인 POT 정산, 학교 대신 가주기 앤티 대납, 판별 이력 생성 기능을 추가한다.

Purpose: 서버 게임 엔진의 핵심 로직 변경은 소켓 핸들러나 클라이언트 UI와 독립적이므로 별도 플랜으로 분리. TDD로 검증.
Output: 확장된 game-engine.ts + 테스트 케이스
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-social-features/11-CONTEXT.md
@.planning/phases/11-social-features/11-RESEARCH.md
@.planning/phases/11-social-features/11-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 추가된 타입 (game.ts) -->
```typescript
// PlayerState 확장 필드:
isAllIn?: boolean;
totalCommitted?: number;

// GameState 확장 필드:
schoolProxyBeneficiaryIds?: string[];

// 신규 인터페이스:
export interface RoundHistoryEntry {
  roundNumber: number;
  winnerId: string;
  winnerNickname: string;
  winnerHandLabel: string;
  pot: number;
  hasTtaengPayment: boolean;
  playerChipChanges: { playerId: string; nickname: string; chipDelta: number }[];
}
```

<!-- 기존 game-engine.ts 핵심 메서드 시그니처 -->
```typescript
private settleChips(): void;           // L207 — 승자에게 pot 전액 합산
private _settleTtaengValue(): void;    // L223 — 땡값 정산
attendSchool(playerId: string): void;  // L387 — 앤티 500원 차감
nextRound(): void;                     // L1720 — 플레이어 상태 리셋
private _updateChipBreakdowns(): void; // 칩 단위 재계산
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 올인 POT — isAllIn/totalCommitted 추적 + settleChipsWithAllIn 정산 + 베팅 스킵</name>
  <files>packages/server/src/game-engine.ts, packages/server/src/game-engine.test.ts</files>
  <read_first>
    - packages/server/src/game-engine.ts
    - packages/server/src/game-engine.test.ts
    - packages/shared/src/types/game.ts
  </read_first>
  <behavior>
    - Test 1: 콜 금액 >= 플레이어 잔액일 때 betAction('call') 후 player.isAllIn === true, player.chips === 0
    - Test 2: 올인 플레이어는 베팅 순환에서 자동 스킵 (nextBettingPlayer가 건너뜀)
    - Test 3: 단일 올인 — totalCommitted=1만인 올인 승자가 4인 게임(A=1만, B=3만, C=5만, D=5만)에서 레벨별로 올바르게 팟 수령 (레벨1=4만 수령, 레벨2는 B/C/D 중 2등 수령, 레벨3은 C/D 중 수령)
    - Test 4: 계단식 다중 올인 — B=1만, C=3만, A=5만, D=5만 시나리오에서 C가 최강 족보일 때: C는 레벨1(4만)+레벨2(6만)=10만 수령, A 또는 D 중 강한 쪽이 레벨3(4만) 수령
    - Test 5: 비올인 승리자가 있을 때 기존 settleChips() 동작 그대로 (hasAllIn=false → settleChips 호출)
    - Test 6: attendSchool()에서 totalCommitted += 500 추적
    - Test 7: betAction()에서 totalCommitted += actualAmount 추적
    - Test 8: nextRound()에서 isAllIn=false, totalCommitted=0 리셋
  </behavior>
  <action>
**game-engine.ts 수정 사항:**

1. **attendSchool()** (L387 근처) — 앤티 차감 후 totalCommitted 추적 추가:
```typescript
p.totalCommitted = (p.totalCommitted ?? 0) + 500;
```

2. **betAction()** 내 call/raise 처리 부분 — totalCommitted 추적 + 올인 판별:
```typescript
// call 처리 부분:
const callAmount = Math.min(player.chips, this.state.currentBetAmount - player.currentBet);
player.chips -= callAmount;
player.currentBet += callAmount;
player.totalBet += callAmount;
player.totalCommitted = (player.totalCommitted ?? 0) + callAmount;
if (player.chips === 0) {
  player.isAllIn = true;
}

// raise 처리 부분도 동일하게 totalCommitted 추적 + 올인 판별
```

3. **베팅 순환에서 올인 플레이어 스킵** — `_getNextActiveBettingIndex()` 또는 nextBettingPlayer 로직에서:
isAllIn이 true인 플레이어를 `!p.isAlive`와 동일하게 스킵. _bettingActed Set에 올인 플레이어를 자동 추가하여 순환 종료 조건에 반영.

4. **settleChipsWithAllIn()** private 메서드 신규 추가 (레벨별 순차 처리 — 계단식 다중 올인 완전 지원):

**알고리즘:** 올인 플레이어들의 totalCommitted 기준으로 팟을 레벨별로 분할하여 각 레벨의 팟을 그 레벨에 기여한 생존자 중 족보 1위가 가져간다. `winnerId` 불필요 — 족보 비교만으로 모든 레벨을 처리한다.

**예시 (계단식 올인):**
- B: 1만 올인, C: 3만 올인, A: 5만, D: 5만 → 총 팟 14만
- 레벨1 (0→1만): 1만×4 = 4만 → B/C/A/D 중 best hand
- 레벨2 (1만→3만, 2만 증분): 2만×3(C/A/D) = 6만 → C/A/D 중 best hand
- 레벨3 (3만→5만, 2만 증분): 2만×2(A/D) = 4만 → A/D 중 best hand
- C가 전체 1등이어도 레벨3은 A/D만 경쟁 → 올바른 사이드팟 처리

```typescript
private settleChipsWithAllIn(): void {
  const hasAllIn = this.state.players.some(p => p.isAllIn);
  if (!hasAllIn) {
    this.settleChips();
    return;
  }

  // 각 플레이어의 족보를 미리 계산 (isAlive = 폴드하지 않은 플레이어)
  const handCache = new Map<string, ReturnType<typeof evaluateHand>>();
  for (const p of this.state.players) {
    if (p.isAlive && p.cards.length === 2) {
      handCache.set(p.id, evaluateHand(p.cards[0]!, p.cards[1]!));
    }
  }

  // totalCommitted 기준으로 고유 레벨 수집 후 오름차순 정렬
  const levels = [...new Set(
    this.state.players.map(p => p.totalCommitted ?? p.totalBet)
  )].filter(v => v > 0).sort((a, b) => a - b);

  let prevLevel = 0;
  for (const level of levels) {
    const increment = level - prevLevel;
    if (increment <= 0) { prevLevel = level; continue; }

    // 이 레벨에 기여한 플레이어 수 (폴드 포함 — 기여한 칩은 팟에 있음)
    const contributorsCount = this.state.players.filter(
      p => (p.totalCommitted ?? p.totalBet) >= level
    ).length;
    const potAtLevel = increment * contributorsCount;

    // 이 레벨을 이길 수 있는 생존자: isAlive(폴드 안함) + 해당 레벨에 기여함
    const candidates = this.state.players.filter(
      p => p.isAlive && (p.totalCommitted ?? p.totalBet) >= level && handCache.has(p.id)
    );

    if (candidates.length > 0) {
      const potWinner = candidates.reduce((best, p) => {
        const result = compareHands(handCache.get(best.id)!, handCache.get(p.id)!);
        // 'b'이면 p가 더 강한 족보
        return result === 'b' ? p : best;
      });
      potWinner.chips += potAtLevel;
    }
    // candidates가 0이면(모두 폴드, 실제로는 발생 불가) 팟 무효화

    prevLevel = level;
  }

  this._updateChipBreakdowns();
}
```

5. **기존 settleChips() 호출 부분 전부를 settleChipsWithAllIn()으로 교체** — _resolveShowdownOriginal, _resolveShowdownThreeCard, _resolveShowdownSharedCard, _resolveShowdownGolla, _resolveShowdownIndian 총 5곳

6. **nextRound()** (L1744 플레이어 리셋 루프) — 추가:
```typescript
p.isAllIn = false;
p.totalCommitted = 0;
```

7. **nextRound()** — schoolProxyBeneficiaryIds 리셋:
```typescript
this.state.schoolProxyBeneficiaryIds = undefined;
```
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/server test -- --grep "allIn|all-in|올인|totalCommitted"</automated>
  </verify>
  <acceptance_criteria>
    - game-engine.ts에 `settleChipsWithAllIn` 메서드가 존재
    - game-engine.ts에 `player.isAllIn = true` 할당이 존재
    - game-engine.ts에 `p.totalCommitted = (p.totalCommitted` 패턴이 존재
    - game-engine.ts의 nextRound()에 `p.isAllIn = false` 리셋이 존재
    - game-engine.ts의 nextRound()에 `p.totalCommitted = 0` 리셋이 존재
    - game-engine.ts의 nextRound()에 `schoolProxyBeneficiaryIds` 리셋이 존재
    - game-engine.test.ts에 올인 관련 테스트가 3개 이상 존재
    - `pnpm --filter @sutda/server test` 전체 통과
  </acceptance_criteria>
  <done>올인 시 자동 감지, 베팅 스킵, 비례 정산이 작동하고 테스트가 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 학교 대신 가주기 attendSchoolProxy + 판별 이력 RoundHistoryEntry 생성</name>
  <files>packages/server/src/game-engine.ts, packages/server/src/game-engine.test.ts</files>
  <read_first>
    - packages/server/src/game-engine.ts (Task 1 적용 후 상태)
    - packages/server/src/game-engine.test.ts
  </read_first>
  <behavior>
    - Test 1: attendSchoolProxy(beneficiaryId, sponsorId) — 수혜자 chips 미차감, 후원자 chips -= 500, attendedPlayerIds에 수혜자 추가
    - Test 2: attendSchoolProxy — 후원자 잔액 < 500일 때 fallback으로 수혜자가 직접 납부
    - Test 3: showdown 완료 후 lastRoundHistory 필드에 RoundHistoryEntry가 생성됨 (roundNumber, winnerId, pot, hasTtaengPayment 정확)
    - Test 4: RoundHistoryEntry.playerChipChanges에 각 플레이어의 chipDelta가 정확히 계산됨
  </behavior>
  <action>
**game-engine.ts — attendSchoolProxy() public 메서드 추가:**
```typescript
attendSchoolProxy(beneficiaryId: string, sponsorId: string): void {
  if (this.state.phase !== 'attend-school') throw new Error('INVALID_PHASE');
  const beneficiary = this.state.players.find(p => p.id === beneficiaryId);
  const sponsor = this.state.players.find(p => p.id === sponsorId);
  if (!beneficiary || !sponsor) throw new Error('INVALID_ACTION');

  // 후원자 잔액 부족 시 fallback — 수혜자가 직접 납부 (Pitfall 5 예방)
  if (sponsor.chips < 500) {
    this.attendSchool(beneficiaryId);
    return;
  }

  // 후원자 칩 차감
  sponsor.chips -= 500;
  sponsor.totalCommitted = (sponsor.totalCommitted ?? 0) + 500;

  // 수혜자는 무료 참여 (chips 차감 없음)
  this.state.pot += 500;
  this.state.attendedPlayerIds.push(beneficiaryId);
  this._updateChipBreakdowns();

  // 전원 등교 완료 체크 (기존 attendSchool 로직 재활용)
  this.completeAttendSchool();
}
```

참고: completeAttendSchool() 메서드가 이미 public으로 노출되어 있음 (Phase 04-01 결정). attendSchoolProxy에서 등교 완료 체크를 위해 동일하게 호출.

**game-engine.ts — 이력 생성을 위한 lastRoundHistory 필드 추가:**
GameEngine 클래스에 public 필드:
```typescript
public lastRoundHistory: RoundHistoryEntry | null = null;
```

**game-engine.ts — _generateRoundHistory() private 메서드 추가:**
```typescript
private _generateRoundHistory(chipsBeforeSettle: Map<string, number>): void {
  if (!this.state.winnerId) return;
  const winner = this.state.players.find(p => p.id === this.state.winnerId);
  if (!winner) return;

  const winnerHand = winner.cards[0] && winner.cards[1]
    ? evaluateHand(winner.cards[0], winner.cards[1])
    : null;

  this.lastRoundHistory = {
    roundNumber: this.state.roundNumber,
    winnerId: this.state.winnerId,
    winnerNickname: winner.nickname,
    winnerHandLabel: winnerHand ? winnerHand.handType : 'unknown',
    pot: this.state.pot,
    hasTtaengPayment: !!this.state.ttaengPayments && this.state.ttaengPayments.length > 0,
    playerChipChanges: this.state.players.map(p => ({
      playerId: p.id,
      nickname: p.nickname,
      chipDelta: p.chips - (chipsBeforeSettle.get(p.id) ?? p.chips),
    })),
  };
}
```

**기존 5개 showdown 메서드에 이력 생성 호출 추가:**
각 _resolveShowdown* 메서드에서 `settleChipsWithAllIn()` 호출 직전에 chips 스냅샷을 기록하고, 정산 후 `_generateRoundHistory(snapshot)` 호출:

```typescript
// 예시: _resolveShowdownOriginal() 내부
// ... 승자 결정 후, 정산 전:
const chipsSnapshot = new Map(this.state.players.map(p => [p.id, p.chips]));
this.settleChipsWithAllIn();
this._settleTtaengValue();
this._generateRoundHistory(chipsSnapshot);
this.state.phase = 'result';
```

5개 showdown 메서드 모두 동일 패턴 적용:
- _resolveShowdownOriginal
- _resolveShowdownThreeCard
- _resolveShowdownSharedCard
- _resolveShowdownGolla
- _resolveShowdownIndian
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/server test</automated>
  </verify>
  <acceptance_criteria>
    - game-engine.ts에 `attendSchoolProxy` 메서드가 존재
    - game-engine.ts에 `sponsor.chips < 500` fallback 조건이 존재
    - game-engine.ts에 `lastRoundHistory` public 필드가 존재
    - game-engine.ts에 `_generateRoundHistory` 메서드가 존재
    - game-engine.ts에 `chipsSnapshot` (또는 chipsBeforeSettle) 패턴이 5개 showdown 메서드에 존재
    - game-engine.test.ts에 `attendSchoolProxy` 관련 테스트가 2개 이상 존재
    - game-engine.test.ts에 `roundHistory` 또는 `lastRoundHistory` 관련 테스트가 1개 이상 존재
    - `pnpm --filter @sutda/server test` 전체 통과
  </acceptance_criteria>
  <done>학교 대납 시 후원자 칩에서 앤티 차감되고, 각 판 종료 시 이력 항목이 자동 생성되며 모든 테스트가 통과한다</done>
</task>

</tasks>

<verification>
- `pnpm --filter @sutda/server test` 전체 통과
- settleChipsWithAllIn이 올인자 유무에 따라 분기 동작
- attendSchoolProxy가 후원자 칩 차감 + 수혜자 무료 참여 처리
- lastRoundHistory가 각 showdown에서 생성됨
- nextRound()에서 isAllIn/totalCommitted/schoolProxyBeneficiaryIds 리셋 확인
</verification>

<success_criteria>
1. 올인 정산이 totalCommitted 비례로 정확히 작동한다 (테스트 통과)
2. 올인 플레이어가 베팅 순환에서 스킵된다 (테스트 통과)
3. 학교 대납이 후원자-수혜자 칩을 올바르게 처리한다 (테스트 통과)
4. 판별 이력이 자동 생성된다 (테스트 통과)
5. 기존 테스트가 깨지지 않는다 (리그레션 없음)
</success_criteria>

<output>
완료 후 `.planning/phases/11-social-features/11-02-SUMMARY.md` 생성
</output>
