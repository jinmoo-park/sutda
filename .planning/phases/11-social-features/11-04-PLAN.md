---
phase: 11-social-features
plan: 04
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - packages/client/src/components/layout/InfoPanel.tsx
  - packages/client/src/components/layout/ResultScreen.tsx
  - packages/client/src/components/layout/BettingPanel.tsx
  - packages/client/src/components/layout/GameTable.tsx
  - packages/client/src/components/game/PlayerSeat.tsx
  - packages/client/src/components/modals/HistoryModal.tsx
  - packages/client/src/store/gameStore.ts
  - packages/client/src/pages/RoomPage.tsx
autonomous: false
requirements: [HIST-01, HIST-02, SCHOOL-PROXY, LATE-JOIN, SESSION-END, ALLIN-POT, UX-02]

must_haves:
  truths:
    - "InfoPanel에 시계 아이콘 이력 버튼이 표시되고 클릭 시 HistoryModal이 열린다"
    - "HistoryModal에 판별 요약이 역순으로 표시된다"
    - "결과 화면에서 승자에게 학교 대신 가주기 UI가 노출되고 체크박스 선택 후 확인하면 서버에 전송된다"
    - "Observer 시트에 관람 중 배지와 다음 판 자동 합류 인디케이터가 표시된다"
    - "플레이어 연결 끊김 시 토스트가 표시되고, 2인 게임 1인 퇴장 시 대기실로 전환된다"
    - "올인 플레이어의 PlayerSeat에 올인 배지가 표시되고 BettingPanel이 비활성화된다"
  artifacts:
    - path: "packages/client/src/components/modals/HistoryModal.tsx"
      provides: "게임 이력 모달"
      min_lines: 40
    - path: "packages/client/src/components/layout/ResultScreen.tsx"
      provides: "학교 대신 가주기 UI 섹션"
      contains: "proxy-ante"
    - path: "packages/client/src/components/game/PlayerSeat.tsx"
      provides: "Observer 배지 + 올인 배지"
      contains: "관람 중"
  key_links:
    - from: "InfoPanel.tsx"
      to: "HistoryModal.tsx"
      via: "Dialog open state"
      pattern: "HistoryModal|historyOpen"
    - from: "ResultScreen.tsx"
      to: "socket proxy-ante emit"
      via: "학교 대신 가주기 확인 버튼"
      pattern: "proxy-ante"
    - from: "RoomPage.tsx"
      to: "sonner toast"
      via: "player-left 이벤트 수신 시 토스트"
      pattern: "player-left|연결이 끊어졌습니다"
---

<objective>
Phase 11의 나머지 클라이언트 UI를 모두 완성한다 — 게임 이력 모달, 학교 대신 가주기 UI, Observer 배지, 세션 종료 토스트, 올인 배지/패널 비활성화, gameStore 이력 상태 연동.

Purpose: 서버 로직(Plan 02/03)이 완성된 후 클라이언트 UI를 한번에 통합. 모든 소셜/기능 UI를 완성하여 Phase 11을 마무리.
Output: HistoryModal 신규 + 기존 컴포넌트 6개 확장 + RoomPage 이벤트 연동
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-social-features/11-CONTEXT.md
@.planning/phases/11-social-features/11-UI-SPEC.md
@.planning/phases/11-social-features/11-01-SUMMARY.md
@.planning/phases/11-social-features/11-02-SUMMARY.md
@.planning/phases/11-social-features/11-03-SUMMARY.md

<interfaces>
<!-- Plan 01에서 정의한 타입 -->
```typescript
export interface RoundHistoryEntry {
  roundNumber: number;
  winnerId: string;
  winnerNickname: string;
  winnerHandLabel: string;
  pot: number;
  hasTtaengPayment: boolean;
  playerChipChanges: { playerId: string; nickname: string; chipDelta: number }[];
}

// PlayerState 확장:
isAllIn?: boolean;
totalCommitted?: number;

// GameState 확장:
schoolProxyBeneficiaryIds?: string[];

// RoomPlayer 확장:
isObserver?: boolean;
observerChips?: number;

// 신규 이벤트:
'game-history': (data: { entries: RoundHistoryEntry[] }) => void;
'proxy-ante-applied': (data: { sponsorNickname: string; beneficiaryNickname: string }) => void;
'player-left': (data: { playerId: string; newHostId?: string; nickname?: string }) => void;
```

<!-- 기존 클라이언트 컴포넌트 props -->
```typescript
// InfoPanel.tsx: gameState prop 사용
// ResultScreen.tsx: gameState, myPlayerId, socket prop 사용
// BettingPanel.tsx: isMyTurn, currentBetAmount, myChips, onAction prop 사용
// PlayerSeat.tsx: player: PlayerState, isMe: boolean prop 사용
// GameTable.tsx: gameState, myPlayerId prop 사용
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HistoryModal + InfoPanel 이력 버튼 + ResultScreen 학교 대신 가주기 + gameStore 이력/이벤트 상태</name>
  <files>packages/client/src/components/modals/HistoryModal.tsx, packages/client/src/components/layout/InfoPanel.tsx, packages/client/src/components/layout/ResultScreen.tsx, packages/client/src/store/gameStore.ts</files>
  <read_first>
    - packages/client/src/components/layout/InfoPanel.tsx
    - packages/client/src/components/layout/ResultScreen.tsx
    - packages/client/src/store/gameStore.ts
    - packages/client/src/components/ui/dialog.tsx
    - .planning/phases/11-social-features/11-UI-SPEC.md
  </read_first>
  <action>
**gameStore.ts 확장:**

1. GameStore 인터페이스에 추가:
```typescript
roundHistory: RoundHistoryEntry[];
```
(import `RoundHistoryEntry` from `@sutda/shared`)

2. 초기 상태에 `roundHistory: []` 추가

3. connect 함수 내 소켓 이벤트 리스너 추가:
```typescript
socket.on('game-history', ({ entries }) => {
  set({ roundHistory: entries });
});

socket.on('proxy-ante-applied', ({ sponsorNickname, beneficiaryNickname }) => {
  toast(`${sponsorNickname}님이 ${beneficiaryNickname}의 학교를 대신 가줬습니다`);
});

socket.on('player-left', ({ nickname }) => {
  if (nickname) {
    toast.error(`${nickname}님 연결이 끊어졌습니다`);
  }
});
```

4. disconnect 액션에서 `roundHistory: []` 초기화 추가

참고: `toast` import — `import { toast } from 'sonner';` (기존 RoomPage에서 사용 중인 패턴 확인)

**HistoryModal.tsx 신규 생성:**

```tsx
import { Dialog, DialogContent, DialogHeader, DialogTitle } from '../ui/dialog';
import type { RoundHistoryEntry } from '@sutda/shared';
import { Badge } from '../ui/badge';

interface HistoryModalProps {
  entries: RoundHistoryEntry[];
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function HistoryModal({ entries, open, onOpenChange }: HistoryModalProps) {
  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="max-w-[480px] max-h-[80vh]">
        <DialogHeader>
          <DialogTitle className="text-xl font-semibold">게임 이력</DialogTitle>
        </DialogHeader>
        {entries.length === 0 ? (
          <p className="text-sm text-muted-foreground text-center py-8">아직 이력이 없습니다</p>
        ) : (
          <div className="max-h-[60vh] overflow-y-auto space-y-2">
            {[...entries].reverse().map((entry) => (
              <div key={entry.roundNumber} className="flex items-center gap-3 p-2 rounded bg-card">
                <span className="text-xs text-muted-foreground w-10 shrink-0">판 {entry.roundNumber}</span>
                <span className="text-sm font-semibold truncate">{entry.winnerNickname}</span>
                <span className="text-sm">{entry.winnerHandLabel}</span>
                <span className="text-sm tabular-nums text-yellow-500 ml-auto">{entry.pot.toLocaleString()}원</span>
                {entry.hasTtaengPayment && (
                  <Badge variant="secondary" className="text-xs shrink-0">땡값</Badge>
                )}
              </div>
            ))}
          </div>
        )}
      </DialogContent>
    </Dialog>
  );
}
```

**InfoPanel.tsx 확장 — 이력 버튼 추가:**

1. 상단에 import 추가:
```typescript
import { Clock } from 'lucide-react';
import { useState } from 'react';
import { HistoryModal } from '../modals/HistoryModal';
import { useGameStore } from '../../store/gameStore';
```

2. 컴포넌트 내부에 상태 + 이력 버튼 추가:
```tsx
const { roundHistory } = useGameStore();
const [historyOpen, setHistoryOpen] = useState(false);

// JSX에 버튼 추가 (기존 InfoPanel 내부 적절한 위치):
<button
  onClick={() => setHistoryOpen(true)}
  disabled={roundHistory.length === 0}
  className="flex items-center gap-1 text-xs text-muted-foreground hover:text-foreground disabled:opacity-50"
  aria-label={roundHistory.length === 0 ? '이력 없음' : '게임 이력'}
>
  <Clock className="h-3.5 w-3.5" />
  이력
</button>
<HistoryModal entries={roundHistory} open={historyOpen} onOpenChange={setHistoryOpen} />
```

**ResultScreen.tsx 확장 — 학교 대신 가주기 UI (per D-08~D-11, UI-SPEC):**

1. 상단에 import 추가:
```typescript
import { useState } from 'react';
```

2. 컴포넌트 내부에 학교 대신 가주기 섹션 추가 (승자에게만 렌더):
```tsx
const [proxyOpen, setProxyOpen] = useState(false);
const [selectedBeneficiaries, setSelectedBeneficiaries] = useState<string[]>([]);

const otherPlayers = gameState.players.filter(p => p.id !== myPlayerId);

const handleProxyConfirm = () => {
  if (selectedBeneficiaries.length === 0) return;
  socket?.emit('proxy-ante', { roomId: gameState.roomId, beneficiaryIds: selectedBeneficiaries });
  setProxyOpen(false);
  setSelectedBeneficiaries([]);
};

const toggleBeneficiary = (id: string) => {
  setSelectedBeneficiaries(prev =>
    prev.includes(id) ? prev.filter(x => x !== id) : [...prev, id]
  );
};

// JSX (winnerId === myPlayerId일 때만):
{gameState.winnerId === myPlayerId && (
  <div className="mt-4">
    <button
      onClick={() => setProxyOpen(!proxyOpen)}
      className="text-sm text-muted-foreground hover:text-foreground"
    >
      학교 대신 가주기 {proxyOpen ? '▲' : '▼'}
    </button>
    {proxyOpen && (
      <div className="mt-2 space-y-2">
        {otherPlayers.map(p => (
          <label key={p.id} className="flex items-center gap-2 text-sm cursor-pointer">
            <input
              type="checkbox"
              checked={selectedBeneficiaries.includes(p.id)}
              onChange={() => toggleBeneficiary(p.id)}
              className="accent-primary"
            />
            <span className={selectedBeneficiaries.includes(p.id) ? 'font-semibold' : ''}>
              {p.nickname}
            </span>
          </label>
        ))}
        <button
          onClick={handleProxyConfirm}
          disabled={selectedBeneficiaries.length === 0}
          className="px-4 py-1.5 text-sm rounded bg-primary text-primary-foreground disabled:opacity-50"
        >
          대신 내주기
        </button>
      </div>
    )}
  </div>
)}
```
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/client build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - HistoryModal.tsx 파일이 존재하고 `게임 이력` 텍스트 포함
    - HistoryModal.tsx에 `아직 이력이 없습니다` 빈 상태 텍스트 포함
    - HistoryModal.tsx에 `DialogContent` import 포함
    - InfoPanel.tsx에 `Clock` lucide 아이콘 import 포함
    - InfoPanel.tsx에 `HistoryModal` import 포함
    - InfoPanel.tsx에 `이력` 버튼 텍스트 포함
    - InfoPanel.tsx에 `이력 없음` aria-label 포함
    - ResultScreen.tsx에 `학교 대신 가주기` 텍스트 포함
    - ResultScreen.tsx에 `proxy-ante` emit 포함
    - ResultScreen.tsx에 `대신 내주기` 버튼 텍스트 포함
    - gameStore.ts에 `roundHistory` 상태 필드 포함
    - gameStore.ts에 `game-history` 소켓 리스너 포함
    - gameStore.ts에 `proxy-ante-applied` 소켓 리스너 포함
    - gameStore.ts에 `player-left` 소켓 리스너 포함
    - `pnpm --filter @sutda/client build` 성공
  </acceptance_criteria>
  <done>이력 모달이 InfoPanel에서 열리고, 결과 화면에 학교 대신 가주기 UI가 승자에게 노출되며, 연결 끊김/학교 대납 토스트가 작동한다</done>
</task>

<task type="auto">
  <name>Task 2: Observer 배지/인디케이터 + 올인 배지/패널 비활성화 + RoomPage 세션 종료 처리</name>
  <files>packages/client/src/components/game/PlayerSeat.tsx, packages/client/src/components/layout/BettingPanel.tsx, packages/client/src/components/layout/GameTable.tsx, packages/client/src/pages/RoomPage.tsx</files>
  <read_first>
    - packages/client/src/components/game/PlayerSeat.tsx
    - packages/client/src/components/layout/BettingPanel.tsx
    - packages/client/src/components/layout/GameTable.tsx
    - packages/client/src/pages/RoomPage.tsx
    - .planning/phases/11-social-features/11-UI-SPEC.md
  </read_first>
  <action>
**PlayerSeat.tsx 확장:**

1. Observer 배지 추가 (D-14, UI-SPEC):
```tsx
// props 인터페이스에 isObserver?: boolean 추가
// 기존 Badge 패턴 확장:
{isObserver && (
  <div className="flex flex-col items-center gap-0.5">
    <Badge variant="outline" className="border-primary text-primary text-xs px-1 py-0">
      관람 중
    </Badge>
    <span className="text-[10px] text-muted-foreground">다음 판 자동 합류</span>
  </div>
)}
```

2. 올인 배지 추가 (UI-SPEC):
```tsx
{player.isAllIn && (
  <Badge variant="outline" className="border-primary text-primary text-xs px-1 py-0">
    올인
  </Badge>
)}
```

3. 재접속 대기 표시:
```tsx
// isConnected prop 추가 (RoomPage에서 roomState 기반 전달)
{!isConnected && (
  <span className="text-[10px] text-muted-foreground">재접속 대기 중</span>
)}
// 최상위 div에 opacity-50 조건부 클래스
```

**BettingPanel.tsx 확장 — 올인 비활성화 (D-21, UI-SPEC):**

1. 올인 상태 판별 — gameStore에서 직접:
```tsx
const me = gameState?.players.find(p => p.id === myPlayerId);
const isMyAllIn = me?.isAllIn ?? false;
```

2. 올인 시 패널 비활성화:
```tsx
className={`... ${isMyAllIn ? 'opacity-50 pointer-events-none' : ''}`}

{isMyAllIn && (
  <p className="text-sm text-muted-foreground text-center">올인 — 베팅 종료</p>
)}
```

3. 올인 시 ring-primary 강조 제거:
```tsx
// 기존: isMyTurn && 'ring-2 ring-primary'
// 변경: isMyTurn && !isMyAllIn && 'ring-2 ring-primary'
```

**GameTable.tsx 확장 — Observer 시트 별도 렌더 + 올인 POT Caption:**

1. Observer 플레이어를 원형 바깥에 표시:
```tsx
const observers = roomState?.players.filter(p => p.isObserver) ?? [];

{observers.length > 0 && (
  <div className="absolute bottom-2 right-2 flex flex-col gap-1">
    {observers.map(obs => (
      <div key={obs.id} className="bg-card/80 rounded px-2 py-1 text-xs flex items-center gap-1.5">
        <span>{obs.nickname}</span>
        <Badge variant="outline" className="border-primary text-primary text-xs px-1 py-0">관람 중</Badge>
      </div>
    ))}
  </div>
)}
```

2. 올인 포함 POT Caption:
```tsx
const hasAllIn = gameState?.players.some(p => p.isAllIn);
{hasAllIn && (
  <span className="text-[10px] text-muted-foreground">올인 포함</span>
)}
```

**RoomPage.tsx — 세션 종료 클라이언트 처리:**

1. roomState.gamePhase === 'waiting' 전환으로 자동 WaitingRoom 렌더링 (기존 FSM)
2. roomState에서 isConnected/isObserver 정보를 PlayerSeat에 prop으로 매핑 전달
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/client build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PlayerSeat.tsx에 `관람 중` 텍스트가 존재
    - PlayerSeat.tsx에 `다음 판 자동 합류` 텍스트가 존재
    - PlayerSeat.tsx에 `올인` 배지 텍스트가 존재
    - BettingPanel.tsx에 `올인 — 베팅 종료` 텍스트가 존재
    - BettingPanel.tsx에 `pointer-events-none` 클래스가 존재
    - BettingPanel.tsx에서 올인 시 ring-primary 강조가 제거됨
    - GameTable.tsx에 Observer 목록 렌더 코드가 존재
    - GameTable.tsx에 `올인 포함` Caption 텍스트가 존재
    - PlayerSeat.tsx에 `재접속 대기 중` 텍스트가 존재
    - `pnpm --filter @sutda/client build` 성공
  </acceptance_criteria>
  <done>Observer 배지, 올인 배지/패널 비활성화, 세션 종료 토스트/대기실 전환이 모든 UI에 반영된다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Phase 11 전체 기능 시각적/기능적 검증</name>
  <files>packages/client/src/pages/RoomPage.tsx</files>
  <action>사용자가 브라우저에서 직접 6개 기능(채팅, 이력, 학교대납, Observer, 세션종료, 올인)을 시각적/기능적으로 검증한다. 자동화 구현 완료 후 수동 확인 단계.</action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm test 2>&1 | tail -5</automated>
  </verify>
  <done>사용자가 "approved"로 확인하거나, 발견된 문제를 설명하여 수정 방향을 제시한다</done>
</task>

</tasks>

<verification>
- `pnpm --filter @sutda/client build` 성공
- `pnpm test` 전체 통과
- 채팅, 이력, 학교 대납, Observer, 세션 종료, 올인 — 6개 기능 모두 UI 레벨에서 확인
</verification>

<success_criteria>
1. HistoryModal이 InfoPanel 버튼으로 열리고 판별 이력이 역순으로 표시된다
2. ResultScreen에 학교 대신 가주기 UI가 승자에게만 노출된다
3. Observer 시트에 배지와 합류 인디케이터가 표시된다
4. 올인 배지가 PlayerSeat에, 비활성 패널이 BettingPanel에 표시된다
5. 연결 끊김 토스트와 2인 퇴장 전환이 작동한다
6. 사용자 시각적 검증 통과 (checkpoint)
</success_criteria>

<output>
완료 후 `.planning/phases/11-social-features/11-04-SUMMARY.md` 생성
</output>
