---
phase: 11-social-features
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - packages/server/src/index.ts
  - packages/server/src/room-manager.ts
autonomous: true
requirements: [LATE-JOIN, SESSION-END]

must_haves:
  truths:
    - "게임 중 입장한 플레이어가 Observer로 추가되어 게임 화면을 볼 수 있다"
    - "Observer는 다음 판 시작 시 자동으로 일반 플레이어로 합류한다"
    - "플레이어 연결 끊김 후 30초 내 재접속하면 복귀하고, 30초 초과 시 퇴장 처리된다"
    - "게임 중 퇴장한 플레이어는 자동 다이 처리되고 다른 플레이어에게 토스트가 표시된다"
    - "2인 게임 중 1명 퇴장 시 남은 플레이어가 대기실로 전환된다"
  artifacts:
    - path: "packages/server/src/index.ts"
      provides: "Observer join-room 분기, 30초 disconnect 타이머, next-round Observer 합류, player-left nickname 전달"
      contains: "isObserver"
    - path: "packages/server/src/room-manager.ts"
      provides: "Observer 포함 방 관리 (선택적 확장)"
  key_links:
    - from: "index.ts join-room 핸들러"
      to: "room-manager.ts joinRoom()"
      via: "게임 중 신규 플레이어 Observer 분기"
      pattern: "isObserver.*true"
    - from: "index.ts disconnect 핸들러"
      to: "index.ts 30초 타이머"
      via: "setTimeout 30_000"
      pattern: "30_000|30000"
    - from: "index.ts next-round 핸들러"
      to: "GameEngine 재생성"
      via: "Observer 합류 시 새 GameEngine 생성"
      pattern: "new GameEngine"
---

<objective>
Observer 모드(뒤늦게 입장)와 세션 종료 처리(30초 타이머 + 강제 퇴장)를 서버 소켓 핸들러에 구현한다.

Purpose: 게임 중 플레이어 입/퇴장 로직은 GameEngine 내부가 아닌 소켓 핸들러 레벨에서 처리. Plan 02(GameEngine)와 병렬 작업 가능.
Output: Observer join/auto-join + 30초 disconnect 타이머 + forcePlayerLeave + 2인 퇴장 처리
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-social-features/11-CONTEXT.md
@.planning/phases/11-social-features/11-RESEARCH.md
@.planning/phases/11-social-features/11-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 추가된 타입 -->
```typescript
// room.ts RoomPlayer 확장:
isObserver?: boolean;
observerChips?: number;

// protocol.ts player-left 확장:
'player-left': (data: { playerId: string; newHostId?: string; nickname?: string }) => void;
```

<!-- 기존 서버 핵심 구조 -->
```typescript
// index.ts — 핵심 패턴
const gameEngines = new Map<string, GameEngine>();
const getEngine = (roomId: string) => gameEngines.get(roomId)!;

// join-room 핸들러: socket.on('join-room', ...) 
//   - 게임 중 재접속: existing.id = playerId 갱신
//   - 게임 중 신규: throw GAME_IN_PROGRESS (← 이 부분을 Observer로 변경)

// disconnect 핸들러:
//   - 대기실: 15초 타이머 → leaveRoom
//   - 게임 중: disconnectPlayer만 호출 (타이머 없음 ← 30초 타이머 추가)

// next-round 핸들러: engine.nextRound() → room-state + game-state broadcast
```

```typescript
// room-manager.ts 핵심 메서드:
joinRoom(roomId, playerId, nickname, initialChips): RoomPlayer;
leaveRoom(roomId, playerId): { removedPlayerId, newHostId? } | null;
disconnectPlayer(roomId, socketId): void;
getRoom(roomId): RoomState | undefined;
```

```typescript
// game-engine.ts 핵심:
class GameEngine {
  constructor(roomId: string, players: RoomPlayer[], mode?: GameMode, roundNumber?: number);
  getState(): GameState;
  getStateFor(playerId: string): GameState;
  nextRound(): void;
  betAction(playerId: string, action: BetAction): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 세션 종료 — 30초 disconnect 타이머 + forcePlayerLeave + 2인 퇴장 처리</name>
  <files>packages/server/src/index.ts, packages/server/src/game-engine.ts</files>
  <read_first>
    - packages/server/src/index.ts (전체 — disconnect 핸들러, 기존 타이머 패턴 확인)
    - packages/server/src/game-engine.ts (betAction, nextRound 부분)
    - packages/server/src/room-manager.ts (leaveRoom, disconnectPlayer 시그니처)
  </read_first>
  <action>
**game-engine.ts — forcePlayerLeave() public 메서드 추가:**
```typescript
forcePlayerLeave(playerId: string): void {
  const player = this.state.players.find(p => p.id === playerId);
  if (!player) return;

  // 베팅 phase 중이면 자동 다이 처리 (D-19)
  const bettingPhases = ['betting', 'betting-1', 'betting-2'];
  if (bettingPhases.includes(this.state.phase) && player.isAlive) {
    try {
      this.betAction(playerId, { type: 'die' });
    } catch {
      // 이미 다이 등 — 무시
      player.isAlive = false;
    }
  } else {
    player.isAlive = false;
  }
}
```

**index.ts — 게임 중 disconnect 타이머 추가:**

1. 파일 상단에 게임용 disconnect 타이머 Map 추가:
```typescript
const gameDisconnectTimers = new Map<string, ReturnType<typeof setTimeout>>();
```

2. disconnect 핸들러의 `room.gamePhase === 'playing'` 분기 수정:
```typescript
// 기존: disconnectPlayer만 호출
// 변경:
if (room.gamePhase === 'playing') {
  roomManager.disconnectPlayer(roomId, socket.id);
  const disconnectedPlayerId = socket.data.playerId;
  const disconnectedNickname = socket.data.nickname;
  const timerKey = `${roomId}:${disconnectedPlayerId}`;

  // 기존 타이머가 있으면 제거 (중복 방지)
  if (gameDisconnectTimers.has(timerKey)) {
    clearTimeout(gameDisconnectTimers.get(timerKey)!);
  }

  const timer = setTimeout(() => {
    gameDisconnectTimers.delete(timerKey);
    const currentRoom = roomManager.getRoom(roomId);
    if (!currentRoom) return;

    const player = currentRoom.players.find(p => p.id === disconnectedPlayerId);
    if (!player || player.isConnected) return; // 이미 재접속함

    // 게임 진행 중이면 자동 다이 (D-19)
    const engine = gameEngines.get(roomId);
    if (engine) {
      try { engine.forcePlayerLeave(disconnectedPlayerId); } catch { /* no-op */ }
    }

    // 방에서 제거
    const result = roomManager.leaveRoom(roomId, disconnectedPlayerId);
    if (result) {
      io.to(roomId).emit('player-left', {
        playerId: result.removedPlayerId,
        newHostId: result.newHostId,
        nickname: disconnectedNickname,
      });

      // 게임 상태 업데이트 broadcast
      if (engine) {
        // per-player emit (getStateFor 사용)
        const room2 = roomManager.getRoom(roomId);
        if (room2) {
          for (const p of room2.players) {
            const sock = Array.from(await io.in(roomId).fetchSockets())
              .find(s => s.data.playerId === p.id);
            if (sock) {
              sock.emit('game-state', engine.getStateFor(p.id) as any);
            }
          }
        }
      }

      // 2인 → 1인 남으면 대기실 전환 (D-18)
      const remainingRoom = roomManager.getRoom(roomId);
      if (remainingRoom && remainingRoom.players.filter(p => !p.isObserver).length < 2) {
        remainingRoom.gamePhase = 'waiting';
        gameEngines.delete(roomId);
        io.to(roomId).emit('room-state', remainingRoom);
      }
    }
  }, 30_000); // D-17: 30초

  gameDisconnectTimers.set(timerKey, timer);
}
```

참고: `fetchSockets()`가 async이므로 타이머 콜백을 async로 변경. 또는 handleGameAction 패턴의 기존 per-player emit 방식을 재활용.

3. **재접속 시 타이머 클리어 — join-room 핸들러에서:**
기존 재접속 로직 근처에 추가:
```typescript
// 재접속 시 게임 disconnect 타이머 클리어
const timerKey = `${roomId}:${existingPlayer.id}`;
if (gameDisconnectTimers.has(timerKey)) {
  clearTimeout(gameDisconnectTimers.get(timerKey)!);
  gameDisconnectTimers.delete(timerKey);
}
```

4. **player-left emit에 nickname 추가** — 기존 leave-room 핸들러에서도:
기존 `io.to(roomId).emit('player-left', { playerId: ..., newHostId: ... })` 호출 부분에 `nickname: socket.data.nickname` 추가.
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/server test</automated>
  </verify>
  <acceptance_criteria>
    - game-engine.ts에 `forcePlayerLeave` public 메서드가 존재
    - index.ts에 `gameDisconnectTimers` Map이 존재
    - index.ts에 `30_000` 또는 `30000` 타이머 값이 존재
    - index.ts의 disconnect 핸들러에 `forcePlayerLeave` 호출이 존재
    - index.ts에 `remainingRoom.players.filter` 또는 유사한 2인 체크 로직이 존재
    - index.ts에 `remainingRoom.gamePhase = 'waiting'` 전환 로직이 존재
    - index.ts에 재접속 시 `gameDisconnectTimers` 클리어 로직이 존재
    - player-left emit에 `nickname` 필드 포함
    - `pnpm --filter @sutda/server test` 통과
  </acceptance_criteria>
  <done>연결 끊김 후 30초 내 재접속하면 복귀, 30초 초과 시 자동 다이 + 퇴장, 2인 게임에서 1인 남으면 대기실 전환이 작동한다</done>
</task>

<task type="auto">
  <name>Task 2: Observer 모드 — join-room Observer 분기 + next-round 자동 합류 + GameEngine 재생성</name>
  <files>packages/server/src/index.ts</files>
  <read_first>
    - packages/server/src/index.ts (Task 1 적용 후 상태, join-room 핸들러, next-round 핸들러)
    - packages/server/src/room-manager.ts (joinRoom 시그니처)
    - packages/shared/src/types/room.ts
  </read_first>
  <action>
**index.ts — join-room 핸들러 수정 (Observer 분기):**

기존 `room.gamePhase === 'playing'` 분기에서 GAME_IN_PROGRESS를 던지는 코드를 수정:

```typescript
// 게임 중 입장 분기
if (room.gamePhase === 'playing') {
  // 재접속 체크 (기존 로직 유지)
  const existing = room.players.find(p => p.nickname === nickname);
  if (existing) {
    // 기존 재접속 로직 그대로 유지
    // ...
  } else {
    // 신규 입장 → Observer로 추가 (D-12, D-15)
    if (room.players.length >= room.maxPlayers) {
      socket.emit('error', { code: 'ROOM_FULL', message: '방이 가득 찼습니다' });
      return;
    }

    const playerId = socket.id;
    const observer: RoomPlayer = {
      id: playerId,
      nickname,
      chips: 0,  // Observer 동안 칩 0 (D-15: 합류 시 observerChips 사용)
      seatIndex: room.players.length,
      isConnected: true,
      isObserver: true,
      observerChips: initialChips,
    };

    room.players.push(observer);
    socket.data = { playerId, nickname, roomId };
    socket.join(roomId);

    // set-player-id 전송
    socket.emit('set-player-id', { playerId });

    // Observer에게 현재 게임 상태 전송 (D-13: 일반 플레이어와 동일한 가시성)
    const engine = gameEngines.get(roomId);
    if (engine) {
      // Observer는 GameEngine players에 없으므로 getState() 사용 (전체 마스킹)
      // 단, 다른 플레이어 카드는 뒷면 — getStateFor에 없는 ID 전달 시 안전한 fallback 확인
      socket.emit('game-state', engine.getStateFor(playerId) as any);
    }

    // 방 상태 업데이트 broadcast
    io.to(roomId).emit('room-state', room);

    // 채팅 이력 전송
    const chatHistory = chatHistories.get(roomId);
    if (chatHistory && chatHistory.length > 0) {
      socket.emit('chat-history', { messages: chatHistory });
    }
  }
  return;
}
```

**index.ts — next-round 핸들러 수정 (Observer 자동 합류):**

기존 `engine.nextRound()` 호출 후, Observer 합류 로직 추가:

```typescript
socket.on('next-round', async ({ roomId }) => {
  try {
    const engine = getEngine(roomId);
    engine.nextRound();

    // Observer → 일반 플레이어 자동 합류 (D-15)
    const room = roomManager.getRoom(roomId)!;
    const observers = room.players.filter(p => p.isObserver);

    if (observers.length > 0) {
      for (const obs of observers) {
        obs.isObserver = false;
        obs.chips = obs.observerChips ?? 100000;  // 입장 시 입력한 초기 칩
        obs.observerChips = undefined;
      }

      // Observer 합류: GameEngine 재생성 (RESEARCH 방법 A — 가장 안전)
      // nextRound() 이후 private 상태가 초기화된 상태이므로 재생성에 문제 없음
      const currentState = engine.getState();
      const newEngine = new GameEngine(
        roomId,
        room.players.filter(p => !p.isObserver),  // 모든 Observer가 이미 해제됨
        currentState.mode,
        currentState.roundNumber,
      );
      gameEngines.set(roomId, newEngine);
    }

    // 이력 수집 — lastRoundHistory가 있으면 gameHistories에 추가
    const lastHistory = engine.lastRoundHistory;
    if (lastHistory) {
      if (!gameHistories.has(roomId)) gameHistories.set(roomId, []);
      gameHistories.get(roomId)!.push(lastHistory);
      // game-history 이벤트로 전체 이력 broadcast
      io.to(roomId).emit('game-history', { entries: gameHistories.get(roomId)! });
    }

    const updatedEngine = getEngine(roomId);
    io.to(roomId).emit('room-state', room);
    // per-player emit
    const sockets = await io.in(roomId).fetchSockets();
    for (const s of sockets) {
      s.emit('game-state', updatedEngine.getStateFor(s.data.playerId) as any);
    }
  } catch (err: any) {
    socket.emit('game-error', { code: err.message, message: err.message });
  }
});
```

참고: `gameHistories` Map은 이 태스크에서 추가:
```typescript
const gameHistories = new Map<string, RoundHistoryEntry[]>();
```
(import `RoundHistoryEntry` from `@sutda/shared`)

**index.ts — proxy-ante 핸들러 추가 (SCHOOL-PROXY 서버 측):**

```typescript
socket.on('proxy-ante', ({ roomId, beneficiaryIds }) => {
  const engine = gameEngines.get(roomId);
  const room = roomManager.getRoom(roomId);
  if (!engine || !room) return;

  // schoolProxyBeneficiaryIds에 저장 — 다음 판 attend-school에서 소비
  const state = engine.getState();
  state.schoolProxyBeneficiaryIds = beneficiaryIds;

  // proxy-ante-applied broadcast (토스트용)
  const sponsorNickname = socket.data.nickname;
  for (const bid of beneficiaryIds) {
    const beneficiary = room.players.find(p => p.id === bid);
    if (beneficiary) {
      io.to(roomId).emit('proxy-ante-applied', {
        sponsorNickname,
        beneficiaryNickname: beneficiary.nickname,
      });
    }
  }
});
```

**index.ts — attend-school 핸들러 수정 (학교 대납 적용):**

기존 `engine.attendSchool(socket.data.playerId)` 호출 전에 proxy 체크 추가:

```typescript
// attend-school 핸들러 내부
const state = engine.getState();
const proxies = state.schoolProxyBeneficiaryIds;
if (proxies && proxies.includes(socket.data.playerId)) {
  // 후원자 찾기 — 직전 판 승자
  const sponsorId = state.winnerId ?? state.players.find(p => p.isDealer)?.id;
  if (sponsorId) {
    engine.attendSchoolProxy(socket.data.playerId, sponsorId);
    // 수혜자 목록에서 제거 (1회성)
    state.schoolProxyBeneficiaryIds = proxies.filter(id => id !== socket.data.playerId);
  } else {
    engine.attendSchool(socket.data.playerId);
  }
} else {
  engine.attendSchool(socket.data.playerId);
}
```

참고: schoolProxyBeneficiaryIds는 result phase에서 proxy-ante 핸들러가 설정하고, attend-school phase에서 소비됨. 판 사이에 winnerId는 nextRound()에서 undefined로 리셋되므로, proxy-ante 핸들러에서 sponsorId도 함께 저장하는 것이 안전. GameState에 `schoolProxySponsorId?: string` 추가를 고려하되, 현재는 `engine.getState().players.find(p => p.isDealer)?.id`로 추적 가능 (nextRound에서 이전 승자가 dealer로 설정됨).
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/server test</automated>
  </verify>
  <acceptance_criteria>
    - index.ts에 `isObserver: true` 할당이 존재 (Observer 생성)
    - index.ts에 `observerChips: initialChips` 패턴이 존재
    - index.ts에 `obs.isObserver = false` 패턴이 존재 (Observer 해제)
    - index.ts에 `new GameEngine` 호출이 next-round 핸들러에 존재 (재생성)
    - index.ts에 `gameHistories` Map이 존재
    - index.ts에 `proxy-ante` 소켓 핸들러가 존재
    - index.ts에 `attendSchoolProxy` 호출이 존재
    - index.ts에 `game-history` emit이 존재
    - `pnpm --filter @sutda/server test` 통과
  </acceptance_criteria>
  <done>게임 중 입장자가 Observer로 추가되고, next-round에서 자동 합류하며, 이력/학교대납 서버 소켓 핸들러가 작동한다</done>
</task>

</tasks>

<verification>
- `pnpm --filter @sutda/server test` 전체 통과
- Observer가 게임 중 입장하여 게임 상태를 수신받을 수 있음
- Observer가 next-round에서 일반 플레이어로 전환됨
- 30초 disconnect 타이머가 게임 중에도 작동
- 2인 게임 1인 퇴장 시 대기실 전환
- proxy-ante 핸들러가 학교 대납을 처리
</verification>

<success_criteria>
1. Observer 입장/자동 합류 파이프라인이 서버에 완성됨
2. 세션 종료 30초 타이머 + 강제 퇴장이 작동함
3. 학교 대납 서버 핸들러가 연결됨
4. 이력 수집 + broadcast가 next-round에서 실행됨
5. 기존 테스트가 깨지지 않음
</success_criteria>

<output>
완료 후 `.planning/phases/11-social-features/11-03-SUMMARY.md` 생성
</output>
