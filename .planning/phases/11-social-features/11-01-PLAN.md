---
phase: 11-social-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/protocol.ts
  - packages/shared/src/types/game.ts
  - packages/shared/src/types/room.ts
  - packages/server/src/index.ts
  - packages/client/src/store/gameStore.ts
  - packages/client/src/components/layout/ChatPanel.tsx
autonomous: true
requirements: [UX-02]

must_haves:
  truths:
    - "플레이어가 텍스트 메시지를 입력하면 같은 방의 모든 플레이어에게 실시간으로 표시된다"
    - "내 메시지는 우측, 상대 메시지는 좌측에 정렬되어 표시된다"
    - "채팅 입력 시 Enter로 전송하고 빈 메시지는 전송 불가하다"
    - "모든 7개 기능의 타입 계약이 shared 패키지에 정의되어 있다"
  artifacts:
    - path: "packages/shared/src/types/protocol.ts"
      provides: "채팅/이력/학교대납/Observer/세션종료/올인 이벤트 타입"
      contains: "send-chat"
    - path: "packages/shared/src/types/game.ts"
      provides: "isAllIn, totalCommitted 플래그"
      contains: "isAllIn"
    - path: "packages/shared/src/types/room.ts"
      provides: "isObserver 플래그"
      contains: "isObserver"
    - path: "packages/client/src/components/layout/ChatPanel.tsx"
      provides: "실제 채팅 UI"
      min_lines: 60
  key_links:
    - from: "packages/client/src/components/layout/ChatPanel.tsx"
      to: "packages/server/src/index.ts"
      via: "socket send-chat / chat-message 이벤트"
      pattern: "send-chat|chat-message"
---

<objective>
Phase 11의 모든 기능에 필요한 공유 타입/프로토콜 계약을 먼저 확립하고, 채팅 기능을 서버+클라이언트 풀스택으로 완성한다.

Purpose: 후속 Plan 02~04가 타입 계약에 의존하여 병렬 작업할 수 있도록 인터페이스를 선행 정의. 채팅은 독립적이므로 타입 정의와 함께 완성한다.
Output: 확장된 shared 타입, 채팅 서버 핸들러, 채팅 클라이언트 UI
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-social-features/11-CONTEXT.md
@.planning/phases/11-social-features/11-RESEARCH.md
@.planning/phases/11-social-features/11-UI-SPEC.md

<interfaces>
<!-- 현재 shared 타입 — 확장 대상 -->

From packages/shared/src/types/protocol.ts:
```typescript
export interface ClientToServerEvents {
  'create-room': (data: { nickname: string; initialChips: number }) => void;
  'join-room': (data: { roomId: string; nickname: string; initialChips: number }) => void;
  // ... 기존 이벤트들
}
export interface ServerToClientEvents {
  'room-created': (data: { roomId: string; roomState: RoomState }) => void;
  'player-left': (data: { playerId: string; newHostId?: string }) => void;
  // ... 기존 이벤트들
}
```

From packages/shared/src/types/game.ts:
```typescript
export interface PlayerState {
  id: string; nickname: string; chips: number;
  cards: (Card | null)[]; isAlive: boolean; isAbsent: boolean;
  isRevealed: boolean; currentBet: number; totalBet: number;
  isDealer: boolean; seatIndex: number;
  chipBreakdown: ChipBreakdown;
  lastBetAction?: LastBetAction;
  selectedCards?: Card[]; openedCardIndex?: 0 | 1;
}
export interface GameState {
  roomId: string; phase: GamePhase; mode: GameMode;
  players: PlayerState[]; pot: number;
  // ... 기타 필드들
}
```

From packages/shared/src/types/room.ts:
```typescript
export interface RoomPlayer {
  id: string; nickname: string; chips: number;
  seatIndex: number; isConnected: boolean;
}
```

From packages/client/src/store/gameStore.ts:
```typescript
interface GameStore {
  socket: AppSocket | null;
  gameState: GameState | null;
  roomState: RoomState | null;
  myPlayerId: string | null;
  error: string | null;
  rechargeRequest: RechargeRequest | null;
  connect: (serverUrl: string) => void;
  disconnect: () => void;
  clearError: () => void;
}
```

From packages/client/src/components/layout/ChatPanel.tsx:
```typescript
// 현재 placeholder — 전체 교체 대상
export function ChatPanel() {
  return (
    <div className="h-16 border-t border-border flex items-center justify-center text-muted-foreground text-sm">
      채팅은 다음 버전에서 제공됩니다
    </div>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 기능 공유 타입/프로토콜 계약 확장</name>
  <files>packages/shared/src/types/protocol.ts, packages/shared/src/types/game.ts, packages/shared/src/types/room.ts</files>
  <read_first>
    - packages/shared/src/types/protocol.ts
    - packages/shared/src/types/game.ts
    - packages/shared/src/types/room.ts
    - packages/shared/src/types/index.ts
  </read_first>
  <action>
**protocol.ts — ClientToServerEvents에 추가:**
```typescript
'send-chat': (data: { roomId: string; text: string }) => void;
'proxy-ante': (data: { roomId: string; beneficiaryIds: string[] }) => void;
```

**protocol.ts — ServerToClientEvents에 추가:**
```typescript
'chat-message': (data: { playerId: string; nickname: string; text: string; timestamp: number }) => void;
'chat-history': (data: { messages: Array<{ playerId: string; nickname: string; text: string; timestamp: number }> }) => void;
'proxy-ante-applied': (data: { sponsorNickname: string; beneficiaryNickname: string }) => void;
'game-history': (data: { entries: RoundHistoryEntry[] }) => void;
```

**protocol.ts — 'player-left' 이벤트에 nickname 필드 추가:**
기존: `'player-left': (data: { playerId: string; newHostId?: string }) => void;`
변경: `'player-left': (data: { playerId: string; newHostId?: string; nickname?: string }) => void;`

**protocol.ts — ErrorPayload.code에 추가:**
`'CHAT_TOO_FAST'` 추가 (서버 측 스팸 방지 — 선택적)

**game.ts — PlayerState에 필드 추가:**
```typescript
isAllIn?: boolean;        // 올인 상태 (베팅 패널 비활성화 대상)
totalCommitted?: number;  // 이번 판 전체 기여 총액 (앤티 포함, 올인 정산용)
```

**game.ts — GameState에 필드 추가:**
```typescript
schoolProxyBeneficiaryIds?: string[];  // 학교 대신 가주기 수혜자 ID 목록 (다음 판 앤티 면제)
```

**game.ts — RoundHistoryEntry 인터페이스 신규 추가 (export):**
```typescript
export interface RoundHistoryEntry {
  roundNumber: number;
  winnerId: string;
  winnerNickname: string;
  winnerHandLabel: string;
  pot: number;
  hasTtaengPayment: boolean;
  playerChipChanges: { playerId: string; nickname: string; chipDelta: number }[];
}
```

**room.ts — RoomPlayer에 필드 추가:**
```typescript
isObserver?: boolean;      // 게임 중 입장한 관람자
observerChips?: number;    // 다음 판 참여 시 사용할 초기 칩
```

**shared/src/types/index.ts:** RoundHistoryEntry export 추가 (기존 re-export 패턴 확인 후 추가)
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/shared build</automated>
  </verify>
  <acceptance_criteria>
    - protocol.ts에 `'send-chat'` 문자열이 존재
    - protocol.ts에 `'chat-message'` 문자열이 존재
    - protocol.ts에 `'proxy-ante'` 문자열이 존재
    - protocol.ts에 `'proxy-ante-applied'` 문자열이 존재
    - protocol.ts에 `'game-history'` 문자열이 존재
    - protocol.ts에 `'chat-history'` 문자열이 존재
    - protocol.ts의 player-left에 `nickname?: string` 포함
    - game.ts에 `isAllIn?: boolean` 포함
    - game.ts에 `totalCommitted?: number` 포함
    - game.ts에 `schoolProxyBeneficiaryIds?: string[]` 포함
    - game.ts에 `export interface RoundHistoryEntry` 포함
    - room.ts에 `isObserver?: boolean` 포함
    - room.ts에 `observerChips?: number` 포함
    - `pnpm --filter @sutda/shared build` 성공 (exit code 0)
  </acceptance_criteria>
  <done>7개 기능(채팅/이력/학교대납/Observer/세션종료/올인)의 공유 타입이 모두 정의되고 shared 빌드가 성공한다</done>
</task>

<task type="auto">
  <name>Task 2: 채팅 서버 핸들러 + ChatPanel UI + gameStore 채팅 상태</name>
  <files>packages/server/src/index.ts, packages/client/src/store/gameStore.ts, packages/client/src/components/layout/ChatPanel.tsx</files>
  <read_first>
    - packages/server/src/index.ts
    - packages/client/src/store/gameStore.ts
    - packages/client/src/components/layout/ChatPanel.tsx
    - .planning/phases/11-social-features/11-UI-SPEC.md
  </read_first>
  <action>
**서버 (index.ts):**

1. 파일 상단에 chatHistories Map 추가:
```typescript
const chatHistories = new Map<string, Array<{ playerId: string; nickname: string; text: string; timestamp: number }>>();
```

2. socket.on 핸들러 블록에 `send-chat` 핸들러 추가:
```typescript
socket.on('send-chat', ({ roomId, text }) => {
  if (!text || text.trim().length === 0) return;
  const msg = {
    playerId: socket.data.playerId,
    nickname: socket.data.nickname,
    text: text.trim().slice(0, 200),
    timestamp: Date.now(),
  };
  if (!chatHistories.has(roomId)) chatHistories.set(roomId, []);
  const history = chatHistories.get(roomId)!;
  history.push(msg);
  if (history.length > 50) history.shift();
  io.to(roomId).emit('chat-message', msg);
});
```

3. join-room 핸들러의 방 참여 성공 직후 (room-state emit 근처), chat-history를 해당 소켓에만 전송:
```typescript
const chatHistory = chatHistories.get(roomId);
if (chatHistory && chatHistory.length > 0) {
  socket.emit('chat-history', { messages: chatHistory });
}
```

4. leave-room / 방 정리 로직에서 chatHistories.delete(roomId) 추가 (방이 빈 경우)

**클라이언트 gameStore.ts:**

1. ChatMessage 인터페이스 추가:
```typescript
interface ChatMessage {
  playerId: string;
  nickname: string;
  text: string;
  timestamp: number;
}
```

2. GameStore 인터페이스에 추가:
```typescript
chatMessages: ChatMessage[];
sendChat: (roomId: string, text: string) => void;
```

3. 초기 상태에 `chatMessages: []` 추가

4. connect 함수 내 소켓 이벤트 리스너 추가:
```typescript
socket.on('chat-message', (msg) => {
  set(state => ({ chatMessages: [...state.chatMessages, msg] }));
});
socket.on('chat-history', ({ messages }) => {
  set({ chatMessages: messages });
});
```

5. sendChat 액션:
```typescript
sendChat: (roomId: string, text: string) => {
  get().socket?.emit('send-chat', { roomId, text });
},
```

6. disconnect 액션에서 chatMessages: [] 초기화 추가

**클라이언트 ChatPanel.tsx — placeholder 전체 교체 (per D-01, D-02, D-03, UI-SPEC):**

```tsx
import { useRef, useEffect, useState } from 'react';
import { useGameStore } from '../../store/gameStore';
import { Send } from 'lucide-react';

export function ChatPanel() {
  const { chatMessages, myPlayerId, sendChat, roomState, socket } = useGameStore();
  const [input, setInput] = useState('');
  const [sendDisabled, setSendDisabled] = useState(false);
  const bottomRef = useRef<HTMLDivElement>(null);
  const listRef = useRef<HTMLDivElement>(null);
  const isAtBottomRef = useRef(true);

  // 자동 스크롤 — 맨 아래에 있을 때만 (UI-SPEC 인터랙션 계약)
  useEffect(() => {
    if (isAtBottomRef.current) {
      bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
    }
  }, [chatMessages]);

  const handleScroll = () => {
    const el = listRef.current;
    if (!el) return;
    isAtBottomRef.current = el.scrollHeight - el.scrollTop - el.clientHeight < 30;
  };

  const handleSend = () => {
    const trimmed = input.trim();
    if (!trimmed || !roomState?.roomId || sendDisabled) return;
    sendChat(roomState.roomId, trimmed);
    setInput('');
    setSendDisabled(true);
    setTimeout(() => setSendDisabled(false), 500); // 500ms 스팸 방지
  };

  const handleKeyDown = (e: React.KeyboardEvent) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      handleSend();
    }
  };

  const isConnected = socket?.connected ?? false;

  return (
    <div className="flex flex-col h-full border-t border-border">
      {/* 메시지 목록 */}
      <div ref={listRef} onScroll={handleScroll} className="flex-1 overflow-y-auto p-3 space-y-2">
        {chatMessages.length === 0 ? (
          <div className="flex items-center justify-center h-full text-muted-foreground text-sm">
            아직 메시지가 없습니다
          </div>
        ) : (
          chatMessages.map((msg, i) => {
            const isMe = msg.playerId === myPlayerId;
            return (
              <div key={`${msg.timestamp}-${i}`} className={`flex ${isMe ? 'justify-end' : 'justify-start'}`}>
                <div className={`max-w-[85%] px-3 py-1.5 rounded-lg ${isMe ? 'bg-primary/10' : 'bg-card'}`}>
                  <div className="flex items-center gap-2">
                    <span className="text-sm font-bold">{msg.nickname}</span>
                    <span className="text-xs text-muted-foreground">
                      {new Date(msg.timestamp).toLocaleTimeString('ko-KR', { hour: '2-digit', minute: '2-digit' })}
                    </span>
                  </div>
                  <p className="text-sm text-foreground break-words">{msg.text}</p>
                </div>
              </div>
            );
          })
        )}
        <div ref={bottomRef} />
      </div>

      {/* 입력 필드 */}
      <div className="flex items-center gap-2 p-2 border-t border-border">
        <input
          type="text"
          value={input}
          onChange={e => setInput(e.target.value.slice(0, 200))}
          onKeyDown={handleKeyDown}
          placeholder={isConnected ? '메시지를 입력하세요' : '채팅을 사용할 수 없습니다'}
          disabled={!isConnected}
          className="flex-1 h-10 px-3 rounded-md border border-input bg-background text-sm placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring disabled:opacity-50"
          maxLength={200}
        />
        <button
          onClick={handleSend}
          disabled={!input.trim() || sendDisabled || !isConnected}
          className="h-10 w-10 flex items-center justify-center rounded-md text-primary hover:bg-accent disabled:opacity-50 disabled:pointer-events-none"
        >
          <Send className="h-4 w-4" />
        </button>
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/shared build && pnpm --filter @sutda/client build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - index.ts에 `chatHistories` Map 선언이 존재
    - index.ts에 `socket.on('send-chat'` 핸들러가 존재
    - index.ts에 `chat-history` emit이 존재
    - gameStore.ts에 `chatMessages: ChatMessage[]` 타입이 존재
    - gameStore.ts에 `sendChat` 함수가 존재
    - gameStore.ts에 `socket.on('chat-message'` 리스너가 존재
    - ChatPanel.tsx에 `아직 메시지가 없습니다` 빈 상태 텍스트가 존재
    - ChatPanel.tsx에 `메시지를 입력하세요` placeholder가 존재
    - ChatPanel.tsx에 `Send` (lucide 아이콘) import가 존재
    - ChatPanel.tsx에 `bg-primary/10` (내 메시지 배경) 클래스가 존재
    - ChatPanel.tsx에 200자 제한 (`maxLength={200}` 또는 `.slice(0, 200)`)이 존재
    - `pnpm --filter @sutda/client build` 성공
  </acceptance_criteria>
  <done>채팅 메시지를 입력하면 서버가 방 전체에 broadcast하고, ChatPanel에 좌/우 정렬로 표시되며, 200자 제한/500ms 스팸방지/자동 스크롤이 작동한다</done>
</task>

</tasks>

<verification>
- `pnpm --filter @sutda/shared build` 성공
- `pnpm --filter @sutda/client build` 성공
- protocol.ts에 send-chat, chat-message, proxy-ante, proxy-ante-applied, game-history, chat-history 이벤트 존재
- game.ts에 isAllIn, totalCommitted, schoolProxyBeneficiaryIds, RoundHistoryEntry 존재
- room.ts에 isObserver, observerChips 존재
- ChatPanel.tsx가 placeholder가 아닌 실제 채팅 UI 구현
</verification>

<success_criteria>
1. shared 빌드 성공 — 7개 기능의 타입 계약이 올바르게 정의됨
2. client 빌드 성공 — ChatPanel이 실제 채팅 UI로 교체됨
3. 채팅 메시지 전송/수신 이벤트 파이프라인이 서버+클라이언트에 구현됨
</success_criteria>

<output>
완료 후 `.planning/phases/11-social-features/11-01-SUMMARY.md` 생성
</output>
