---
phase: 03-websocket
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/protocol.ts
  - packages/shared/src/types/room.ts
  - packages/shared/src/index.ts
  - packages/server/package.json
  - packages/server/src/index.ts
  - packages/server/src/room-manager.ts
autonomous: true
requirements: [INFRA-01, INFRA-03, INFRA-04, INFRA-06]

must_haves:
  truths:
    - "Socket.IO 서버가 포트에 바인딩되어 WebSocket 연결을 수락한다"
    - "RoomManager가 방 생성 시 8자리 UUID 기반 ID를 반환한다"
    - "방에 7번째 플레이어 추가 시 에러를 반환한다"
    - "ClientToServerEvents / ServerToClientEvents 타입이 shared 패키지에서 import 가능하다"
  artifacts:
    - path: "packages/shared/src/types/protocol.ts"
      provides: "Socket.IO 이벤트 타입 계약 (ClientToServerEvents, ServerToClientEvents)"
      exports: ["ClientToServerEvents", "ServerToClientEvents"]
    - path: "packages/shared/src/types/room.ts"
      provides: "RoomState, RoomPlayer 타입 정의"
      exports: ["RoomState", "RoomPlayer"]
    - path: "packages/server/src/room-manager.ts"
      provides: "인메모리 방 관리 클래스 (생성, 참여, 퇴장, 조회)"
      exports: ["RoomManager"]
    - path: "packages/server/src/index.ts"
      provides: "Socket.IO 서버 진입점"
      min_lines: 20
  key_links:
    - from: "packages/server/src/index.ts"
      to: "packages/shared/src/types/protocol.ts"
      via: "import ClientToServerEvents, ServerToClientEvents"
      pattern: "import.*protocol"
    - from: "packages/server/src/room-manager.ts"
      to: "packages/shared/src/types/room.ts"
      via: "import RoomState, RoomPlayer"
      pattern: "import.*room"
---

<objective>
Socket.IO 메시지 프로토콜 타입을 shared 패키지에 정의하고, Socket.IO 서버를 부트스트랩하며, 인메모리 RoomManager 클래스를 구현한다.

Purpose: Phase 4 게임 엔진이 의존할 실시간 통신 계층과 방 상태 관리의 기반을 확립한다.
Output: 타입이 정의된 shared 패키지, Socket.IO 서버 진입점, RoomManager 클래스
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-websocket/03-CONTEXT.md

<interfaces>
<!-- Phase 1에서 생성된 기존 타입. 새 타입 정의 시 이 패턴을 따른다. -->

From packages/shared/src/types/game.ts:
```typescript
export type GamePhase =
  | 'waiting' | 'mode-select' | 'shuffling' | 'cutting'
  | 'dealing' | 'betting' | 'showdown' | 'result' | 'finished';

export type GameMode =
  | 'original' | 'three-card' | 'shared-card' | 'regret' | 'indian';

export interface PlayerState {
  id: string;
  nickname: string;
  chips: number;
  cards: Card[];
  isAlive: boolean;
  isRevealed: boolean;
  currentBet: number;
  isDealer: boolean;
  seatIndex: number;
}

export interface GameState {
  roomId: string;
  phase: GamePhase;
  mode: GameMode;
  players: PlayerState[];
  pot: number;
  currentPlayerIndex: number;
  currentBetAmount: number;
  roundNumber: number;
}
```

From packages/shared/src/index.ts:
```typescript
export type { CardRank, CardAttribute, Card } from './types/card';
export { GWANG_RANKS, YEOLKKEUT_RANKS, getCardAttribute } from './types/card';
export type { GamePhase, GameMode, PlayerState, GameState } from './types/game';
export type { HandType, HandResult } from './types/hand';
export { createDeck } from './deck';
export { evaluateHand } from './hand/evaluator';
export { compareHands } from './hand/compare';
export { checkGusaTrigger } from './hand/gusa';
```

From packages/server/package.json:
```json
{
  "name": "@sutda/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@sutda/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7",
    "tsx": "^4"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Socket.IO 프로토콜 타입 + RoomState 타입 정의</name>
  <files>packages/shared/src/types/protocol.ts, packages/shared/src/types/room.ts, packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/types/game.ts (기존 GameState, PlayerState 타입 확인)
    - packages/shared/src/index.ts (현재 export 목록 확인)
    - .planning/phases/03-websocket/03-CONTEXT.md (D-07~D-10 이벤트 프로토콜 결정사항)
  </read_first>
  <action>
**1) `packages/shared/src/types/room.ts` 생성:**

```typescript
/** 방에 참여한 플레이어 (게임 시작 전 대기실 상태) */
export interface RoomPlayer {
  id: string;           // socket.id 기반 또는 UUID
  nickname: string;
  chips: number;        // 초기 칩 금액 (만원 단위, 기본 100000)
  seatIndex: number;    // 자리 번호 (0부터, 입장 순서)
  isConnected: boolean; // 현재 연결 상태 (재접속 처리용)
}

/** 방 상태 */
export interface RoomState {
  roomId: string;           // 8자리 UUID 기반 코드 (per D-01)
  hostId: string;           // 방장 플레이어 id (per D-11, D-12)
  players: RoomPlayer[];    // 현재 방에 있는 플레이어 목록
  maxPlayers: number;       // 최대 인원 (6, per INFRA-03)
  gamePhase: 'waiting' | 'playing'; // 대기 vs 게임 진행 중
  createdAt: number;        // Date.now() 타임스탬프
}
```

**2) `packages/shared/src/types/protocol.ts` 생성 (per D-07, D-08, D-09):**

```typescript
import type { RoomState, RoomPlayer } from './room';

/** 에러 응답 (per UI-SPEC 에러 메시지 계약) */
export interface ErrorPayload {
  code: 'ROOM_NOT_FOUND' | 'ROOM_FULL' | 'NICKNAME_TAKEN' | 'NOT_HOST' | 'MIN_PLAYERS' | 'GAME_IN_PROGRESS' | 'INVALID_CHIPS';
  message: string;
}

/** 클라이언트 -> 서버 이벤트 */
export interface ClientToServerEvents {
  'create-room': (data: { nickname: string; initialChips: number }) => void;
  'join-room': (data: { roomId: string; nickname: string; initialChips: number }) => void;
  'leave-room': (data: { roomId: string }) => void;
  'start-game': (data: { roomId: string }) => void;
}

/** 서버 -> 클라이언트 이벤트 */
export interface ServerToClientEvents {
  'room-created': (data: { roomId: string; roomState: RoomState }) => void;
  'room-state': (data: RoomState) => void;
  'player-joined': (data: RoomPlayer) => void;
  'player-left': (data: { playerId: string; newHostId?: string }) => void;
  'error': (data: ErrorPayload) => void;
}

/** Socket.IO 서버 간 이벤트 (사용하지 않지만 타입 완전성) */
export interface InterServerEvents {}

/** Socket 데이터 (소켓별 저장 데이터) */
export interface SocketData {
  playerId: string;
  nickname: string;
  roomId: string;
}
```

**3) `packages/shared/src/index.ts` 업데이트 — 기존 export 유지, 아래 추가:**

```typescript
export type { RoomState, RoomPlayer } from './types/room';
export type { ClientToServerEvents, ServerToClientEvents, InterServerEvents, SocketData, ErrorPayload } from './types/protocol';
```

기존 export 행들은 그대로 유지하고, 마지막에 위 2줄을 추가한다.
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && npx turbo run build --filter=@sutda/shared 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/types/room.ts 파일이 존재하고 `export interface RoomState` 와 `export interface RoomPlayer` 를 포함한다
    - packages/shared/src/types/protocol.ts 파일이 존재하고 `export interface ClientToServerEvents` 와 `export interface ServerToClientEvents` 를 포함한다
    - protocol.ts에 'create-room', 'join-room', 'leave-room', 'start-game' 이벤트가 정의되어 있다
    - protocol.ts에 'room-created', 'room-state', 'player-joined', 'player-left', 'error' 이벤트가 정의되어 있다
    - protocol.ts에 ErrorPayload 인터페이스가 ROOM_NOT_FOUND, ROOM_FULL, NICKNAME_TAKEN, NOT_HOST, MIN_PLAYERS, GAME_IN_PROGRESS, INVALID_CHIPS 코드를 포함한다
    - packages/shared/src/index.ts에 `RoomState`, `RoomPlayer`, `ClientToServerEvents`, `ServerToClientEvents` export가 있다
    - `npx turbo run build --filter=@sutda/shared` 가 성공한다 (exit code 0)
  </acceptance_criteria>
  <done>shared 패키지에 Socket.IO 프로토콜 타입과 RoomState 타입이 정의되어 빌드 성공하고, server 패키지에서 import 가능하다</done>
</task>

<task type="auto">
  <name>Task 2: Socket.IO 서버 부트스트랩 + RoomManager 클래스 구현</name>
  <files>packages/server/package.json, packages/server/src/index.ts, packages/server/src/room-manager.ts</files>
  <read_first>
    - packages/server/package.json (현재 의존성 확인)
    - packages/server/src/index.ts (현재 스텁 상태 확인)
    - packages/server/tsconfig.json (컴파일러 옵션 확인)
    - packages/shared/src/types/room.ts (Task 1에서 생성한 타입)
    - packages/shared/src/types/protocol.ts (Task 1에서 생성한 타입)
    - .planning/phases/03-websocket/03-CONTEXT.md (D-01, D-14 결정사항)
  </read_first>
  <action>
**1) Socket.IO 의존성 설치 — `packages/server/package.json` 업데이트:**

pnpm으로 서버 패키지에 socket.io 추가:
```bash
cd packages/server && pnpm add socket.io
```

이후 package.json의 dependencies에 `"socket.io": "^4"` 가 추가된다.

devDependencies에 vitest 추가 (Plan 02 테스트 준비):
```bash
cd packages/server && pnpm add -D vitest @types/node
```

**2) `packages/server/src/room-manager.ts` 생성:**

인메모리 Map 기반 방 관리 (per D-14). 다음 메서드를 구현:

```typescript
import type { RoomState, RoomPlayer } from '@sutda/shared';
import { randomUUID } from 'crypto';

export class RoomManager {
  private rooms: Map<string, RoomState> = new Map();

  /** 방 생성 — 8자리 UUID 코드 반환 (per D-01) */
  createRoom(hostId: string, nickname: string, initialChips: number): RoomState {
    const roomId = randomUUID().slice(0, 8); // per D-01
    const host: RoomPlayer = {
      id: hostId,
      nickname,
      chips: initialChips,
      seatIndex: 0,
      isConnected: true,
    };
    const room: RoomState = {
      roomId,
      hostId,
      players: [host],
      maxPlayers: 6,
      gamePhase: 'waiting',
      createdAt: Date.now(),
    };
    this.rooms.set(roomId, room);
    return room;
  }

  /** 방 조회 */
  getRoom(roomId: string): RoomState | undefined {
    return this.rooms.get(roomId);
  }

  /** 방 참여 — 에러 시 ErrorPayload의 code를 문자열로 throw */
  joinRoom(roomId: string, playerId: string, nickname: string, initialChips: number): { room: RoomState; player: RoomPlayer } {
    const room = this.rooms.get(roomId);
    if (!room) throw new Error('ROOM_NOT_FOUND');
    if (room.gamePhase === 'playing') {
      // 재접속 확인 (per D-04, D-05)
      const existing = room.players.find(p => p.nickname === nickname);
      if (existing) {
        existing.id = playerId; // 새 소켓 ID로 갱신
        existing.isConnected = true;
        return { room, player: existing };
      }
      throw new Error('GAME_IN_PROGRESS');
    }
    // waiting 상태에서 닉네임 중복 체크 (per D-06)
    if (room.players.some(p => p.nickname === nickname)) {
      throw new Error('NICKNAME_TAKEN');
    }
    if (room.players.length >= room.maxPlayers) {
      throw new Error('ROOM_FULL');
    }
    const player: RoomPlayer = {
      id: playerId,
      nickname,
      chips: initialChips,
      seatIndex: room.players.length,
      isConnected: true,
    };
    room.players.push(player);
    return { room, player };
  }

  /** 플레이어 퇴장 — 방장 승계 처리 (per D-12) */
  leaveRoom(roomId: string, playerId: string): { room: RoomState; removedPlayerId: string; newHostId?: string } | null {
    const room = this.rooms.get(roomId);
    if (!room) return null;
    const idx = room.players.findIndex(p => p.id === playerId);
    if (idx === -1) return null;
    const removedPlayerId = room.players[idx].id;
    room.players.splice(idx, 1);
    // 방이 비면 삭제
    if (room.players.length === 0) {
      this.rooms.delete(roomId);
      return { room, removedPlayerId };
    }
    // seatIndex 재정렬
    room.players.forEach((p, i) => { p.seatIndex = i; });
    // 방장 승계: 방장이 나간 경우 입장 순서 기준 다음 (per D-12)
    let newHostId: string | undefined;
    if (room.hostId === removedPlayerId) {
      room.hostId = room.players[0].id;
      newHostId = room.players[0].id;
    }
    return { room, removedPlayerId, newHostId };
  }

  /** 연결 끊김 처리 — isConnected = false 설정 */
  disconnectPlayer(roomId: string, playerId: string): void {
    const room = this.rooms.get(roomId);
    if (!room) return;
    const player = room.players.find(p => p.id === playerId);
    if (player) player.isConnected = false;
  }

  /** 게임 시작 가능 여부 체크 (per D-11) */
  canStartGame(roomId: string, playerId: string): { ok: true } | { ok: false; code: string } {
    const room = this.rooms.get(roomId);
    if (!room) return { ok: false, code: 'ROOM_NOT_FOUND' };
    if (room.hostId !== playerId) return { ok: false, code: 'NOT_HOST' };
    if (room.players.length < 2) return { ok: false, code: 'MIN_PLAYERS' };
    if (room.gamePhase === 'playing') return { ok: false, code: 'GAME_IN_PROGRESS' };
    return { ok: true };
  }

  /** 게임 시작 — gamePhase를 'playing'으로 전환 */
  startGame(roomId: string): RoomState | null {
    const room = this.rooms.get(roomId);
    if (!room) return null;
    room.gamePhase = 'playing';
    return room;
  }

  /** 초기 칩 유효성 검증 (만원 단위, per INFRA-06) */
  static validateChips(chips: number): boolean {
    return Number.isInteger(chips) && chips > 0 && chips % 10000 === 0;
  }

  /** 방 삭제 */
  deleteRoom(roomId: string): void {
    this.rooms.delete(roomId);
  }

  /** 전체 방 수 (디버그/테스트용) */
  get roomCount(): number {
    return this.rooms.size;
  }
}
```

**3) `packages/server/src/index.ts` — Socket.IO 서버 부트스트랩:**

```typescript
import { createServer } from 'http';
import { Server } from 'socket.io';
import type {
  ClientToServerEvents,
  ServerToClientEvents,
  InterServerEvents,
  SocketData,
} from '@sutda/shared';
import { RoomManager } from './room-manager.js';

const PORT = Number(process.env.PORT) || 3001;

const httpServer = createServer();
const io = new Server<
  ClientToServerEvents,
  ServerToClientEvents,
  InterServerEvents,
  SocketData
>(httpServer, {
  cors: {
    origin: process.env.CLIENT_ORIGIN || 'http://localhost:5173',
    methods: ['GET', 'POST'],
  },
});

const roomManager = new RoomManager();

// 에러 메시지 맵 (per UI-SPEC 에러 메시지 계약)
const ERROR_MESSAGES: Record<string, string> = {
  ROOM_NOT_FOUND: '존재하지 않는 방입니다. 링크를 다시 확인해주세요.',
  ROOM_FULL: '방이 가득 찼습니다. (최대 6명)',
  NICKNAME_TAKEN: '이미 사용 중인 닉네임입니다. 다른 닉네임을 입력해주세요.',
  NOT_HOST: '방장만 게임을 시작할 수 있습니다.',
  MIN_PLAYERS: '최소 2명이 필요합니다.',
  GAME_IN_PROGRESS: '게임이 이미 진행 중입니다.',
  INVALID_CHIPS: '초기 칩은 10,000원 단위로 입력해주세요.',
};

function emitError(socket: { emit: (event: 'error', data: { code: string; message: string }) => void }, code: string) {
  socket.emit('error', {
    code: code as any,
    message: ERROR_MESSAGES[code] || '알 수 없는 오류가 발생했습니다.',
  });
}

io.on('connection', (socket) => {
  // create-room 핸들러
  socket.on('create-room', ({ nickname, initialChips }) => {
    if (!RoomManager.validateChips(initialChips)) {
      return emitError(socket, 'INVALID_CHIPS');
    }
    const room = roomManager.createRoom(socket.id, nickname, initialChips);
    socket.data.playerId = socket.id;
    socket.data.nickname = nickname;
    socket.data.roomId = room.roomId;
    socket.join(room.roomId);
    socket.emit('room-created', { roomId: room.roomId, roomState: room });
  });

  // join-room 핸들러
  socket.on('join-room', ({ roomId, nickname, initialChips }) => {
    if (!RoomManager.validateChips(initialChips)) {
      return emitError(socket, 'INVALID_CHIPS');
    }
    try {
      const { room, player } = roomManager.joinRoom(roomId, socket.id, nickname, initialChips);
      socket.data.playerId = socket.id;
      socket.data.nickname = nickname;
      socket.data.roomId = roomId;
      socket.join(roomId);
      // 참여한 플레이어에게 전체 방 상태 전송
      socket.emit('room-state', room);
      // 기존 플레이어들에게 새 플레이어 알림
      socket.to(roomId).emit('player-joined', player);
    } catch (err: any) {
      emitError(socket, err.message);
    }
  });

  // leave-room 핸들러
  socket.on('leave-room', ({ roomId }) => {
    const result = roomManager.leaveRoom(roomId, socket.id);
    if (result) {
      socket.leave(roomId);
      io.to(roomId).emit('player-left', {
        playerId: result.removedPlayerId,
        newHostId: result.newHostId,
      });
    }
  });

  // start-game 핸들러
  socket.on('start-game', ({ roomId }) => {
    const check = roomManager.canStartGame(roomId, socket.id);
    if (!check.ok) {
      return emitError(socket, check.code);
    }
    const room = roomManager.startGame(roomId);
    if (room) {
      io.to(roomId).emit('room-state', room);
    }
  });

  // 연결 끊김 처리
  socket.on('disconnect', () => {
    const { roomId } = socket.data;
    if (roomId) {
      const room = roomManager.getRoom(roomId);
      if (room && room.gamePhase === 'playing') {
        // 게임 중이면 연결만 끊김 처리 (재접속 대기, per D-05)
        roomManager.disconnectPlayer(roomId, socket.id);
      } else {
        // 대기실이면 퇴장 처리
        const result = roomManager.leaveRoom(roomId, socket.id);
        if (result) {
          io.to(roomId).emit('player-left', {
            playerId: result.removedPlayerId,
            newHostId: result.newHostId,
          });
        }
      }
    }
  });
});

httpServer.listen(PORT, () => {
  console.log(`Sutda server listening on port ${PORT}`);
});

export { io, roomManager, httpServer };
```
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && npx turbo run build --filter=@sutda/server 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/server/package.json의 dependencies에 `socket.io` 가 포함된다
    - packages/server/src/room-manager.ts가 존재하고 `export class RoomManager` 를 포함한다
    - RoomManager에 createRoom, getRoom, joinRoom, leaveRoom, disconnectPlayer, canStartGame, startGame, validateChips 메서드가 있다
    - packages/server/src/index.ts에 `new Server<` 로 타입이 지정된 Socket.IO 서버가 생성된다
    - index.ts에 'create-room', 'join-room', 'leave-room', 'start-game', 'disconnect' 이벤트 핸들러가 등록되어 있다
    - ERROR_MESSAGES 맵에 ROOM_NOT_FOUND, ROOM_FULL, NICKNAME_TAKEN, NOT_HOST, MIN_PLAYERS, GAME_IN_PROGRESS, INVALID_CHIPS 7개 한국어 메시지가 있다
    - `npx turbo run build --filter=@sutda/server` 가 성공한다 (exit code 0)
  </acceptance_criteria>
  <done>Socket.IO 서버가 타입 안전하게 부트스트랩되고, RoomManager가 방 생성/참여/퇴장/재접속/게임시작 전체 로직을 처리한다</done>
</task>

</tasks>

<verification>
1. `npx turbo run build` 가 shared + server 모두 성공
2. `packages/shared/src/types/protocol.ts` 에 ClientToServerEvents/ServerToClientEvents 타입 존재
3. `packages/server/src/room-manager.ts` 에 RoomManager 클래스 존재
4. `packages/server/src/index.ts` 에 Socket.IO 서버 + 이벤트 핸들러 존재
</verification>

<success_criteria>
- shared 패키지에서 `import type { ClientToServerEvents, ServerToClientEvents, RoomState, RoomPlayer } from '@sutda/shared'` 가 가능하다
- server 패키지에 Socket.IO 4.x가 설치되어 있고 타입 안전한 서버가 구성되어 있다
- RoomManager가 INFRA-01(방 생성/고유 링크), INFRA-03(6명 제한), INFRA-04(서버 권위), INFRA-06(만원 단위 칩) 요건을 충족한다
</success_criteria>

<output>
완료 후 `.planning/phases/03-websocket/03-01-SUMMARY.md` 생성
</output>
