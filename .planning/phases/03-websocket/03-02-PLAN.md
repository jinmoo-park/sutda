---
phase: 03-websocket
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/server/src/room-manager.test.ts
  - packages/server/src/integration.test.ts
  - packages/server/vitest.config.ts
  - packages/server/package.json
  - packages/server/src/index.ts
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-05, INFRA-06]

must_haves:
  truths:
    - "방 생성 시 8자리 roomId가 반환되고 room-created 이벤트가 발행된다"
    - "닉네임만 입력하면 방에 참여할 수 있고 player-joined 이벤트가 발행된다"
    - "7번째 플레이어 참여 시 ROOM_FULL 에러가 반환된다"
    - "게임 중 연결이 끊긴 플레이어가 같은 닉네임으로 재접속하면 기존 자리에 복귀한다"
    - "만원 단위가 아닌 칩 입력 시 INVALID_CHIPS 에러가 반환된다"
  artifacts:
    - path: "packages/server/src/room-manager.test.ts"
      provides: "RoomManager 단위 테스트"
      min_lines: 80
    - path: "packages/server/src/integration.test.ts"
      provides: "Socket.IO 이벤트 통합 테스트"
      min_lines: 60
    - path: "packages/server/vitest.config.ts"
      provides: "서버 패키지 vitest 설정"
  key_links:
    - from: "packages/server/src/room-manager.test.ts"
      to: "packages/server/src/room-manager.ts"
      via: "import RoomManager"
      pattern: "import.*RoomManager"
    - from: "packages/server/src/integration.test.ts"
      to: "packages/server/src/index.ts"
      via: "Socket.IO 클라이언트 연결 테스트"
      pattern: "socket\\.io-client"
---

<objective>
RoomManager 단위 테스트와 Socket.IO 통합 테스트를 작성하여 INFRA-01~06 요건을 검증한다.

Purpose: 방 생성, 참여, 인원 제한, 재접속, 칩 검증 등 모든 핵심 시나리오를 자동화된 테스트로 보장한다.
Output: RoomManager 단위 테스트, Socket.IO 이벤트 통합 테스트, vitest 설정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-websocket/03-CONTEXT.md
@.planning/phases/03-websocket/03-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 핵심 인터페이스 -->

From packages/shared/src/types/room.ts:
```typescript
export interface RoomPlayer {
  id: string;
  nickname: string;
  chips: number;
  seatIndex: number;
  isConnected: boolean;
}

export interface RoomState {
  roomId: string;
  hostId: string;
  players: RoomPlayer[];
  maxPlayers: number;
  gamePhase: 'waiting' | 'playing';
  createdAt: number;
}
```

From packages/shared/src/types/protocol.ts:
```typescript
export interface ClientToServerEvents {
  'create-room': (data: { nickname: string; initialChips: number }) => void;
  'join-room': (data: { roomId: string; nickname: string; initialChips: number }) => void;
  'leave-room': (data: { roomId: string }) => void;
  'start-game': (data: { roomId: string }) => void;
}

export interface ServerToClientEvents {
  'room-created': (data: { roomId: string; roomState: RoomState }) => void;
  'room-state': (data: RoomState) => void;
  'player-joined': (data: RoomPlayer) => void;
  'player-left': (data: { playerId: string; newHostId?: string }) => void;
  'error': (data: ErrorPayload) => void;
}

export interface ErrorPayload {
  code: 'ROOM_NOT_FOUND' | 'ROOM_FULL' | 'NICKNAME_TAKEN' | 'NOT_HOST' | 'MIN_PLAYERS' | 'GAME_IN_PROGRESS' | 'INVALID_CHIPS';
  message: string;
}
```

From packages/server/src/room-manager.ts (public API):
```typescript
export class RoomManager {
  createRoom(hostId: string, nickname: string, initialChips: number): RoomState;
  getRoom(roomId: string): RoomState | undefined;
  joinRoom(roomId: string, playerId: string, nickname: string, initialChips: number): { room: RoomState; player: RoomPlayer };
  leaveRoom(roomId: string, playerId: string): { room: RoomState; removedPlayerId: string; newHostId?: string } | null;
  disconnectPlayer(roomId: string, playerId: string): void;
  canStartGame(roomId: string, playerId: string): { ok: true } | { ok: false; code: string };
  startGame(roomId: string): RoomState | null;
  static validateChips(chips: number): boolean;
  deleteRoom(roomId: string): void;
  get roomCount(): number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: vitest 설정 + RoomManager 단위 테스트</name>
  <files>packages/server/vitest.config.ts, packages/server/src/room-manager.test.ts, packages/server/package.json</files>
  <read_first>
    - packages/server/package.json (현재 스크립트 및 의존성)
    - packages/server/tsconfig.json (컴파일러 설정)
    - packages/server/src/room-manager.ts (테스트 대상 코드)
    - packages/shared/src/types/room.ts (RoomState, RoomPlayer 타입)
  </read_first>
  <action>
**1) `packages/server/vitest.config.ts` 생성:**

```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    include: ['src/**/*.test.ts'],
  },
});
```

**2) `packages/server/package.json` scripts에 test 추가:**

기존 scripts 섹션에 `"test": "vitest run"`, `"test:watch": "vitest"` 추가.

**3) `packages/server/src/room-manager.test.ts` 생성:**

다음 테스트 케이스를 모두 포함:

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { RoomManager } from './room-manager.js';

describe('RoomManager', () => {
  let rm: RoomManager;

  beforeEach(() => {
    rm = new RoomManager();
  });

  describe('createRoom', () => {
    it('8자리 roomId를 가진 방을 생성한다', () => {
      const room = rm.createRoom('host-1', '방장', 100000);
      expect(room.roomId).toHaveLength(8);
      expect(room.hostId).toBe('host-1');
      expect(room.players).toHaveLength(1);
      expect(room.players[0].nickname).toBe('방장');
      expect(room.players[0].chips).toBe(100000);
      expect(room.players[0].seatIndex).toBe(0);
      expect(room.gamePhase).toBe('waiting');
      expect(room.maxPlayers).toBe(6);
    });

    it('생성된 방을 getRoom으로 조회할 수 있다', () => {
      const room = rm.createRoom('host-1', '방장', 100000);
      expect(rm.getRoom(room.roomId)).toBe(room);
    });

    it('roomCount가 증가한다', () => {
      expect(rm.roomCount).toBe(0);
      rm.createRoom('h1', 'A', 100000);
      expect(rm.roomCount).toBe(1);
      rm.createRoom('h2', 'B', 100000);
      expect(rm.roomCount).toBe(2);
    });
  });

  describe('joinRoom', () => {
    it('방에 플레이어를 추가한다 (INFRA-02)', () => {
      const room = rm.createRoom('host', '방장', 100000);
      const { player } = rm.joinRoom(room.roomId, 'p2', '참여자', 50000);
      expect(player.nickname).toBe('참여자');
      expect(player.chips).toBe(50000);
      expect(player.seatIndex).toBe(1);
      expect(rm.getRoom(room.roomId)!.players).toHaveLength(2);
    });

    it('존재하지 않는 방에 참여하면 ROOM_NOT_FOUND 에러 (INFRA-02)', () => {
      expect(() => rm.joinRoom('nonexist', 'p1', 'A', 100000)).toThrow('ROOM_NOT_FOUND');
    });

    it('7번째 플레이어는 ROOM_FULL 에러 (INFRA-03)', () => {
      const room = rm.createRoom('h', '방장', 100000);
      for (let i = 1; i <= 5; i++) {
        rm.joinRoom(room.roomId, `p${i}`, `P${i}`, 100000);
      }
      expect(rm.getRoom(room.roomId)!.players).toHaveLength(6);
      expect(() => rm.joinRoom(room.roomId, 'p7', 'P7', 100000)).toThrow('ROOM_FULL');
    });

    it('대기실에서 동일 닉네임은 NICKNAME_TAKEN 에러 (D-06)', () => {
      const room = rm.createRoom('h', '방장', 100000);
      expect(() => rm.joinRoom(room.roomId, 'p2', '방장', 100000)).toThrow('NICKNAME_TAKEN');
    });

    it('게임 중 같은 닉네임으로 재접속하면 기존 자리로 복귀 (INFRA-05, D-05)', () => {
      const room = rm.createRoom('h', '방장', 100000);
      rm.joinRoom(room.roomId, 'p2', '참여자', 100000);
      rm.startGame(room.roomId);
      // 재접속: 새 소켓 ID로 같은 닉네임
      const { player } = rm.joinRoom(room.roomId, 'new-socket', '참여자', 100000);
      expect(player.id).toBe('new-socket');
      expect(player.seatIndex).toBe(1);
      expect(player.isConnected).toBe(true);
    });

    it('게임 중 새로운 닉네임은 GAME_IN_PROGRESS 에러 (D-05)', () => {
      const room = rm.createRoom('h', '방장', 100000);
      rm.joinRoom(room.roomId, 'p2', '참여자', 100000);
      rm.startGame(room.roomId);
      expect(() => rm.joinRoom(room.roomId, 'p3', '신규', 100000)).toThrow('GAME_IN_PROGRESS');
    });
  });

  describe('leaveRoom', () => {
    it('플레이어를 제거하고 seatIndex를 재정렬한다', () => {
      const room = rm.createRoom('h', 'A', 100000);
      rm.joinRoom(room.roomId, 'p2', 'B', 100000);
      rm.joinRoom(room.roomId, 'p3', 'C', 100000);
      rm.leaveRoom(room.roomId, 'p2');
      const updated = rm.getRoom(room.roomId)!;
      expect(updated.players).toHaveLength(2);
      expect(updated.players.map(p => p.seatIndex)).toEqual([0, 1]);
    });

    it('방장이 나가면 다음 플레이어가 방장이 된다 (D-12)', () => {
      const room = rm.createRoom('h', 'A', 100000);
      rm.joinRoom(room.roomId, 'p2', 'B', 100000);
      const result = rm.leaveRoom(room.roomId, 'h');
      expect(result!.newHostId).toBe('p2');
      expect(rm.getRoom(room.roomId)!.hostId).toBe('p2');
    });

    it('마지막 플레이어가 나가면 방이 삭제된다', () => {
      const room = rm.createRoom('h', 'A', 100000);
      rm.leaveRoom(room.roomId, 'h');
      expect(rm.getRoom(room.roomId)).toBeUndefined();
      expect(rm.roomCount).toBe(0);
    });
  });

  describe('disconnectPlayer', () => {
    it('isConnected를 false로 설정한다', () => {
      const room = rm.createRoom('h', 'A', 100000);
      rm.disconnectPlayer(room.roomId, 'h');
      expect(rm.getRoom(room.roomId)!.players[0].isConnected).toBe(false);
    });
  });

  describe('canStartGame', () => {
    it('방장이고 2명 이상이면 시작 가능', () => {
      const room = rm.createRoom('h', 'A', 100000);
      rm.joinRoom(room.roomId, 'p2', 'B', 100000);
      expect(rm.canStartGame(room.roomId, 'h')).toEqual({ ok: true });
    });

    it('방장이 아니면 NOT_HOST', () => {
      const room = rm.createRoom('h', 'A', 100000);
      rm.joinRoom(room.roomId, 'p2', 'B', 100000);
      expect(rm.canStartGame(room.roomId, 'p2')).toEqual({ ok: false, code: 'NOT_HOST' });
    });

    it('1명이면 MIN_PLAYERS', () => {
      const room = rm.createRoom('h', 'A', 100000);
      expect(rm.canStartGame(room.roomId, 'h')).toEqual({ ok: false, code: 'MIN_PLAYERS' });
    });

    it('이미 게임 중이면 GAME_IN_PROGRESS', () => {
      const room = rm.createRoom('h', 'A', 100000);
      rm.joinRoom(room.roomId, 'p2', 'B', 100000);
      rm.startGame(room.roomId);
      expect(rm.canStartGame(room.roomId, 'h')).toEqual({ ok: false, code: 'GAME_IN_PROGRESS' });
    });
  });

  describe('validateChips (INFRA-06)', () => {
    it('만원 단위는 유효', () => {
      expect(RoomManager.validateChips(10000)).toBe(true);
      expect(RoomManager.validateChips(100000)).toBe(true);
      expect(RoomManager.validateChips(50000)).toBe(true);
    });

    it('만원 단위가 아니면 무효', () => {
      expect(RoomManager.validateChips(5000)).toBe(false);
      expect(RoomManager.validateChips(15000)).toBe(false);
      expect(RoomManager.validateChips(0)).toBe(false);
      expect(RoomManager.validateChips(-10000)).toBe(false);
      expect(RoomManager.validateChips(10001)).toBe(false);
    });
  });
});
```
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda/packages/server" && npx vitest run src/room-manager.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/server/vitest.config.ts 파일이 존재하고 `defineConfig` 를 사용한다
    - packages/server/package.json에 `"test": "vitest run"` 스크립트가 있다
    - packages/server/src/room-manager.test.ts가 존재한다
    - 테스트 파일에 `describe('createRoom'`, `describe('joinRoom'`, `describe('leaveRoom'`, `describe('canStartGame'`, `describe('validateChips'` 블록이 있다
    - ROOM_FULL 테스트가 6명 채운 후 7번째 참여 시도를 검증한다
    - NICKNAME_TAKEN 테스트가 대기실에서 동일 닉네임을 검증한다
    - 재접속 테스트가 게임 중 같은 닉네임으로 새 소켓 ID 복귀를 검증한다
    - `npx vitest run src/room-manager.test.ts` 가 모든 테스트 통과한다
  </acceptance_criteria>
  <done>RoomManager의 모든 공개 메서드가 테스트로 검증되고, INFRA-01~06 요건에 해당하는 시나리오가 커버된다</done>
</task>

<task type="auto">
  <name>Task 2: Socket.IO 통합 테스트</name>
  <files>packages/server/src/integration.test.ts, packages/server/package.json, packages/server/src/index.ts</files>
  <read_first>
    - packages/server/src/index.ts (서버 진입점, export된 io/httpServer/roomManager)
    - packages/server/src/room-manager.ts (RoomManager API)
    - packages/shared/src/types/protocol.ts (이벤트 타입)
    - packages/shared/src/types/room.ts (RoomState, RoomPlayer 타입)
    - packages/server/vitest.config.ts (테스트 설정)
  </read_first>
  <action>
**1) socket.io-client 개발 의존성 추가:**

```bash
cd packages/server && pnpm add -D socket.io-client
```

**2) `packages/server/src/index.ts` 수정 (포트 충돌 방지):**

index.ts 하단의 `httpServer.listen(PORT, ...)` 를 NODE_ENV 조건부로 변경한다. 이 변경이 통합 테스트 파일 작성보다 **먼저** 수행되어야 한다. 변경하지 않으면 테스트 시 EADDRINUSE 포트 충돌이 발생한다.

```typescript
// index.ts 하단 수정 — 기존 httpServer.listen(...) 호출을 아래로 교체
if (process.env.NODE_ENV !== 'test') {
  httpServer.listen(PORT, () => {
    console.log(`Sutda server listening on port ${PORT}`);
  });
}
```

**3) `packages/server/src/integration.test.ts` 생성:**

Socket.IO 클라이언트로 실제 서버에 연결하여 이벤트 플로우를 검증한다.

```typescript
import { describe, it, expect, beforeAll, afterAll, afterEach } from 'vitest';
import { io as ioc, Socket as ClientSocket } from 'socket.io-client';
import type { ServerToClientEvents, ClientToServerEvents, RoomState } from '@sutda/shared';
import { httpServer, roomManager } from './index.js';

type TestClient = ClientSocket<ServerToClientEvents, ClientToServerEvents>;

const PORT = 0; // OS가 빈 포트 할당
let serverUrl: string;

function createClient(): TestClient {
  return ioc(serverUrl, {
    transports: ['websocket'],
    forceNew: true,
  });
}

function waitForEvent<T>(socket: TestClient, event: string): Promise<T> {
  return new Promise((resolve) => {
    (socket as any).once(event, (data: T) => resolve(data));
  });
}

beforeAll((done) => {
  httpServer.listen(0, () => {
    const addr = httpServer.address();
    if (addr && typeof addr !== 'string') {
      serverUrl = `http://localhost:${addr.port}`;
    }
    done();
  });
});

afterAll((done) => {
  httpServer.close(done);
});

describe('Socket.IO 통합 테스트', () => {
  const clients: TestClient[] = [];

  afterEach(() => {
    clients.forEach(c => c.disconnect());
    clients.length = 0;
  });

  it('방 생성 시 room-created 이벤트를 수신한다 (INFRA-01)', async () => {
    const client = createClient();
    clients.push(client);

    await new Promise<void>((resolve) => client.on('connect', resolve));

    client.emit('create-room', { nickname: '방장', initialChips: 100000 });
    const data = await waitForEvent<{ roomId: string; roomState: RoomState }>(client, 'room-created');

    expect(data.roomId).toHaveLength(8);
    expect(data.roomState.hostId).toBeTruthy();
    expect(data.roomState.players).toHaveLength(1);
    expect(data.roomState.players[0].nickname).toBe('방장');
  });

  it('다른 플레이어가 방에 참여하면 player-joined 이벤트를 수신한다 (INFRA-02)', async () => {
    const host = createClient();
    const joiner = createClient();
    clients.push(host, joiner);

    await new Promise<void>((resolve) => host.on('connect', resolve));
    host.emit('create-room', { nickname: '방장', initialChips: 100000 });
    const { roomId } = await waitForEvent<{ roomId: string }>(host, 'room-created');

    await new Promise<void>((resolve) => joiner.on('connect', resolve));

    const joinedPromise = waitForEvent<any>(host, 'player-joined');
    joiner.emit('join-room', { roomId, nickname: '참여자', initialChips: 50000 });
    const joined = await joinedPromise;

    expect(joined.nickname).toBe('참여자');
    expect(joined.chips).toBe(50000);
  });

  it('만원 단위가 아닌 칩으로 방 생성 시 INVALID_CHIPS 에러 (INFRA-06)', async () => {
    const client = createClient();
    clients.push(client);

    await new Promise<void>((resolve) => client.on('connect', resolve));

    client.emit('create-room', { nickname: '방장', initialChips: 5000 });
    const error = await waitForEvent<{ code: string; message: string }>(client, 'error');

    expect(error.code).toBe('INVALID_CHIPS');
    expect(error.message).toContain('10,000원');
  });

  it('존재하지 않는 방에 참여하면 ROOM_NOT_FOUND 에러', async () => {
    const client = createClient();
    clients.push(client);

    await new Promise<void>((resolve) => client.on('connect', resolve));

    client.emit('join-room', { roomId: 'nonexist', nickname: 'A', initialChips: 100000 });
    const error = await waitForEvent<{ code: string }>(client, 'error');

    expect(error.code).toBe('ROOM_NOT_FOUND');
  });

  it('방장만 게임을 시작할 수 있다 (D-11)', async () => {
    const host = createClient();
    const joiner = createClient();
    clients.push(host, joiner);

    await new Promise<void>((resolve) => host.on('connect', resolve));
    host.emit('create-room', { nickname: '방장', initialChips: 100000 });
    const { roomId } = await waitForEvent<{ roomId: string }>(host, 'room-created');

    await new Promise<void>((resolve) => joiner.on('connect', resolve));
    joiner.emit('join-room', { roomId, nickname: '참여자', initialChips: 100000 });
    await waitForEvent<any>(joiner, 'room-state');

    // 비방장이 시작 시도
    joiner.emit('start-game', { roomId });
    const error = await waitForEvent<{ code: string }>(joiner, 'error');
    expect(error.code).toBe('NOT_HOST');

    // 방장이 시작
    const statePromise = waitForEvent<RoomState>(host, 'room-state');
    host.emit('start-game', { roomId });
    const state = await statePromise;
    expect(state.gamePhase).toBe('playing');
  });
});
```

**중요 구현 참고:**
- `httpServer.listen(0)` 으로 테스트 시 빈 포트 사용 -- 기존 서버와 충돌 방지
- `beforeAll`/`afterAll` 에서 httpServer를 시작/종료하되, index.ts에서 이미 `httpServer.listen(PORT)` 를 호출하므로, index.ts를 수정하여 테스트 시 자동 listen을 방지해야 할 수 있음
- **해결 방법:** 위 Step 2에서 index.ts 하단의 `httpServer.listen(PORT, ...)` 를 조건부로 변경 완료
- vitest.config.ts에 `env: { NODE_ENV: 'test' }` 설정 추가가 필요할 수 있으며, 또는 vitest가 자동으로 NODE_ENV=test를 설정하는지 확인한다.
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda/packages/server" && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/server/src/integration.test.ts 파일이 존재한다
    - 통합 테스트에 'room-created', 'player-joined', 'INVALID_CHIPS', 'ROOM_NOT_FOUND', 'NOT_HOST' 시나리오가 포함된다
    - packages/server/package.json devDependencies에 `socket.io-client` 가 포함된다
    - packages/server/src/index.ts에 `process.env.NODE_ENV !== 'test'` 조건부 listen이 있다
    - `npx vitest run` 이 room-manager.test.ts와 integration.test.ts 모두 통과한다
  </acceptance_criteria>
  <done>단위 테스트와 통합 테스트가 모두 통과하여 방 생성, 참여, 에러 처리, 게임 시작 플로우가 검증된다</done>
</task>

</tasks>

<verification>
1. `cd packages/server && npx vitest run` -- 모든 테스트 통과
2. room-manager.test.ts: 방 생성(8자리 ID), 참여, 6명 제한, 닉네임 중복, 재접속 복귀, 방장 승계, 칩 검증 테스트 통과
3. integration.test.ts: Socket.IO 이벤트 room-created, player-joined, error 수신 테스트 통과
</verification>

<success_criteria>
- RoomManager 단위 테스트가 INFRA-01(방 생성), INFRA-02(참여), INFRA-03(6명 제한), INFRA-05(재접속), INFRA-06(만원 단위 칩) 전체를 커버한다
- Socket.IO 통합 테스트가 실제 WebSocket 연결을 통해 이벤트 프로토콜을 검증한다
- 모든 테스트가 `npx vitest run` 으로 60초 이내에 완료된다
</success_criteria>

<output>
완료 후 `.planning/phases/03-websocket/03-02-SUMMARY.md` 생성
</output>
