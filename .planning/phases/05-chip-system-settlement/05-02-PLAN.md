---
phase: 05-chip-system-settlement
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/server/src/room-manager.ts
  - packages/server/src/room-manager.test.ts
  - packages/server/src/index.ts
  - packages/server/src/index.test.ts
autonomous: true
requirements: [CHIP-03, CHIP-05]

must_haves:
  truths:
    - "플레이어가 재충전 요청을 보내면 다른 모든 플레이어에게 투표 팝업이 전달된다"
    - "전원 동의 시 요청자의 칩이 충전되고 전체에게 결과가 브로드캐스트된다"
    - "1명이라도 거부하면 즉시 재충전이 취소되고 전체에게 결과가 브로드캐스트된다"
    - "베팅 시 유효 스택 상한을 초과하는 금액은 INVALID_ACTION 에러가 발생한다"
    - "칩 정산 결과가 Socket.IO game-state 브로드캐스트에 반영된다"
  artifacts:
    - path: "packages/server/src/room-manager.ts"
      provides: "재충전 요청/투표 관리 메서드"
      contains: "requestRecharge"
    - path: "packages/server/src/index.ts"
      provides: "recharge-request, recharge-vote Socket.IO 핸들러"
      contains: "recharge-request"
    - path: "packages/server/src/room-manager.test.ts"
      provides: "재충전 플로우 단위 테스트"
      contains: "recharge"
    - path: "packages/server/src/index.test.ts"
      provides: "재충전 Socket.IO 통합 테스트"
      contains: "recharge"
  key_links:
    - from: "packages/server/src/index.ts"
      to: "packages/server/src/room-manager.ts"
      via: "recharge-request 핸들러에서 roomManager.requestRecharge 호출"
      pattern: "roomManager\\.requestRecharge"
    - from: "packages/server/src/index.ts"
      to: "packages/server/src/room-manager.ts"
      via: "recharge-vote 핸들러에서 roomManager.processRechargeVote 호출"
      pattern: "roomManager\\.processRechargeVote"
    - from: "packages/server/src/index.ts"
      to: "packages/server/src/game-engine.ts"
      via: "bet-action에서 effectiveMaxBet 검증"
      pattern: "effectiveMaxBet"
---

<objective>
재충전 투표 플로우와 Socket.IO 핸들러 연결, 유효 스택 상한 서버 검증을 구현한다.

Purpose: 칩 재충전(전원 동의 투표)이 서버에서 올바르게 관리되고, 베팅 시 유효 스택 상한이 서버에서 검증되며, 모든 칩 변동이 클라이언트에 브로드캐스트된다.
Output: RoomManager 재충전 메서드, Socket.IO 핸들러, 유효 스택 검증, 통합 테스트
</objective>

<execution_context>
@.planning/phases/05-chip-system-settlement/05-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-chip-system-settlement/05-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 추가된 타입 (executor는 이 타입들이 이미 존재한다고 가정) -->

From packages/shared/src/types/game.ts (Plan 01 이후):
```typescript
export interface ChipBreakdown {
  ten_thousand: number;
  five_thousand: number;
  one_thousand: number;
  five_hundred: number;
}

// PlayerState에 추가됨:
chipBreakdown: ChipBreakdown;

// GameState에 추가됨:
effectiveMaxBet?: number;
```

From packages/shared/src/types/protocol.ts (Plan 01 이후):
```typescript
// ClientToServerEvents에 추가됨:
'recharge-request': (data: { roomId: string; amount: number }) => void;
'recharge-vote': (data: { roomId: string; approved: boolean }) => void;

// ServerToClientEvents에 추가됨:
'recharge-requested': (data: { requesterId: string; requesterNickname: string; amount: number }) => void;
'recharge-vote-update': (data: { votedCount: number; totalNeeded: number; approved: boolean }) => void;
'recharge-result': (data: { requesterId: string; approved: boolean; newChips?: number }) => void;
```

From packages/server/src/game-engine.ts (Plan 01 이후):
```typescript
static calculateChipBreakdown(chips: number): ChipBreakdown;
calculateEffectiveMaxBet(playerId: string): number;
```

From packages/server/src/room-manager.ts (현재):
```typescript
export class RoomManager {
  private rooms: Map<string, RoomState>;
  createRoom(hostId, nickname, initialChips): RoomState;
  getRoom(roomId): RoomState | undefined;
  joinRoom(roomId, playerId, nickname, initialChips): { room, player };
  leaveRoom(roomId, playerId): { room, removedPlayerId, newHostId? } | null;
  disconnectPlayer(roomId, playerId): void;
  canStartGame(roomId, playerId): { ok: true } | { ok: false; code: string };
  startGame(roomId): RoomState | null;
  static validateChips(chips: number): boolean;
}
```

From packages/server/src/index.ts (현재):
```typescript
// handleGameAction 패턴:
function handleGameAction(socket, roomId, action: () => void): void {
  try { action(); const engine = getEngine(roomId); io.to(roomId).emit('game-state', engine.getState()); }
  catch (err) { socket.emit('game-error', ...); }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RoomManager 재충전 플로우 + 유효 스택 검증 로직</name>
  <files>
    packages/server/src/room-manager.ts,
    packages/server/src/room-manager.test.ts
  </files>
  <read_first>
    packages/server/src/room-manager.ts,
    packages/server/src/room-manager.test.ts,
    packages/shared/src/types/room.ts,
    packages/shared/src/types/protocol.ts
  </read_first>
  <behavior>
    - Test: requestRecharge — 유효한 요청 시 rechargeRequest 상태가 생성된다
    - Test: requestRecharge — 이미 진행 중인 재충전 요청이 있으면 에러 throw('RECHARGE_IN_PROGRESS')
    - Test: requestRecharge — amount가 만원 단위가 아니면 에러 throw('INVALID_CHIPS')
    - Test: processRechargeVote — 전원 동의(approved=true) 시 { complete: true, approved: true } 반환
    - Test: processRechargeVote — 1명 거부(approved=false) 시 즉시 { complete: true, approved: false } 반환
    - Test: processRechargeVote — 아직 투표 완료 아닌 경우 { complete: false, votedCount, totalNeeded } 반환
    - Test: applyRecharge — 승인 시 요청자 chips += amount, chipBreakdown 갱신 불필요(RoomPlayer에는 chipBreakdown 없음)
    - Test: 요청자 본인은 투표 불가 (투표 대상에서 제외)
  </behavior>
  <action>
    **RoomManager에 재충전 관련 상태 및 메서드 추가:**

    private 필드 추가:
    ```typescript
    private rechargeRequests: Map<string, {
      requesterId: string;
      amount: number;
      votes: Map<string, boolean>;  // playerId -> approved
      totalNeeded: number;  // 요청자 제외 전체 인원 수
    }> = new Map();
    ```

    `requestRecharge(roomId: string, requesterId: string, amount: number)` 메서드:
    - room 존재 확인 (없으면 throw 'ROOM_NOT_FOUND')
    - 이미 진행 중인 요청 확인 (있으면 throw 'RECHARGE_IN_PROGRESS')
    - amount 만원 단위 확인 (RoomManager.validateChips 재사용, 실패 시 throw 'INVALID_CHIPS')
    - rechargeRequests에 저장: requesterId, amount, 빈 votes Map, totalNeeded = room.players.length - 1
    - 반환: { requesterId, requesterNickname, amount, totalNeeded }
      - requesterNickname은 room.players에서 requesterId로 찾기

    `processRechargeVote(roomId: string, voterId: string, approved: boolean)` 메서드:
    - rechargeRequest 존재 확인 (없으면 throw 'RECHARGE_NOT_FOUND')
    - voterId === requesterId면 throw 'INVALID_ACTION' (자기 투표 불가)
    - votes에 voterId: approved 저장
    - approved === false면 즉시 완료: rechargeRequests.delete(roomId), return { complete: true, approved: false }
    - 모든 투표 완료 확인 (votes.size === totalNeeded):
      - true면 return { complete: true, approved: true }
    - 아직 미완료면 return { complete: false, votedCount: votes.size, totalNeeded }

    `applyRecharge(roomId: string)` 메서드:
    - rechargeRequest 존재 확인
    - room.players에서 requesterId 찾아 chips += amount
    - rechargeRequests.delete(roomId)
    - 반환: { requesterId, newChips: player.chips }

    **테스트 작성 (room-manager.test.ts에 'describe("재충전 플로우")' 추가):**
    위 behavior 목록 전체 커버.
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/server test -- --run --grep "재충전"</automated>
  </verify>
  <acceptance_criteria>
    - packages/server/src/room-manager.ts에 `requestRecharge` 메서드 존재
    - packages/server/src/room-manager.ts에 `processRechargeVote` 메서드 존재
    - packages/server/src/room-manager.ts에 `applyRecharge` 메서드 존재
    - packages/server/src/room-manager.ts에 `private rechargeRequests: Map` 필드 존재
    - packages/server/src/room-manager.test.ts에 `재충전` describe 블록 존재
    - `pnpm --filter @sutda/server test -- --run --grep "재충전"` 통과
  </acceptance_criteria>
  <done>
    RoomManager에 재충전 요청/투표/적용 메서드가 구현되고 모든 단위 테스트가 통과한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: Socket.IO 핸들러 연결 + 유효 스택 서버 검증 + 통합 테스트</name>
  <files>
    packages/server/src/index.ts,
    packages/server/src/index.test.ts
  </files>
  <read_first>
    packages/server/src/index.ts,
    packages/server/src/index.test.ts,
    packages/server/src/room-manager.ts,
    packages/server/src/game-engine.ts,
    packages/shared/src/types/protocol.ts
  </read_first>
  <action>
    **1. packages/server/src/index.ts 수정:**

    ERROR_MESSAGES에 추가:
    ```typescript
    RECHARGE_IN_PROGRESS: '이미 재충전 요청이 진행 중입니다.',
    RECHARGE_NOT_FOUND: '재충전 요청을 찾을 수 없습니다.',
    INSUFFICIENT_CHIPS: '칩이 부족합니다.',
    ```

    `recharge-request` 핸들러 추가 (io.on('connection') 내부):
    ```typescript
    socket.on('recharge-request', ({ roomId, amount }) => {
      try {
        const result = roomManager.requestRecharge(roomId, socket.data.playerId, amount);
        // 요청자 제외 다른 플레이어에게 투표 요청 전송
        socket.to(roomId).emit('recharge-requested', {
          requesterId: result.requesterId,
          requesterNickname: result.requesterNickname,
          amount: result.amount,
        });
        // 요청자에게는 투표 진행 상태 전송
        socket.emit('recharge-vote-update', {
          votedCount: 0,
          totalNeeded: result.totalNeeded,
          approved: true,
        });
      } catch (err: any) {
        socket.emit('game-error', {
          code: err.message || 'UNKNOWN_ERROR',
          message: ERROR_MESSAGES[err.message] || err.message || '알 수 없는 오류',
        });
      }
    });
    ```

    `recharge-vote` 핸들러 추가:
    ```typescript
    socket.on('recharge-vote', ({ roomId, approved }) => {
      try {
        const result = roomManager.processRechargeVote(roomId, socket.data.playerId, approved);
        if (result.complete) {
          if (result.approved) {
            const rechargeResult = roomManager.applyRecharge(roomId);
            io.to(roomId).emit('recharge-result', {
              requesterId: rechargeResult.requesterId,
              approved: true,
              newChips: rechargeResult.newChips,
            });
            // 게임 엔진의 PlayerState에도 칩 반영
            const engine = gameEngines.get(roomId);
            if (engine) {
              const gameState = engine.getState() as GameState;
              const gamePlayer = gameState.players.find(p => p.id === rechargeResult.requesterId);
              if (gamePlayer) {
                (gamePlayer as any).chips = rechargeResult.newChips;
                (gamePlayer as any).chipBreakdown = GameEngine.calculateChipBreakdown(rechargeResult.newChips);
              }
              io.to(roomId).emit('game-state', engine.getState() as GameState);
            }
          } else {
            const request = (roomManager as any).rechargeRequests?.get(roomId);
            io.to(roomId).emit('recharge-result', {
              requesterId: socket.data.playerId, // 거부 시에는 요청자 ID가 필요 — requestRecharge에서 저장한 값을 processRechargeVote에서 반환하도록 함
              approved: false,
            });
          }
        } else {
          // 투표 진행 상태 업데이트 (방 전체에)
          io.to(roomId).emit('recharge-vote-update', {
            votedCount: result.votedCount!,
            totalNeeded: result.totalNeeded!,
            approved: true,
          });
        }
      } catch (err: any) {
        socket.emit('game-error', {
          code: err.message || 'UNKNOWN_ERROR',
          message: ERROR_MESSAGES[err.message] || err.message || '알 수 없는 오류',
        });
      }
    });
    ```

    NOTE: processRechargeVote 반환값에서 거부 시에도 requesterId를 반환하도록 RoomManager 메서드를 수정해야 한다. 반환 타입:
    ```typescript
    { complete: true; approved: boolean; requesterId: string }
    | { complete: false; votedCount: number; totalNeeded: number }
    ```

    **2. bet-action 핸들러에 유효 스택 검증 추가:**

    기존 bet-action 핸들러의 handleGameAction 콜백 내에서, 레이즈 시 effectiveMaxBet 초과 여부를 검증:
    ```typescript
    socket.on('bet-action', ({ roomId, action }) => {
      handleGameAction(socket, roomId, () => {
        const engine = getEngine(roomId);
        if (action.type === 'raise') {
          const currentPlayer = (engine.getState() as GameState).players.find(
            p => p.seatIndex === (engine.getState() as GameState).currentPlayerIndex
          );
          if (currentPlayer) {
            const effectiveMax = engine.calculateEffectiveMaxBet(currentPlayer.id);
            const callAmount = (engine.getState() as GameState).currentBetAmount - currentPlayer.currentBet;
            const totalNeeded = callAmount + action.amount;
            if (totalNeeded > effectiveMax) {
              throw new Error('INSUFFICIENT_CHIPS');
            }
          }
        }
        engine.processBetAction(socket.data.playerId, action);
      });
    });
    ```

    **3. GameEngine import에 추가 (이미 있으면 확인만):**
    ```typescript
    import { GameEngine } from './game-engine.js';
    ```
    — 이미 존재함. `calculateChipBreakdown` static 메서드 접근 확인.

    **4. 통합 테스트 (index.test.ts에 추가):**

    `describe('재충전 플로우 통합')`:
    - 방 생성 + 2명 참여 + 게임 시작
    - player1이 recharge-request 전송 (amount: 50000)
    - player2가 recharge-requested 이벤트 수신 확인
    - player2가 recharge-vote (approved: true) 전송
    - 전원 recharge-result 수신 확인 (approved: true, newChips 포함)

    `describe('재충전 거부 통합')`:
    - player2가 recharge-vote (approved: false) 전송
    - 전원 recharge-result 수신 확인 (approved: false)

    `describe('유효 스택 상한 초과 레이즈 거부')`:
    - 게임 진행 후 베팅 단계에서 상대방 잔액 초과 레이즈 시도
    - game-error 이벤트 수신 확인 (INSUFFICIENT_CHIPS)
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/server test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - packages/server/src/index.ts에 `socket.on('recharge-request'` 핸들러 존재
    - packages/server/src/index.ts에 `socket.on('recharge-vote'` 핸들러 존재
    - packages/server/src/index.ts에 `RECHARGE_IN_PROGRESS` 에러 메시지 존재
    - packages/server/src/index.ts에 `INSUFFICIENT_CHIPS` 에러 메시지 존재
    - packages/server/src/index.ts의 bet-action 핸들러에 `effectiveMaxBet` 또는 `calculateEffectiveMaxBet` 검증 로직 존재
    - packages/server/src/index.test.ts에 `재충전` 관련 테스트 존재
    - `pnpm --filter @sutda/server test -- --run` 전체 통과 (exit code 0)
  </acceptance_criteria>
  <done>
    재충전 요청/투표/결과가 Socket.IO를 통해 올바르게 브로드캐스트되고, 유효 스택 상한 초과 베팅이 서버에서 거부되며, 모든 통합 테스트가 통과한다.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @sutda/shared build` 성공
- `pnpm --filter @sutda/server test -- --run` 전체 통과
- 재충전 플로우: 요청 -> 투표 -> 결과 전체 흐름 테스트 통과
- 유효 스택 상한 초과 레이즈 거부 테스트 통과
- 기존 모든 테스트(게임 엔진, 방 관리, 통합) 영향 없음
</verification>

<success_criteria>
- 재충전 요청/투표 메서드가 RoomManager에 구현되고 단위 테스트 통과
- Socket.IO 핸들러가 recharge-request, recharge-vote 이벤트를 처리
- 유효 스택 상한이 서버에서 검증됨
- 전체 테스트 스위트 통과
</success_criteria>

<output>
완료 후 `.planning/phases/05-chip-system-settlement/05-02-SUMMARY.md` 생성
</output>
