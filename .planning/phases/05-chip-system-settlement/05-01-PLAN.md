---
phase: 05-chip-system-settlement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/game.ts
  - packages/shared/src/types/protocol.ts
  - packages/shared/src/types/index.ts
  - packages/shared/src/index.ts
  - packages/server/src/game-engine.ts
  - packages/server/src/game-engine.test.ts
autonomous: true
requirements: [CHIP-01, CHIP-02, CHIP-04, CHIP-05]

must_haves:
  truths:
    - "승자 결정 시 pot 금액이 승자의 chips에 합산되고 패배자의 chips가 정확히 차감된다"
    - "등교(앤티) 시 각 플레이어의 chips가 500원 차감된다"
    - "베팅(콜/레이즈) 시 플레이어의 chips가 정확히 차감된다"
    - "chipBreakdown이 큰 단위부터 그리디 방식으로 올바르게 계산된다"
    - "effectiveMaxBet이 생존자 기준 유효 스택 상한으로 정확히 계산된다"
    - "동점 재경기 시 pot이 유지되고 칩 이동이 발생하지 않는다"
  artifacts:
    - path: "packages/shared/src/types/game.ts"
      provides: "ChipBreakdown 인터페이스, PlayerState.chipBreakdown 필드, GameState.effectiveMaxBet 필드"
      contains: "interface ChipBreakdown"
    - path: "packages/shared/src/types/protocol.ts"
      provides: "recharge 관련 이벤트 타입 추가"
      contains: "recharge-request"
    - path: "packages/server/src/game-engine.ts"
      provides: "settleChips, calculateChipBreakdown, calculateEffectiveMaxBet 메서드"
      contains: "settleChips"
    - path: "packages/server/src/game-engine.test.ts"
      provides: "칩 정산, chipBreakdown, effectiveMaxBet 테스트"
      contains: "settleChips"
  key_links:
    - from: "packages/server/src/game-engine.ts"
      to: "packages/shared/src/types/game.ts"
      via: "ChipBreakdown import"
      pattern: "ChipBreakdown"
    - from: "GameEngine._resolveShowdown"
      to: "GameEngine.settleChips"
      via: "승자 결정 직후 호출"
      pattern: "this\\.settleChips"
---

<objective>
칩 정산 로직과 칩 관련 타입 확장을 구현한다.

Purpose: 판 결과에 따른 칩 이동(정산), 칩 잔액 추적, chipBreakdown 계산, effectiveMaxBet 계산이 올바르게 작동하도록 한다.
Output: 확장된 shared 타입, GameEngine 칩 정산 메서드, 칩 차감/합산 로직, TDD 테스트
</objective>

<execution_context>
@.planning/phases/05-chip-system-settlement/05-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- 현재 GameEngine 핵심 인터페이스 -->

From packages/shared/src/types/game.ts:
```typescript
export interface PlayerState {
  id: string;
  nickname: string;
  chips: number;
  cards: Card[];
  isAlive: boolean;
  isRevealed: boolean;
  currentBet: number;
  isDealer: boolean;
  seatIndex: number;
}

export interface GameState {
  roomId: string;
  phase: GamePhase;
  mode: GameMode;
  players: PlayerState[];
  pot: number;
  currentPlayerIndex: number;
  currentBetAmount: number;
  roundNumber: number;
  deck: Card[];
  dealerSelectCards?: { playerId: string; cardIndex: number }[];
  isTtong: boolean;
  attendedPlayerIds: string[];
  winnerId?: string;
  tiedPlayerIds?: string[];
}
```

From packages/server/src/game-engine.ts:
```typescript
// 정산 삽입 지점:
// - _resolveShowdown() 내 this.state.winnerId = best.player.id 직후
// - attendSchool() 내 this.state.pot += 500 직전 (chips 차감)
// - processBetAction() 내 콜/레이즈 금액 pot 추가 시 (chips 차감)

// 기존 processBetAction의 콜 로직:
case 'call': {
  const callAmount = this.state.currentBetAmount - player.currentBet;
  player.currentBet += callAmount;
  this.state.pot += callAmount;
  break;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shared 타입 확장 + GameEngine 칩 정산/계산 메서드 구현</name>
  <files>
    packages/shared/src/types/game.ts,
    packages/shared/src/types/protocol.ts,
    packages/shared/src/types/index.ts,
    packages/shared/src/index.ts,
    packages/server/src/game-engine.ts,
    packages/server/src/game-engine.test.ts
  </files>
  <read_first>
    packages/shared/src/types/game.ts,
    packages/shared/src/types/protocol.ts,
    packages/shared/src/types/index.ts,
    packages/shared/src/index.ts,
    packages/server/src/game-engine.ts,
    packages/server/src/game-engine.test.ts
  </read_first>
  <behavior>
    - Test: calculateChipBreakdown(13500) => { ten_thousand: 1, five_thousand: 0, one_thousand: 3, five_hundred: 1 }
    - Test: calculateChipBreakdown(0) => { ten_thousand: 0, five_thousand: 0, one_thousand: 0, five_hundred: 0 }
    - Test: calculateChipBreakdown(500) => { ten_thousand: 0, five_thousand: 0, one_thousand: 0, five_hundred: 1 }
    - Test: calculateChipBreakdown(25500) => { ten_thousand: 2, five_thousand: 1, one_thousand: 0, five_hundred: 1 }
    - Test: attendSchool시 플레이어 chips가 500원 차감된다 (100000 -> 99500)
    - Test: processBetAction 콜시 callAmount만큼 chips가 차감된다
    - Test: processBetAction 레이즈시 callAmount + raiseAmount만큼 chips가 차감된다
    - Test: 승자 결정 시(winnerId 세팅) pot 전액이 승자 chips에 합산된다
    - Test: 승자 결정 후 pot이 0으로 리셋되지 않는다 (result phase에서 표시 목적으로 유지, nextRound에서 리셋)
    - Test: 동점 재경기(rematch-pending) 시 chips 이동 없음, pot 유지
    - Test: 생존자 1명만 남아 result phase 진입 시에도 pot이 승자에게 합산
    - Test: calculateEffectiveMaxBet — 내 잔액 < 상대 최대 잔액이면 내 잔액 반환
    - Test: calculateEffectiveMaxBet — 내 잔액 >= 상대 최대 잔액이면 생존자 중 두 번째로 큰 잔액 반환
    - Test: calculateEffectiveMaxBet — 다이한 플레이어는 계산에서 제외
    - Test: calculateEffectiveMaxBet — 생존자 1명(나만)이면 0 반환
    - Test: 각 플레이어의 chipBreakdown이 game-state에 포함된다
    - Test: effectiveMaxBet이 game-state에 포함된다
  </behavior>
  <action>
    **1. packages/shared/src/types/game.ts 수정:**

    ChipBreakdown 인터페이스 추가:
    ```typescript
    export interface ChipBreakdown {
      ten_thousand: number;
      five_thousand: number;
      one_thousand: number;
      five_hundred: number;
    }
    ```

    PlayerState에 chipBreakdown 필드 추가:
    ```typescript
    chipBreakdown: ChipBreakdown;  // 칩 단위별 개수 (per CHIP-04, D-12)
    ```

    GameState에 effectiveMaxBet 필드 추가:
    ```typescript
    effectiveMaxBet?: number;  // 현재 턴 플레이어의 유효 스택 상한 (per D-11)
    ```

    **2. packages/shared/src/types/protocol.ts 수정:**

    ErrorPayload.code에 추가:
    ```typescript
    | 'RECHARGE_IN_PROGRESS'
    | 'RECHARGE_NOT_FOUND'
    | 'INSUFFICIENT_CHIPS'
    ```

    ClientToServerEvents에 추가:
    ```typescript
    'recharge-request': (data: { roomId: string; amount: number }) => void;
    'recharge-vote': (data: { roomId: string; approved: boolean }) => void;
    ```

    ServerToClientEvents에 추가:
    ```typescript
    'recharge-requested': (data: { requesterId: string; requesterNickname: string; amount: number }) => void;
    'recharge-vote-update': (data: { votedCount: number; totalNeeded: number; approved: boolean }) => void;
    'recharge-result': (data: { requesterId: string; approved: boolean; newChips?: number }) => void;
    ```

    **3. packages/shared/src/types/index.ts 수정:**

    ChipBreakdown export 추가:
    ```typescript
    export type { ChipBreakdown } from './game';
    ```

    **4. packages/shared/src/index.ts 수정:**

    ChipBreakdown re-export 추가:
    ```typescript
    export type { ChipBreakdown } from './types/game';
    ```

    **5. packages/server/src/game-engine.ts 수정:**

    ChipBreakdown import 추가.

    `calculateChipBreakdown` static 메서드 추가:
    ```typescript
    static calculateChipBreakdown(chips: number): ChipBreakdown {
      let remaining = chips;
      const ten_thousand = Math.floor(remaining / 10000);
      remaining %= 10000;
      const five_thousand = Math.floor(remaining / 5000);
      remaining %= 5000;
      const one_thousand = Math.floor(remaining / 1000);
      remaining %= 1000;
      const five_hundred = Math.floor(remaining / 500);
      return { ten_thousand, five_thousand, one_thousand, five_hundred };
    }
    ```

    `_updateChipBreakdowns` private 메서드 추가 — 모든 플레이어의 chipBreakdown을 chips 기준으로 갱신:
    ```typescript
    private _updateChipBreakdowns(): void {
      for (const p of this.state.players) {
        p.chipBreakdown = GameEngine.calculateChipBreakdown(p.chips);
      }
    }
    ```

    `calculateEffectiveMaxBet` 메서드 추가:
    ```typescript
    calculateEffectiveMaxBet(playerId: string): number {
      const player = this.state.players.find(p => p.id === playerId);
      if (!player) return 0;
      const alivePlayers = this.state.players.filter(p => p.isAlive && p.id !== playerId);
      if (alivePlayers.length === 0) return 0;
      const myAvailable = player.chips;
      const maxOpponent = Math.max(...alivePlayers.map(p => p.chips));
      if (myAvailable < maxOpponent) return myAvailable;
      // 내가 가장 부자면 두 번째로 큰 잔액이 천장
      const opponentChips = alivePlayers.map(p => p.chips).sort((a, b) => b - a);
      return opponentChips[0]; // 생존 상대 중 최대값 (= 두 번째로 큰 전체 잔액)
    }
    ```

    `_updateEffectiveMaxBet` private 메서드 — 현재 턴 플레이어의 effectiveMaxBet 계산하여 state에 반영:
    ```typescript
    private _updateEffectiveMaxBet(): void {
      if (this.state.phase !== 'betting') {
        this.state.effectiveMaxBet = undefined;
        return;
      }
      const currentPlayer = this.state.players.find(p => p.seatIndex === this.state.currentPlayerIndex);
      if (!currentPlayer) {
        this.state.effectiveMaxBet = undefined;
        return;
      }
      this.state.effectiveMaxBet = this.calculateEffectiveMaxBet(currentPlayer.id);
    }
    ```

    `settleChips` private 메서드 추가 — 승자에게 pot 합산 (per D-01):
    ```typescript
    private settleChips(): void {
      if (!this.state.winnerId) return;
      const winner = this.state.players.find(p => p.id === this.state.winnerId);
      if (winner) {
        winner.chips += this.state.pot;
      }
      // pot은 result phase에서 표시용으로 유지, nextRound()에서 0 리셋
    }
    ```

    **기존 메서드 수정:**

    `attendSchool` — pot += 500 전에 chips 차감 추가:
    ```typescript
    const player = this.state.players.find(p => p.id === playerId);
    if (player) {
      player.chips -= 500;
    }
    this.state.pot += 500;
    ```

    `processBetAction` — 콜/레이즈에 chips 차감 추가:
    ```typescript
    case 'call': {
      const callAmount = this.state.currentBetAmount - player.currentBet;
      player.chips -= callAmount;  // 추가
      player.currentBet += callAmount;
      this.state.pot += callAmount;
      break;
    }
    case 'raise': {
      // ... 기존 로직
      const totalDeducted = callAmount + action.amount;
      player.chips -= totalDeducted;  // 추가
      // ... 나머지 기존 로직
    }
    ```

    `_resolveShowdown` — winnerId 세팅 직후 settleChips 호출:
    ```typescript
    this.state.winnerId = best.player.id;
    this.settleChips();  // 추가 (per D-01)
    this.state.phase = 'result';
    ```

    `_advanceBettingTurn` — 생존자 1명만 남아 result로 갈 때도 정산:
    ```typescript
    if (alivePlayers.length <= 1) {
      if (alivePlayers.length === 1) {
        this.state.winnerId = alivePlayers[0].id;
        this.settleChips();  // 추가
      }
      this.state.phase = 'result';
      return;
    }
    ```

    constructor에서 초기 chipBreakdown 설정:
    ```typescript
    chipBreakdown: GameEngine.calculateChipBreakdown(p.chips),
    ```

    `_dealCards` 끝에 `_updateChipBreakdowns()` 및 `_updateEffectiveMaxBet()` 호출.
    `_advanceBettingTurn` 내 next player 결정 후 `_updateChipBreakdowns()` 및 `_updateEffectiveMaxBet()` 호출.
    `settleChips` 끝에 `_updateChipBreakdowns()` 호출.
    `attendSchool` 완료 시 `_updateChipBreakdowns()` 호출.

    **6. 테스트 작성 (game-engine.test.ts에 describe 블록 추가):**

    위 behavior 목록의 모든 케이스를 커버하는 테스트 추가.
    GameEngine 생성 시 players에 chips 값을 설정하고, 각 액션 후 chips 변동 검증.
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/shared build && pnpm --filter @sutda/server test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/types/game.ts에 `interface ChipBreakdown` 정의 존재
    - packages/shared/src/types/game.ts의 PlayerState에 `chipBreakdown: ChipBreakdown` 필드 존재
    - packages/shared/src/types/game.ts의 GameState에 `effectiveMaxBet?: number` 필드 존재
    - packages/shared/src/types/protocol.ts에 `recharge-request`, `recharge-vote` 이벤트 타입 존재
    - packages/shared/src/types/protocol.ts에 `recharge-requested`, `recharge-vote-update`, `recharge-result` 이벤트 타입 존재
    - packages/server/src/game-engine.ts에 `settleChips` 메서드 존재
    - packages/server/src/game-engine.ts에 `calculateChipBreakdown` static 메서드 존재
    - packages/server/src/game-engine.ts에 `calculateEffectiveMaxBet` 메서드 존재
    - packages/server/src/game-engine.ts의 attendSchool에 `player.chips -= 500` 존재
    - packages/server/src/game-engine.ts의 processBetAction call case에 `player.chips -=` 존재
    - packages/server/src/game-engine.test.ts에 `settleChips` 또는 `칩 정산` 관련 describe 블록 존재
    - `pnpm --filter @sutda/shared build` 성공 (exit code 0)
    - `pnpm --filter @sutda/server test -- --run` 성공 (exit code 0)
  </acceptance_criteria>
  <done>
    칩 정산 로직이 GameEngine에 통합되어 모든 테스트가 통과한다. 승자 결정 시 pot이 승자에게 합산되고, 등교/베팅 시 chips가 정확히 차감된다. chipBreakdown과 effectiveMaxBet이 game-state에 포함된다.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @sutda/shared build` 성공
- `pnpm --filter @sutda/server test -- --run` 전체 통과
- GameEngine 생성 후 players[].chipBreakdown 값 검증
- attendSchool 호출 후 chips 감소 검증
- 베팅 후 chips 감소 검증
- 승자 결정 후 pot -> winner.chips 합산 검증
</verification>

<success_criteria>
- 모든 기존 테스트 + 새 칩 정산 테스트 통과
- shared 빌드 성공
- ChipBreakdown, effectiveMaxBet 타입이 shared에서 export됨
- 프로토콜 타입에 recharge 이벤트 추가됨
</success_criteria>

<output>
완료 후 `.planning/phases/05-chip-system-settlement/05-01-SUMMARY.md` 생성
</output>
