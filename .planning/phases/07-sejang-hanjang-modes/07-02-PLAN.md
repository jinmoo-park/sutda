---
phase: 07-sejang-hanjang-modes
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - packages/client/src/components/modals/ModeSelectModal.tsx
  - packages/client/src/components/modals/SharedCardSelectModal.tsx
  - packages/client/src/components/game/SharedCardDisplay.tsx
  - packages/client/src/components/layout/HandPanel.tsx
  - packages/client/src/components/layout/GameTable.tsx
  - packages/client/src/pages/RoomPage.tsx
autonomous: false

requirements: [MODE-SJ-01, MODE-SJ-02, MODE-SH-01, MODE-SH-02, MODE-SH-03, MODE-SH-04]

must_haves:
  truths:
    - "딜러가 ModeSelectModal에서 세장섯다/한장공유 모드를 선택할 수 있다"
    - "비딜러는 모드 선택 버튼이 disabled 상태이다"
    - "세장섯다 card-select phase에서 HandPanel이 3장 카드에 선택 토글 UI를 보여준다"
    - "정확히 2장 선택 시에만 '선택 완료' 버튼이 활성화된다"
    - "한장공유에서 딜러에게 20장 앞면 그리드가 보이고, 비딜러에게는 뒷면이 보인다"
    - "한장공유에서 공유 카드가 GameTable 중앙에 항상 표시된다"
  artifacts:
    - path: "packages/client/src/components/modals/ModeSelectModal.tsx"
      provides: "세장섯다/한장공유 버튼 추가된 모달"
      contains: "세장섯다"
    - path: "packages/client/src/components/modals/SharedCardSelectModal.tsx"
      provides: "DealerSelectModal 기반 공유카드 선택 모달"
      contains: "SharedCardSelectModal"
    - path: "packages/client/src/components/game/SharedCardDisplay.tsx"
      provides: "테이블 중앙 공유카드 표시 컴포넌트"
      contains: "SharedCardDisplay"
    - path: "packages/client/src/components/layout/HandPanel.tsx"
      provides: "card-select 모드 카드 토글 + 선택 완료 버튼"
      contains: "card-select"
    - path: "packages/client/src/components/layout/GameTable.tsx"
      provides: "sharedCard 중앙 렌더링"
      contains: "SharedCardDisplay"
    - path: "packages/client/src/pages/RoomPage.tsx"
      provides: "SharedCardSelectModal + 신규 phase 연동"
      contains: "SharedCardSelectModal"
  key_links:
    - from: "ModeSelectModal.tsx"
      to: "socket select-mode"
      via: "emit('select-mode', { roomId, mode: 'three-card' | 'shared-card' })"
      pattern: "select-mode.*three-card|shared-card"
    - from: "HandPanel.tsx"
      to: "socket select-cards"
      via: "onSelectCards prop → emit('select-cards', { roomId, cardIndices })"
      pattern: "select-cards"
    - from: "SharedCardSelectModal.tsx"
      to: "socket set-shared-card"
      via: "emit('set-shared-card', { roomId, cardIndex })"
      pattern: "set-shared-card"
---

<objective>
세장섯다/한장공유 모드의 클라이언트 UI를 구현하고 서버와 연동한다.

Purpose: Plan 01에서 완성된 서버 계약(새로운 phase, 이벤트)에 맞게 UI를 구현하여 플레이어가 브라우저에서 두 모드를 실제로 플레이할 수 있게 한다.
Output: ModeSelectModal 확장, SharedCardSelectModal 신규, HandPanel 카드 선택 모드, GameTable 공유카드 표시, RoomPage 통합
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-sejang-hanjang-modes/07-CONTEXT.md
@.planning/phases/07-sejang-hanjang-modes/07-UI-SPEC.md
@.planning/phases/07-sejang-hanjang-modes/07-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 추가된 타입/이벤트 계약 -->

From packages/shared/src/types/game.ts (Plan 01 이후):
```typescript
export type GamePhase = ... | 'betting-1' | 'dealing-extra' | 'card-select' | 'betting-2' | 'shared-card-select';

export interface PlayerState {
  // ... 기존 필드 ...
  selectedCards?: Card[];  // 세장섯다: 3장 중 선택한 2장
}

export interface GameState {
  // ... 기존 필드 ...
  sharedCard?: Card;  // 한장공유: 공유 카드
}
```

From packages/shared/src/types/protocol.ts (Plan 01 이후):
```typescript
'select-cards': (data: { roomId: string; cardIndices: number[] }) => void;
'set-shared-card': (data: { roomId: string; cardIndex: number }) => void;
```

From packages/client/src/components/modals/ModeSelectModal.tsx (현재):
```typescript
interface ModeSelectModalProps {
  open: boolean;
  roomId: string;
  isDealer: boolean;
}
// 현재 '오리지날 섯다' 버튼 1개만 존재
```

From packages/client/src/components/layout/HandPanel.tsx (현재):
```typescript
interface HandPanelProps {
  myPlayer: PlayerState | null;
}
// 카드 표시 + 족보 배지. card-select 모드 없음
```

From packages/client/src/components/layout/GameTable.tsx (현재):
```typescript
interface GameTableProps {
  players: PlayerState[];
  myPlayerId: string | null;
  currentPlayerIndex: number;
  pot: number;
  visibleCardCounts?: Record<string, number>;
}
// 중앙에 팟만 표시. sharedCard 없음
```

From packages/client/src/components/modals/DealerSelectModal.tsx (참조용 — 재사용 패턴):
```typescript
// 20장 그리드, CardFace/CardBack, hover:ring-2 hover:ring-primary 패턴
// disabled 시 cursor-not-allowed opacity-40
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ModeSelectModal 확장 + SharedCardSelectModal 신규 + SharedCardDisplay + HandPanel 카드 선택 모드</name>
  <files>
    packages/client/src/components/modals/ModeSelectModal.tsx,
    packages/client/src/components/modals/SharedCardSelectModal.tsx,
    packages/client/src/components/game/SharedCardDisplay.tsx,
    packages/client/src/components/layout/HandPanel.tsx
  </files>
  <read_first>
    packages/client/src/components/modals/ModeSelectModal.tsx,
    packages/client/src/components/modals/DealerSelectModal.tsx,
    packages/client/src/components/layout/HandPanel.tsx,
    packages/client/src/components/game/CardFace.tsx,
    packages/client/src/components/game/CardBack.tsx,
    packages/client/src/store/gameStore.ts,
    packages/shared/src/types/game.ts,
    packages/shared/src/types/protocol.ts,
    .planning/phases/07-sejang-hanjang-modes/07-UI-SPEC.md,
    .planning/phases/07-sejang-hanjang-modes/07-CONTEXT.md
  </read_first>
  <action>
    ## ModeSelectModal.tsx 수정 (per D-12, UI-SPEC 인터랙션 계약 #1)

    기존 단일 '오리지날 섯다' 버튼 아래에 2개 버튼 추가.
    세로 스택 레이아웃 (flex-col gap-2):

    ```tsx
    <div className="flex flex-col gap-2 py-2">
      <Button className="w-full" variant="outline"
        onClick={() => socket?.emit('select-mode', { roomId, mode: 'original' })}
        disabled={!isDealer}>
        오리지날 섯다
      </Button>
      <Button className="w-full" variant="outline"
        onClick={() => socket?.emit('select-mode', { roomId, mode: 'three-card' })}
        disabled={!isDealer}>
        세장섯다
      </Button>
      <Button className="w-full" variant="outline"
        onClick={() => socket?.emit('select-mode', { roomId, mode: 'shared-card' })}
        disabled={!isDealer}>
        한장공유
      </Button>
    </div>
    ```

    비딜러 메시지: "선 플레이어가 모드를 선택 중입니다..." (기존 유지)

    ## SharedCardSelectModal.tsx 신규 생성 (per D-12, D-13, UI-SPEC 인터랙션 계약 #3)

    DealerSelectModal 패턴 기반. Props: `{ open: boolean; roomId: string }`.

    핵심 차이점:
    - 딜러에게는 20장 **전부 CardFace** (앞면 공개) — `isDealer ? <CardFace card={deck[i]} /> : <CardBack />`
    - 비딜러에게는 20장 전부 CardBack (뒷면), 클릭 불가
    - 클릭 시 `socket?.emit('set-shared-card', { roomId, cardIndex })` (per D-14)
    - 모달 제목: "공유 카드 선택" (DialogTitle)
    - 딜러 description: "모든 플레이어와 공유할 카드를 선택하세요."
    - 비딜러 description: "선 플레이어가 공유 카드를 선택 중입니다..."
    - 선택 후 상태: ring-2 ring-primary (DealerSelectModal 패턴과 동일)
    - disabled 상태: cursor-not-allowed opacity-40
    - onInteractOutside/onEscapeKeyDown: preventDefault (모달 닫힘 방지)

    gameState와 myPlayerId는 `useGameStore()`에서 가져옴.
    isDealer 판단: `gameState?.players.find(p => p.id === myPlayerId)?.isDealer ?? false`
    deck은 `gameState?.deck ?? []` (서버에서 deck을 game-state에 포함하여 브로드캐스트)

    ## SharedCardDisplay.tsx 신규 생성 (per D-14, UI-SPEC 인터랙션 계약 #4)

    Props: `{ card: Card }`. 한장공유 모드에서 테이블 중앙에 공유 카드 표시.

    ```tsx
    import type { Card } from '@sutda/shared';
    import { CardFace } from './CardFace';

    interface SharedCardDisplayProps {
      card: Card;
    }

    export function SharedCardDisplay({ card }: SharedCardDisplayProps) {
      return (
        <div className="flex flex-col items-center gap-1">
          <p className="text-xs text-muted-foreground">공유 카드</p>
          <div className="ring-2 ring-primary/50 rounded-md">
            <CardFace card={card} className="w-20 h-30" />
          </div>
        </div>
      );
    }
    ```

    크기: w-20 h-30 (80px x 120px, 기본 w-16 h-24의 1.25배)
    CardFace에 className prop이 없으면 wrapper div로 크기 제어:
    `<div className="w-20 h-[120px] ring-2 ring-primary/50 rounded-md"><CardFace card={card} /></div>`
    (CardFace 현재 구조를 read_first로 확인 후 적절히 조정)

    ## HandPanel.tsx 수정 (per D-09, D-10, D-11, UI-SPEC 인터랙션 계약 #2)

    Props 확장:
    ```typescript
    interface HandPanelProps {
      myPlayer: PlayerState | null;
      phase?: GamePhase;                          // 추가
      onSelectCards?: (indices: number[]) => void; // 추가
    }
    ```

    card-select 모드 로직:
    - `const isCardSelectMode = phase === 'card-select' && myPlayer?.isAlive && !(myPlayer?.selectedCards?.length);`
    - `const [selectedIndices, setSelectedIndices] = useState<number[]>([]);`
    - phase가 card-select가 아니면 selectedIndices 리셋 (useEffect)

    카드 토글:
    ```typescript
    const toggleCard = (idx: number) => {
      if (!isCardSelectMode) return;
      setSelectedIndices(prev =>
        prev.includes(idx)
          ? prev.filter(i => i !== idx)
          : prev.length < 2 ? [...prev, idx] : prev
      );
    };
    ```

    카드 렌더링 수정 — isCardSelectMode일 때 클릭 가능 + 시각 피드백:
    - 선택된 카드: `ring-2 ring-primary scale-105 opacity-100` + transition
    - 미선택 카드: `opacity-70 hover:opacity-100 hover:ring-1 hover:ring-primary/50` + cursor-pointer
    - 카드를 button으로 감싸거나 onClick 추가

    선택 완료 버튼:
    - `selectedIndices.length === 2`일 때만 활성화
    - 비활성 시 텍스트: "2장을 선택하세요 ({selectedIndices.length}/2)"
    - 활성 시: "선택 완료" 버튼, variant="default"
    - 클릭 → `onSelectCards?.(selectedIndices)` → 버튼 loading 상태

    이미 제출한 경우 (`myPlayer?.selectedCards?.length === 2`):
    - 텍스트: "선택 완료! 다른 플레이어를 기다리는 중..."
    - 선택 토글 비활성화

    족보 표시 로직 수정:
    - card-select phase에서는 3장이므로 evaluateHand를 호출하지 않거나, selectedCards가 있으면 selectedCards로 evaluateHand 호출
    - 기본: cards.length >= 2일 때 evaluateHand(cards[0], cards[1])
    - selectedCards가 있으면: evaluateHand(selectedCards[0], selectedCards[1])

    한장공유 모드에서 myPlayer.cards.length === 1이고 gameState?.sharedCard가 있으면:
    - evaluateHand(cards[0], sharedCard)로 족보 표시
    - 이를 위해 props에 `sharedCard?: Card` 추가하거나, gameState를 store에서 직접 읽기
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/client build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ModeSelectModal.tsx에 "세장섯다" 문자열 존재
    - ModeSelectModal.tsx에 "한장공유" 문자열 존재
    - ModeSelectModal.tsx에 `mode: 'three-card'` emit 존재
    - ModeSelectModal.tsx에 `mode: 'shared-card'` emit 존재
    - SharedCardSelectModal.tsx 파일 존재
    - SharedCardSelectModal.tsx에 `set-shared-card` emit 존재
    - SharedCardSelectModal.tsx에 CardFace와 CardBack 조건 분기 존재
    - SharedCardDisplay.tsx 파일 존재
    - SharedCardDisplay.tsx에 "공유 카드" 문자열 존재
    - SharedCardDisplay.tsx에 `ring-2 ring-primary/50` 클래스 존재
    - HandPanel.tsx에 `card-select` 문자열 존재
    - HandPanel.tsx에 `selectedIndices` state 존재
    - HandPanel.tsx에 `onSelectCards` prop 존재
    - HandPanel.tsx에 "선택 완료" 문자열 존재
    - HandPanel.tsx에 "2장을 선택하세요" 문자열 존재
    - `pnpm --filter @sutda/client build` 성공 (TypeScript 컴파일 에러 없음)
  </acceptance_criteria>
  <done>
    4개 컴포넌트가 구현/수정됨: ModeSelectModal(확장), SharedCardSelectModal(신규), SharedCardDisplay(신규), HandPanel(card-select 모드 추가). 클라이언트 빌드 성공.
  </done>
</task>

<task type="auto">
  <name>Task 2: GameTable 공유카드 표시 + RoomPage 통합 (신규 phase/모달 연결)</name>
  <files>
    packages/client/src/components/layout/GameTable.tsx,
    packages/client/src/pages/RoomPage.tsx
  </files>
  <read_first>
    packages/client/src/components/layout/GameTable.tsx,
    packages/client/src/pages/RoomPage.tsx,
    packages/client/src/components/modals/SharedCardSelectModal.tsx,
    packages/client/src/components/game/SharedCardDisplay.tsx,
    packages/client/src/components/layout/HandPanel.tsx,
    packages/client/src/store/gameStore.ts,
    packages/shared/src/types/game.ts,
    .planning/phases/07-sejang-hanjang-modes/07-UI-SPEC.md
  </read_first>
  <action>
    ## GameTable.tsx 수정

    Props에 sharedCard 추가:
    ```typescript
    interface GameTableProps {
      players: PlayerState[];
      myPlayerId: string | null;
      currentPlayerIndex: number;
      pot: number;
      visibleCardCounts?: Record<string, number>;
      sharedCard?: Card;      // 추가
      mode?: GameMode;        // 추가
    }
    ```

    중앙 팟 표시 div 하단에 SharedCardDisplay 조건부 렌더:
    ```tsx
    import { SharedCardDisplay } from '@/components/game/SharedCardDisplay';
    import type { Card, GameMode } from '@sutda/shared';
    ```

    데스크톱 레이아웃의 `<div className="absolute inset-0 flex items-center justify-center pointer-events-none">` 내부,
    팟 표시 div 아래에 추가:
    ```tsx
    {mode === 'shared-card' && sharedCard && (
      <div className="mt-2">
        <SharedCardDisplay card={sharedCard} />
      </div>
    )}
    ```

    모바일 레이아웃에도 동일하게 팟 요약 div 하단에 추가:
    ```tsx
    {mode === 'shared-card' && sharedCard && (
      <div className="flex justify-center py-1">
        <SharedCardDisplay card={sharedCard} />
      </div>
    )}
    ```

    ## RoomPage.tsx 수정

    1) import 추가:
    ```tsx
    import { SharedCardSelectModal } from '@/components/modals/SharedCardSelectModal';
    import type { GamePhase } from '@sutda/shared';
    ```

    2) GameTable에 sharedCard, mode prop 전달:
    ```tsx
    <GameTable
      players={gameState.players}
      myPlayerId={myPlayerId}
      currentPlayerIndex={gameState.currentPlayerIndex}
      pot={gameState.pot}
      visibleCardCounts={...}
      sharedCard={gameState.sharedCard}
      mode={gameState.mode}
    />
    ```

    3) HandPanel에 phase, onSelectCards prop 전달:
    ```tsx
    <HandPanel
      myPlayer={myPlayer}
      phase={gameState.phase as GamePhase}
      onSelectCards={(indices) => {
        socket?.emit('select-cards', { roomId: roomId!, cardIndices: indices });
      }}
    />
    ```

    4) SharedCardSelectModal 추가 (모달 섹션, ModeSelectModal 아래):
    ```tsx
    <SharedCardSelectModal
      open={phase === 'shared-card-select'}
      roomId={roomId!}
    />
    ```

    5) BettingPanel 표시 조건 수정 — betting-1, betting-2 phase에서도 표시:
    기존:
    ```tsx
    {phase === 'betting' && !showCardConfirm && (
      <BettingPanel ... />
    )}
    ```
    변경:
    ```tsx
    {(phase === 'betting' || phase === 'betting-1' || phase === 'betting-2') && !showCardConfirm && (
      <BettingPanel ... />
    )}
    ```

    6) cutting → dealing 전환 감지 수정 — 세장섯다에서 cutting → betting-1 전환 시에도 딜링 애니메이션:
    기존: `prevPhaseRef.current === 'cutting' && gameState?.phase === 'betting'`
    변경: `prevPhaseRef.current === 'cutting' && (gameState?.phase === 'betting' || gameState?.phase === 'betting-1')`

    7) 베팅/커팅 phase 벗어남 감지 수정:
    기존: `if (p !== 'betting' && p !== 'cutting')`
    변경: `if (p !== 'betting' && p !== 'betting-1' && p !== 'betting-2' && p !== 'cutting' && p !== 'card-select')`

    8) 한장공유 HandPanel에 sharedCard 전달 (족보 표시 위해):
    HandPanel의 props에 `sharedCard` 추가가 필요하면 Task 1에서 이미 처리됨.
    RoomPage에서 전달:
    ```tsx
    <HandPanel
      myPlayer={myPlayer}
      phase={gameState.phase as GamePhase}
      onSelectCards={...}
      sharedCard={gameState.mode === 'shared-card' ? gameState.sharedCard : undefined}
    />
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/client build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - GameTable.tsx에 `SharedCardDisplay` import 존재
    - GameTable.tsx에 `sharedCard` prop 존재
    - GameTable.tsx에 `mode === 'shared-card'` 조건 렌더 존재
    - RoomPage.tsx에 `SharedCardSelectModal` import 존재
    - RoomPage.tsx에 `phase === 'shared-card-select'` 조건으로 SharedCardSelectModal open 존재
    - RoomPage.tsx에 `select-cards` emit 존재
    - RoomPage.tsx의 BettingPanel 조건에 `betting-1` 또는 `betting-2` 문자열 존재
    - RoomPage.tsx의 cutting 전환 감지에 `betting-1` 조건 존재
    - `pnpm --filter @sutda/client build` 성공
  </acceptance_criteria>
  <done>
    GameTable에 공유카드 표시, RoomPage에 SharedCardSelectModal 연결, BettingPanel의 betting-1/2 phase 대응, HandPanel에 phase/onSelectCards 전달, 딜링 애니메이션 세장섯다 대응. 클라이언트 빌드 성공.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 세장섯다/한장공유 모드 수동 검증</name>
  <files>packages/client/src/pages/RoomPage.tsx</files>
  <action>
    수동 검증 태스크 — 자동화 불가한 브라우저 UI 플로우 검증.
    세장섯다와 한장공유 모드의 전체 UI+서버 통합을 브라우저 2탭으로 확인한다.
    - ModeSelectModal에 3개 모드 버튼 (오리지날/세장섯다/한장공유)
    - 세장섯다: 2장 배분 → 1차 베팅 → 3번째 카드 → 2장 선택 → 2차 베팅 → 결과
    - 한장공유: 딜러가 공유카드 선택 → 각자 1장 → 베팅 → 결과
    - 공유카드 테이블 중앙 표시
    - 오리지날 모드 회귀 확인
  </action>
  <verify>
    사전 준비: 서버 pnpm --filter @sutda/server dev, 클라이언트 pnpm --filter @sutda/client dev 실행.

    세장섯다 테스트:
    1. 브라우저 탭 2개로 같은 방 입장 (2명)
    2. 게임 시작 후 모드 선택 모달에서 "세장섯다" 클릭
    3. 셔플/기리 후 2장 배분 확인
    4. 1차 베팅 (콜/레이즈) 진행 확인
    5. 3번째 카드 배분 후 카드 선택 UI 표시 확인
    6. 카드 2장 선택 → "선택 완료" 버튼 활성화 → 클릭
    7. 2차 베팅 후 결과 화면 확인

    한장공유 테스트:
    1. 다음 판에서 "한장공유" 선택
    2. 딜러에게 20장 앞면 그리드 표시 확인
    3. 비딜러에게는 뒷면만 보이는지 확인
    4. 딜러가 카드 선택 후 중앙에 공유카드 표시 확인
    5. 각자 1장 배분 → 베팅 → 결과 확인
    6. 공유카드 + 내 카드 조합 족보가 HandPanel에 표시되는지 확인

    오리지날 모드 회귀 테스트:
    1. 다음 판에서 "오리지날 섯다" 선택
    2. 기존 플로우 (2장 → 베팅 → 결과) 정상 작동 확인
  </verify>
  <done>사용자가 세장섯다/한장공유/오리지날 3개 모드 모두 수동 검증 완료하고 "approved" 입력</done>
</task>

</tasks>

<verification>
- `pnpm -r test --run` — 전체 모노레포 테스트 통과
- `pnpm --filter @sutda/client build` — 클라이언트 빌드 성공
- 수동 검증: 세장섯다/한장공유 모드 전체 플레이 가능
</verification>

<success_criteria>
1. ModeSelectModal에 오리지날/세장섯다/한장공유 3개 버튼이 표시된다
2. 세장섯다: card-select phase에서 3장 카드 중 2장을 토글 선택할 수 있고, 선택 완료 후 2차 베팅이 진행된다
3. 한장공유: 딜러에게 20장 앞면이 보이고, 선택한 공유카드가 테이블 중앙에 표시된다
4. 한장공유: 각 플레이어 1장 + 공유카드 조합 족보가 HandPanel에 표시된다
5. 기존 오리지날 모드가 변경 없이 정상 작동한다
6. 클라이언트 빌드 성공 (TypeScript 에러 없음)
</success_criteria>

<output>
After completion, create `.planning/phases/07-sejang-hanjang-modes/07-02-SUMMARY.md`
</output>
