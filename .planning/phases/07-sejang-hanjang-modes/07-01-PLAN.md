---
phase: 07-sejang-hanjang-modes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/game.ts
  - packages/shared/src/types/protocol.ts
  - packages/server/src/game-engine.ts
  - packages/server/src/index.ts
  - packages/server/src/__tests__/game-engine-sejang.test.ts
  - packages/server/src/__tests__/game-engine-hanjang.test.ts
autonomous: true
requirements: [MODE-SJ-01, MODE-SJ-02, MODE-SH-01, MODE-SH-02, MODE-SH-03, MODE-SH-04]

must_haves:
  truths:
    - "select-mode에서 'three-card' 또는 'shared-card'를 선택하면 GameEngine이 해당 모드로 전환된다"
    - "세장섯다: dealing → betting-1 → dealing-extra → card-select → betting-2 → showdown 전환이 작동한다"
    - "세장섯다: 3장 중 2장을 선택하면 selectedCards에 저장되고 해당 카드로 족보가 판정된다"
    - "한장공유: shared-card-select phase에서 딜러가 공유 카드를 지정할 수 있다"
    - "한장공유: 각 플레이어에게 1장만 배분되고 sharedCard와 조합하여 족보가 판정된다"
    - "기존 오리지날 모드가 변경 없이 정상 작동한다"
  artifacts:
    - path: "packages/shared/src/types/game.ts"
      provides: "GamePhase에 5개 신규 phase, PlayerState에 selectedCards, GameState에 sharedCard"
      contains: "betting-1"
    - path: "packages/shared/src/types/protocol.ts"
      provides: "select-cards, set-shared-card 이벤트 타입"
      contains: "select-cards"
    - path: "packages/server/src/game-engine.ts"
      provides: "세장섯다/한장공유 전체 FSM 로직"
      contains: "selectCards"
    - path: "packages/server/src/index.ts"
      provides: "select-cards, set-shared-card Socket.IO 핸들러"
      contains: "select-cards"
    - path: "packages/server/src/__tests__/game-engine-sejang.test.ts"
      provides: "세장섯다 모드 단위 테스트"
    - path: "packages/server/src/__tests__/game-engine-hanjang.test.ts"
      provides: "한장공유 모드 단위 테스트"
  key_links:
    - from: "packages/server/src/game-engine.ts"
      to: "packages/shared/src/types/game.ts"
      via: "GamePhase/PlayerState/GameState 타입 import"
      pattern: "import.*GamePhase|GameState|PlayerState"
    - from: "packages/server/src/index.ts"
      to: "packages/server/src/game-engine.ts"
      via: "select-cards/set-shared-card 핸들러가 engine 메서드 호출"
      pattern: "selectCards|setSharedCard"
    - from: "packages/server/src/game-engine.ts"
      to: "@sutda/shared evaluateHand"
      via: "세장섯다 showdown에서 selectedCards로 evaluateHand 호출"
      pattern: "evaluateHand.*selectedCards"
---

<objective>
세장섯다와 한장공유 모드의 서버 백엔드 전체를 구현한다.

Purpose: 두 모드의 GameEngine FSM 로직, 타입 확장, Socket.IO 핸들러, 테스트를 한번에 완성하여 Plan 02 (클라이언트 UI)가 바로 연결할 수 있는 서버 계약을 확립한다.
Output: 5개 신규 GamePhase, 2개 신규 소켓 이벤트, GameEngine 메서드 4개 추가, 단위 테스트 2파일
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sejang-hanjang-modes/07-CONTEXT.md
@.planning/phases/07-sejang-hanjang-modes/07-RESEARCH.md
@.planning/phases/07-sejang-hanjang-modes/07-VALIDATION.md

<interfaces>
<!-- 현재 타입 계약 — executor가 읽고 확장해야 할 핵심 -->

From packages/shared/src/types/game.ts:
```typescript
export type GamePhase =
  | 'waiting' | 'dealer-select' | 'attend-school' | 'mode-select'
  | 'shuffling' | 'cutting' | 'dealing' | 'betting'
  | 'showdown' | 'result' | 'rematch-pending' | 'finished';

export type GameMode =
  | 'original' | 'three-card' | 'shared-card' | 'regret' | 'indian';

export interface PlayerState {
  id: string; nickname: string; chips: number; cards: Card[];
  isAlive: boolean; isAbsent: boolean; isRevealed: boolean;
  currentBet: number; isDealer: boolean; seatIndex: number;
  chipBreakdown: ChipBreakdown; lastBetAction?: LastBetAction;
}

export interface GameState {
  roomId: string; phase: GamePhase; mode: GameMode;
  players: PlayerState[]; pot: number; currentPlayerIndex: number;
  currentBetAmount: number; roundNumber: number; deck: Card[];
  dealerSelectCards?: { playerId: string; cardIndex: number }[];
  isTtong: boolean; attendedPlayerIds: string[];
  winnerId?: string; tiedPlayerIds?: string[];
  rematchDealerId?: string; dealerSelectEligibleIds?: string[];
  effectiveMaxBet?: number;
  openingBettorSeatIndex?: number | null;
}
```

From packages/shared/src/types/protocol.ts:
```typescript
export interface ClientToServerEvents {
  'select-mode': (data: { roomId: string; mode: GameMode }) => void;
  'bet-action': (data: { roomId: string; action: BetAction }) => void;
  // ... 기타 이벤트
}
```

From packages/server/src/game-engine.ts (핵심 메서드):
```typescript
selectMode(playerId: string, mode: GameMode): void
private _dealCards(): void
processBetAction(playerId: string, action: BetAction): void
private _advanceBettingTurn(): void
private _isBettingComplete(): boolean
private _resolveShowdown(): void
private _updateEffectiveMaxBet(): void
private _getAlivePlayersInCounterClockwiseOrder(dealerSeatIndex, alivePlayers): PlayerState[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 공유 타입 확장 + GameEngine 세장섯다/한장공유 로직 구현</name>
  <files>
    packages/shared/src/types/game.ts,
    packages/shared/src/types/protocol.ts,
    packages/server/src/game-engine.ts,
    packages/server/src/__tests__/game-engine-sejang.test.ts,
    packages/server/src/__tests__/game-engine-hanjang.test.ts
  </files>
  <read_first>
    packages/shared/src/types/game.ts,
    packages/shared/src/types/protocol.ts,
    packages/server/src/game-engine.ts,
    packages/shared/src/index.ts,
    .planning/phases/07-sejang-hanjang-modes/07-CONTEXT.md,
    .planning/phases/07-sejang-hanjang-modes/07-RESEARCH.md
  </read_first>
  <behavior>
    세장섯다 테스트 (game-engine-sejang.test.ts):
    - Test 1: selectMode('three-card') 후 state.mode === 'three-card'
    - Test 2: 세장섯다 딜링 후 phase === 'betting-1' (NOT 'betting')
    - Test 3: betting-1 완료 → phase === 'card-select', 생존자 cards.length === 3
    - Test 4: selectCards([0,1]) 성공 → player.selectedCards.length === 2
    - Test 5: 모든 생존자 selectCards 완료 → phase === 'betting-2'
    - Test 6: betting-2 완료 → showdown, 승자 결정 (selectedCards 기반 evaluateHand)
    - Test 7: betting-1에서 다이한 플레이어는 3번째 카드 미배분 (cards.length === 2)
    - Test 8: selectCards에서 중복 인덱스 거부 (i0 === i1 → INVALID_ACTION)
    - Test 9: selectCards에서 인덱스 범위 밖 거부 (< 0 or >= 3 → INVALID_ACTION)

    한장공유 테스트 (game-engine-hanjang.test.ts):
    - Test 1: selectMode('shared-card') 후 phase === 'shared-card-select'
    - Test 2: setSharedCard(dealerPlayerId, cardIndex) 성공 → state.sharedCard 설정, deck에서 제거
    - Test 3: setSharedCard 후 phase === 'shuffling'
    - Test 4: 비딜러가 setSharedCard 호출 → NOT_YOUR_TURN 에러
    - Test 5: 한장공유 딜링 후 각 플레이어 cards.length === 1
    - Test 6: 한장공유 showdown에서 evaluateHand(cards[0], sharedCard) 기반 승자 결정
    - Test 7: setSharedCard에 유효하지 않은 cardIndex → INVALID_ACTION 에러
  </behavior>
  <action>
    ## 1단계: 공유 타입 확장

    **packages/shared/src/types/game.ts:**

    GamePhase 유니온에 5개 추가:
    ```
    | 'betting-1'          // 세장섯다 1차 베팅
    | 'dealing-extra'      // 세장섯다 3번째 카드 배분 (서버 자동)
    | 'card-select'        // 세장섯다 3장 중 2장 선택
    | 'betting-2'          // 세장섯다 2차 베팅
    | 'shared-card-select' // 한장공유 공유카드 선택
    ```

    PlayerState에 필드 추가 (per D-04):
    ```typescript
    selectedCards?: Card[];  // 세장섯다: 3장 중 선택한 2장
    ```

    GameState에 필드 추가 (per D-05):
    ```typescript
    sharedCard?: Card;  // 한장공유: 공유 카드
    ```

    GameMode 타입은 기존 'three-card' | 'shared-card' 유지 (이미 정의되어 있으므로 변경 불필요, RESEARCH.md 권장사항 따름).

    **packages/shared/src/types/protocol.ts:**

    ClientToServerEvents에 2개 이벤트 추가:
    ```typescript
    'select-cards': (data: { roomId: string; cardIndices: number[] }) => void;
    'set-shared-card': (data: { roomId: string; cardIndex: number }) => void;
    ```

    ## 2단계: GameEngine 수정

    **packages/server/src/game-engine.ts:**

    (a) BETTING_PHASES 상수 추가 (클래스 상단):
    ```typescript
    private static readonly BETTING_PHASES: GamePhase[] = ['betting', 'betting-1', 'betting-2'];
    ```

    (b) processBetAction phase 체크 수정:
    - 기존: `if (this.state.phase !== 'betting')`
    - 변경: `if (!GameEngine.BETTING_PHASES.includes(this.state.phase as any))`

    (c) _updateEffectiveMaxBet phase 체크 수정:
    - 기존: `if (this.state.phase !== 'betting')`
    - 변경: `if (!GameEngine.BETTING_PHASES.includes(this.state.phase as any))`

    (d) selectMode 메서드 수정 — 한장공유 분기 추가:
    ```typescript
    selectMode(playerId: string, mode: GameMode): void {
      this.assertPhase('mode-select');
      const player = this.state.players.find(p => p.id === playerId);
      if (!player || !player.isDealer) throw new Error('NOT_YOUR_TURN');
      this.state.mode = mode;
      if (mode === 'shared-card') {
        this.state.phase = 'shared-card-select';
      } else {
        this.state.phase = 'shuffling';
      }
    }
    ```

    (e) 신규 메서드 setSharedCard:
    ```typescript
    setSharedCard(playerId: string, cardIndex: number): void {
      this.assertPhase('shared-card-select');
      const player = this.state.players.find(p => p.id === playerId);
      if (!player || !player.isDealer) throw new Error('NOT_YOUR_TURN');
      if (cardIndex < 0 || cardIndex >= this.state.deck.length) throw new Error('INVALID_ACTION');
      this.state.sharedCard = this.state.deck[cardIndex];
      this.state.deck = this.state.deck.filter((_, i) => i !== cardIndex);
      this.state.phase = 'shuffling';
    }
    ```

    (f) _dealCards 수정 — 모드 분기:
    ```typescript
    private _dealCards(): void {
      if (this.state.mode === 'three-card') {
        this._dealCardsSejang();
        return;
      }
      if (this.state.mode === 'shared-card') {
        this._dealCardsHanjang();
        return;
      }
      // 기존 오리지날 로직 그대로 유지 (아래 모든 기존 코드)
      // ...
    }
    ```

    (g) 신규 private 메서드 _dealCardsSejang:
    - 반시계 방향으로 alive 플레이어에게 2장 배분 (기존 일반 딜링과 동일)
    - 배분 후 `this.state.phase = 'betting-1'` 설정
    - `this.state.currentPlayerIndex = dealerSeatIndex`, `this.state.openingBettorSeatIndex = dealerSeatIndex`
    - `this._bettingActed = new Set()`, `this._updateChipBreakdowns()`, `this._updateEffectiveMaxBet()`

    (h) 신규 private 메서드 _dealExtraCardForSejang:
    - betting-1 완료 직후 호출
    - `this.state.players.filter(p => p.isAlive)` 대상으로 반시계 순서로 deck.shift() 1장씩 push
    - `this.state.phase = 'card-select'`

    (i) 신규 public 메서드 selectCards:
    ```typescript
    selectCards(playerId: string, cardIndices: number[]): void {
      this.assertPhase('card-select');
      if (cardIndices.length !== 2) throw new Error('INVALID_ACTION');
      const player = this.state.players.find(p => p.id === playerId);
      if (!player || !player.isAlive) throw new Error('INVALID_ACTION');
      if (player.selectedCards && player.selectedCards.length === 2) throw new Error('ALREADY_ATTENDED');
      const [i0, i1] = cardIndices;
      if (i0 === i1 || i0 < 0 || i0 >= 3 || i1 < 0 || i1 >= 3) throw new Error('INVALID_ACTION');
      player.selectedCards = [player.cards[i0], player.cards[i1]];
      // 모든 생존자 선택 완료 확인
      const alivePlayers = this.state.players.filter(p => p.isAlive);
      const allSelected = alivePlayers.every(p => p.selectedCards && p.selectedCards.length === 2);
      if (allSelected) {
        this.state.phase = 'betting-2';
        const dealerSeatIndex = this.getDealerSeatIndex();
        this.state.currentPlayerIndex = dealerSeatIndex;
        this.state.openingBettorSeatIndex = dealerSeatIndex;
        this._bettingActed = new Set();
        this.state.currentBetAmount = 0;
        // betting-2 진입 시 모든 생존자 currentBet도 0 리셋 (새 베팅 라운드)
        alivePlayers.forEach(p => { p.currentBet = 0; });
        this._updateEffectiveMaxBet();
      }
    }
    ```

    (j) _advanceBettingTurn 수정 — betting-1 완료 시 dealing-extra 분기:
    기존 `if (this._isBettingComplete())` 블록 내부에 모드 분기 추가:
    ```typescript
    if (this._isBettingComplete()) {
      if (this.state.phase === 'betting-1') {
        // 세장섯다: 3번째 카드 배분 → card-select
        this._dealExtraCardForSejang();
        return;
      }
      if (this.state.phase === 'betting-2') {
        // 세장섯다: selectedCards 기반 showdown
        this.state.players.filter(p => p.isAlive).forEach(p => { p.isRevealed = true; });
        this._resolveShowdownSejang();
        return;
      }
      // 오리지날 / 한장공유: 기존 로직
      this.state.players.filter(p => p.isAlive).forEach(p => { p.isRevealed = true; });
      this._resolveShowdown();
      return;
    }
    ```

    (k) 신규 private 메서드 _resolveShowdownSejang:
    - alivePlayers에서 각 player의 selectedCards로 evaluateHand 호출
    - fallback: `selectedCards?.[0] ?? cards[0]`, `selectedCards?.[1] ?? cards[1]`
    - 이후 기존 _resolveShowdown과 동일한 compareHands + gusa check + settleChips 로직

    (l) 신규 private 메서드 _dealCardsHanjang:
    - _getAlivePlayersInCounterClockwiseOrder로 alive 플레이어 정렬
    - 각 플레이어에게 deck.shift() 1장씩만 배분
    - `this.state.phase = 'betting'` (한장공유는 기존 betting phase 사용)
    - dealerSeatIndex 기준 currentPlayerIndex, openingBettorSeatIndex 설정

    (m) _resolveShowdown 수정 — 한장공유 분기:
    기존 `_resolveShowdown` 메서드의 evaluateHand 호출 부분에서 모드 분기:
    ```typescript
    const hands = alivePlayers.map(p => ({
      player: p,
      hand: this.state.mode === 'shared-card'
        ? evaluateHand(p.cards[0], this.state.sharedCard!)
        : evaluateHand(p.cards[0], p.cards[1]),
    }));
    ```
    이렇게 하면 한장공유는 별도 showdown 메서드 없이 기존 메서드 내에서 분기 처리.

    (n) nextRound 수정 — selectedCards/sharedCard 초기화 추가:
    ```typescript
    // 기존 플레이어 리셋 루프 내에 추가:
    p.selectedCards = undefined;
    // state 리셋에 추가:
    this.state.sharedCard = undefined;
    ```

    (o) startRematch 수정 — selectedCards 초기화 추가:
    ```typescript
    // 기존 플레이어 리셋 루프 내에 추가:
    p.selectedCards = undefined;
    ```

    ## 3단계: 테스트 작성

    **game-engine-sejang.test.ts:**
    - 2명 플레이어 기준 헬퍼 함수로 GameEngine 생성 (mode='three-card', roundNumber=2로 dealer-select skip)
    - 모든 phase 전환을 순서대로 테스트 (selectMode → shuffle → cut → dealing → betting-1 → card-select → betting-2 → showdown → result)
    - 엣지 케이스: betting-1 다이, selectCards 잘못된 인덱스

    **game-engine-hanjang.test.ts:**
    - 2명 플레이어 기준 헬퍼 함수 (mode='shared-card', roundNumber=2)
    - phase 전환: selectMode → shared-card-select → setSharedCard → shuffle → cut → dealing → betting → showdown → result
    - 엣지 케이스: 비딜러 setSharedCard, 잘못된 cardIndex, sharedCard가 deck에서 제거되었는지 확인

    ## 4단계: Socket.IO 핸들러 추가

    **packages/server/src/index.ts:**

    select-cards 핸들러:
    ```typescript
    socket.on('select-cards', ({ roomId, cardIndices }) => {
      handleGameAction(socket, roomId, () => {
        getEngine(roomId).selectCards(socket.data.playerId, cardIndices);
      });
    });
    ```

    set-shared-card 핸들러:
    ```typescript
    socket.on('set-shared-card', ({ roomId, cardIndex }) => {
      handleGameAction(socket, roomId, () => {
        getEngine(roomId).setSharedCard(socket.data.playerId, cardIndex);
      });
    });
    ```

    **주의사항:**
    - getDealerSeatIndex()는 private이므로 내부에서만 사용. 테스트에서 필요하면 getState()로 players에서 isDealer인 플레이어를 찾아 seatIndex 확인.
    - 기존 오리지날 모드 테스트가 깨지지 않도록 processBetAction phase 체크 변경 후 기존 테스트 실행하여 확인.
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && pnpm --filter @sutda/server test --run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/types/game.ts에 `'betting-1'` 문자열이 GamePhase 유니온에 존재
    - packages/shared/src/types/game.ts에 `'dealing-extra'` 문자열이 GamePhase 유니온에 존재
    - packages/shared/src/types/game.ts에 `'card-select'` 문자열이 GamePhase 유니온에 존재
    - packages/shared/src/types/game.ts에 `'betting-2'` 문자열이 GamePhase 유니온에 존재
    - packages/shared/src/types/game.ts에 `'shared-card-select'` 문자열이 GamePhase 유니온에 존재
    - packages/shared/src/types/game.ts의 PlayerState에 `selectedCards?: Card[]` 존재
    - packages/shared/src/types/game.ts의 GameState에 `sharedCard?: Card` 존재
    - packages/shared/src/types/protocol.ts에 `'select-cards'` 이벤트 타입 존재
    - packages/shared/src/types/protocol.ts에 `'set-shared-card'` 이벤트 타입 존재
    - packages/server/src/game-engine.ts에 `selectCards` public 메서드 존재
    - packages/server/src/game-engine.ts에 `setSharedCard` public 메서드 존재
    - packages/server/src/game-engine.ts에 `BETTING_PHASES` 상수가 'betting', 'betting-1', 'betting-2'를 포함
    - packages/server/src/game-engine.ts의 processBetAction이 BETTING_PHASES.includes를 사용
    - packages/server/src/index.ts에 `socket.on('select-cards'` 핸들러 존재
    - packages/server/src/index.ts에 `socket.on('set-shared-card'` 핸들러 존재
    - packages/server/src/__tests__/game-engine-sejang.test.ts 파일 존재, 테스트 통과
    - packages/server/src/__tests__/game-engine-hanjang.test.ts 파일 존재, 테스트 통과
    - `pnpm --filter @sutda/server test --run` 전체 통과 (기존 테스트 포함)
  </acceptance_criteria>
  <done>
    세장섯다/한장공유 모드의 서버 로직이 완전히 구현됨.
    - 세장섯다: dealing → betting-1 → dealing-extra → card-select → betting-2 → showdown 전환이 테스트로 검증됨
    - 한장공유: mode-select → shared-card-select → setSharedCard → shuffling → dealing(1장) → betting → showdown 전환이 테스트로 검증됨
    - 기존 오리지날 모드 테스트가 모두 통과
    - 2개 신규 Socket.IO 핸들러 연결 완료
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @sutda/server test --run` — 세장섯다/한장공유 테스트 + 기존 테스트 전체 통과
- `pnpm --filter @sutda/shared build` — 타입 변경 후 shared 패키지 빌드 성공
- `pnpm -r test --run` — 전체 모노레포 테스트 통과
</verification>

<success_criteria>
1. 세장섯다 모드: selectMode → shuffle → cut → dealing(2장) → betting-1 → dealing-extra(3번째) → card-select → betting-2 → showdown → result 전체 플로우가 단위 테스트로 검증됨
2. 한장공유 모드: selectMode → shared-card-select → setSharedCard → shuffle → cut → dealing(1장) → betting → showdown → result 전체 플로우가 단위 테스트로 검증됨
3. 기존 오리지날 모드 테스트 전부 통과 (regression 없음)
4. Socket.IO에 select-cards, set-shared-card 핸들러 추가 완료
</success_criteria>

<output>
After completion, create `.planning/phases/07-sejang-hanjang-modes/07-01-SUMMARY.md`
</output>
