---
phase: 15-bet07-check-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - BET-07
must_haves:
  truths:
    - "REQUIREMENTS.md Traceability 테이블에 BET-07이 Phase 15로 등록되어 있다"
    - "REQUIREMENTS.md에서 BET-07 체크박스가 [x]로 표시되어 있다"
    - "BET-07 요구사항의 코드 구현이 서버/클라이언트/공유타입 3곳에서 확인된다"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "BET-07 Traceability 등록 및 체크박스 업데이트"
      contains: "BET-07 | Phase 15 | Complete"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "packages/shared/src/types/game.ts"
      via: "BetAction type에 check 포함 확인"
      pattern: "type: 'check'"
    - from: ".planning/REQUIREMENTS.md"
      to: "packages/server/src/game-engine.ts"
      via: "processBetAction에서 check case 처리 확인"
      pattern: "case 'check'"
    - from: ".planning/REQUIREMENTS.md"
      to: "packages/client/src/components/layout/BettingPanel.tsx"
      via: "체크 버튼 UI 구현 확인"
      pattern: "canCheck"
---

<objective>
BET-07 체크 기능을 REQUIREMENTS.md Traceability 테이블에 공식 등록하고, 요구사항 체크박스를 완료로 표시한다.

Purpose: v1-MILESTONE-AUDIT.md에서 발견된 BET-07 orphaned gap을 해소한다. 코드에 이미 구현된 체크 기능의 공식 추적 기록을 만든다.
Output: REQUIREMENTS.md 업데이트 (Traceability 등록 + 체크박스 완료)
</objective>

<execution_context>
@C:/Users/Jinmoo Park/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jinmoo Park/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BET-07 구현 코드 존재 확인 및 REQUIREMENTS.md 업데이트</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (현재 상태 확인 — BET-07 체크박스 및 Traceability)
    - packages/shared/src/types/game.ts (BetAction type에 { type: 'check' } 존재 확인)
    - packages/server/src/game-engine.ts (processBetAction case 'check' 구현 확인)
    - packages/client/src/components/layout/BettingPanel.tsx (canCheck 로직 및 체크 버튼 확인)
    - packages/server/src/game-engine.test.ts (체크 관련 테스트 존재 확인)
  </read_first>
  <action>
REQUIREMENTS.md에서 두 가지를 수정한다:

1. **BET-07 체크박스 완료 표시** (약 51번째 줄):
   변경 전: `- [ ] **BET-07**: 아직 아무도 베팅하지 않은 상태에서 선 플레이어(및 해당 순서의 플레이어)는 "체크"를 선택할 수 있다. 체크는 베팅 없이 다음 플레이어로 넘기며, 이후 레이즈 발생 시 다시 콜/레이즈 기회가 주어진다`
   변경 후: `- [x] **BET-07**: 아직 아무도 베팅하지 않은 상태에서 선 플레이어(및 해당 순서의 플레이어)는 "체크"를 선택할 수 있다. 체크는 베팅 없이 다음 플레이어로 넘기며, 이후 레이즈 발생 시 다시 콜/레이즈 기회가 주어진다 — Validated in Phase 15: BettingPanel.tsx canCheck + processBetAction check case`

2. **Traceability 테이블에서 BET-07 행 업데이트** (약 256번째 줄):
   변경 전: `| BET-07 | Phase 15 | Pending |`
   변경 후: `| BET-07 | Phase 15 | Complete |`

코드 구현 확인 체크리스트 (read_first 파일에서 확인 — 수정하지 않음):
- packages/shared/src/types/game.ts: `BetAction` union에 `{ type: 'check' }` 존재
- packages/shared/src/types/game.ts: `LastBetAction.type`에 `'check'` 포함
- packages/shared/src/types/game.ts: `GameState.openingBettorSeatIndex` 필드 존재
- packages/server/src/game-engine.ts: `processBetAction()` case 'check' — currentBetAmount===0 검증, openingBettorSeatIndex 일치 검증, 선 권한 소멸(null 설정)
- packages/client/src/components/layout/BettingPanel.tsx: `canCheck = callAmount === 0 && isEffectiveSen` 조건, 체크 버튼 onClick에서 `{ type: 'check' }` emit
- packages/server/src/game-engine.test.ts: '체크: currentBetAmount===0일 때 가능', '체크: currentBetAmount>0일 때 에러', '전원 체크 시 showdown 전환' 등 테스트 존재
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && grep -n "\[x\] \*\*BET-07\*\*" .planning/REQUIREMENTS.md && grep -n "BET-07 | Phase 15 | Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md 약 51번째 줄에 `[x] **BET-07**` 패턴이 존재한다
    - .planning/REQUIREMENTS.md 약 51번째 줄에 `Validated in Phase 15` 문구가 포함된다
    - .planning/REQUIREMENTS.md Traceability 테이블에 `BET-07 | Phase 15 | Complete` 행이 존재한다
    - .planning/REQUIREMENTS.md에 `BET-07 | Phase 15 | Pending` 행이 존재하지 않는다
  </acceptance_criteria>
  <done>BET-07 요구사항이 체크박스 [x]로 표시되고, Traceability 테이블에 Phase 15 / Complete로 등록됨</done>
</task>

</tasks>

<verification>
- `grep "\[x\] \*\*BET-07\*\*" .planning/REQUIREMENTS.md` 결과가 1줄 반환
- `grep "BET-07 | Phase 15 | Complete" .planning/REQUIREMENTS.md` 결과가 1줄 반환
- `grep "\[ \] \*\*BET-07\*\*" .planning/REQUIREMENTS.md` 결과가 0줄 (빈 체크박스 없음)
</verification>

<success_criteria>
REQUIREMENTS.md에서 BET-07이 [x] 완료 표시되고 Traceability 테이블에 Phase 15 / Complete로 등록되어 orphaned 상태가 해소됨
</success_criteria>

<output>
완료 후 `.planning/phases/15-bet07-check-verification/15-01-SUMMARY.md` 생성
</output>
