---
phase: 15-bet07-check-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-bet07-check-verification/15-VERIFICATION.md
autonomous: true
requirements:
  - BET-07
must_haves:
  truths:
    - "BET-07 체크 기능의 서버/클라이언트/타입 구현이 검증 보고서에 기록된다"
    - "체크 관련 테스트가 모두 통과한다"
    - "VERIFICATION.md에 BET-07 요구사항 충족 근거가 명시된다"
  artifacts:
    - path: ".planning/phases/15-bet07-check-verification/15-VERIFICATION.md"
      provides: "BET-07 체크 기능 공식 검증 보고서"
      contains: "BET-07"
  key_links:
    - from: ".planning/phases/15-bet07-check-verification/15-VERIFICATION.md"
      to: "packages/server/src/game-engine.test.ts"
      via: "테스트 실행 결과 참조"
      pattern: "체크.*테스트"
---

<objective>
BET-07 체크 기능의 공식 VERIFICATION.md를 작성한다. 서버 엔진, 클라이언트 UI, 공유 타입, 테스트 커버리지를 검증하여 Phase 04 VERIFICATION.md의 형식에 맞는 검증 보고서를 생성한다.

Purpose: v1-MILESTONE-AUDIT.md에서 식별된 BET-07 orphaned gap을 완전히 해소하기 위한 공식 검증 기록 생성
Output: 15-VERIFICATION.md
</objective>

<execution_context>
@C:/Users/Jinmoo Park/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jinmoo Park/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1-MILESTONE-AUDIT.md
@.planning/phases/04-original-mode-game-engine/04-VERIFICATION.md

<interfaces>
<!-- BET-07 구현 위치 - 검증 대상 코드 -->

From packages/shared/src/types/game.ts:
```typescript
export type BetAction =
  | { type: 'call' }
  | { type: 'raise'; amount: number }
  | { type: 'die' }
  | { type: 'check' };

// GameState 내부:
openingBettorSeatIndex?: number | null;  // 선 권한 보유자 seatIndex (null = 이미 행사됨)
```

From packages/server/src/game-engine.ts (processBetAction case 'check'):
```typescript
case 'check': {
  if (this.state.currentBetAmount > 0) {
    throw new Error('INVALID_ACTION: cannot check when currentBetAmount > 0');
  }
  if (this.state.openingBettorSeatIndex !== player.seatIndex) {
    throw new Error('INVALID_ACTION: only the opening bettor can check');
  }
  this.state.openingBettorSeatIndex = null;
  player.lastBetAction = { type: 'check' };
  break;
}
```

From packages/client/src/components/layout/BettingPanel.tsx:
```typescript
const canCheck = callAmount === 0 && isEffectiveSen;
// Button: onClick={() => { play('bet-check'); emitAction({ type: 'check' }); }}
```

From packages/server/src/game-engine.test.ts (체크 관련 테스트):
- '체크: currentBetAmount===0일 때 가능하고 베팅 없이 진행된다'
- '체크: currentBetAmount>0일 때 에러'
- '종료: 전원 체크 시 showdown 전환'
- '레이즈 후 순환: 다른 플레이어에게 다시 차례가 돌아옴' (체크 후 레이즈 시나리오 포함)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 체크 관련 테스트 실행 및 결과 수집</name>
  <files>없음 (테스트 실행만)</files>
  <read_first>
    - packages/server/src/game-engine.test.ts (체크 관련 테스트 블록 위치 확인)
  </read_first>
  <action>
서버 game-engine 테스트를 실행하여 체크 관련 테스트가 모두 통과하는지 확인한다.

실행 명령:
```bash
cd "C:/Users/Jinmoo Park/Desktop/sutda" && npx vitest run packages/server/src/game-engine.test.ts --reporter=verbose 2>&1 | head -200
```

결과에서 다음 테스트 통과 여부를 확인:
- '체크: currentBetAmount===0일 때 가능하고 베팅 없이 진행된다'
- '체크: currentBetAmount>0일 때 에러'
- '종료: 전원 체크 시 showdown 전환'
- '종료: 모든 생존자 currentBet === currentBetAmount이고 모두 액션 완료 시 showdown 전환'
- '레이즈 후 순환: 다른 플레이어에게 다시 차례가 돌아옴'

테스트 결과 출력을 기록해둔다 (Task 2에서 VERIFICATION.md에 포함).
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && npx vitest run packages/server/src/game-engine.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vitest 실행 결과에 "Tests ... passed" 메시지가 포함된다
    - 체크 관련 테스트 5개 이상이 통과한다
    - 실패한 테스트가 0개이다
  </acceptance_criteria>
  <done>game-engine.test.ts 전체 테스트 통과 확인, 체크 관련 테스트 결과 수집 완료</done>
</task>

<task type="auto">
  <name>Task 2: 15-VERIFICATION.md 작성</name>
  <files>.planning/phases/15-bet07-check-verification/15-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-original-mode-game-engine/04-VERIFICATION.md (검증 보고서 형식 참조)
    - packages/shared/src/types/game.ts (BetAction type, openingBettorSeatIndex 확인)
    - packages/server/src/game-engine.ts (processBetAction case 'check' 전체 로직 확인)
    - packages/client/src/components/layout/BettingPanel.tsx (canCheck, 체크 버튼 구현 확인)
    - packages/client/src/pages/RoomPage.tsx (isEffectiveSen 계산 로직 확인)
  </read_first>
  <action>
Phase 04 VERIFICATION.md 형식을 참고하여 15-VERIFICATION.md를 생성한다.

프론트매터 필드: phase=15-bet07-check-verification, verified={현재 ISO 타임스탬프}, status=passed, score="7/7 must-haves verified", re_verification=false

본문 구조:

1. Phase 목표: BET-07 체크 기능이 공식적으로 추적되고 검증된다
2. 검증 일시/상태

3. 목표 달성 여부 - 관찰 가능한 진실 테이블 (7개 항목):

   | # | 진실 | 상태 | 근거 |
   |---|------|------|------|
   | 1 | 아무도 베팅하지 않은 상태(currentBetAmount===0)에서 선 플레이어가 체크를 선택할 수 있다 | VERIFIED | processBetAction case 'check' - currentBetAmount===0 조건 검증, 테스트 '체크: currentBetAmount===0일 때 가능' 통과 |
   | 2 | 체크는 베팅 없이 다음 플레이어로 턴을 넘긴다 | VERIFIED | check case에서 player.lastBetAction = { type: 'check' } 설정 후 _advanceBettingTurn() 호출, currentBet 변경 없음 |
   | 3 | 이후 레이즈 발생 시 체크한 플레이어에게 다시 콜/레이즈 기회가 주어진다 | VERIFIED | 레이즈 시 _bettingActed를 레이즈 플레이어만 남기고 초기화, 테스트 '레이즈 후 순환' 통과 |
   | 4 | currentBetAmount > 0일 때 체크가 거부된다 | VERIFIED | 'INVALID_ACTION: cannot check when currentBetAmount > 0' 에러, 테스트 '체크: currentBetAmount>0일 때 에러' 통과 |
   | 5 | 선 권한이 없는 플레이어의 체크가 거부된다 | VERIFIED | 'INVALID_ACTION: only the opening bettor can check' 에러, openingBettorSeatIndex 불일치 검증 |
   | 6 | 전원 체크 시 베팅이 종료되고 showdown으로 전환된다 | VERIFIED | 테스트 '전원 체크 시 showdown 전환' 통과 |
   | 7 | 클라이언트 UI에 체크 버튼이 표시되고 적절한 조건에서만 활성화된다 | VERIFIED | BettingPanel.tsx canCheck = callAmount === 0 && isEffectiveSen |

4. 필수 아티팩트 검증:
   - packages/shared/src/types/game.ts: BetAction에 { type: 'check' } 존재, openingBettorSeatIndex 필드 존재
   - packages/server/src/game-engine.ts: processBetAction() case 'check' 완전 구현
   - packages/client/src/components/layout/BettingPanel.tsx: canCheck 조건 + 체크 버튼 렌더링
   - packages/client/src/pages/RoomPage.tsx: isEffectiveSen 계산 (openingBettorSeatIndex 비교)
   - packages/server/src/game-engine.test.ts: 체크 관련 테스트 5개+ 존재

5. 테스트 커버리지: Task 1에서 수집한 테스트 실행 결과 포함

6. 요구사항 매핑:
   | 요구사항 | 상태 | 근거 |
   |---------|------|------|
   | BET-07 | SATISFIED | 서버 엔진, 클라이언트 UI, 공유 타입, 테스트 커버리지 모두 확인 |

7. 결론: BET-07 orphaned gap 해소 - 코드 구현 확인 + 공식 검증 기록 생성 완료
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && test -f .planning/phases/15-bet07-check-verification/15-VERIFICATION.md && grep -c "VERIFIED" .planning/phases/15-bet07-check-verification/15-VERIFICATION.md && grep "status: passed" .planning/phases/15-bet07-check-verification/15-VERIFICATION.md && grep "BET-07" .planning/phases/15-bet07-check-verification/15-VERIFICATION.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/15-bet07-check-verification/15-VERIFICATION.md 파일이 존재한다
    - 파일 프론트매터에 `status: passed`가 포함된다
    - 파일 본문에 "VERIFIED" 문자열이 7회 이상 등장한다 (7개 진실)
    - 파일 본문에 "BET-07" 문자열이 3회 이상 등장한다
    - 파일 본문에 "SATISFIED" 문자열이 1회 이상 등장한다
    - 파일에 `processBetAction` 문자열이 포함된다 (서버 구현 근거)
    - 파일에 `BettingPanel` 문자열이 포함된다 (클라이언트 구현 근거)
    - 파일에 `openingBettorSeatIndex` 문자열이 포함된다 (선 권한 메커니즘 근거)
  </acceptance_criteria>
  <done>15-VERIFICATION.md가 Phase 04 형식에 맞게 작성되고, BET-07의 7개 세부 진실이 모두 VERIFIED로 기록됨</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/15-bet07-check-verification/15-VERIFICATION.md` 성공
- `grep -c "VERIFIED" 15-VERIFICATION.md` 결과가 7 이상
- `grep "status: passed" 15-VERIFICATION.md` 결과가 1줄
- `npx vitest run packages/server/src/game-engine.test.ts` 전체 통과
</verification>

<success_criteria>
BET-07 체크 기능에 대한 공식 VERIFICATION.md가 존재하고, 서버/클라이언트/타입/테스트 4개 계층의 구현 검증이 기록되며, 모든 관련 테스트가 통과함
</success_criteria>

<output>
완료 후 `.planning/phases/15-bet07-check-verification/15-02-SUMMARY.md` 생성
</output>
