---
phase: 10.1-stitch-ui
plan: 03
type: execute
wave: 2
depends_on: ["10.1-01"]
files_modified:
  - packages/client/src/components/layout/GameTable.v2.tsx
  - packages/client/src/components/game/PlayerSeat.v2.tsx
autonomous: false
requirements:
  - STITCH-GAMETABLE
  - STITCH-PLAYERSEAT
must_haves:
  truths:
    - "GameTable.v2.tsx가 원본 prop interface를 완전히 준수하면서 D-09 군용담요 스타일이 적용되어 있다"
    - "PlayerSeat.v2.tsx가 기존 HwatuCard 컴포넌트를 import하여 사용한다 (3D flip 보존)"
    - "원형 자리 배치가 2~6인 모든 인원에서 동작한다"
  artifacts:
    - path: "packages/client/src/components/layout/GameTable.v2.tsx"
      provides: "Stitch 기반 게임 테이블"
      min_lines: 60
    - path: "packages/client/src/components/game/PlayerSeat.v2.tsx"
      provides: "Stitch 기반 플레이어 시트"
      min_lines: 40
  key_links:
    - from: "GameTable.v2.tsx"
      to: "PlayerSeat.v2.tsx"
      via: "players.map → PlayerSeat 렌더링"
      pattern: "PlayerSeat"
    - from: "PlayerSeat.v2.tsx"
      to: "HwatuCard"
      via: "카드 표시에 HwatuCard 컴포넌트 사용"
      pattern: "import.*HwatuCard"
---

<objective>
게임 테이블(GameTable) + 플레이어 시트(PlayerSeat) UI 고도화

Purpose: 게임의 핵심 화면인 게임 테이블과 개별 플레이어 시트를 D-09 군용담요 컨셉으로 개선한다.
Output: GameTable.v2.tsx, PlayerSeat.v2.tsx 생성 및 육안 확인 후 swap
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-stitch-ui/10.1-CONTEXT.md
@.planning/phases/10.1-stitch-ui/10.1-RESEARCH.md
@.planning/phases/10.1-stitch-ui/DESIGN.md
@.planning/phases/10.1-stitch-ui/10.1-01-SUMMARY.md
@packages/client/src/components/layout/GameTable.tsx
@packages/client/src/components/game/PlayerSeat.tsx
@packages/client/src/components/game/HwatuCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: GameTable + PlayerSeat Stitch 생성 및 React 변환</name>
  <files>packages/client/src/components/layout/GameTable.v2.tsx, packages/client/src/components/game/PlayerSeat.v2.tsx</files>
  <read_first>
    - .planning/phases/10.1-stitch-ui/DESIGN.md
    - .planning/phases/10.1-stitch-ui/10.1-01-SUMMARY.md (Stitch 도구 파라미터 실측)
    - packages/client/src/components/layout/GameTable.tsx (원본 — 레이아웃 + 원형 배치 로직)
    - packages/client/src/components/game/PlayerSeat.tsx (원본 — 모바일/데스크톱 이중 렌더, HwatuCard 사용)
    - packages/client/src/components/game/HwatuCard.tsx (v2에서도 반드시 import하여 사용)
  </read_first>
  <action>
**GameTable Stitch 생성:**

1. `mcp__stitch__generate_screen_from_text` 호출:
   - 프롬프트: "섯다 게임 테이블. D-09 군용담요 컨셉 — 어두운 올리브 배경 위에 플레이어들이 원형으로 배치. 중앙에 판돈(pot) 표시. 배경 이미지 오버레이(/img/background.jpg). 군용 캔버스 질감. 플레이어 2~6명 원형 배치. 각 자리에 닉네임, 칩 잔액, 카드 2장 표시 영역. 현재 차례인 플레이어는 올리브 그린 하이라이트."
   - projectId, designSystemId 사용

2. HTML → React TSX + Tailwind 변환:
   - 원본 GameTable.tsx prop interface 완전 준수:
     ```typescript
     interface GameTableProps {
       players: PlayerState[];
       myPlayerId: string | null;
       currentPlayerIndex: number;
       pot: number;
       visibleCardCounts?: Record<string, number>;
       sharedCard?: Card;
       mode?: GameMode;
       dealingComplete?: boolean;
       myFlippedCardIndices?: Set<number>;
     }
     ```
   - 원형 배치 CSS custom properties 로직 원본에서 그대로 가져오기
   - 모바일/데스크톱 반응형 로직 유지 (md 이상: 원형 배치, 미만: flex 아이템)
   - PlayerSeat 컴포넌트 렌더링 부분은 `PlayerSeat`를 import하여 사용 (이후 v2로 교체)

**PlayerSeat Stitch 생성:**

1. `mcp__stitch__generate_screen_from_text` 호출:
   - 프롬프트: "섯다 게임 개별 플레이어 자리. D-09 군용담요 컨셉. 닉네임 레이블(명조 폰트), 칩 잔액 표시, 화투 카드 2장 영역(카드는 별도 컴포넌트), 현재 차례 시 올리브 그린 테두리 glow. 컴팩트 사이즈(데스크톱 원형 배치용). 다이 상태 시 opacity 감소."

2. HTML → React TSX + Tailwind 변환:
   - 원본 PlayerSeat.tsx prop interface 완전 준수
   - **핵심: HwatuCard 컴포넌트를 반드시 import하여 카드 표시에 사용** (함정 4 방지)
     ```typescript
     import { HwatuCard } from '../game/HwatuCard';
     ```
   - 모바일/데스크톱 이중 렌더 패턴 유지
   - 카드 flip 인터랙션은 HwatuCard가 처리하므로 건드리지 않음

**양쪽 모두:**
- Stitch output의 시각 디자인만 활용하고, 게임 로직/배치 로직은 원본에서 가져온다
- TypeScript 컴파일 오류 없는 상태 확인
  </action>
  <verify>
    <automated>test -f "packages/client/src/components/layout/GameTable.v2.tsx" && test -f "packages/client/src/components/game/PlayerSeat.v2.tsx" && grep -c "HwatuCard" "packages/client/src/components/game/PlayerSeat.v2.tsx" && echo "Both v2 files exist, PlayerSeat uses HwatuCard"</automated>
  </verify>
  <acceptance_criteria>
    - packages/client/src/components/layout/GameTable.v2.tsx 파일이 존재한다
    - packages/client/src/components/game/PlayerSeat.v2.tsx 파일이 존재한다
    - PlayerSeat.v2.tsx에 "import.*HwatuCard" 패턴이 존재한다 (함정 4 방지)
    - GameTable.v2.tsx에 "PlayerState" 타입 참조가 포함되어 있다
    - GameTable.v2.tsx에 pot 표시 로직이 포함되어 있다
    - PlayerSeat.v2.tsx의 props가 원본과 동일하다 (seatIndex, totalPlayers, player, isMe, isCurrentTurn 등)
  </acceptance_criteria>
  <done>GameTable.v2.tsx와 PlayerSeat.v2.tsx가 D-09 컨셉으로 생성되고 HwatuCard 연동 유지됨</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: GameTable + PlayerSeat 육안 확인 및 swap (D-07)</name>
  <what-built>Stitch 기반 GameTable.v2.tsx와 PlayerSeat.v2.tsx — D-09 군용담요 컨셉의 게임 핵심 화면</what-built>
  <how-to-verify>
    1. GameTable.tsx → GameTable.v2.tsx, PlayerSeat.tsx → PlayerSeat.v2.tsx import 임시 변경
    2. `pnpm dev` 실행 후 게임 시작하여 게임 테이블 화면 확인
    3. 확인 항목:
       - 원형 자리 배치가 정상 동작하는가 (2~6인)
       - D-09 군용담요 컨셉이 반영되었는가
       - 카드 flip 인터랙션이 정상 동작하는가 (HwatuCard 보존)
       - 판돈(pot) 중앙 표시가 정상인가
       - 현재 차례 플레이어 하이라이트가 정상인가
    4. 만족 시 "approved" — Claude가 원본 swap 진행
    5. 불만족 시 구체적 피드백
  </how-to-verify>
  <resume-signal>"approved" 또는 수정 피드백</resume-signal>
</task>

</tasks>

<verification>
- GameTable.v2.tsx가 원형 배치, pot 표시, 반응형을 정상 지원한다
- PlayerSeat.v2.tsx가 HwatuCard를 import하여 3D flip이 보존된다
- D-09 컨셉이 시각적으로 반영되어 있다
</verification>

<success_criteria>
- GameTable.v2.tsx, PlayerSeat.v2.tsx 생성 완료
- 개발 서버에서 게임 화면 정상 렌더링 및 기존 기능 동작
- 사용자 승인 후 swap 완료
</success_criteria>

<output>
완료 후 `.planning/phases/10.1-stitch-ui/10.1-03-SUMMARY.md` 생성
</output>
