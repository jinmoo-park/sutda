---
phase: 10.1-stitch-ui
plan: 07
type: execute
wave: 6
depends_on: ["10.1-02", "10.1-03", "10.1-04", "10.1-05", "10.1-06"]
files_modified:
  - packages/client/src/components/ui/button.tsx
  - packages/client/src/components/ui/badge.tsx
  - packages/client/src/components/ui/card.tsx
  - packages/client/src/components/ui/dialog.tsx
  - packages/client/src/components/layout/ResultScreen.v2.tsx
autonomous: false
requirements:
  - STITCH-COMMON-UI
  - STITCH-RESULTSCREEN
must_haves:
  truths:
    - "shadcn/ui 컴포넌트(Button, Badge, Card, Dialog)가 D-09 군용담요 컨셉에 맞게 커스터마이징되어 있다"
    - "ResultScreen.v2.tsx가 원본 prop interface를 준수하면서 D-09 스타일이 적용되어 있다"
    - "pnpm dev로 전체 게임 플로우가 정상 동작한다 (리그레션 없음)"
  artifacts:
    - path: "packages/client/src/components/ui/button.tsx"
      provides: "D-09 커스터마이징된 Button 컴포넌트"
    - path: "packages/client/src/components/layout/ResultScreen.v2.tsx"
      provides: "Stitch 기반 결과 화면"
      min_lines: 40
  key_links:
    - from: "button.tsx"
      to: "모든 컴포넌트"
      via: "공통 UI 요소로 전체 앱에 영향"
      pattern: "import.*Button.*from.*ui/button"
---

<objective>
공통 UI 요소 커스터마이징 + ResultScreen + 전체 리그레션 확인

Purpose: 모든 v2 컴포넌트에서 공통으로 사용되는 shadcn/ui 요소를 D-09 컨셉에 맞게 커스터마이징하고, 마지막 미처리 레이아웃 컴포넌트(ResultScreen)를 처리하고, 전체 리그레션을 확인한다.
Output: 커스터마이징된 공통 UI + ResultScreen.v2 + 전체 동작 확인
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-stitch-ui/10.1-CONTEXT.md
@.planning/phases/10.1-stitch-ui/DESIGN.md
@.planning/phases/10.1-stitch-ui/10.1-01-SUMMARY.md
@packages/client/src/components/ui/button.tsx
@packages/client/src/components/ui/badge.tsx
@packages/client/src/components/ui/card.tsx
@packages/client/src/components/ui/dialog.tsx
@packages/client/src/components/layout/ResultScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn/ui 공통 컴포넌트 D-09 커스터마이징 + ResultScreen.v2</name>
  <files>packages/client/src/components/ui/button.tsx, packages/client/src/components/ui/badge.tsx, packages/client/src/components/ui/card.tsx, packages/client/src/components/ui/dialog.tsx, packages/client/src/components/layout/ResultScreen.v2.tsx</files>
  <read_first>
    - .planning/phases/10.1-stitch-ui/DESIGN.md
    - packages/client/src/components/ui/button.tsx
    - packages/client/src/components/ui/badge.tsx
    - packages/client/src/components/ui/card.tsx
    - packages/client/src/components/ui/dialog.tsx
    - packages/client/src/components/layout/ResultScreen.tsx
  </read_first>
  <action>
**shadcn/ui 커스터마이징:**

shadcn/ui 컴포넌트들의 Tailwind 클래스를 D-09 군용담요 컨셉에 맞게 수정한다. 주의: shadcn/ui 컴포넌트는 v2 파일 없이 원본을 직접 수정한다 (모든 v2 컴포넌트가 이미 swap 완료된 후이므로).

**button.tsx:**
- default variant: 올리브 그린 배경(`bg-primary`), 더 각진 모서리(`rounded-sm` 또는 `rounded-none + border`), 군용 장비 느낌
- destructive variant: 바랜 빨간색, 경고 느낌
- outline variant: 올리브 보더, 투명 배경
- 호버 시: 미세한 밝기 변화 (글로시하지 않게)
- 폰트: 명조 weight 변경은 하지 않음 (기존 유지)

**badge.tsx:**
- 군용 태그 느낌 — 올리브/카키 배경에 밝은 텍스트
- 각진 모서리

**card.tsx:**
- 어두운 올리브 배경, 미세한 보더
- 군용 캔버스/패브릭 텍스처 느낌의 그림자

**dialog.tsx:**
- 오버레이: 더 어두운 반투명 올리브
- 컨텐츠: 어두운 올리브 배경, 군용 느낌 보더
- 각진 모서리 허용

**ResultScreen.v2.tsx:**

1. Stitch 프롬프트: "섯다 게임 결과 화면. D-09 군용담요 컨셉. 승자 강조(올리브 그린 하이라이트), 각 플레이어의 카드 공개, 족보명 표시, 칩 변동(+/- 금액), 땡값 내역(있는 경우), '다음 판' 버튼. 재경기 시 img/regame 오버레이."

2. React 변환:
   - 원본 prop interface 완전 준수: `gameState: GameState, myPlayerId: string | null, roomId: string, isRematch?: boolean`
   - 원본의 승자/패자 표시, 칩 변동 계산, 땡값 표시, 다음 판 버튼 로직 그대로 유지
   - HwatuCard 컴포넌트 사용
   - 재경기 오버레이 `/img/regame.png` 유지
  </action>
  <verify>
    <automated>test -f "packages/client/src/components/layout/ResultScreen.v2.tsx" && echo "ResultScreen.v2 exists" && cd "packages/client" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ResultScreen.v2.tsx 파일이 존재한다
    - ResultScreen.v2.tsx에 "GameState" 타입 참조가 포함되어 있다
    - ResultScreen.v2.tsx에 "HwatuCard" import가 포함되어 있다
    - button.tsx에 D-09 관련 변경이 적용되어 있다 (원본 대비 클래스 변경)
    - TypeScript 컴파일 오류가 없다 (또는 기존 수준과 동일)
  </acceptance_criteria>
  <done>shadcn/ui 공통 컴포넌트가 D-09 컨셉으로 커스터마이징되고 ResultScreen.v2가 생성됨</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 전체 리그레션 확인 (D-07 + D-08)</name>
  <what-built>Phase 10.1 전체 작업 결과 — 모든 v2 컴포넌트 swap 완료 + shadcn/ui 커스터마이징 + ResultScreen.v2</what-built>
  <how-to-verify>
    1. `pnpm dev` 실행
    2. 전체 게임 플로우 테스트:
       a. 메인 화면 → 방 생성
       b. 대기실 → 게임 시작
       c. 모드 선택 → 등교 → 셔플 → 기리 → 카드 배분
       d. 카드 flip → 베팅 → 결과 화면
       e. 다음 판 진행
    3. D-09 군용담요 컨셉이 전체적으로 일관성 있게 적용되었는가 확인
    4. 기존 기능이 모두 정상 동작하는가 확인
    5. `pnpm test` 실행하여 리그레션 테스트 확인 (D-08)
    6. 만족 시 "approved"
  </how-to-verify>
  <resume-signal>"approved" — Phase 10.1 완료. 수정 필요 시 구체적 피드백.</resume-signal>
</task>

</tasks>

<verification>
- shadcn/ui 컴포넌트가 D-09 컨셉에 맞게 커스터마이징되어 있다
- ResultScreen.v2가 원본 기능을 보존하면서 D-09 스타일이 적용되어 있다
- 전체 게임 플로우가 리그레션 없이 동작한다
- `pnpm test` 통과 (D-08)
</verification>

<success_criteria>
- shadcn/ui 커스터마이징 완료
- ResultScreen.v2 생성 + swap 완료
- 전체 게임 플로우 육안 확인 완료
- `pnpm test` 통과
</success_criteria>

<output>
완료 후 `.planning/phases/10.1-stitch-ui/10.1-07-SUMMARY.md` 생성
</output>
