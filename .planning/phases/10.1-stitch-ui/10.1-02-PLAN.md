---
phase: 10.1-stitch-ui
plan: 02
type: execute
wave: 2
depends_on: ["10.1-01"]
files_modified:
  - packages/client/src/pages/MainPage.v2.tsx
  - packages/client/src/components/layout/WaitingRoom.v2.tsx
autonomous: false
requirements:
  - STITCH-MAINPAGE
  - STITCH-WAITINGROOM
must_haves:
  truths:
    - "MainPage.v2.tsx가 원본과 동일한 기능(방 생성, 방 참여)을 수행하면서 D-09 군용담요 스타일이 적용되어 있다"
    - "WaitingRoom.v2.tsx가 원본 prop interface(RoomState, myPlayerId, roomId)를 완전히 준수한다"
    - "개발 서버에서 두 컴포넌트가 정상 렌더링된다"
  artifacts:
    - path: "packages/client/src/pages/MainPage.v2.tsx"
      provides: "Stitch 기반 메인 페이지 새 껍데기"
      min_lines: 50
    - path: "packages/client/src/components/layout/WaitingRoom.v2.tsx"
      provides: "Stitch 기반 대기실 새 껍데기"
      min_lines: 40
  key_links:
    - from: "MainPage.v2.tsx"
      to: "socket.io client"
      via: "방 생성/참여 emit"
      pattern: "emit.*create-room|join-room"
    - from: "WaitingRoom.v2.tsx"
      to: "RoomState"
      via: "props.roomState"
      pattern: "roomState.*players"
---

<objective>
메인 화면(MainPage) + 대기실(WaitingRoom) UI 고도화

Purpose: 사용자가 처음 접하는 두 화면을 D-09 군용담요 컨셉으로 프로페셔널하게 개선한다.
Output: MainPage.v2.tsx, WaitingRoom.v2.tsx 생성 및 육안 확인 후 swap
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-stitch-ui/10.1-CONTEXT.md
@.planning/phases/10.1-stitch-ui/10.1-RESEARCH.md
@.planning/phases/10.1-stitch-ui/DESIGN.md
@.planning/phases/10.1-stitch-ui/10.1-01-SUMMARY.md
@packages/client/src/pages/MainPage.tsx
@packages/client/src/components/layout/WaitingRoom.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: MainPage + WaitingRoom Stitch 생성 및 React 변환</name>
  <files>packages/client/src/pages/MainPage.v2.tsx, packages/client/src/components/layout/WaitingRoom.v2.tsx</files>
  <read_first>
    - .planning/phases/10.1-stitch-ui/DESIGN.md
    - .planning/phases/10.1-stitch-ui/10.1-01-SUMMARY.md (Stitch projectId, designSystemId, 도구 파라미터 실측 결과)
    - packages/client/src/pages/MainPage.tsx (원본 — 기능 로직 파악)
    - packages/client/src/components/layout/WaitingRoom.tsx (원본 — prop interface 및 기능 로직 파악)
  </read_first>
  <action>
**MainPage Stitch 생성:**

1. `mcp__stitch__generate_screen_from_text` 호출:
   - 프롬프트: "한국 화투 카드 게임(섯다) 메인 화면. D-09 컨셉: 공장에서 군용담요 깔아놓고 하는 느낌. 올리브 드랩/카키 팔레트, 거친 산업적 텍스처. 메인 타이틀 이미지 영역(상단), 닉네임 입력 필드, '방 만들기' 버튼(primary 올리브 그린), '방 참여하기' 버튼(secondary), 방 코드 입력 필드(조건부 표시). 어두운 배경(hsl 70 15% 8%), 명조 폰트, 군용 장비 느낌의 각진 버튼."
   - 10.1-01-SUMMARY.md에서 파악한 projectId 사용

2. `mcp__stitch__get_screen`으로 HTML output 추출

3. HTML → React TSX + Tailwind 변환:
   - inline style → Tailwind 클래스 변환
   - 원본 MainPage.tsx의 모든 useState, socket emit 로직 통합
   - `useNavigate`, `socket.emit('create-room')`, `socket.emit('join-room')` 등 기존 로직 그대로 유지
   - 타이틀 이미지: `<img src="/img/main_tilte.jpg" />` 유지
   - 파일명: `packages/client/src/pages/MainPage.v2.tsx`

**WaitingRoom Stitch 생성:**

1. `mcp__stitch__generate_screen_from_text` 호출:
   - 프롬프트: "섯다 게임 대기실 화면. D-09 군용담요 컨셉. 방 코드 표시(복사 버튼 포함), 참여자 목록(닉네임 + 칩 금액), 방장에게만 '게임 시작' 버튼, 올리브/카키 팔레트, 명조 폰트, 군용 캔버스 질감."

2. HTML → React TSX + Tailwind 변환:
   - 원본 WaitingRoom.tsx prop interface 완전 준수:
     ```typescript
     interface WaitingRoomProps {
       roomState: RoomState;
       myPlayerId: string | null;
       roomId: string;
     }
     ```
   - 기존 이벤트 핸들러(socket emit) 그대로 연결
   - 파일명: `packages/client/src/components/layout/WaitingRoom.v2.tsx`

**양쪽 모두:**
- Stitch output이 만족스럽지 않으면 `mcp__stitch__edit_screens`로 수정 시도
- 최종 output은 TypeScript 컴파일 오류 없는 상태여야 함
- D-09 컨셉이 시각적으로 반영되어야 함 (올리브/카키 색상, 거친 텍스처 느낌)
  </action>
  <verify>
    <automated>test -f "packages/client/src/pages/MainPage.v2.tsx" && test -f "packages/client/src/components/layout/WaitingRoom.v2.tsx" && echo "Both v2 files exist"</automated>
  </verify>
  <acceptance_criteria>
    - packages/client/src/pages/MainPage.v2.tsx 파일이 존재한다
    - packages/client/src/components/layout/WaitingRoom.v2.tsx 파일이 존재한다
    - MainPage.v2.tsx에 "create-room" 또는 "join-room" emit 로직이 포함되어 있다
    - WaitingRoom.v2.tsx에 "RoomState" 타입 참조가 포함되어 있다
    - WaitingRoom.v2.tsx의 props가 원본과 동일하다 (roomState, myPlayerId, roomId)
    - 두 파일 모두 TypeScript import 문이 올바르다
  </acceptance_criteria>
  <done>MainPage.v2.tsx와 WaitingRoom.v2.tsx가 D-09 컨셉으로 생성되고 원본 기능 로직이 통합됨</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: MainPage + WaitingRoom 육안 확인 및 swap (D-07)</name>
  <what-built>Stitch 기반 MainPage.v2.tsx와 WaitingRoom.v2.tsx — D-09 군용담요 컨셉의 새 UI</what-built>
  <how-to-verify>
    1. RoomPage.tsx에서 MainPage import를 MainPage.v2로 임시 변경하거나, App.tsx 라우팅에서 MainPage.v2를 연결
    2. `pnpm dev` 실행 후 브라우저에서 http://localhost:5173 접속
    3. 메인 화면 확인: 군용담요 컨셉 반영 여부, 닉네임 입력, 방 생성/참여 동작
    4. 방 생성 후 대기실 화면 확인: 참여자 목록, 방 코드 복사, 게임 시작 버튼
    5. 만족 시 "approved" — Claude가 원본 파일과 swap 진행
    6. 불만족 시 구체적 피드백 제공 — Claude가 수정 후 재확인
  </how-to-verify>
  <resume-signal>"approved" 입력 시 원본 swap 진행. 수정 필요 시 구체적 피드백 제공.</resume-signal>
</task>

</tasks>

<verification>
- MainPage.v2.tsx가 원본의 방 생성/참여 기능을 완전히 보존한다
- WaitingRoom.v2.tsx가 원본 prop interface를 완전히 준수한다
- D-09 군용담요 컨셉이 시각적으로 반영되어 있다
- 사용자(진무)가 개발 서버에서 육안 확인 후 승인한다
</verification>

<success_criteria>
- MainPage.v2.tsx, WaitingRoom.v2.tsx 파일 생성 완료
- 개발 서버에서 두 화면이 정상 렌더링되고 기존 기능 동작
- 사용자 승인 후 원본 파일과 swap 완료
</success_criteria>

<output>
완료 후 `.planning/phases/10.1-stitch-ui/10.1-02-SUMMARY.md` 생성
</output>
