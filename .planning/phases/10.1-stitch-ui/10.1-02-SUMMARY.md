---
phase: 10.1-stitch-ui
plan: "02"
subsystem: ui
tags: [stitch, react, d-09, mainpage, waitingroom, wool-texture, brass-gradient]

requires:
  - phase: 10.1-stitch-ui
    plan: "01"
    provides: "Stitch projectId, designSystemId, DESIGN.md"

provides:
  - "MainPage.v2.tsx — Stitch 레이아웃 기반 메인 화면 (wool-texture, brass gradient, 방 생성/참여 기능 완전 보존)"
  - "WaitingRoom.v2.tsx — Stitch 레이아웃 기반 대기실 (ROOM_ID 헤더, 플레이어 카드, START GAME 3D 버튼)"

affects:
  - 10.1-03

tech-stack:
  added: []
  patterns:
    - "Stitch HTML을 React 변환 시 레이아웃 구조(div 계층, 시각 언어)를 primary skeleton으로 사용"
    - "wool-texture: radial-gradient dot-grain + backgroundColor 조합으로 CSS로 구현"
    - "brass gradient: linear-gradient(135deg, #e4c379 0%, #b69853 100%) — 3D offset shadow 함께"
    - "surface layering: background → surface_container_low → surface_container → surface_container_high 계층"
    - "ROOM_ID uppercase 표시 패턴 (roomId.toUpperCase())"

key-files:
  created:
    - "packages/client/src/pages/MainPage.v2.tsx"
    - "packages/client/src/components/layout/WaitingRoom.v2.tsx"
  modified: []

key-decisions:
  - "Stitch가 생성한 HTML 레이아웃 구조(div hierarchy, CSS classes)를 React의 primary skeleton으로 사용 — 기존 React 구조에 색상만 입히는 방식 탈피"
  - "Stitch 생성 첫 번째 스크린(게임 진행 화면)을 메인 입장 화면에 맞게 재구성하되 시각 언어(wool texture, brass gradient, 3D offset button)는 그대로 채용"
  - "STITCH_API_KEY 방식으로 -d 플래그 사용 (Bun.file 의존 -f 플래그 불가)"
  - "import.meta.env 에러는 기존 코드 공유 이슈, 개발 서버 정상 동작"

requirements-completed:
  - STITCH-MAINPAGE
  - STITCH-WAITINGROOM

duration: 40min
completed: "2026-04-01"
---

# Phase 10.1 Plan 02: MainPage + WaitingRoom Stitch 기반 UI 재작성 Summary

**Stitch "Iron & Felt" / "Tactical Heritage" 디자인 시스템에서 wool-texture, brass gradient, 3D offset 버튼, surface layering 레이아웃 구조를 직접 채용하여 MainPage.v2.tsx와 WaitingRoom.v2.tsx를 재작성 — 원본 게임 로직 완전 보존**

## Performance

- **Duration:** ~40분
- **Started:** 2026-04-01T20:00Z
- **Completed:** 2026-04-01T20:40Z
- **Tasks:** 2 (Task 1 완료 후 재작성, Task 2 체크포인트)
- **Files modified:** 2

## Accomplishments

### Stitch 스크린 생성

- MainPage 스크린: `generate_screen_from_text` → screenId `cdf4cc22e2864efa985777a04a63b791`
  - Stitch가 "Iron & Felt" 디자인 시스템 자동 생성 (olive drabs, aged brass, wool texture)
  - 레이아웃: wool-texture 배경, TopAppBar, 중앙 게임 카드 영역, brass gradient 베팅 버튼, BottomNavBar

- WaitingRoom 스크린: `generate_screen_from_text` → screenId `e50578d52c844cb7b4229cc6cec70c6f`
  - Stitch가 "Tactical Heritage" 디자인 시스템 자동 생성
  - 레이아웃: ROOM_ID TopAppBar + COPY 버튼, 플레이어 카드(rank/chip/HOST/READY 배지), 3D START GAME 버튼

### Stitch 레이아웃 → React 변환

**MainPage.v2.tsx** (293줄):
- wool-texture: `backgroundColor: '#121410'` + `backgroundImage: 'radial-gradient(#252a22 0.5px, transparent 0.5px)'`
- TopAppBar: 연결 상태 인디케이터 (녹색 점 + ONLINE/CONN 표시)
- 타이틀 이미지: `aspectRatio: '4/3'`, sepia/brightness 필터, 하단 vignette 그라디언트
- 섹션 헤더: `▸ ENTRY // COMMAND INIT` 스탬프 레이블 스타일
- Brass primary 버튼: `linear-gradient(135deg, #e4c379 → #b69853)` + 3D offset shadow (`translate(3px, 3px)`)
- 원본 로직: `socket.emit('create-room')`, `navigate('/room/:id')` 완전 보존

**WaitingRoom.v2.tsx** (296줄):
- ROOM_ID 헤더: `roomId.toUpperCase()` 표시, COPY 버튼 (copied 상태 피드백)
- 플레이어 카드: HOST(brass badge), ME(secondary badge), 순번 아이콘, 칩 금액, 진행 바
- 빈 슬롯 플레이스홀더: dashed border + "Awaiting Reinf." 텍스트
- START GAME 버튼: 방장만 표시, `canStart` 조건, 3D offset shadow, brass gradient
- 대기 메시지: 방장 외 참여자에게 "PROTOCOL ALPHA-9 // 방장 대기 중..." 표시
- 원본 Prop Interface 완전 준수: `roomState: RoomState`, `myPlayerId: string | null`, `roomId: string`

## Task Commits

1. **Task 1 (이전 세션):** Stitch 생성 + 첫 번째 React 변환 — `fd1697e`
2. **Task 2 (이번 세션):** Stitch 레이아웃 구조 기반 재작성 — `9a59af0`

## Files Created/Modified

- `packages/client/src/pages/MainPage.v2.tsx` — 293줄, Stitch 기반 메인 화면
- `packages/client/src/components/layout/WaitingRoom.v2.tsx` — 296줄, Stitch 기반 대기실

## Stitch 생성 결과 분석

Stitch가 생성한 "Iron & Felt" 디자인 시스템은 D-09 군용담요 컨셉과 매우 잘 매칭됨:
- **Surface hierarchy**: background(#121410) → surface_container_low(#1a1c18) → surface_container(#1e201c) → surface_container_highest(#333531)
- **Brass accent**: primary(#e4c379) — aged brass와 일치
- **Wool texture**: radial dot-grain CSS pattern
- **Typography philosophy**: Space Grotesk (headline) + Work Sans (body) — 군용 장비 스탬프 느낌
- **Component patterns**: border-left-4 플레이어 카드, 3D offset 버튼, dashed 빈 슬롯

## Deviations from Plan

### Stitch 레이아웃 구조 우선 채용 (사용자 피드백 반영)

**Found during:** Task 2 체크포인트 사용자 피드백
**Issue:** Task 1 결과물이 기존 React 구조에 D-09 색상만 입힌 수준 — Stitch의 시각적 우수한 레이아웃 미활용
**Fix:** Stitch HTML에서 레이아웃 구조(wool texture CSS, surface layering, brass gradient 버튼, 3D offset, 플레이어 카드 형태)를 primary skeleton으로 채용하여 완전 재작성
**Files modified:** MainPage.v2.tsx, WaitingRoom.v2.tsx
**Commit:** 9a59af0

### Stitch -f 플래그 Bun 의존성 이슈

**Found during:** Stitch 스크린 생성 중
**Issue:** `npx @_davideast/stitch-mcp tool -f file.json` 사용 시 `Bun.file()` 호출로 Node.js 환경에서 "Bun is not defined" 에러
**Fix:** `-d '{"json":"data"}'` 인라인 방식으로 대체 (list_projects 등 간단한 도구에서는 정상 동작 확인)
**No files modified**

### MainPage 스크린 — 게임 진행 화면으로 생성됨

**Found during:** Stitch HTML 확인
**Issue:** 프롬프트가 game의 입장 화면이 아닌 게임 진행 화면(베팅 버튼, 화투 카드)을 생성
**Fix:** 게임 진행 화면의 시각 언어(wool texture, brass gradient, asymmetric layout)를 입장 화면 UX에 맞게 재적용
**No additional commit needed**

## Known Stubs

없음 — 모든 기능 로직(create-room emit, join-room navigate, start-game emit, 방장 확인, canStart 체크) 완전 구현됨.

## Self-Check

- FOUND: `packages/client/src/pages/MainPage.v2.tsx`
- FOUND: `packages/client/src/components/layout/WaitingRoom.v2.tsx`
- FOUND: commit `9a59af0` (feat(10.1-02): Stitch 레이아웃 구조 기반 MainPage.v2 + WaitingRoom.v2 재작성)
- VERIFIED: `create-room` emit 로직 MainPage.v2.tsx에 존재
- VERIFIED: `start-game` emit 로직 WaitingRoom.v2.tsx에 존재
- VERIFIED: RoomState prop interface WaitingRoom.v2.tsx에 준수

## Self-Check: PASSED

---
*Phase: 10.1-stitch-ui*
*Plan: 02*
*Completed: 2026-04-01*
