---
phase: 16-session-end-visual-fix
plan: "02"
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - .planning/phases/16-session-end-visual-fix/16-VERIFICATION.md
autonomous: true
requirements:
  - SESSION-END
must_haves:
  truths:
    - "Phase 16 수정 사항이 코드 트레이서빌리티 + 구현 증거로 문서화되었다"
    - "SESSION-END 요구사항의 모든 계층(서버/클라이언트/타입)이 검증 기록으로 남았다"
  artifacts:
    - path: ".planning/phases/16-session-end-visual-fix/16-VERIFICATION.md"
      provides: "Phase 16 검증 보고서"
      contains: "status: passed"
  key_links: []
---

<objective>
Phase 16 SESSION-END gap closure 수정 사항을 VERIFICATION.md로 문서화한다. Phase 15 패턴을 따라 코드 트레이서빌리티 + 구현 증거를 기록한다 (per D-04).

Purpose: SESSION-END 요구사항이 서버/클라이언트/공유타입 전 계층에서 완전히 구현되었음을 공식 검증 기록으로 남긴다
Output: `.planning/phases/16-session-end-visual-fix/16-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-session-end-visual-fix/16-CONTEXT.md
@.planning/phases/16-session-end-visual-fix/16-01-SUMMARY.md
</context>

<interfaces>
<!-- Phase 15 VERIFICATION.md 포맷 참조 — 동일 구조 사용 -->

VERIFICATION.md 필수 섹션:
1. frontmatter (phase, verified, status, score, re_verification)
2. 목표 달성 여부 — Observable Truths 표
3. 필수 아티팩트 검증 표
4. 핵심 연결 검증 (Key Links)
5. 결론
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Phase 16 VERIFICATION.md 작성</name>
  <files>.planning/phases/16-session-end-visual-fix/16-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/15-bet07-check-verification/15-VERIFICATION.md (포맷/구조 참조)
    - .planning/phases/16-session-end-visual-fix/16-01-SUMMARY.md (Plan 01 실행 결과)
    - .planning/phases/16-session-end-visual-fix/16-CONTEXT.md (요구사항 및 결정)
    - packages/server/src/index.ts (808~810번 라인 — disconnect 핸들러 수정 확인)
    - packages/client/src/components/layout/GameTable.tsx (isConnected prop 전달 확인)
    - packages/client/src/components/game/PlayerSeat.tsx (opacity-50 적용 확인, ~21/63/102번 라인)
    - packages/shared/src/types/room.ts (RoomPlayer.isConnected 정의 확인)
    - packages/server/src/room-manager.ts (disconnectPlayer 구현, ~99번 라인)
    - .planning/v1-MILESTONE-AUDIT.md (SESSION-END partial gap 원본 설명)
  </read_first>
  <action>
Phase 15 VERIFICATION.md 포맷을 그대로 따라 16-VERIFICATION.md를 작성한다 (per D-04).

frontmatter 필드: phase=16-session-end-visual-fix, verified=(현재 시각 ISO 8601), status=passed, score="N/N must-haves verified", re_verification=false

**검증해야 할 Observable Truths (코드에서 실제 확인):**

1. `RoomPlayer` 타입에 `isConnected: boolean` 필드가 정의되어 있다
   - 근거: `packages/shared/src/types/room.ts` 에서 grep

2. `roomManager.disconnectPlayer()`가 `player.isConnected = false`로 설정한다
   - 근거: `packages/server/src/room-manager.ts` ~99번 라인

3. disconnect 핸들러 game-playing 분기에서 `disconnectPlayer()` 직후 `room-state` emit이 존재한다
   - 근거: `packages/server/src/index.ts` 808-809번 라인 (Plan 01에서 추가)

4. `GameTable.tsx`에서 `PlayerSeat`에 `isConnected` prop을 전달한다
   - 근거: cbeac35 commit, `isConnected={roomState?.players.find(...)?.isConnected ?? true}`

5. `PlayerSeat.tsx`에서 `!isConnected` 시 `opacity-50` 클래스를 적용한다
   - 근거: `PlayerSeat.tsx` ~21/63/102번 라인

**필수 아티팩트:** 위 5개 파일의 관련 코드 라인 번호와 내용을 표로 정리

**핵심 연결 (Key Links):**
- disconnect 핸들러 -> disconnectPlayer() -> room-state emit -> 클라이언트 roomState 갱신 -> GameTable isConnected prop -> PlayerSeat opacity-50
- 각 연결의 코드 근거를 grep 패턴으로 기록

**결론:** SESSION-END 요구사항이 서버/클라이언트/공유타입 전 계층에서 완전히 구현됨. Phase 16에서 해소된 gap: disconnect 핸들러 game-playing 분기의 room-state emit 누락.
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && test -f .planning/phases/16-session-end-visual-fix/16-VERIFICATION.md && grep -c "status: passed" .planning/phases/16-session-end-visual-fix/16-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/16-session-end-visual-fix/16-VERIFICATION.md` 파일이 존재한다
    - frontmatter에 `status: passed` 문자열이 포함된다
    - `grep -c "VERIFIED" .planning/phases/16-session-end-visual-fix/16-VERIFICATION.md` 결과가 5 이상이다 (5개 truth 각각 검증)
    - `grep -c "isConnected" .planning/phases/16-session-end-visual-fix/16-VERIFICATION.md` 결과가 3 이상이다
    - `grep -c "room-state" .planning/phases/16-session-end-visual-fix/16-VERIFICATION.md` 결과가 2 이상이다
    - `grep "score:" .planning/phases/16-session-end-visual-fix/16-VERIFICATION.md` 에 숫자가 포함된다
  </acceptance_criteria>
  <done>SESSION-END 요구사항의 전 계층 구현이 코드 근거와 함께 검증 문서로 기록되었다</done>
</task>

</tasks>

<verification>
1. VERIFICATION.md 파일이 존재하고 Phase 15와 동일한 구조를 따른다
2. 모든 Observable Truth에 코드 근거(파일명 + 라인 번호)가 기록되었다
3. frontmatter status가 passed이다
</verification>

<success_criteria>
- 16-VERIFICATION.md가 존재하고 status: passed 상태이다
- 5개 Observable Truth 모두 VERIFIED 상태이다
- 코드 트레이서빌리티가 파일명 + 라인 번호 수준으로 기록되었다
</success_criteria>

<output>
완료 후 `.planning/phases/16-session-end-visual-fix/16-02-SUMMARY.md` 생성
</output>
