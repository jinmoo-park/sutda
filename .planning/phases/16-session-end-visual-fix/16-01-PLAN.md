---
phase: 16-session-end-visual-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/index.ts
autonomous: true
requirements:
  - SESSION-END
user_setup: []

must_haves:
  truths:
    - "게임 중 플레이어 disconnect 시 다른 클라이언트의 roomState.players[i].isConnected가 false로 업데이트된다"
    - "PlayerSeat에 opacity-50 클래스가 적용되어 재접속 대기 시각 표시가 동작한다"
  artifacts:
    - path: "packages/server/src/index.ts"
      provides: "disconnect 핸들러 game-playing 분기에서 room-state emit"
      contains: "io.to(roomId).emit('room-state'"
  key_links:
    - from: "packages/server/src/index.ts"
      to: "클라이언트 roomState"
      via: "io.to(roomId).emit('room-state', roomManager.getRoom(roomId))"
      pattern: "io\\.to\\(roomId\\)\\.emit\\('room-state'"
    - from: "packages/client/src/components/layout/GameTable.tsx"
      to: "packages/client/src/components/game/PlayerSeat.tsx"
      via: "isConnected prop 전달"
      pattern: "isConnected=\\{roomState"
---

<objective>
서버 disconnect 핸들러의 game-playing 분기에서 `roomManager.disconnectPlayer()` 호출 직후 `room-state` 이벤트를 emit하여, 클라이언트 roomState가 갱신되고 PlayerSeat의 `opacity-50` 시각 표시가 정상 동작하도록 한다.

Purpose: 게임 중 접속이 끊긴 플레이어를 다른 참가자가 시각적으로 확인할 수 있는 데이터 흐름 완성 (per D-01, D-02)
Output: `packages/server/src/index.ts` 1줄 수정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-session-end-visual-fix/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: disconnect 핸들러 game-playing 분기에 room-state emit 추가</name>
  <files>packages/server/src/index.ts</files>
  <read_first>
    - packages/server/src/index.ts (특히 800~825번 라인 — disconnect 핸들러 game-playing 분기)
    - packages/server/src/room-manager.ts (disconnectPlayer 메서드 확인)
  </read_first>
  <action>
`packages/server/src/index.ts` 808번 라인 `roomManager.disconnectPlayer(roomId, socket.id);` 직후(809번 라인 이전)에 다음 1줄을 추가한다 (per D-01):

```typescript
io.to(roomId).emit('room-state', roomManager.getRoom(roomId));
```

이 패턴은 index.ts 전역에서 동일하게 사용되는 room-state broadcast 패턴이다 (per D-02). 기존 대기실 disconnect 핸들러에도 동일한 패턴이 이미 존재한다.

위치 정리:
- 808: `roomManager.disconnectPlayer(roomId, socket.id);`
- 809 (NEW): `io.to(roomId).emit('room-state', roomManager.getRoom(roomId));`
- 이후 기존 코드 (`const disconnectedPlayerId = ...`) 계속

공유 타입 변경 없음, 신규 이벤트 추가 없음 (per D-02).
클라이언트 측 GameTable.tsx의 isConnected prop 전달은 cbeac35 commit에서 이미 구현됨 — 수정 불필요 (per D-03).
  </action>
  <verify>
    <automated>cd "C:/Users/Jinmoo Park/Desktop/sutda" && grep -n "disconnectPlayer(roomId, socket.id)" packages/server/src/index.ts -A 1 | grep "room-state"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/server/src/index.ts`에서 `roomManager.disconnectPlayer(roomId, socket.id)` 직후 라인에 `io.to(roomId).emit('room-state', roomManager.getRoom(roomId))` 문자열이 존재한다
    - grep 명령 `grep -c "io.to(roomId).emit('room-state', roomManager.getRoom(roomId))" packages/server/src/index.ts` 결과가 기존 값보다 1 증가한다
    - `pnpm --filter server build` 빌드 성공한다
  </acceptance_criteria>
  <done>disconnect 핸들러 game-playing 분기에서 disconnectPlayer() 직후 room-state가 emit되어 클라이언트 roomState가 갱신된다</done>
</task>

</tasks>

<verification>
1. `grep -n "disconnectPlayer" packages/server/src/index.ts -A 2` 로 room-state emit이 직후에 존재하는지 확인
2. `pnpm --filter server build` 로 TypeScript 컴파일 성공 확인
3. 데이터 흐름 확인: disconnectPlayer() -> room-state emit -> 클라이언트 roomState 갱신 -> GameTable isConnected prop -> PlayerSeat opacity-50
</verification>

<success_criteria>
- disconnect 핸들러 game-playing 분기에 `io.to(roomId).emit('room-state', roomManager.getRoom(roomId))` 1줄이 추가되었다
- 서버 빌드가 성공한다
- 기존 코드 구조에 영향 없다 (1줄 추가만)
</success_criteria>

<output>
완료 후 `.planning/phases/16-session-end-visual-fix/16-01-SUMMARY.md` 생성
</output>
