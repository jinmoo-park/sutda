node_modules/
dist/
.turbo/
*.tsbuildinfo

# Claude Code 로컬 설정 및 서브에이전트 임시 작업 폴더
.claude/settings.local.json
.claude/worktrees/
.clone/

# 루트 임시 작업 파일 (packages/client/public/ 에 이미 복사됨)
background.jpg
font/

# 루트 이미지 파일
img/
/*.png
/*.jpg
/*.jpeg
/*.webp
/*.gif

# 루트 HTML 파일 (stitch 목업, 예시 파일)
/*.html

# 루트 MD 파일 (stitch 문서 등)
/*.md

# Stitch 관련 파일
.planning/ui-reviews/

# 디자인 테스트 페이지
**/design_tests/

# AI Watchdog 분석 결과 (커밋 대상 아님 — VM에서 동기화)
.debug/

# 환경 변수 및 MCP 설정 (API 키 포함)
.env
.env.local
.env*.local
.mcp.json
